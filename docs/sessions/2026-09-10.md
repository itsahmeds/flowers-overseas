# Session 2026-09-10

## 17:29 [S:2026-09-10T17-29]

Orchestrator session spanning 2026-09-09 evening to 2026-09-10 17:29 (Fable orchestrator, Opus subagents). Companion handoff: `docs/sessions/2026-09-10-orchestrator-handoff.md`.

### Build Info
- **Commits on `main` (this session):** `cf6b561` (#38), `40eba98` (#37), `2760df8` (#36), `20545de`, `f46d2e5`, `d890f61`, `0015eb9`, `6eb5609`, `461dd3a`, `a9bdab1` (#39), `ec74549` (#42), `c9c4abb` (#41), `6e1e43d` (#43), `786a587`, `a0a4300` (#40), `bac0a71`, `223d6e2` (#44), `fae3655`, `3e44783` (#45), `3d6ec85` (#46), `02aae28` (#48), `e929862` (#47), `673af3d` (handoff)
- **Build ID:** no deploy (Vercel previews rate-limited; production untouched)
- **Files changed:** `TASKS.md`, `docs/decisions-log.md`, `plan/13-open-questions.md`, `specs/006-seed-catalogue-import-imagery-pipeline.md` (§14 A1–A5), `docs/sessions/2026-09-10-orchestrator-handoff.md`, plus the thirteen merged PRs' trees (`src/modules/catalog/**`, `src/modules/ui/{consent,home,media}/**`, `seed/**`, `content/imagery/**`, `scripts/*.ts`, `tests/**`, `messages/*.json`, `docs/compliance/*`)

### 1. Re-check and merge of the 09-09 fix rounds (PRs 36, 37, 38)

**Problem:** The previous session ended with three PRs pushed but unmerged; their `ready_for_review` CI event had been consumed before the fix commits, so no spine run existed for the heads.

**What was tried:** Ran the gates locally per worktree (unit, lint, typecheck, cold build, `budget:client-js`; consent e2e 114 + a11y 6 against a local `pnpm start` for PR 36; `catalogue:check` for PR 37). One unit failure per worktree when run concurrently — the known `dev-os.test.ts` shared-`$TMPDIR` flake, green alone. Light-reviewed PR 38 (internal schemas) and ruled on its two open items.

**Outcome:** All three squash-merged; TASK-051/062/072 `done`. Rulings recorded: ADR-0017 clarification (projected `seed/data/*.json` carry the authored camelCase records byte-identical to `src/config/catalogue/`; `to*Row()` applied at import time; taxonomy/categories/occasions generated too); `SeedPriceSchema.sku` not `productSku`; plan/03 §9 cannot express Romanian Orthodox Easter or Polish name days → seventh rule type as a spec 009 amendment. First recorded as TASKS.md open decision A11, which broke `tests/unit/tasks-open-decisions.test.ts` (hard-pinned to A1–A10 mirroring plan/13 §A) — moved to plan/13 **B15** instead.

### 2. Seed / imagery lane: TASK-074, 077, 078, 075, 076

**Problem:** Spec 006's Phase-0 half after the schema skeleton: price file projections, imagery provenance, the variant CLI, the gate, and the differ.

**What was tried / found:**
- TASK-074 (PR 39): per-destination `prices/{ISO2}.json` + `addon-prices/{ISO2}.json` generated by `seed:project`; per-file refinements pin one destination and one currency; five AC-6 fixtures. The agent found `seed/data/occasions.json` **stale on main** (PR 38 projected before PR 37 renamed `17Mai`→`mai17`) — the ADR-0017 gate caught it; regenerated in the PR.
- TASK-077 (PR 42): style guide verbatim, 13 prompt files / 31 records, `seed/schema/prompts.ts` canonicalisation (fixed field order, whitespace collapse, NFC, sorted params; hash covers the whole record), authored `media.json` with 31 pending `ai` assets, generator `to-be-confirmed` pinned by test with `docs/compliance/imagery-generator-terms.md` as the pending founder record. Found two slot vocabularies (`mediaSlots` vs UI `MEDIA_SLOTS`) → TASK-079 to map once.
- TASK-078 (PR 44): `seed/media-variants.ts`, `sharp` 0.35.4 exact devDependency, 15-file ladder, EXIF/GPS stripped with ICC kept, **encoder pinned to one thread** because libaom AVIF bytes vary by thread count (86 062 B @1 vs 88 980 B @8) — without it AC-13/AC-25 determinism is unachievable. Finding: the 6 MB committed-bytes cap cannot fit 31 assets at the full ladder → TASK-080 must pick slots/widths.
- TASK-075 (PR 46): `seed/check.ts` composing nine rule families from `seed/copy.ts`, `checkCatalogue` and the schemas; 18 declarative overlay fixtures; `--report` renders the §11 health report (PL 20 category / 13 occasion pages, demo countries 0, `de`/`pl` 100 % machine → not ready).
- TASK-076 (PR 48): `seed/diff.ts` + `seed/target.ts`, snapshot in `seed/snapshot/{table}.json` (not under `seed/data/`, which the gate enumerates), report gains an `orphans` column → spec 006 §14 A5.

**Outcome:** All five merged (light orchestrator reviews, internal tooling); TASK-074/075/076/077/078 `done`.

### 3. Catalogue lane: TASK-063, 064, 065

**What was tried / found:**
- TASK-063 (PR 43): `read.ts` behind `catalogProviders()`; counts not verdicts (the 6 appears nowhere); `resolveFacets` returns literal `false` for indexability; barrel AC-2 pin narrowed to "only `static/index.ts` imports `*.data.ts`".
- TASK-064 (PR 45): `Tier`/`Addon` read models with `vatRateBp` per destination and no amount; CRD Art. 22 preselection absence pinned at the type level (`WithoutFields<Addon,…>` → `never`) plus a 15th `catalogue:check` mode; flag seam as a fourth provider. No CI ran at all — every gate run by the orchestrator.
- TASK-065 (PR 47): `pricing/{money,vat,resolve}.ts`; BigInt half-up division; VAT from gross with the rate in the denominator; `priceVersion = cp:{sku}:{iso}:{tierKey}:{activeFrom}[+kind@date]`. Full review **PASS**: type-level detector proven by mutation outside the repo; VAT verified against an independent 35 007-case oracle with 0 mismatches. Six nits to TASK-069, the substantive one an unimplemented surcharge-VAT-rate guard documented in a comment.

**Outcome:** All three merged; TASK-063/064/065 `done`. TASK-066 dispatched (mid-build at close).

### 4. Homepage hero/finder (TASK-052, PR 40) and seed copy (TASK-073, PR 41) — full reviews FAIL then merge

**TASK-052:** the previous session's agent left uncommitted work in `fo-wt-052`; re-dispatched to continue. Review FAIL on (1) the type-ahead list never closing after a pick (no Escape/blur, covering the town label at 390 px) and (2) two consent-settings mobile baselines re-shot at 390×320 over a `next/dynamic` race in `tests/visual/consent.spec.ts` (silently disabling TASK-051's gate). Fix round: `open` flag + Escape + focusout, three new e2e tests with a hit-test; baselines restored byte-identical to main; the spec now waits on four elements inside the loaded panel; ninth banned word `network`; `/` no longer loads the finder chunk (deep import of `@/modules/ui/fonts`); LCP element re-attributed to the consent sheet body. A rebase onto TASK-073's copy forced a two-line `SiteHeader.tsx` cast change (TS2589). Merged after the orchestrator ran 62 home e2e, consent + home visual, 12 a11y locally.

**TASK-073:** 139 `en` rows, 4 `en-gb` overrides, 278 machine-flagged `de`/`pl` rows. Review FAIL on two unbacked delivery-timing claims ("next working day in most cities we cover", "not deliver early or late") — `countries.ts` deliberately refuses to guess `delivery_days`. Fix round replaced six strings (bands held), reworded seven pointer sentences rather than allow-listing, exported `DELIVERY_TIMING_PATTERN` for TASK-075. Decisions became spec 006 §14 A1 (`catalog.*`), A2 (first-person honesty label), A4 (no delivery timing in copy).

**Outcome:** Both merged; TASK-052/073 `done`. Nits carried to TASK-053/054/055/084.

### 5. Client-JS budget diagnosis → TASK-085 widened

**Problem:** PR 40 added +3.8 KB br to every document including `/`, which renders none of its components.

**What was found:** `src/modules/i18n/error-document.ts` statically imports all of `messages/en.json` for four strings and is reachable from `global-error.tsx`; Turbopack tree-shakes the JSON only below ≈9.4 KB, so copy tasks pay 0 B or 3.4 KB depending on which side of the cliff `en.json` lands. PR 41 found a second entry point: `messages.ts` has no `server-only` and is reachable from a client island (values found in a client chunk). Confirmed by two reviewers inspecting built chunks.

**Outcome:** TASK-085 row widened (error document strings from a tiny source; `messages.ts` `server-only`; budget assertion on chunk contents; fix `client-js-budget.ts` attribution). PR 50 (TASK-085, ready) lands `/` at 116 778 B and every locale document at 122 360 B br — inside 131 072 B for the first time — with a Chromium e2e asserting the script's model equals the fetched set; `server-only` deliberately not added to `messages.ts` (plain-Node importers), replaced by an import-closure test; awaiting full review.

### 6. GitHub Actions billing block

**Problem:** From 2026-09-09 17:00 UTC, `build` (then every) job failed in ~3 s with zero steps and no log.

**What was tried:** `gh run rerun --failed` twice (same failure); read the check-run annotation via the API: "The job was not started because recent account payments have failed or your spending limit needs to be increased". Branch protection query returned 403 (private repo, free plan) → no required checks enforced.

**Outcome:** Founder item. Standing rule adopted and recorded in TASKS.md and memory: merge on the CI jobs that ran + cold build + task suites locally at the exact head; do not rerun while blocked; re-toggle draft/ready once restored.

### 7. Process incidents and session close

- Network outage (~2026-09-09 late) stalled four agents; all re-dispatched to continue from worktree state (TASK-075 had already opened its PR; 085/079/065 restarted). Six leftover agent `next start` servers killed by PID.
- Twice the TASKS.md bookkeeping chain ran inside a worktree after `cd`, committing to the wrong branch and rejecting the push — recovered; rule: use `git -C /abs/path`, never chain after `cd <worktree>`.
- Bug in my worktree setup loop wrote `TASK-0063`/`TASK-0073` into `active-task`; agents corrected it.
- Closed at 47/85 with PR 49 (TASK-079) awaiting full review, PR 50 (TASK-085) ready awaiting full review, `fo-wt-066` mid-build. Handoff in `docs/sessions/2026-09-10-orchestrator-handoff.md`.
