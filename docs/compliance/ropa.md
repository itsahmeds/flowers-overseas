# Record of Processing Activities (GDPR Art. 30)
Updated by the reviewer whenever a spec adds or changes a data flow. Source of truth for the privacy policy's tables. See `plan/07-compliance.md` §1.2 for the initial flows.

**Cookies and client storage:** `cookie-register.md` in this directory is the register of every
cookie and storage key the application sets, with its classification, lifetime and the user action
that creates it — and since spec 004 its table is generated from `src/config/cookies.ts`
(`pnpm cookies:check`). It is the source of truth for the cookie policy page, as this file is for
the privacy policy's processing tables.

**VAT rates:** `vat-rates.md` in this directory records the two VAT rates authored per
destination in `src/config/catalogue/prices.data.ts` (flowers and standard), where each figure
came from, and which of them are still "provisional — accountant to confirm" per `plan/06` §4
item 4. It is the gate on a country flip from `demo` to `live`, as this file is for the privacy
policy's processing tables.

**Two provisioning acts are RoPA-affecting.** Rows 6 and 7 (Neon, Cloudflare R2) were added by
TASK-013 when the project and the buckets were created, *before* any personal data reached them, so
the record describes the stores from the day they exist rather than from the day they fill up. The
DPAs for both are still outstanding — see "DPAs still to be filed" below.

**One env variable is a RoPA-affecting act.** Setting `NEXT_PUBLIC_GA4_MEASUREMENT_ID` activates
row 4 below: Google becomes an active processor, the privacy policy must already name it, and the
data-processing terms must already be accepted and filed. The procedure, the order of the steps and
what must be true before the variable is set are in `docs/runbooks/analytics-consent.md`. No code
change is involved, which is exactly why it needs a runbook — a value pasted into an env store is
the easiest way to acquire an undeclared processor.

| # | Activity | Data subjects | Categories | Purpose | Lawful basis | Recipients / processors | Retention | Transfers | Spec |
|---|---|---|---|---|---|---|---|---|---|
| 1 | Application error monitoring, **server and edge runtimes only** (inert until `SENTRY_DSN` is set in Vercel, TASK-007; the browser SDK was removed from public routes by TASK-043 for the client-JS budget, so no error event is sent from a visitor's browser in Phase 0 — it returns on the checkout routes in spec 013 and this row is updated then) | Website visitors; founder/staff admin users | Error events and stack traces, `request_id`, release (commit SHA), deployment environment, coarse device/browser data. No PII by construction: `sendDefaultPii: false`; `beforeSend` drops `request.cookies`, `request.data`, `request.query_string`, `user.ip_address` and redacts the `src/lib/logger.ts` PII key list (`email`, `phone`, `phone_e164`, `*name`, `address*`, `card*`, `message`, `ip`, `authorization`, `cookie`, `postal_code`, `session_token`, `public_token`, `object_key` — widened by spec 002 §8, TASK-013) from `extra`, `tags`, `user`, `contexts`, `breadcrumbs`, request headers, and replaces free-text `message`/`exception` values, at any depth (AC-13, T-14) | Detect, diagnose and fix application errors; service integrity | Legitimate interest, Art. 6(1)(f) | Sentry (Functional Software, Inc.; Sentry GmbH EU establishment), EU region `de.sentry.io` (spec 001 §13 Q6). DPA to be signed at org creation and filed in `docs/compliance/` | Sentry default: 90 days for error events | None; data stays in the Sentry EU region. Revisit (DPF/SCCs) if the org is ever moved to the US region | 001 (TASK-005, PR #5) |
| 2 | Website hosting, edge routing and serverless function execution (platform logs) | Website visitors; founder/staff admin users | Platform-generated request metadata: IP address, user agent, timestamp, HTTP method, path, status code, response time, `x-vercel-id`, function region. No application PII by construction (`src/lib/logger.ts` redaction, AC-12; no PII in URLs per `CLAUDE.md`); Vercel Authentication SSO nonces on previews are set by the platform | Serve the website and API routes; platform security, abuse prevention, operational diagnostics | Legitimate interest, Art. 6(1)(f) | Vercel Inc. (US). Function execution pinned to `fra1` (Frankfurt) via committed `vercel.json` (spec 001 §5/§8); global CDN terminates requests at the nearest edge node. Vercel DPA accepted at project creation, to be filed in `docs/compliance/` | Per Vercel log retention; on the current Hobby plan runtime logs are kept 1 hour, build logs persist with the deployment. Log drain to an EU store is a Phase 1 item (`plan/08` §7) and gets its own row | Onward transfer to the US possible for platform logs/metadata; covered by the Vercel DPA (EU-US DPF and/or SCCs). Revisit at the Phase 1 log-drain decision | 001 (TASK-007, PR #7) |
| 3 | **Consent records** — proof that a consent decision was made, and what it was (spec 004 §8, TASK-050). Written when a visitor presses Accept all, Reject all or Save in the consent banner; the browser keeps its own copy in the `fo_consent` cookie (`cookie-register.md`) and `POST /api/consent` records the decision server-side | Website visitors | Four fields and no more: an **unlinkable random `cid`** the browser generates with `crypto.randomUUID()`, the ISO timestamp of the decision, the two category answers (`analytics`, `marketing`) and the consent-policy version. **No IP address, no user agent, no `Referer`, no page URL and no cookie is read** — not redacted afterwards, *not read*: the handler touches `content-type` and `content-length` and nothing else, asserted by a spy on `Headers.get` in `tests/unit/consent-route.test.ts`. So the record is not linkable to a person by us, and a DSAR cannot be answered from it (`dsar.md`) because there is nothing to look up | Demonstrate that consent was obtained, and what it covered, as the ePrivacy/GDPR regime requires of anyone who asks | **Legal obligation, Art. 6(1)(c) with Art. 7(1)** — the duty to be able to demonstrate consent. Not consent itself: one cannot ask for consent to store the proof of consent | None. Phase 0 writes one structured log line (`logConsentSink`), so the only recipient is the platform log of `ropa.md` row 2; spec 002 adds the durable `consent_log` table in the Neon (Frankfurt) database of row 6 behind the same `ConsentSink` interface, and this row is updated with that processor then | **5 years** after withdrawal or expiry of the consent (`plan/07` §1.2). Phase 0's log line expires with the platform's log retention (row 2), which is *shorter* than the obligation — an accepted Phase 0 gap, closed by spec 002's table, and recorded here rather than glossed over | None | 004 (TASK-050) |
| 4 | **Google Analytics 4** — audience and behaviour measurement. **Declared but inactive**: `NEXT_PUBLIC_GA4_MEASUREMENT_ID` is unset in CI, locally, on previews and in production, so no tag element exists, no request is made to `googletagmanager.com` and no analytics cookie is set (spec 004 AC-21, asserted by `tests/e2e/consent.spec.ts`) | Website visitors | When active: the `plan/04` §14 dimensions — page path, referrer, device and browser class, coarse geography derived by Google, and the `consent_state` dimension of spec 023. No personal field is ever sent (no email, no name, no address, no order id that resolves to a person). Under Consent Mode v2 **default denied** the tag may send only cookieless pings until an accept; after an accept it sets `_ga` and `_ga_*` | Measure which corridors, locales and pages produce orders, so the Phase 1 catalogue and content work is decided from traffic rather than guesses | **Consent, Art. 6(1)(a)** plus ePrivacy Art. 5(3) for the storage. No legitimate-interest analytics, no pre-consent measurement beyond the cookieless default-denied pings | Google Ireland Ltd / Google LLC, as processor under the Google Ads Data Processing Terms. Not yet accepted, because the property does not exist: signing them and filing them in `docs/compliance/` is part of turning the id on | GA4 data-retention setting, to be set to **14 months** at property creation (`plan/07` §1.2) | Onward transfer to the US possible; covered by the DPF certification and the SCCs in Google's terms, with IP anonymisation and `ads_data_redaction` on. To be re-assessed at activation, not before | 004 (TASK-050 declares it; spec 023 owns the event schema) |
| 5 | **Occasion-reminder signup** — the footer's email field (spec 004 design round 6, TASK-049). **Declared, and it stores nothing**: `POST /api/reminders` validates the submission, hands it to a `ReminderSink`, and the Phase-0 sink (`discardReminderSink`) discards it — no database (spec 002 is parked, so there is no `subscriber` table), no email sent (no transport before spec 017), no cookie, and **the address is never logged**: the only log line is the counter `reminder_signup: 1`, asserted by a logger spy in `tests/unit/reminders.test.ts`. Double opt-in is therefore the *first* thing that will ever store an address, and the footer says so before the visitor types (`footer.reminders.consent`) | Website visitors who submit the form | One field: an email address, held only for the duration of the request and then dropped. No name, no IP, no user agent, no `Referer` — the handler reads `content-type` and `content-length` and nothing else | Send an email a few days before an occasion the buyer chose to be reminded of | **Consent, Art. 6(1)(a)** — and it is not yet collected: the address is stored only after the subscriber confirms it from the confirmation email (double opt-in), which spec 017 sends. Until then there is no processing to base | None. Nothing is stored and nothing is transmitted; spec 017 adds Resend as processor and the `subscriber` record behind the same `ReminderSink` interface, and this row is rewritten then | Nothing is retained in Phase 0. From spec 017: until unsubscribe, plus the consent proof per row 3's retention | None | 004 (TASK-049 declares it; spec 017 owns the send and the store) |
| 6 | **Database hosting** — the application database (Neon Postgres, project `old-moon-05172629`, branch `production`). **Declared ahead of any personal data**: TASK-013 provisions the project and puts its two connection strings in the env contract; the first table is created by TASK-014 and the first personal row exists only once orders do (spec 002 §5.1, ADR-0015) | Buyers; recipients; partner staff; founder/staff admin users | Once the schema lands: buyer account and contact data, recipient name, address and phone, order and payment records, consent records, partner and payout data, Auth.js users/sessions. No card data (Stripe/Mollie hold it), no full IP (`consent_log.source_ip_truncated` only), no `email` column on `recipient*` (`plan/07` §1.3, enforced by `db:check`, AC-27). Tenant isolation is a database control, not a query convention: RLS policies keyed on per-request session variables, and the runtime role has no `BYPASSRLS` (spec 002 §5.5, AC-16 to AC-18) | Store and serve every entity the service needs: catalogue, orders, fulfilment, payments, consent, audit | Contract, Art. 6(1)(b) for order and fulfilment data; legal obligation, Art. 6(1)(c) for invoice and consent records; legitimate interest, Art. 6(1)(f) for audit and abuse prevention | **Neon Inc.** (US company) as processor. The project, its compute and its storage are in **Frankfurt, `aws-eu-central-1`** — chosen at project creation and not changeable afterwards. Nightly logical backups go to row 7's private R2 bucket. **DPA: not yet accepted or filed** (neon.com/dpa) — see "DPAs still to be filed" below | Per `retention_policy` (spec 002 §8, `plan/07` §10): recipient contact pseudonymised 90 days after delivery, delivery photos 2 years, buyer personal fields 3 years with the invoice archive retained 10 years, fraud signals 12 months, consent records 5 years after withdrawal. Backups expire at 30 days (row 7) | Data at rest and in use stays in Frankfurt. Onward access by Neon support personnel outside the EU is possible under the DPA's SCCs; to be confirmed when the DPA is filed | 002 (TASK-013 provisions; TASK-014 creates the schema) |
| 7 | **Object storage** — Cloudflare R2, two buckets in the **EU jurisdiction**: `flowersoverseas-media` (public product and editorial imagery) and `flowersoverseas-backups` (**private**, nightly `pg_dump` artefacts, 30-day lifecycle rule). **Declared ahead of any personal data**: TASK-013 adds the bucket and credential contract, spec 006 writes the first object, and the backup workflow is spec 002's TASK-029 | Buyers; recipients; partner staff | Media bucket: product and editorial imagery only — no delivery photos before spec 006's private-asset rules (`delivery_proof` requires a private, EXIF-stripped asset, AC-19). Backups bucket: a full logical dump, so once real orders exist it contains **every** personal field of row 6. Object keys are treated as personal data in logs (`object_key` is on the `src/lib/logger.ts` redaction list, spec 002 §8) | Serve imagery to visitors; hold the nightly database backup so the service is restorable | Legitimate interest, Art. 6(1)(f) — serving the site and being able to restore it; the backup inherits the lawful basis of the data it copies | **Cloudflare, Inc.** as processor, EU-jurisdiction buckets (storage restricted to EU data centres; chosen at bucket creation, not changeable). Access is one scoped API token with object read/write on these two buckets only; the backups bucket has no public access. **DPA: not yet accepted or filed** (Cloudflare customer DPA) — see below | Media: life of the asset. Backups: **30 days**, by the bucket lifecycle rule (spec 002 AC-25) | Buckets are EU-jurisdiction; Cloudflare is a US company, so support access from outside the EU is possible under the DPA's SCCs. To be confirmed when the DPA is filed | 002 (TASK-013 provisions; TASK-029 writes the backups) |
| 8 | **Language suggestion** — the country behind the locale popup (spec 003 §14 A14, TASK-119). When, and only when, a visitor's `navigator.languages` name no locale we publish, the suggestion island fetches `GET /api/geo`; the route reads the country the edge network already derived (`cf-ipcountry`, or `x-vercel-ip-country` on the cold-standby platform) and answers `{"country":"DE"}`. The browser uses it once, to decide whether to offer another language, and the visitor answers the question once | Website visitors | **One field: an IP-derived ISO 3166-1 alpha-2 country code.** The IP address itself is never read by the application — the edge resolved it before the request arrived — and nothing else about the request is read: no user agent, no `Referer`, no page URL, no cookie. The country exists in memory for the length of one request and is **never stored and never logged** (the route imports no logger; `tests/unit/geo-route.test.ts` asserts it writes no console line and that no IP-shaped marker survives into the response). The visitor's *answer* is the `fo_locale` cookie (`cookie-register.md`), which holds a language code and no location | Offer the site in a language the visitor is more likely to read, with one tap back to English | **Legitimate interest, Art. 6(1)(f)** — a courtesy that is only ever an offer. The balancing is what keeps it: nothing is redirected, blocked or priced by country (EU 2018/302 is satisfied because the visitor chooses), the data is the coarsest location there is, it is used once and kept nowhere, and the alternative (asking every visitor in English) is worse for the visitor than for us | None. The country is derived by the edge network in front of the app (row 2 / spec 040's Cloudflare and Railway rows) and read only by our own route; it reaches no third party, no analytics property and no database | **None.** Nothing is retained: no row, no log line, no cookie carrying a country | None | 003 (§14 A14, TASK-119) |
| (further rows: populate from plan/07 §1.2 with the schema of spec 002) | | | | | | | | | |

## DPAs still to be filed

Neither of the two processors added above has a signed, filed data-processing agreement yet. This
is recorded as an open item rather than glossed over, and neither row may be described as "covered"
until the corresponding file exists in this directory.

| Processor | Instrument | Status | Owner / next step |
|---|---|---|---|
| Neon Inc. (row 6) | Neon Data Processing Addendum, `neon.com/dpa` | **Not accepted, not filed.** No personal data is in the database yet: the project holds no tables until TASK-014 and no personal rows until orders exist | Founder: accept the DPA in the Neon dashboard and save the executed copy as `docs/compliance/dpa-neon.pdf`, **before** the first personal row is written |
| Cloudflare, Inc. (row 7) | Cloudflare customer Data Processing Addendum | **Not accepted, not filed.** The buckets are empty of personal data: no media asset and no backup exists yet | Founder: accept the DPA in the Cloudflare dashboard and save the executed copy as `docs/compliance/dpa-cloudflare.pdf`, **before** the first backup or delivery photo is written |
| Vercel Inc. (row 2) | Vercel DPA, accepted at project creation | Accepted, **not yet filed** in this directory (spec 001 carry-forward) | Founder: export and save as `docs/compliance/dpa-vercel.pdf` |
| Sentry (row 1) | Sentry DPA | To be signed at org creation; inert until `SENTRY_DSN` is set | Founder, at org creation |

A processor added to the stack without its DPA filed here is a finding, not a detail: Art. 28(3)
requires the agreement to be in place **before** processing begins, which is why both rows above
are written now — while the stores are empty — rather than after the first order.
