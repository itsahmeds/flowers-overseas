# TASK-046 — Client-JS budget restatement, zod-free client locale data, CSP and security headers: `plan/01` §7's budget restated as ≤120 KB **Brotli** transfer (spec §13 Q13 option (a)), zod removed from the client bundle via a zod-free locale-data module for the `global-error` document, per-environment `Content-Security-Policy-Report-Only` from `next.config.ts` with a build-time hash for the consent bootstrap, HSTS/`X-Content-Type-Options`/`Referrer-Policy`/`frame-ancestors`/`Permissions-Policy`/COOP, `POST /api/csp-report`, `CSP_REPORT_ONLY` env, ADR-0016

Row: `TASKS.md` → TASK-046. Brief written by `pnpm tasks:migrate` (spec 001 §14 A15, AC-34);
keep it current by editing this file, not the row.

## Binding

Branch `task/TASK-046-budget-csp-security-headers`. Second of the two unblocked tasks (spec §12 task 2); independent of TASK-045 — it touches `next.config.ts`, `src/lib`, `src/config` and CI, not CSS or components. Spec §13's 2026-09-08 resolution note is binding and overrides §5 defaults where they differ. **Write ADR-0016** in this PR (`/adr CSP shape on cached HTML`, §13 Q5 accepted): allowlist + build-time `'sha256-'` hash for the single ≤1 KB inline Consent Mode block on cached routes, per-request nonce + `'strict-dynamic'` on `no-store` routes deferred to the spec that ships the first one; the `plan/01` §9 "nonces" sentence is superseded, not edited. **`vercel.live` decision (spec 001's second deferred row, `/review 8`):** the origin appears in `script-src`, `connect-src` and `frame-src` of the **preview** policy only and is absent from the production policy, both header strings asserted (AC-23) — `frame-src` because the feedback widget renders in an iframe, disclosed in ADR-0016 and pinned by the whole-string tests — this is what lets `tests/e2e/shell.spec.ts` drop its non-local allowance later. Budget: spec 003 TASK-043 already removed browser Sentry (spec §13 Q8 needs no work), so the remaining client-bundle offender is zod, reached from the `global-error` document's locale lookup — ship a zod-free `src/config/locale-data.ts`-shaped module (plain typed constants, still the single source with the zod-parsed registry validating it in a unit test) so no public route's client bundle contains zod, and restate the budget in `lighthouserc.json`, `plan/01`-derived docs and `scripts/` comments as ≤120 KB Brotli / 122 880 B script transfer. `lighthouse` stays informational until TASK-056 flips it. Escalate rather than guess if the Brotli number still exceeds the budget after zod leaves: the fallback is spec §13 Q13 option (b) and it is a founder decision. Tests: T-25, plus a unit assertion that the built client chunks for `/`, `/en` and `/de` contain neither `@sentry/` nor `zod`.  **Delivered (PR #26, `/review` pending):** ADR-0016 accepted (`docs/adr/ADR-0016-csp-allowlist-hash-on-cached-html.md`) with the decisions-log line and the `plan/00` §6 row; `src/lib/csp.ts` + `next.config.ts` `headers()` sending `Content-Security-Policy-Report-Only` per environment (`vercel.live` in preview's `script-src` / `connect-src` / `frame-src`, absent from production, both whole strings asserted) plus `X-Content-Type-Options`, `Referrer-Policy`, `X-Frame-Options: DENY`, a 16-entry `Permissions-Policy`, COOP and production-only HSTS; `POST /api/csp-report` (both shapes, 415 on a wrong content type, 204 on everything else, never 5xx, only `directive` + `blocked_origin` logged); `CSP_REPORT_ONLY` (absent ⇒ report-only, only `false` enforces); `src/config/locales.data.ts` + `src/modules/i18n/error-document.ts` taking zod off every public route's client bundle; `scripts/client-js-budget.ts` now measuring Brotli against 122 880 B and asserting no route ships `zod` or `@sentry/`. `docs/architecture.md` §4's CSP row and its `vercel.live` note removed; §2 records the two-step narrowing. **Bundle, before → after** (browser-measured against `pnpm build && pnpm start`, Brotli q11 over every `.js` the page fetches, lazy chunks included): `/` 226 575 B gz / 190 706 B br → 136 067 / **116 393 (113.7 KB) — within, 6 487 B spare**; `/en` and `/de` 240 633 / 203 197 → 150 992 / **129 638 (126.6 KB) — 6 758 B, 5.5% over**. **ESCALATED, founder decision, blocks TASK-056 (AC-24, AC-25): spec 004 §13 Q13 option (b).** `/` is within the restated budget and the locale documents are 6 758 B over, and the remainder is not application code: `/`'s whole 116 393 B is the framework floor (react-dom 62 564 + App Router runtime 39 763 + ~13.5 KB bootstrap and route shells), leaving 6 487 B, while `NextIntlClientProvider` + `@formatjs` costs 10 705 B and the suggestion-banner island's own chunk 2 173 B. Options: (a) raise the budget (128 KB br = 131 072 B covers today's locale documents with 1.4 KB spare; the floor grows with Next); (b) drop the client message provider from `src/app/[locale]/layout.tsx` — its only client consumers are the suggestion-banner island, which already receives everything as props, and `[locale]/error.tsx`, both of which could read the zod-free path this task added, but that is a spec 003 surface and changes AC-28's island contract; (c) exempt locale documents in `lighthouserc.json`, i.e. loosening, listed only to be rejected. Not loosened here. Nothing blocks merge: both gates keep `continue-on-error: true` and TASK-056 owns the flip. **Two out-of-scope repairs, disclosed in the PR:** (1) `origin/main`'s `PHASE_0_SPECS_APPROVED` `3 / 12` → `4 / 12` flip, which this branch carried until `main` shipped it as `97ac494`; the duplicate was dropped in the rebase and the fix is `main`'s. (2) `tests/unit/no-adhoc-intl.test.ts`'s whole-`src/` ESLint test given an explicit 60 s timeout after five added files pushed it past Vitest's 5 s default on the runner. **Measured on the preview, informational:** `lighthouse` red on `resource-summary:script:size` — `/` 141 769 B, `/en` 249 766 B, `/de` 249 762 B against 122 880 — and on `/de` performance 0.90 / LCP 3 088 ms. Reconciled in `lighthouserc.json`'s note (`/review 26` item 5): Lighthouse's transfer size **is** Brotli and it **does** include the platform's `vercel.live` feedback script, which a protected preview injects, production does not serve and our build output does not contain — `/`'s 141 769 B is our 116 393 B plus 25 376 B of `vercel.live`, and `/en`'s 249 766 B was our then-198.4 KB plus ~48 KB of it. With this round's build a locale document should read ≈178 KB there and still be red. The attributable measurement is `pnpm budget:client-js`; deciding where the blocking measurement is taken (production URLs, or a first-party-only budget) is TASK-056's. **`/review 26` round 1 FAIL, addressed:** (1) zod was still fetched on every locale page — `LocaleSuggestionBannerIsland.tsx` → `hints.ts` → `schemas.ts` → zod, on a chunk `[locale]/layout.tsx` loads unconditionally through `next/dynamic({ ssr: false })`, 70.8 KB br. `hints.ts` now imports no validator: `LANGUAGE_RANGE_PATTERN` and `isLaunchLocaleCode` (from the zod-free `locales.data.ts`) carry the two rules, `schemas.ts` is built from the same constants and stays the server-side boundary parser, and `tests/unit/i18n-hints-zod-free.test.ts` walks the import graph from the island, its loader and `global-error.tsx` and proves the predicates and the schemas accept and reject the same values. (2) `scripts/client-js-budget.ts` was blind to `next/dynamic`: it now follows `.next/server/app/<entry>/react-loadable-manifest.json`, counts those chunks and scans them, prints the document / lazy split, and `tests/unit/client-js-budget.test.ts` has a fixture whose document is clean and whose lazy chunk carries zod. Its static total is an upper bound — `/` is charged 12.6 KB for a chunk group `(chooser)` never renders (129 271 B against the browser's 116 393 B), locale documents match the browser exactly — and which number the blocking gate reads is TASK-056's. (3)–(5) numbers re-measured with a browser and rewritten in the PR body, this row, `README.md`, `docs/architecture.md` §2, `lighthouserc.json`, the CI comment and `src/app/(chooser)/page.tsx`. Non-blocking: `CSP_REPORT_MAX_BYTES` (16 KB) declared-`Content-Length` guard on `/api/csp-report`; the per-instance limiter's consequence for the enforce flip recorded in ADR-0016; the `vercel.live` `frame-src` wording aligned above; README's e2e section now says the server is yours to start. **Left to TASK-050 by design:** the consent bootstrap's `'sha256-'` hash and the GA4 origin land in the same PR as the script they authorise (`cspHeader`'s `inlineHashes` slot is the documented seam, empty today).

## Read

- `specs/004-*.md` — read `## 0. Index` first, then only the sections the ACs below name
- `docs/codebase-map.md` — where everything lives
- `src/lib`
- `src/config`
- `plan/01`
- `tests/e2e/shell.spec.ts`
- `src/config/locale-data.ts`
- `docs/adr/ADR-0016-csp-allowlist-hash-on-cached-html.md`
- `plan/00`
- `src/lib/csp.ts`
- `src/config/locales.data.ts`
- `src/modules/i18n/error-document.ts`
- `scripts/client-js-budget.ts`
- `docs/architecture.md`

## Carry-forwards

_None recorded._

## Escalations

_None recorded._

## Result

Done. PR [#26](https://github.com/itsahmeds/flowers-overseas/pull/26); `/review` pass recorded in `TASKS.md`.
