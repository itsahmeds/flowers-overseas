# TASK-051 — `ConsentBanner` and `ConsentSettings` islands: bottom sheet on mobile / inline card on desktop, three equal-weight controls, `fo_consent` read/write, `Esc` closes without deciding, per-category settings panel from the register, the footer "Cookie settings" re-open wiring, `consent.*` messages

Row: `TASKS.md` → TASK-051. Brief written by `pnpm tasks:migrate` (spec 001 §14 A15, AC-34);
keep it current by editing this file, not the row.

## Binding

Branch `task/TASK-051-consent-banner-islands`. Spec §13's 2026-09-08 resolution note is binding and overrides §5 defaults where they differ. Design source of truth (match pixel-for-pixel): `docs/design/homepage-v1/README.md`, `docs/design/homepage-v1/tokens.css`, `docs/design/homepage-v1/homepage-desktop.dc.html`, `docs/design/homepage-v1/homepage-mobile.dc.html`, `docs/design/homepage-v1/identity.dc.html`, `content/brand/mark.svg`. (the canvas has no consent sheet drawn — style it from the tokens as a `--color-paper` sheet with `--rule` top border, `.label` heading and the canvas's neutral button treatment; the accent green is reserved for the finder's `Continue`, so the three consent controls are ink-on-paper with identical weight). Non-modal, focus not stolen, overlays rather than reflows (CLS delta 0), `aria-live="polite"`, `Esc` records nothing and the banner returns next page view (dismissal is not consent). Accept / Reject / Settings must be **equal in rendered width class, font size, weight and contrast** — AC-20 is asserted from computed styles, not from a screenshot, so write the assertion first and let it drive the markup; no pre-ticked non-essential category; the settings panel lists every register entry in the category with purpose and lifetime, essential locked-on with the reason. `fo_consent`: first-party, `Path=/`, `SameSite=Lax`, `Secure` outside dev, not `HttpOnly`, JSON `{v, a, m, ts, cid}`, 6 months reject / 12 months accept, written **only** on an explicit user action and never by the server on a cached response (spec 001 AC-15, spec 003 AC-12 both stay green). A forged or unparsable value is treated as no consent and rewritten — carry spec 003 `/review 23`'s ruling forward: the cookie reader must not throw on malformed percent-escapes and the island fails closed. Settings panel dynamically imported on open so the default path pays for the banner only. `z-index` from `--layer-banner`/`--layer-overlay` so the sheet paints **above** the spec 003 suggestion banner (AC-13, verified by TASK-055). Tests: T-19, T-21, T-22, and the four banner + three settings states in `/dev/components`.

## Read

- `specs/004-*.md` — read `## 0. Index` first, then only the sections the ACs below name
- `docs/codebase-map.md` — where everything lives
- `docs/design/homepage-v1/README.md`
- `docs/design/homepage-v1/tokens.css`
- `docs/design/homepage-v1/homepage-desktop.dc.html`
- `docs/design/homepage-v1/homepage-mobile.dc.html`
- `docs/design/homepage-v1/identity.dc.html`
- `content/brand/mark.svg`
- `src/config/cookies.ts`
- `src/modules/ui/consent/`
- `src/app/[locale]/layout.tsx`
- `tests/unit/consent-view.test.ts`
- `tests/e2e/consent-banner.spec.ts`
- `tests/unit/consent-islands.test.tsx`

## Carry-forwards

- **Carried from `/review 28` item 8:** add the assertion that **every `purposeKey` in `src/config/cookies.ts` resolves in the message catalogue** — it cannot be written in TASK-050, because the `consent.*` namespace is this task's to create and `pnpm i18n:check` refuses unused keys. **Delivered (PR #36, `/review` pending).** `src/modules/ui/consent/`: `ConsentBanner` (a synchronous Server Component that resolves every string and projects the cookie register), `ConsentBannerLoader` (`next/dynamic`, `ssr: false`), `ConsentBannerIsland`, `ConsentBannerView` (+ `ConsentSavedView`), `ConsentSettingsPanel` in its own chunk imported on open, `ConsentGallery` for the seven §5.3 states, plus the zod-free `consentCookie.ts` and the types-only `consentTypes.ts`. Mounted once per **localised** document by `src/app/[locale]/layout.tsx`, before the suggestion banner and above it in the z-scale; `/` has no locale layout and therefore no sheet, so spec 003 AC-7's zero-application-JS chooser is intact. **Q13 option (b) applied to these islands:** copy, categories, lifetimes, the policy version, the endpoint and the footer's `CONSENT_REOPEN_ATTRIBUTE` all cross as props, so no message catalogue, no register and no zod reaches the browser; `consent.*` is deliberately **not** in `namespacesFor("localeDocument")`. `fo_consent` written only on a press (`Path=/`, `SameSite=Lax`, `Secure` off `localhost`, not `HttpOnly`, JSON `{v,a,m,ts,cid}`, 365 d for any acceptance / 183 d for a refusal, both read from the register); a forged, truncated, mis-shaped or stale-version value is deleted and the sheet asks again; the record is POSTed with `credentials: "omit"` and `referrerPolicy: "no-referrer"`, so no cookie and no page URL leave with it. `gtag('consent','update')` on every decision, granting exactly what was given. Tests: 67 unit, 84 e2e (42 × desktop and mobile, four locales), 6 a11y, 4 visual with **8 new `darwin/` baselines** (`consent-{shown,settings}-{en,de}-{mobile,desktop}.png`; `linux/` stays TASK-053's one-off refresh per `/review 30`). The `/review 28` item-8 assertion is in `tests/unit/consent-view.test.ts`: every register `purposeKey` resolves in all four locales through a translator whose fallback throws, and no `consent.cookies.*` branch exists without a register row. **ESCALATION — §14 A1's client-JS budget cannot be met by this task** (accepted as informational by the spec §14 A1 addendum; option (b) is TASK-085's). Re-measured on a cold build after the rebase onto main, with the TASK-048 header in the tree: `/` 116 426 B in the browser (14 646 B under; `budget:client-js`'s pessimistic total is 131 667 B because it charges both island chunk groups plus the message chunk to the chooser's entry), `/en` and `/de` **132 044 B** browser and script alike, i.e. **972 B (0.74 %) over 131 072 B**. The sheet costs **2 363 B** Brotli in one lazy chunk (+967 B nested chunk fetched only when the panel is opened, which the script's route-manifest read does not count — disclosed in the PR); 97.3 % of the budget is the framework floor before this task's first line. Nothing was raised: `Button` was not imported into the island (−900 B, replaced by one shared control constant pinned to `Button`'s skin by a unit test), and `ConsentGallery` was kept **off** the `ui` barrel because a `"use client"` module reachable from the barrel lands in every route's document JS (−1 990 B on `/` and on each locale document). §14 A1's named remedy — Q13 option (b) for the *suggestion* banner, dropping `NextIntlClientProvider` and the client message payload from the locale document (−10 705 B) — is **TASK-085's** since the addendum of 2026-09-09, and TASK-053's finder island plus the ~30–35 KB GA4 tag are the other two consumers of it. `budget:client-js` is still `continue-on-error` in CI. Row left `in_review` rather than `blocked` because the task is complete and every other gate is green; the orchestrator may flip it if it wants the budget resolved first. **Rulings recorded in the PR (reviewer to confirm):** (1) the flow artboard's dimmed, focus-trapped sheet is superseded by §5.3/AC-17/§8's non-modal, focus-preserving, blocks-nothing sheet — the artboard's own note says the consent sheet is "not yet specified"; (2) `Esc` steps back one layer (panel → sheet → gone), recording nothing, because closing everything at once left focus on a detached node; (3) the sheet covers the suggestion banner while a decision is pending (the specified z-order and the artboard's sequence), so four TASK-041 e2e tests and the footer/shell/pseudo-rtl visual specs now seed a recorded refusal first — spec 003 AC-28's matrix is otherwise unchanged and green, and offsetting the banner clear of the sheet stays **TASK-055's** (AC-13); (4) `/dev/components` gains the two consent checkboxes, which §3 permits in the same sentence that forbids a form layer, so its "no form control" test now counts controls outside the footer *and* outside the consent panel and asserts the consent controls exactly; (5) the sheet caps itself at `85dvh` and scrolls internally, because eleven open rows are taller than a 390×844 phone and a bottom-anchored `fixed` sheet cannot be scrolled back (WCAG 1.4.10); (6) the `marketing` category is disclosed with an honest "nothing in this group yet" note rather than hidden, so the first marketing cookie is not consented to on a panel that never named it. **Round 2 (`/review 36`):** rebased onto `origin/main` (six conflicts resolved so the locale layout keeps the header, the footer and both banner mounts, `/dev/components` keeps every section, and the message metadata keeps both key sets); each category toggle is now a label-wrapped, full-width row measuring **358 × 44 px at 390 px** (was 13 × 18) with the browser assertion in `tests/e2e/consent-banner.spec.ts` and the markup pinned in `tests/unit/consent-islands.test.tsx`; an empty category with no note renders no element at all; the stale "one `Button` component" paragraph in `ConsentBannerView` corrected; the sheet's byte figure is one number (2 363 B) in the row and the runbook; the four `consent-settings-*` `darwin/` baselines regenerated (the four `consent-shown-*` are element-clipped and unchanged). **Correction (round 2, `/review 36` item 2):** the earlier claim that HSTS blocks a local Lighthouse run was false. `next start` sends **no** `Strict-Transport-Security` header — `sendsHsts` is production-only — and `pnpm lighthouse` completes locally. The real limitation is that `next start` serves chunks with no `Content-Encoding`, so Lighthouse's script-size audit reads identity bytes (169 223 B on `/en`) which cannot be compared to a Brotli budget; `pnpm budget:client-js` is the local equivalent and its numbers are above. That is why the CI job measures a deployed preview. Docs: `docs/runbooks/analytics-consent.md` §4a. No new dependency; RoPA unchanged (the consent-record row is TASK-050's and this task only sends to it).

## Escalations

An escalation is recorded in the migrated prose above (search `ESCALATION`). New escalations go here, one dated bullet each.

## Result

Done. PR [#36](https://github.com/itsahmeds/flowers-overseas/pull/36); `/review` pass recorded in `TASKS.md`.
