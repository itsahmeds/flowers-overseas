# TASK-039 — Review gate and the hreflang generator: `src/modules/i18n/review.ts` (`unreviewedShare`, `localeBetaTag`, `isLocaleIndexable`), `alternates.ts` (`alternatesFor`), and the fifth-locale "a new locale is data" proof

Row: `TASKS.md` → TASK-039. Brief written by `pnpm tasks:migrate` (spec 001 §14 A15, AC-34);
keep it current by editing this file, not the row.

## Binding

Branch `task/TASK-039-review-gate-alternates`. §6's answer to the one real risk in this spec: four locale prefixes whose `de` and `pl` catalogues are echoed English is textbook duplicate content, and `isLocaleIndexable()` (`isLaunch` and unreviewed share ≤ 5%) is the hard gate spec 007 consumes for robots meta, hreflang membership and sitemap membership (`plan/02` §10, §12, `plan/03` §6.4). With echoed drafts it returns `false` for `de` and `pl`, which is the correct answer; marking every `de` key reviewed in a fixture manifest flips it with no code change. It is pure and synchronous so `generateMetadata` and the sitemap builder get the same answer. `alternatesFor()` is the single generator `plan/02` §8 mandates — spec 007's `seo` module wraps it and must not reimplement it: `x-default → /en`, the regional aliases that share a URL (`en-IE`, `en-NL`, `en-150`, `de-DE`, `de-AT`, `pl-PL`), reciprocal and complete by construction, no entry for a non-indexable locale and structurally none for a pseudo-locale, in the shape `tests/fixtures/seo/hreflang/*.json` already consumes so spec 001's `validate-hreflang` CLI inherits real fixtures. A cross-locale canonical is unexpressible by construction. AC-31 tests `plan/09`'s promise: a fifth locale with no catalogue renders, is non-indexable and beta-tagged, with no change under `src/app/` or `src/modules/`. Tests: T-14, T-24, T-31. From `/review 17`: Google honours only ISO 3166-1 alpha-2 region subtags in `hreflang` and ignores UN M.49 (`en-150`) — verify against plan/02 §3 before emitting it in `alternatesFor()`; and `formattingTag` (TASK-044) must never leak into hreflang output.  **Implemented on `task/TASK-039-review-gate-alternates`, PR #21, `in_review` 2026-09-08.** Two spec-silent points decided and argued in the PR body rather than improvised. (1) **`en-150` is configured data but is not emitted in hreflang**: Google documents ISO 639-1 language plus an optional ISO 3166-1 alpha-2 region (and `x-default`), UN M.49 `150` is not alpha-2, and one unsupported value can void the whole annotation — `emitInHreflang()` filters non-alpha-2 regions for every locale rather than special-casing one value, so a Phase 4 `es-419` inherits the protection; `plan/02` §3's alias table needs that correction recorded → **carry-forward for TASK-043's doc pass**, entry added to `docs/decisions-log.md`. (2) **`alternatesFor()` returns `[]` when the x-default locale is not indexable** rather than promoting another locale or emitting a cluster without `x-default` (unreachable while `en` is the authored source; pinned by a test). **Unreviewed-share definition** (module header + tests): unreviewed keys ÷ keys in the *resolved* catalogue, where a key inherited from a fallback locale counts as reviewed only when that locale shares the primary language subtag — `en-gb` ← `en` is reviewed English (share 0, indexable), `de`/`fr` ← `en` is untranslated (share 1), so the thin override, the echoed drafts and the catalogue-less fifth locale all get their correct answer from one rule with no special case. `alternatesFor(target, { baseUrl })` takes the origin as an argument so the module stays pure (§5.3) and the `NEXT_PUBLIC_SITE_URL` read stays at spec 007's call site. `tests/fixtures/seo/hreflang/` now holds real fixtures generated by `scripts/seo/generate-hreflang-fixtures.ts`, so `pnpm seo:validate` gates live data (2 fixtures ok) and a test asserts regeneration is byte-identical; `tests/unit/seo-validate-hreflang.test.ts`'s "no fixtures" assertion updated accordingly. `LocaleSwitcher` renders the `plan/03` §6 beta marker with `betaLocales` defaulted to `localeBetaTag()`, so no page computes a share and AC-31 stays true; one new key `common.beta` (`en` human/reviewed, `de`/`pl` re-drafted, `i18n:draft` still byte-identical on a re-run). `isLocaleIndexable` is exported only — robots/metadata consumption is spec 007. Unit 853 green (+37, 0 skipped); both new modules mutation-checked. CI 19/20 green (e2e, a11y, visual, seo-validate included); `lighthouse` red and informational — the pre-existing `resource-summary.script.size` budget carried by TASK-043.

## Read

- `specs/003-*.md` — read `## 0. Index` first, then only the sections the ACs below name
- `docs/codebase-map.md` — where everything lives
- `plan/02`
- `plan/03`
- `tests/fixtures/seo/hreflang/*.json`
- `plan/09`
- `src/app/`
- `src/modules/`
- `docs/decisions-log.md`
- `tests/fixtures/seo/hreflang/`
- `scripts/seo/generate-hreflang-fixtures.ts`
- `tests/unit/seo-validate-hreflang.test.ts`

## Carry-forwards

_None recorded._

## Escalations

_None recorded._

## Result

Done. PR [#21](https://github.com/itsahmeds/flowers-overseas/pull/21); `/review` pass recorded in `TASKS.md`.
