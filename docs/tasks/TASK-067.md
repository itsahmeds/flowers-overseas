# TASK-067 — Projections and the price identity: `pricing/project.ts` — `priceProjection(locale, …)` deriving the display currency from the locale's `currencyDefault`, `priceTable(product, country)` with its <=6-currency / <=512 B refinement, `fromPrice`, `offerProjection()` — plus the `catalog` message namespace in `messages/en.json` and the schema-identity test against spec 001's `validate-schema` CLI

Row: `TASKS.md` → TASK-067. Brief written by `pnpm tasks:migrate` (spec 001 §14 A15, AC-34);
keep it current by editing this file, not the row.

## Binding

Branch `task/TASK-067-price-projections-identity`. Spec 005 §13's 2026-09-09 resolution note is binding: Q1 EUR/GBP `x90`, PLN `x9`, HUF `x90`; Q2 ECB daily rates, 2.5% buffer, 48 h max age then destination currency; Q3 add-ons priced per country with their own VAT rate; Q4 plain stem-count / S-M-L / single tier names; Q5 30-minute signed quote, secret lands in spec 010; Q6 middle tier preselected via `product_tier.is_default`; Q7 surcharges as dated rows; Q8 all three schema amendments folded into spec 002's `0003` (spec 002 §14 A1); Q9 this spec owns the dataset in `src/config/catalogue/*.data.ts`; Q10 no money on destination-less hubs; Q11 EUR/GBP/PLN only behind `currency.{code}` flags. **AC-11 is the manual-action guard of `plan/02` §15**: `offerProjection().price` formatted at the currency's exponent equals what `formatMoney(displayPrice, locale)` renders, `priceCurrency` equals the locale default, `shippingRate` is 0 (delivery is in the price by definition), `hasMerchantReturnPolicy` is `MerchantReturnNotPermitted` (perishables, `plan/07` §2.1), **exactly one `Offer`** per PDP and **no `Offer` at all** for a `demo` country (ADR-0007). Feed the projection to spec 001's `validate-schema` CLI so the `Offer.price == visiblePrice` check that TASK-009 shipped finally has something to validate. **AC-9 is the i18n rule most easily broken**: the display currency comes from the locale, never the `fo_currency` cookie — no exported function accepts a currency override except `priceTable()`, and the same (product, country, locale) must yield a byte-identical projection with any cookie value present in the environment, which is what keeps cached HTML identical for every visitor with no `Vary` (`plan/02` §3, `plan/03` §1). The client repaint island that consumes `priceTable` is **spec 008's, paid from 008's budget** (§3). New `en` keys: `catalog.tier.stems` (ICU plural — Polish few/many/other for 1/2/5/22 is a required test case, AC-23), `catalog.tier.size.{s,m,l}`, `catalog.tier.single`, `catalog.price.inclusive` (a legal formula per locale, not a translation — `plan/07` §4), `catalog.price.from`, `catalog.addon.{key}.{name,description}`, `catalog.addon.allergen`, `catalog.facet.{facet}.{value}`, `catalog.availability.{inStock,outOfStock,countryDemo,noPartner,fxUnavailable}`, `catalog.surcharge.{sunday,peakDay}`. `en-gb` overrides only where British wording differs; `de`/`pl` re-drafted by `pnpm i18n:draft` and therefore unreviewed, which correctly keeps those locales non-indexable. VAT rates render through `formatPercentFromBasisPoints`; no `Intl` object is constructed in this module (`fo/no-adhoc-intl`). **No client JavaScript**: spec 004 §14 A1's budget is 131 072 B Brotli per document and TASK-046 measured `/en` and `/de` at 129 638 B, so the headroom is 1 434 B and this module must add zero application bytes to any client chunk (AC-3 / AC-22 measure +-0 B, not "small"). Gates: `lint`, `typecheck`, `test-unit`, `i18n-check`, `catalogue-check`, `seo:validate`, `build`, `budget:client-js`. Tests: T-07, T-08, T-09, T-20, T-21. **Implemented on `task/TASK-067-price-projections-identity`, PR #52, `in_review` 2026-09-11.** `fromPrice` was already shipped in destination currency by TASK-065's `pricing/resolve.ts`, so the projection half is `fromPriceProjection()`; both are exported and documented as two halves of one figure. `PriceProjection` carries `destinationCountry`, `priceValidUntil` and `vatRateText` beyond §5.2's field list — the first two are the `Offer` inputs §6 names (`eligibleRegion`, `priceValidUntil`) and had to travel on the projection for `offerProjection()` to be derivable from it alone, which is what makes the identity structural; the third is §7's `formatPercentFromBasisPoints` call. The projection's `availability` field is deferred to TASK-068, which owns `availability()` and `AvailabilitySchema`; this task ships `catalogAvailabilityKeys` (the five `catalog.availability.*` keys) and `offerProjection`'s `availability`/`availabilityKey` pair, which TASK-068 drives through `{ inStock: false }`. `<=512 B` is read as UTF-8 bytes of `JSON.stringify(table)` (`serialisedByteLength()`). `currency.{code}` flag rows land in `static/index.ts` (EUR/GBP/PLN on). `moneyDecimalString()` joins spec 003's formatters so the JSON-LD and the HTML are digit-shifted by one implementation. `de`/`pl` re-drafted; two values hand-authored as `source: human, reviewed: false` because a machine echo cannot express them — `catalog.tier.stems` (AC-23's Polish few/many/other) and `catalog.price.inclusive` (§7's per-locale legal formula). No `en-gb` override: nothing differs in British English, asserted. `MAX_PRICE_TABLE_*`, `FROM_PRICE_LABEL_KEY` and `catalogAvailabilityKeys` are deliberately not barrel-exported (AC-2's committed test allows only schemas, value sets and functions). Gates local (CI billing-blocked): lint, typecheck, format:check clean; `pnpm test` 2 821 passed / 126 files, 0 failed; `i18n:check`, `catalogue:check`, `seo:validate`, `check:no-db` clean; cold `pnpm build` green; `budget:client-js` within budget with client chunk bytes 621 948 B on both `origin/main` and this branch (+-0 B).

## Read

- `specs/005-*.md` — read `## 0. Index` first, then only the sections the ACs below name
- `docs/codebase-map.md` — where everything lives
- `src/config/catalogue/*.data.ts`
- `plan/02`
- `plan/07`
- `plan/03`

## Carry-forwards

_None recorded._

## Escalations

_None recorded._

## Result

Done. PR [#52](https://github.com/itsahmeds/flowers-overseas/pull/52); `/review` pass recorded in `TASKS.md`.
