# TASK-048 — `SiteHeader` — the approved commerce anatomy: utility strip (cutoff, price-includes, guarantee), masthead with the `mark.svg` logo lockup at the inline start, wide search field with a dark Search button, labelled `Sign in` / `My orders` / `Basket (0)`, `EN · EUR` locale-switcher + currency chip, and the full category row from `categories.ts`; sticky, reserved-height, zero-CLS

Row: `TASKS.md` → TASK-048. Brief written by `pnpm tasks:migrate` (spec 001 §14 A15, AC-34);
keep it current by editing this file, not the row.

## Binding

Branch `task/TASK-048-site-header-commerce`. Spec §13's 2026-09-08 resolution note is binding and overrides §5 defaults where they differ. The §5.3 "56 px mobile, wordmark + switcher + currency chip" header is **superseded** by the canvas's commerce header; AC-7's testable content becomes: reserved fixed height per breakpoint (mobile masthead + scrollable category row, desktop utility strip + masthead + category row), header box byte-identical pre/post hydration, CLS ≤0.05, logo → locale home, `LocaleSwitcher` markup and the `src/modules/i18n` barrel unchanged (spec 003 AC-3 still green). Record the height numbers taken from the two artboards in the PR body — they are what the visual baselines pin. Design source of truth (match pixel-for-pixel): `docs/design/homepage-v1/README.md`, `docs/design/homepage-v1/tokens.css`, `docs/design/homepage-v1/homepage-desktop.dc.html`, `docs/design/homepage-v1/homepage-mobile.dc.html`, `docs/design/homepage-v1/identity.dc.html`, `content/brand/mark.svg`. **The search field is UI only in 004 — there is no search backend; spec 008 wires it.** Ship it as a real `<form>` whose labelled input and Search button are present, accessible and keyboard-operable, `action` pointing at an unpublished `site-links.ts` target and therefore rendered **disabled-free but inert** (no `action` to a non-200 URL — AC-14 forbids it): concretely, the form has no `action`/`method` and no client handler in Phase 0, with `aria-describedby` copy from `nav.search.help` saying search opens with the shop; the reviewer checks that submitting it neither navigates nor 404s. `Sign in` / `My orders` / `Basket (0)` are likewise labelled text-with-icon items whose targets are unpublished (010/013/014 own them) and so render as text, not links. Currency chip stays server-rendered text (`en`→EUR, `en-gb`→GBP, `de`→EUR, `pl`→PLN) with a localised `aria-label`, byte-identical with and without an `fo_currency` cookie, zero `Set-Cookie` (AC-8); the currency **menu** is spec 008's. Category row wraps rather than truncates (German compounds, `en-XA`); tap targets ≥44 px; no hover-only affordance. `Mark` component from TASK-045's icon set — never an inline `<svg>` at the call site. Photography is a placeholder until the founder supplies imagery: every `.photo` box in the canvas renders the `--color-photo` token gradient with its uppercase caption and **no `<img>`** (`plan/10` §3 honesty rule); the consolidated "photo slots" list is part of TASK-053's PR body. Tests: T-09, T-10, and the header states added to `/dev/components`. Search placeholder copy (founder): "Search flowers, occasions, a city or a country" — the search key in `nav.*` must say so in all locales; spec 008 wires the backend to match products, occasions, cities and countries. **Amended by `/review 31` (spec §14 A4, 2026-09-09):** the search band renders as the artboards literally draw it — placeholder text plus a `Search` label, no `<input>`/`<button>` — until spec 008 publishes search (the "labelled input, keyboard-operable, disabled-free but inert" clause is withdrawn as unsatisfiable at zero JS); the `LocaleSwitcher` and the currency chip live in the utility strip, not the category row; every rendered link meets 44 px. **Implemented on `task/TASK-048-site-header-commerce`, PR #31, `in_review` 2026-09-09; `/review 31` round 1 FAIL addressed in the same PR (spec §14 A4).** AC-7/AC-8 proven by `tests/e2e/header.spec.ts` (T-09, T-10) and `tests/unit/ui-site-header.test.tsx`; AC-14's header half proven in both directions, the published one through a mocked registry so a `published: true` flip is shown to produce `localePath(...)` with **no template edit**. **Measurements after the fix round:** client JS unchanged at **129 638 B Brotli on `/en` and `/de`** (budget 131 072 B) because the header ships **zero** client JavaScript; **reserved height 245 px mobile / 183 px desktop**, identical at 390 px and 412 px and at 1280 px and 1440 px in `en`, `en-gb`, `de`, `pl` and `ar-XB` (`en-XA` grows the desktop row to 263 px, the specified wrap); the currency chip at 390 px sits at x 327–371, y 75–105, inside the header's visible box; every rendered link measures 44 px tall at both artboard widths and the WhatsApp icon-link 44 × 44. **The three §14 A4 changes:** the search band is the text the artboards draw (no `<form>`, `<input>` or `<button>`, no focus stop, `nav.search.help` as the screen-reader sentence); the `LocaleSwitcher` and the chip moved to the utility strip on every breakpoint, which restored the canvas's single 52 px category row; every link is padded to 44 px from the header's wrapper, so `src/modules/i18n` is still untouched. **Two consequences the orchestrator should note:** the utility strip is 44 px rather than the artboards' 34 (it now carries links) and runs to three lines at 390 px (113 px), because spec 003's switcher is four language names — the canvas's compact `EN · EUR` control, which spec 008 owns with the currency menu, is what brings both numbers back down; and from `md` to `xl` the strip prints the short cutoff line and the help channel only, the two standing claims joining at `2xl`, because four claims plus the two controls need 1 375 px of the 1 328 px the 1440 px artboard has (`flex-wrap` keeps a longer translation wrapping rather than overflowing). Also: `mobileOrder` added to `src/config/categories.ts` so the mobile artboard's own row order is data and one `order-*` utility rather than a second list; Tailwind's arbitrary `min-[…]:` variant compiled the whole stylesheet down to its base layer with no error raised (reproduced twice on a clean `.next`), so named breakpoints only — pinned by an assertion. **Gates, all local (no CI on a draft):** lint, typecheck, **1 827** unit (one pre-existing failure on `origin/main`, see below), format, env, no-db, layout, i18n, build, budget (byte-identical), **268** e2e, **12** visual, **28** a11y — all green against `pnpm start` on :3600. **Carry-forward:** the four header baselines and the three shared ones (`en`, `de`, `ar-XB`) exist for **`darwin/` only**; the `linux/` half must come from the `visual` job's artifact on the next full CI run (the TASK-045 procedure). **Rebased on `origin/main` `b08e0ec`** (PR #30's footer and TASK-059 round 2 merged): conflicts were `layout.tsx` (both mounts kept), `playwright.config.ts` (main's glob form kept), the gallery page and its e2e (both sections kept, `SECTIONS` reordered so the footer follows the header and the contrast manifest is last), `TASKS.md`, and the three shared darwin baselines — `en`, `de`, `ar-XB` regenerated, plus `footer-en-mobile`/`footer-de-mobile`, which now show the sticky header overlaying the top of the colophon in an element screenshot (the footer's own markup is untouched). `tests/e2e/banner.spec.ts`'s five `main nav` selectors were re-scoped to `[data-fo-header-switcher] nav`, because the switcher left `<main>` for the header. **One pre-existing failure on `origin/main`, not this PR's:** `tests/unit/company-config.test.ts` asserts `company.description` against the desktop artboard, and TASK-059 round 2 rewrote the artboard to the first-person voice of §14 A5 while the catalogue still says "International flower relay…" — TASK-084 owns that copy. Reproduced with `origin/main`'s `messages/en.json` in place of this branch's. `pnpm lighthouse` not run: it needs a deployed preview and stays informational until TASK-056. **Round 2 addendum (§14 A4 addendum) — RESOLVED, implemented as mechanism (iii).** The orchestrator's final ruling (spec §14 "A4 addendum", now in the spec at `06828d0`) is (iii): `SiteHeader` returns a fragment — the utility strip first as a plain `<div data-fo-utility>` that scrolls with the page, then `<header role="banner" data-fo-header>` (masthead + category row) with `position: sticky; top: 0`. The strip is **not** a landmark (no `role="region"`, no new label key — "same copy" holds); its `tel:`/`wa.me` links, the `LocaleSwitcher` (still `nav[aria-label="Change language"]`) and the currency chip are unchanged, just outside the banner. Measured on the served build: sticky `[data-fo-header]` **132 px at 390 px / 138 px at 1440 px**, `[data-fo-utility]` 113 / 45, so the chrome AC-7 reserves is unchanged as the sum — **245 / 183** — with CLS still 0 and the JS-off boxes still identical to the hydrated ones. Chip at 390 px: x 327→371, y 75→105, inside `[data-fo-utility]`'s box and the viewport, neither box scrolling horizontally. Re-scoped over both elements: the 44 px-link, `href`-200, no-form-control and chip-visibility e2e assertions, the axe `include()`, the unit band slicing, and the four `header-*` baselines, which became eight (`header-{desktop,de,mobile,rtl}-{utility,banner}.png`). One stated deviation from the ruling's test plan: the scroll assertion runs on `/en` at 390×300 (scroll 600) and 1440×300 (scroll 235, the page's maximum) rather than on `/dev/components` — the gallery renders a **demo copy** of the header inside `<main>` in a bordered wrapper whose containing block is the header's own box, so that instance scrolls with the page by construction (measured: top 7 277 px after a 600 px scroll) and cannot show stickiness. Both widths assert masthead top 0 and the whole strip above the viewport. Zero client JS unchanged and byte-identical: 129 638 B Brotli on `/en` and `/de`. Earlier round-2 escalation, kept for the record: The addendum asks for `position: sticky` on an inner wrapper holding the masthead + category row with the outer `<header role="banner">` (all three bands, `[data-fo-header]`) left static. A sticky box is bounded by its parent's padding box, and the masthead + category row sit at the bottom of the header, so the inner wrapper has zero px of room: measured on the served DOM at 390 px it ends up at top **−25** (masthead off the viewport), not 0. Two structures do produce the addendum's numbers (sticky 132 mobile / 138 desktop, chrome total unchanged at 245 / 183), and each breaks one sentence of it. **(ii)** negative sticky `top` on the outer header — keeps the landmark, the `[data-fo-header]` scope and every AC-7 number, but needs the strip height as a per-breakpoint literal while the strip's height is content-driven (113 px <640 · 93 px 640–767 · 85 px 768–~890 · 65 px ~900–1279 · 45 px 1280–1535 · 65 px 1536–~1900 · 45 px at 1920; two boundaries are not Tailwind breakpoints and arbitrary `min-[…]:` variants are banned here), so any mapping leaves a 20 px strip sliver pinned at 900–1023 px or clips 20 px of the masthead at ≥~1900 px. **(iii)** strip and a sticky `<header>` as body-level siblings (one fragment, same DOM order) — exact at every width and locale, but the `banner` landmark stops containing the strip (needs a ruling: leave it outside a landmark, or add `role="region"` + a new `nav.utility.label` key), `[data-fo-header]` scopes the sticky part only (132 / 138, total still 245 / 183 over two elements), and the 44 px-link, chip-visibility, `href`, axe-scope, four `header-*` baselines and unit band-slicing assertions all re-scope. Also measured: the requested "scroll 600 px" e2e cannot run on `/en` — the Phase-0 homepage scrolls 138 px at 390 × 844 and 0 px at 1440 × 900, so it needs a short viewport or `/dev/components`. Question and evidence in PR #31 (`## Round 2 addendum`).

## Read

- `specs/004-*.md` — read `## 0. Index` first, then only the sections the ACs below name
- `docs/codebase-map.md` — where everything lives
- `src/modules/i18n`
- `docs/design/homepage-v1/README.md`
- `docs/design/homepage-v1/tokens.css`
- `docs/design/homepage-v1/homepage-desktop.dc.html`
- `docs/design/homepage-v1/homepage-mobile.dc.html`
- `docs/design/homepage-v1/identity.dc.html`
- `content/brand/mark.svg`
- `plan/10`
- `tests/e2e/header.spec.ts`
- `tests/unit/ui-site-header.test.tsx`
- `src/config/categories.ts`
- `tests/e2e/banner.spec.ts`

## Carry-forwards

_None recorded._

## Escalations

_None recorded._

## Result

Done. PR [#31](https://github.com/itsahmeds/flowers-overseas/pull/31); `/review` pass recorded in `TASKS.md`.
