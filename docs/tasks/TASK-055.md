# TASK-055 — Styling pass on the inherited surfaces: the `/` chooser, the localised 404 and the 500 boundary styled with the design system, and the spec 003 suggestion banner restyled under the consent sheet in the z-layer scale

Row: `TASKS.md` → TASK-055. Brief written by `pnpm tasks:migrate` (spec 001 §14 A15, AC-34);
keep it current by editing this file, not the row.

## Binding

Branch `task/TASK-055-chooser-error-banner-styling`. Spec §13's 2026-09-08 resolution note is binding and overrides §5 defaults where they differ. Design source of truth (match pixel-for-pixel): `docs/design/homepage-v1/README.md`, `docs/design/homepage-v1/tokens.css`, `docs/design/homepage-v1/homepage-desktop.dc.html`, `docs/design/homepage-v1/homepage-mobile.dc.html`, `docs/design/homepage-v1/identity.dc.html`, `content/brand/mark.svg`. (the chooser and the error documents are not on the canvas — build them from the tokens as centred `--measure` columns with the `Mark` lockup, `.display` heading and `.label` metadata, matching the masthead's type treatment so the three documents read as one site). Contracts that must not move: `/` stays 200, `noindex,follow`, **zero application JS** (so no client island, no consent banner, no suggestion banner on the chooser — it has no locale to suggest), zero `Set-Cookie`, no `Vary`; the 404 keeps status 404 and its x-default document; the 500 boundary renders **without** the client provider and without the machinery that just failed — TASK-046's zod-free locale data is what keeps that true, so re-assert it here. Each error document offers the locale home and, once published, the destinations section (unpublished → no link, AC-14). Suggestion-banner restyle is **tokens, spacing and z-layer only**: no behaviour change, spec 003 AC-28's matrix (T-28) re-run green unchanged, the overlay background token that spec 003 left as a deferred item resolved to a real token here, and the sheet ordering proven — consent above, suggestion below. Also close spec 003's deferred `role="status"` live-region pattern note (`docs/architecture.md` §2–§4) by making the banner's region the documented pattern the design system exports. Tests: T-14, T-15.

## Read

- `specs/004-*.md` — read `## 0. Index` first, then only the sections the ACs below name
- `docs/codebase-map.md` — where everything lives
- `docs/design/homepage-v1/README.md`
- `docs/design/homepage-v1/tokens.css`
- `docs/design/homepage-v1/homepage-desktop.dc.html`
- `docs/design/homepage-v1/homepage-mobile.dc.html`
- `docs/design/homepage-v1/identity.dc.html`
- `content/brand/mark.svg`
- `docs/architecture.md`
- `src/app/globals.css`
- `tests/visual/home.spec.ts`
- `src/modules/ui/layout/noticeShell.ts`
- `tests/e2e/notices.spec.ts`
- `src/modules/ui/primitives/a11y.tsx`

## Carry-forwards

- **From `/review 31` round 2 (2026-09-09):** the mobile menu `<button disabled>` in `SiteHeader` is a dead affordance (looks enabled, does nothing) — render the menu glyph as non-interactive text/icon until spec 008 publishes nav targets, or give it a visibly disabled treatment; same rule as §14 A4's search band.
- **From `/review 40`:** `<h1>` renders 28/42 px against the artboards' 34/46 — root cause `--text-display-s` clamp in `src/app/globals.css` (TASK-045); reconcile the token with TASK-059's system files. `tests/visual/home.spec.ts` suppresses the language banner but not the consent sheet, which hides the cutoff line in `home-desktop-hero.png`. **Implemented on `task/TASK-055-chooser-error-banner-styling`, PR #55, `in_review` 2026-09-11.** The four documents a visitor reaches without choosing anything share one skin: `src/modules/ui/layout/noticeShell.ts` (import-free Tailwind class strings — the two 500 boundaries are Client Components whose chunk Next attaches to every document, and `/` may reach no client module, so they cannot share a *component*) plus `layout/NoticeDocument.tsx` for the chooser and the 404. Centred `--measure` column, masthead lockup (26/40 mark, 19/26 wordmark, 0.04em), `.label` metadata (the status code — digits, so not copy), `.display` `<h1>`; the chooser's locale list is the wireframe's bordered grid with each endonym in the display voice and its path in the label voice (lowercase, `<bdi>`). Contracts asserted in `tests/e2e/notices.spec.ts` (T-14, 24 tests): `/` 200 · `noindex,follow` · zero `Set-Cookie` · no negotiation `Vary` · no consent sheet, no suggestion banner, no live region, no `<button>` · empty cookie jar after `networkidle`; 404 status 404 on `/nope`, `/does-not-exist`, `/en/does-not-exist`, `/de/nope`, `/pl/x/y`, x-default `lang`/`dir`, and every link on it resolves 200 (AC-14 — `destinations` is unpublished, so the second action is not rendered at all rather than as inert button-shaped text). Banner restyle is tokens/spacing/z-layer only: `bg-surface`/`border-border-strong`/`text-ink` (spec 003's deferred overlay-background item resolved) and `layer-banner` replacing a raw `z-50`; T-28's 46 tests re-run green unchanged, plus three new AC-13 tests (`elementFromPoint` proves the consent sheet paints above; the computed `z-index` equals `--layer-banner`/`--layer-overlay`; the live region is mounted and empty before there is anything to announce). Spec 003's deferred `role="status"` note is closed by `LiveRegion` in `src/modules/ui/primitives/a11y.tsx` — permanently mounted, `aria-live="polite"`, `aria-atomic` — which the banner implements (attributes pinned against it in the unit test, since a Client Component in `src/modules/i18n` may not import the `ui` barrel) and which `docs/architecture.md` §2/§3/§4 documents as the pattern for every later price/date/cutoff announcement. `SiteHeader`'s mobile menu `<button disabled>` is now a decorative `aria-hidden` glyph keeping the 44 px box (`/review 31` round 2) — the header contains no control at all; `nav.menu.label` kept with `retained: true` for spec 008. `chooser.intro` rewritten to the founder-approved wireframe's first-person copy (§14 A5). New: `src/config/company.data.ts` (import-free `TRADING_NAME`, in the check-layout manifest) and `errorHomePath()` in `error-document.ts`, both proved equal to `COMPANY.tradingName` and `localePath(locale,"home")`. Gates, all local (CI billing-blocked): lint, typecheck, `format:check`, `pnpm test` 2884/2884 (+86), `test:e2e` 462 (2 skipped), `test:a11y` 46, `test:visual` 24, `i18n:check`, `check-layout` (20 config modules), `check:no-db`, `seo:validate`, cold `pnpm build`, `budget:client-js` within budget. Measured against `e348670`: `/` 114.0 → 114.6 KB br (+699 B gzip, measured by fetching every script of the document — the cost of styling the last-resort 500, whose chunk Next attaches to every document), locale documents 119.5 → 121.4 KB br; both inside the 128 KB budget of §14 A1. Baselines regenerated (darwin only — no CI to write `linux/`): `home.png`, `header-mobile-banner.png`, and six new (`chooser-{desktop,mobile}`, `not-found-{desktop,mobile}`, `suggestion-banner-{desktop,mobile}`). **Two items not delivered, both escalated in the PR:** (1) the `--text-display-s` reconciliation — the shipped clamp is already byte-identical to `docs/design/system/tokens.css`'s `--text-display-s-fluid`, while the homepage artboards draw the hero `<h1>` at an inline 46/34 px and block 1 of the same system file plus every TASK-059 wireframe `<h1>` says 42 px, so the system files and the artboards disagree and the row says to escalate rather than pick; (2) no e2e/visual coverage of the two 500 documents, because reaching an error boundary needs a route that throws on purpose — asserted at unit level instead, and TASK-056's AC-26 needs the same mechanism. Also fixed in passing: an unescaped `|` in TASK-069's row, added by the `/review 54` docs commit, which failed `pnpm test`'s ledger check on main. `tests/visual/home.spec.ts` already suppressed the consent sheet (added in `/review 40`'s fix round) and `home-desktop-hero.png` has no cutoff line — verified, nothing to do.

## Escalations

_None recorded._

## Result

Done. PR [#55](https://github.com/itsahmeds/flowers-overseas/pull/55); `/review` pass recorded in `TASKS.md`.
