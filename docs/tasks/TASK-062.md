# TASK-062 — Price dataset, FX snapshot and `pnpm catalogue:check`: `prices.data.ts` for the 8 seeded countries inside the `plan/10` §2.3 bands with `surcharge_kind` rows and `active_from`/`active_to`, `fx.data.ts` (one dated ECB snapshot, `rate_ppm` integers), the `catalogue:check` CLI with all eight failure modes and its per-country coverage step summary, and the `catalogue-check` CI job on `needs: typecheck`

Row: `TASKS.md` → TASK-062. Brief written by `pnpm tasks:migrate` (spec 001 §14 A15, AC-34);
keep it current by editing this file, not the row.

## Binding

Branch `task/TASK-062-price-dataset-catalogue-check`. Spec 005 §13's 2026-09-09 resolution note is binding: Q1 EUR/GBP `x90`, PLN `x9`, HUF `x90`; Q2 ECB daily rates, 2.5% buffer, 48 h max age then destination currency; Q3 add-ons priced per country with their own VAT rate; Q4 plain stem-count / S-M-L / single tier names; Q5 30-minute signed quote, secret lands in spec 010; Q6 middle tier preselected via `product_tier.is_default`; Q7 surcharges as dated rows; Q8 all three schema amendments folded into spec 002's `0003` (spec 002 §14 A1); Q9 this spec owns the dataset in `src/config/catalogue/*.data.ts`; Q10 no money on destination-less hubs; Q11 EUR/GBP/PLN only behind `currency.{code}` flags. Psychological endings come from §13 Q1's binding answer — EUR/GBP `x90`, PLN `x9`, HUF `x90` — which means **`src/config/currencies.ts` (spec 003) must be corrected in this PR**: PLN moves from `x90` to `x9` and HUF from `x9` to `x90`, three lines plus their test, and `plan/10` §2.3's 149/199/269/359 zł bands become satisfiable. Surcharges are dated rows (+EUR 4 equivalent Sunday, +EUR 6 equivalent Valentine's/Women's Day), never a multiplier. `catalogue:check` exits non-zero with one line per problem for each of: SKU with no tier; SKU with no active price for a seeded country; two active rows for one (product, country, tier, surcharge); zero or two default tiers; a price outside its band; an unknown facet value; a `labelKey` missing from `messages/en.json`; a projection key set disagreeing with spec 002 §5.1 (AC-5) — one temporary-mutation fixture per mode. Job placement follows spec 001 §14 A9 (`needs: typecheck`, as `i18n-check` and `seed-check` do) and must be added to `scripts/branch-protection.ts`'s derived required set. CI minutes: draft first, run gates locally, push once, `gh pr ready` (TASK-057). Gates: `catalogue-check`, `lint`, `typecheck`, `test-unit`, `build`. Tests: T-03, T-04. **From TASK-061 (2026-09-09):** the `catalogue:check` mode "a `labelKey` missing from `messages/en.json`" needs the `catalog` namespace, which is TASK-067's AC-22 — seed the keys this task checks (marked `retained` until 067 uses them) rather than leaving the mode vacuous. Rebase on `origin/main` **before** `gh pr ready`, or the spine never runs. **Implemented 2026-09-09, PR #37 (draft -> ready, gates local).** One authored record per destination in `prices.data.ts` (currency, flower + standard VAT rate, the eight `plan/10` §2.3 bands, an authored tier ladder in each, the two surcharge amounts, the six add-on prices), expanded into **3 416** `country_price` rows (1 652 retail = 236 tiers x 7 destinations, 588 open-ended Sunday rows, 1 176 closed-window peak-day rows for 14 Feb / 8 Mar 2027) and **42** `addon_country_price` rows; `fx.data.ts` commits **9** euro-base ECB rates dated 2026-09-08 with `FX_BUFFER_BP = 250` applied at conversion time and `MAX_FX_AGE_HOURS = 48` reported, not failed. `catalogue:check` implements **13** modes (the eight AC-5 names plus `rounding-ending`, `float-money`, `addon-price`, `fx-snapshot`, `destination-drift`), one fixture each as a one-field mutation of the check's input, and prints the per-destination coverage table; new `catalogue-check` CI job on `needs: typecheck`, derived automatically into `branch-protection`. Tests: 54 new unit (`catalogue-prices` 21, `catalogue-check` 27, `catalogue-fx` 6) plus 7 amended files; suite 1 999 green; `budget:client-js` byte-identical to `ff58840`. **Three corrections called out in the PR, none optional:** (a) `src/config/currencies.ts` PLN `x90`->`x9` and HUF `x9`->`x90` per §13 Q1 (pre-accepted on ADR-0017's decisions-log row); (b) TASK-061's `catalog.facet.occasion.17Mai` -> `.mai17`, because spec 003's message-key alphabet forbids a digit-initial segment in **both** `MessagesSchema` and `MessageMetaManifestSchema`, so the old key could never be authored or reviewed — `messageKeyLeaf` now moves a leading number to the end and `CatalogMessageKeySchema` was tightened to that alphabet; (c) 89 `catalog.*` label keys seeded in `messages/en.json` as `retained` with `de`/`pl` drafts (AC-22 stays TASK-067's; the strings deserve a founder read). **Bands hold exactly; steps are approximate** (spec 005 §14 A1, the orchestrator's ruling on `/review 37`): every tier of every product sits inside its `plan/10` §2.3 band in every currency — no funeral row above 799 zł on PL or EUR 180 elsewhere — and the \"~+30% / +60%\" steps bend to fit (+12%/+23% in the narrowest band, +25%/+47% in the widest). The single funeral band is split into four adjacent, non-overlapping sub-bands spanning its own bounds because funeral pieces carry a `price_tier` facet. Round 1's smallest-tier-only reading is gone: 1 500 of 1 652 retail rows re-laddered, `band` mode now asserts every tier row, and `catalogue-prices.test.ts` re-checks all 1 652 against a transcription of §2.3 that does not read the dataset's own bands. **Seven destinations, not `plan/10` §2.1's eight:** GB is the first buyer market (ADR-0002), not a destination in spec 004's registry, and the `destination-drift` mode fails the moment the two sets disagree. **Founder/accountant action:** VAT rates for the six `demo` destinations are provisional and on `plan/06` §4 item 4's list; only PL's 8% / 23% are plan-documented. For TASK-066/067: `endsInRoundingStyle()` and the two FX constants are authored here and must be re-exported, never restated. **Round 1 fix (2026-09-09):** re-laddered under §14 A1 (1 500 rows moved; widest compression DE/PL/RO `funeral_luxury` at +12%/+23%, 37% of the +60% target); `band` mode asserts every tier and the schema refinement with it; `addon-price` mode asserts `vatRateBp === standardVatRateBp` (PL chocolates at 800 bp fails); new **14th mode `surcharge-amount`** gates each destination's +EUR 4 / +EUR 6 equivalents and every expanded surcharge row against a transcription in the check script (DE Sunday 4 000 fails); `ambiguous-price` also gates overlapping closed windows; nine FX magnitudes pinned; `docs/compliance/vat-rates.md` records the provisional per-destination rates and is the gate on a `demo` -> `live` flip. Five neuter-the-check mutations and three break-the-data mutations verified. Known residual, stated in the PR: `catalogue:check` reads the bands from the same file it checks, so a *widened band* is caught only by `catalogue-prices.test.ts`'s independent transcription, not by the gate.

## Read

- `specs/005-*.md` — read `## 0. Index` first, then only the sections the ACs below name
- `docs/codebase-map.md` — where everything lives
- `src/config/catalogue/*.data.ts`
- `src/config/currencies.ts`
- `plan/10`
- `messages/en.json`
- `scripts/branch-protection.ts`
- `plan/06`
- `docs/compliance/vat-rates.md`

## Carry-forwards

_None recorded._

## Escalations

_None recorded._

## Result

Done. PR [#37](https://github.com/itsahmeds/flowers-overseas/pull/37); `/review` pass recorded in `TASKS.md`.
