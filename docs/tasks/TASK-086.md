# TASK-086 — Agent orientation Dev OS: `docs/codebase-map.md` generator + `--check`, per-task brief files `docs/tasks/TASK-NNN.md` with lossless migration of every row's notes and the 400-character notes cap in the ledger check, `## 0. Index` blocks in every spec via `pnpm specs:index`, and agent/skill definitions that read the map and the brief first

Row: `TASKS.md` → TASK-086. Brief written by `pnpm tasks:migrate` (spec 001 §14 A15, AC-34);
keep it current by editing this file, not the row.

## Binding

Branch `task/TASK-086-agent-orientation-map-briefs`. This task creates the brief convention, so this file is its own first example. Internal tooling → light orchestrator review. Spec 001 §14 A15 is the whole specification; tests T-34…T-37.

## Read

- `specs/001-*.md` — read `## 0. Index` first, then only the sections the ACs below name
- `docs/codebase-map.md` — where everything lives
- `docs/tasks/TASK-086.md`

## Carry-forwards

_None recorded._

## Escalations

_None recorded._

## Result

_Pending._
