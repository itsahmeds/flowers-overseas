# SPEC-002 — Schema v1: every entity, portable Postgres, RLS, migrations, seed

| Field | Value |
|---|---|
| Status | approved |
| Phase | 0 |
| Plan refs | plan/01 §3 §4 §5 §8 §9 §10 · plan/02 §4 §5 §6 §7 §8 §9 §10 · plan/03 §5 §6 §7 §8 §9 §10 · plan/05 (page → data columns) · plan/06 §4 §5 · plan/07 §1 §3 §4 §10 · plan/08 §5 §7 §10 · plan/09 Phase 0 week 15–21 Sep · plan/10 §1 §2 §4 · plan/11 §1 §2 §3 · plan/12 §2 §4 §5 §7 · plan/13 §A A3 A4, §D D6 |
| ADRs | ADR-0015 (portable Postgres on Neon, R2, Auth.js — supersedes ADR-0008 for the data layer) · ADR-0009 (event-sourced orders) · ADR-0002 (first corridor UK→PL) · ADR-0003 (locales en/en-gb/de/pl) · ADR-0007 (index only true pages) · ADR-0014 (AI seed imagery) · ADR-0006 (no IP redirects) |
| Author / date | spec-writer via /spec · 2026-09-08 |
| Approved by / date | Ahmed (founder), delegated to orchestrator ("you know better") · 2026-09-08 |

## 1. Problem

Spec 001 shipped the repository, the gates and a deployable shell with no database: `DATABASE_URL` is a placeholder, `supabase/migrations/` holds a `.gitkeep`, `pnpm db:check` prints `no migrations`, the integration Vitest project contains a single `describe.skip` with a TODO citing this spec, and `fo/no-float-money` exists but is disabled. Everything the roadmap schedules after this week — `003` i18n, `005` catalogue and pricing, `006` seed import, `007`/`008`/`009` corridor, shop and PDP pages, `010` checkout, `011` for-florists, `012` admin — reads or writes rows that do not exist. `plan/09` reserves `002` for 15–21 Sep 2026: "schema v1 (all entities in 01 §4, migrations, RLS, seed scripts)".

ADR-0015 (accepted 2026-09-08) changed what that means. Supabase is not used for anything: the database is plain, portable Postgres on **Neon Free (Frankfurt)** today and self-hosted Hetzner Postgres later by dump-and-restore; every image and private upload lives in **Cloudflare R2**; identity is **Auth.js (or Better Auth)** with accounts and sessions in our own tables. So the schema must use nothing a stock Postgres lacks — no `auth.uid()`, no Supabase RLS helpers, no storage buckets addressed through a vendor SDK — and tenant isolation must be expressed as policies keyed on session variables that our own server sets per request. Neon's free tier also caps monthly compute hours, which makes "the database is never on the critical path of a cached page" a cost constraint as well as an SEO one, and makes a cheap usage probe part of the schema deliverable rather than an afterthought.

This spec is therefore the whole data layer at once: the Neon project and its two connection URLs, Drizzle config and generated types, every entity in `plan/01` §4.1 grouped by the module that owns it, the money rule as a column convention with a check constraint, versioned migrations each with a rollback, an RLS design portable to any Postgres, the Auth.js tables and the partner role model, R2 object keys and variant metadata (no bytes in the database), pg-boss's own schema, the order state machine as reference data plus database-level guards, idempotent seed scripts, the real `db:check`, the integration test suite that replaces `describe.skip`, nightly logical backups to R2, and the RoPA rows that make Neon and Cloudflare recorded processors. It ships no user-facing surface.

## 2. Scope

Each bullet is testable; the AC that covers it is in §9.

**Provisioning, connection, configuration**
- Neon project in **Frankfurt (eu-central-1)**, one database, two connection strings in the env contract: `DATABASE_URL` (pooled, PgBouncer transaction mode — used by web requests) and `DATABASE_URL_UNPOOLED` (direct — used by migrations, `db:check`, pg-boss and the backup workflow). Q6 fixes the pg-boss half.
- `src/lib/db.ts`: a single `postgres`-driver pool + Drizzle instance, `withDbContext(ctx, fn)` opening one transaction per request and issuing the `SET LOCAL` statements of §5.5 before any query, and `withAdminContext` / `withSystemContext` variants. No module constructs its own client; `lib/env.ts` is the only source of the URL.
- `drizzle.config.ts` (schema glob, `out` = the migrations directory of Q2, `dialect: "postgresql"`, `strict: true`, `verbose: true`) and `pnpm db:generate` / `db:migrate` / `db:rollback` / `db:studio` scripts. Generated types are committed (`CLAUDE.md`: "Generated DB types").
- **Deletion of `ALLOW_PLACEHOLDER_ENV`** from `src/lib/env.schema.ts`, `.env.example`, `docs/runbooks/vercel-setup.md`, `docs/architecture.md` §4 and both Vercel environments, together with the three Supabase keys (`NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`) and their entries in `PLACEHOLDER_VALUES` / `REAL_VALUE_REQUIRED`. This is the first task of this spec (spec 001 §14 A-record and `/review 7` carry-forward).
- New env keys: `DATABASE_URL_UNPOOLED`, `R2_ACCOUNT_ID`, `R2_BUCKET`, `R2_ACCESS_KEY_ID`, `R2_SECRET_ACCESS_KEY`, `R2_PUBLIC_BASE_URL`, `NEON_API_KEY` (optional; usage probe), `NEON_PROJECT_ID` (optional). `pnpm env:check` keeps `.env.example` and the zod schema in lockstep in both directions.
- Migrations directory decision (Q2) applied everywhere in one PR: `drizzle.config.ts`, `MIGRATIONS_DIR` in `scripts/db-check.ts`, the `DIRECTORIES` manifest in `scripts/check-layout.ts`, `docs/architecture.md` §2 and §5, `plan/01` §5's `supabase/migrations/` line recorded as superseded by ADR-0015 (the plan is not edited; the architecture doc carries the current truth).

**Schema, by owning module** (every entity of `plan/01` §4.1; full column notes in §5.1)
- `i18n`: `locale`, `message_catalog` (admin-editable overrides + translation-review metadata; the runtime UI catalogue stays repo JSON per `plan/03` §5).
- `geo`: `country`, `country_translation`, `country_locale_content`, `region`, `city`, `city_translation`, `postcode_zone`, `country_holiday`, `occasion`, `occasion_translation`, `occasion_country`, `currency`, `fx_rate`.
- `catalog`: `product`, `product_translation`, `category`, `category_translation`, `product_category`, `product_occasion`, `product_tier`, `addon`, `addon_translation`, `addon_country_price`, `country_price`, `media_asset`, `media_variant`, `product_media`, `product_media_alt`.
- `partners`: `fulfillment_partner`, `partner_translation`, `partner_member`, `partner_coverage`, `partner_blackout`, `partner_catalog_mapping`, `partner_application`, `payout`, `payout_line`.
- `customers`: `customer`, `address`, `recipient`, `recipient_address`, `consent_log`.
- `orders`: `order`, `order_line`, `order_event`, `order_status` (reference), `order_transition` (reference: `from_status × event → to_status`, seeded from `plan/11` §2), `order_assignment`, `substitution_note`, `delivery_proof`, `review`.
- `payments`: `payment`, `refund`, `webhook_inbox`.
- `notifications`: `outbox`, `outbox_attempt`.
- `analytics`/platform: `feature_flag`, `feature_flag_scope`.
- `admin`/ops: `audit_log`, `retention_policy`, `db_usage_sample`.
- `auth`: `users`, `accounts`, `sessions`, `verification_tokens` (Auth.js Drizzle-adapter shape, names pinned by Q1) plus `user_role` and the `partner_member` linkage above.
- Money convention on every monetary column: `*_minor bigint NOT NULL` paired with a `*_currency char(3)` (or a row-level `currency`) constrained by `CHECK (currency ~ '^[A-Z]{3}$')` and an FK to `currency`; never `numeric`, never `float`. `bigint` because HUF/RON magnitudes exceed comfort in `integer`. `fo/no-float-money` is **enabled** in `eslint.config.mjs` in the same PR (`plan/12` §2 "Money"; spec 001 §2 promised "enforced on real code from 005" — this spec brings it forward because 002 is the first code with money columns and the rule is free).
- No Postgres extension is created. `gen_random_uuid()` is core from PG13; `unaccent` (search) and `citext` (not used — case-insensitive email is a `lower(email)` unique index) stay out so the dump restores anywhere. Every timestamp is `timestamptz`, every calendar date is `date`, all text is `text` in NFC.

**RLS and roles (portable; §5.5 has the mechanism)**
- Two database roles created by migration `0001`: `app_owner` (owns every object; used only by migrations and `db:check`, over the direct URL) and `app_web` (the runtime role in `DATABASE_URL`, `NOSUPERUSER`, no `BYPASSRLS`, `GRANT`ed DML only). RLS therefore applies to the application by construction rather than by policy discipline.
- Per-request session variables set by `withDbContext`: `app.role` (`public` | `customer` | `partner` | `admin` | `system`), `app.user_id`, `app.partner_ids` (comma-separated), `app.request_id`. Every policy reads them via `current_setting('app.…', true)`; nothing reads a JWT.
- `ENABLE ROW LEVEL SECURITY` on every table plus an explicit deny-by-default (no permissive policy for `app.role = 'public'` except on the public catalogue/geo reference tables). Partner-scoped tables (`partner_*`, `payout*`, `order_assignment`, `delivery_proof`, and `order`/`order_line` through the assignment) are readable only when the row's `partner_id` is in `app.partner_ids`. Customer-scoped tables key on `app.user_id` via `customer.user_id`.
- `db:check` fails when a table has no RLS enabled or no policy, unless it is on a documented exemption list (pure reference data).

**Order integrity (DB-level, defence in depth)**
- `order_event` is append-only: `BEFORE UPDATE OR DELETE` trigger raises; `seq` is monotonic per order enforced by `UNIQUE (order_id, seq)` and assigned inside the transition transaction.
- `order.status` may change only when the session variable `app.allow_status_write = 'on'` is set — a flag only `orderService.transition` sets (spec 015). A `BEFORE UPDATE OF status ON "order"` trigger raises `ORDER_STATUS_DIRECT_WRITE` otherwise, and additionally rejects any `(from, event, to)` triple absent from `order_transition`. This is the database half of the guarantee whose lint half (`fo/no-direct-order-status-write`) has been active since spec 001 (ADR-0009, `plan/12` §2).
- `order_transition` reference rows are seeded from `plan/11` §2 verbatim so spec 015 reads guards from data instead of hard-coding them.

**Jobs, storage seam, probes**
- pg-boss installed and bootstrapped into its own schema (`pgboss`), owned by `app_owner`, with `app_web` granted usage; `src/jobs/index.ts` registry with a versioned zod payload schema per job name; queue names registered here for `media.derive_variants`, `retention.sweep`, `neon.usage_probe`, `db.backup_verify`, plus the names later specs implement (`order.route`, `assignment.timeout`, `notification.deliver`, `fx.refresh`, `sitemap.regenerate`, `review.request`, `payout.accrue` — registered as declarations only).
- `src/lib/storage.ts`: the R2 seam — `objectKey(kind, id, variant)`, `put`, `getSignedUrl(ttl)`, `delete`, `head` — typed and unit-tested against an in-memory fake. The S3-API implementation and the variant-generation worker are **spec 006** (§3); 002 ships the interface, the key convention, the bucket/env contract and the `media_variant` table.
- `GET /api/internal/db-health` (secret header `INTERNAL_CRON_SECRET`, `no-store`, `X-Robots-Tag: noindex`): `{ ok, latency_ms, latest_migration, pending_jobs }`. `GET /api/health` stays database-free so uptime pings never wake Neon (spec 001 AC-14 unchanged).
- `neon.usage_probe` (daily): reads the Neon API for compute-hours and storage consumed this billing period, writes one `db_usage_sample` row, logs at `info`, and logs `warn` + Sentry message at 70% and 90% of the free-tier cap (ADR-0015 "the cap is monitored from spec 002's health job").
- `.github/workflows/db-backup.yml` (nightly + manual): `pg_dump -Fc` over the direct URL, upload to R2 under `backups/YYYY/MM/DD/`, 30-day lifecycle, then `db.backup_verify` restores the newest dump into the CI Postgres 16 service and asserts row counts on five tables. ADR-0015 guardrail: "automated backups to R2 from the first migration".

**Seed (idempotent, `source = 'seed'`)**
- `seed/` scripts upserting by natural key (`country.iso2`, `product.sku`, `partner.code`, `locale.code`, `occasion.key`), never touching a row with `source = 'real'` (`plan/10` §4), runnable repeatedly and in CI: `pnpm db:seed`.
- Content: 4 locales (`en`, `en-gb`, `de`, `pl` — ADR-0003, A1); 8 countries `status='demo'` with currency, VAT rate, IANA zone and cutoff from `plan/10` §2.5 (PL, DE, UK explicitly named in this spec's minimum; FR, ES, IT, RO, NL from `plan/10` §2.1); 10 currencies with minor-unit exponents; PL/DE/UK holidays 2026–2027; the occasion set of `plan/10` §1.1 with `occasion_country` rules as data per `plan/03` §9, including the five dates of `plan/09` line 3 (Valentine's 14 Feb 2027, Women's Day 8 Mar 2027, Mothering Sunday 14 Mar 2027, DE Muttertag 9 May 2027, PL Dzień Matki 26 May 2027) — with D6's verification note; the **84-product skeleton** of `plan/10` §2.1 (40 bouquets, 14 arrangements, 8 plants, 10 funeral, 12 gift sets) with SKU, type, flower, colour, style, tier and `en` name/slug, tiers per `plan/10` §2.2, `de`/`pl` translations deliberately **absent** (absence is the noindex gate, §6); 6 add-ons with country prices; `country_price` rows from the A4 bands; 3 demo partners per seeded country with coverage, capacity 15/day and payout mappings; **zero** reviews, **zero** delivery photos, **zero** media assets (imagery is spec 006).
- `tests/fixtures/index.ts` reserved names filled: `occasionDates` (2026–2027, per country), `currencies` (minor units + expected `Intl` output), `addresses` and `phones` for PL/DE/UK per `plan/03` §8 — the same fixtures 003/005/009 consume.

**Gates, CI, docs**
- `pnpm db:check` becomes real (§5.2): rollback-file pairing (already), unique ordered versions (already), **schema drift** (apply all migrations to a scratch database, then `drizzle-kit generate` from the TS schema must produce an empty diff), **RLS coverage**, **money-pair coverage** (every `*_minor` column has a currency column in the same table or a documented exemption), **PII column ban** (no `email` column on `recipient`, §8).
- CI: `test-integration` runs for real against the existing Postgres 16 service container; `db-check` gains a service container; `db:seed` runs in the integration job so idempotency is proven twice per run; step summaries print applied-migration list, RLS coverage and the drift verdict.
- Docs: `docs/runbooks/database.md` (new: connect, generate, migrate, roll back one migration, restore a dump, create/drop a Neon branch, what to do when the compute cap is near); `docs/architecture.md` §1 diagram (Supabase nodes → Neon + R2), §2 layout, §3 module table (data owner = 002), §4 (the `ALLOW_PLACEHOLDER_ENV` row is deleted; a Neon-compute-cap row is added); `docs/compliance/ropa.md` rows 3–4 (Neon, Cloudflare) and the flow rows of `plan/07` §1.2 that this schema makes storable; `.env.example`; `README.md` scripts table.

## 3. Non-goals

- **UI of any kind** — no page, component, admin screen or form. `012` (admin), `007`–`011` (pages).
- **next-intl runtime**, locale routing, `messages/*.json`, formatters, pseudo-locale → **spec 003**. 002 ships the `locale` / `message_catalog` tables and the fixtures 003 consumes.
- **Catalogue query and pricing logic** (tier maths, FX buffer, psychological rounding, availability, "from" prices) → **spec 005**; cutoff/holiday/occasion-date evaluation → **spec 009** (`docs/architecture.md` §3 already assigns `geo` logic to 009). 002 ships the columns, the rule rows and the fixtures those functions must satisfy.
- **`orderService.transition` runtime** → **spec 015**. Decided here and stated plainly because the brief asks: `plan/11` §2 says "this machine, implemented in `015`", `plan/09` lists `015` as "order state machine + events + outbox", and `docs/architecture.md` §3 records the `orders` module as owned by 015. 002 ships the tables, the `order_transition` guard rows, the append-only trigger and the direct-write trigger; 015 ships the service, the event zod schemas, the outbox write and the buyer-visible projection. Routing (`016`), payments (`013`/`014`) unchanged.
- **R2 implementation**: the S3-API client, image upload, EXIF stripping and variant generation → **spec 006** (imagery pipeline, ADR-0014). 002 ships `lib/storage.ts` as an interface with a fake, the key convention, the `media_asset`/`media_variant` tables and the job declaration.
- **Seed *content***: product descriptions (60–90 words), category and occasion copy, corridor guides, imagery and its prompts → **spec 006** and the founder's content tasks. 002 seeds skeletons and `en` names/slugs only.
- **Consent banner, GA4, CRM sync, invoices** → 004, 023, 032, 018. The `consent_log` and `audit_log` tables exist here; their writers do not.
- **Hetzner migration** — deferred by ADR-0015 with a recorded revisit trigger. 002's only obligation is to use nothing that would block a dump-and-restore, and to prove it (AC-31).
- **Editing `CLAUDE.md`, `.claude/hooks/*` or `plan/*`.** Q2's answer may require a one-line guard change; that is a founder action listed in §12, not an edit by this spec.
- **Neon paid tier, PITR, read replicas.** Free tier plus nightly dumps to R2 is the Phase 0 posture (ADR-0015; `plan/13` §F).

## 4. User stories

- As the **founder**, I run `pnpm db:migrate && pnpm db:seed` against a fresh database and get a complete, honest demo dataset — eight countries, 84 products, three demo florists per country, a 2026–27 occasion calendar — so that specs 005–012 build against real rows instead of mocks.
- As the **backend implementer of spec 015**, the legal transitions are rows I can read and the database refuses an illegal or unguarded status write, so a bug in my service is caught by Postgres and not by a customer.
- As a **florist partner** (spec 027, Phase 2), I can only ever read rows belonging to my own `partner_id`, because the policy is in the database and does not depend on the route that queried it.
- As the **founder taking a country live**, I flip `country.status` from `demo` to `live` in admin and the corridor, category, occasion and product pages become indexable, because indexability is derived from data (`plan/10` §4) and no deploy is involved.
- As **Googlebot**, every price I see in an `Offer` is the single active `country_price` row for that (product, country) with VAT included, and no page is indexable while its translation is missing or unreviewed, because the schema makes both facts queryable (`plan/02` §9, `plan/03` §6).
- As a **recipient**, the platform holds my name, address and phone and no email address at all, deletes my contact details 90 days after delivery, and can prove it, because minimisation and retention are columns and a job rather than a policy document (`plan/07` §1.3, §10).
- As the **founder watching the bill**, a daily probe tells me how much of Neon's free compute allowance the month has used before it runs out mid-demo (ADR-0015).
- As a **future auditor (GDPR Art. 30)**, Neon (Frankfurt) and Cloudflare are recorded processors with data categories, basis, retention and transfer notes from the first migration.

## 5. Design

### 5.1 Data model changes (tables, columns, migrations + rollback)

Conventions applied to every table. Primary keys are `uuid` defaulting to `gen_random_uuid()`, except reference tables keyed by a natural code (`locale.code`, `currency.code`, `order_status.key`). Every table carries `created_at timestamptz NOT NULL DEFAULT now()` and, where it is ever updated, `updated_at timestamptz NOT NULL DEFAULT now()` maintained by one shared `set_updated_at()` trigger — sitemap `<lastmod>` reads these (`plan/02` §10: "the real max `updated_at`", never "now"). Seeded rows carry `source text NOT NULL DEFAULT 'seed' CHECK (source IN ('seed','real'))` on the eight tables `plan/10` §4 lists, which is what makes the seed idempotent and non-destructive. Status columns are `text` with a `CHECK` against an explicit value list rather than a Postgres `enum`, because adding a value to an enum inside a transaction is awkward and `CHECK` constraints dump and restore identically. Money is `*_minor bigint` + currency, per §2. Deletes are `ON DELETE RESTRICT` by default; `CASCADE` only from a parent to its own translation/variant rows.

**`i18n`** — `locale(code PK, bcp47, name, is_launch, rtl, fallback_code)`; `message_catalog(id, locale_code FK, namespace, key, value, source CHECK IN ('human','machine'), reviewed bool, reviewed_by, reviewed_at, source_hash, UNIQUE (locale_code, namespace, key))`. Design note, because `plan/01` §4.1 and `plan/03` §5 could be read as contradicting each other: the **runtime** UI catalogue stays `messages/{locale}.json` in the repo (spec 003, `plan/03` §5), and `message_catalog` is the database side of the same model — admin-editable overrides plus the review metadata (`source`, `reviewed`, `source_hash`) that `plan/03` §6's queue and the `noindex` gate need for DB-authored content. Nothing reads it at runtime until 003/012.

**`geo`** — `currency(code PK, minor_unit_exponent smallint NOT NULL, rounding_style CHECK IN ('x99','x90','x9','none'))` seeded EUR, GBP, PLN, RON, CZK, HUF, SEK, NOK, DKK, CHF. `country(id, iso2 UNIQUE, status CHECK IN ('demo','live','disabled'), currency_code FK, vat_rate_bp integer, iana_zone, same_day_cutoff_local time, delivery_days text[], sunday_delivery CHECK IN ('none','peak','always'), guide_published bool, supply_model CHECK IN ('principal','agent') NULL, source, created_at, updated_at)` — `vat_rate_bp` in basis points keeps VAT an integer (`plan/12` §2); `supply_model` is nullable until B1 (`plan/06` §4) and lives on `country` **and** on `order` (snapshot). `country_translation(country_id, locale_code, name, slug, UNIQUE (locale_code, slug))` carries the exonyms of `plan/03` §5. `country_locale_content(country_id, locale_code, state CHECK IN ('guide','live'), h1, intro_md, faq jsonb, local_flowers_md, taboos_md, version integer, reviewed bool, reviewed_by, reviewed_at, UNIQUE (country_id, locale_code, state))` is the corridor-page source of `plan/02` §5.2. `region`, `city(region_id, country_id, iana_zone NULL, is_indexable bool)`, `city_translation(city_id, locale_code, name, slug, UNIQUE (locale_code, country_id, slug))`, `postcode_zone(country_id, prefix, city_id)`, `country_holiday(country_id, date, name, closed bool, UNIQUE (country_id, date))`. `occasion(id, key UNIQUE, kind CHECK IN ('evergreen','seasonal'))`, `occasion_translation(occasion_id, locale_code, name, slug, intro_md, reviewed bool, UNIQUE (locale_code, slug))`, `occasion_country(occasion_id, country_id, rule_type CHECK IN ('fixed','nth_weekday','last_weekday','easter_offset','lent_sunday','none'), rule jsonb, observed bool, indexable_override bool NULL, promo_start_offset_days integer, UNIQUE (occasion_id, country_id))` — rule types verbatim from `plan/03` §9; the evaluator is spec 009. `fx_rate(base_code, quote_code, rate_ppm bigint, as_of date, source, UNIQUE (base_code, quote_code, as_of))` — parts-per-million integer, not a float.

**`catalog`** — `product(id, sku UNIQUE, product_type CHECK IN ('bouquet','arrangement','plant','funeral','gift_set','hamper','voucher'), primary_flower, colour_primary, style, price_tier CHECK IN ('essential','classic','premium','luxury'), substitution_class, vase_included bool, stem_count integer NULL, freshness_days integer, partner_only bool DEFAULT false, status CHECK IN ('draft','active','retired'), retired_at, source)` — the six facets of `plan/10` §1.1. `product_translation(product_id, locale_code, name, slug, description_md, seo_title, seo_description, translation_status CHECK IN ('machine','human'), reviewed bool, reviewed_by, reviewed_at, source_hash, UNIQUE (locale_code, slug), UNIQUE (product_id, locale_code))`. `product_tier(product_id, tier_key, label_key, stems integer, sort)`. `category`, `category_translation` (same translation shape), `product_category(product_id, category_id, sort)`, `product_occasion(product_id, occasion_id)`. `addon(id, key UNIQUE, kind, allergen_note_required bool)`, `addon_translation`, `addon_country_price(addon_id, country_id, retail_minor, currency_code, active_from date, active_to date NULL)`. `country_price(id, product_id, country_id, tier_key NULL, retail_minor bigint, currency_code FK, vat_rate_bp integer, surcharge_kind NULL CHECK IN ('sunday','peak_day'), active_from date NOT NULL, active_to date NULL, source)` with **`EXCLUDE USING gist`**-free portability: overlap is prevented by a partial unique index `UNIQUE (product_id, country_id, tier_key, surcharge_kind) WHERE active_to IS NULL` plus a `CHECK (active_to IS NULL OR active_to > active_from)`, so exactly one active retail row exists per (product, country, tier, surcharge) — this is what makes "schema price = visible price" (§6) a database fact and preserves the 30-day price history the Omnibus Directive needs (`plan/07` §2.1): rows are superseded by setting `active_to`, never updated in place.
Media, per ADR-0015 (keys, not bytes): `media_asset(id, kind CHECK IN ('product','delivery_proof','brand','partner'), bucket, object_key UNIQUE, mime, width, height, bytes, checksum_sha256, visibility CHECK IN ('public','private'), source CHECK IN ('ai','photo','partner'), generator_prompt_hash NULL, generator_seed NULL, exif_stripped bool NOT NULL DEFAULT false, review_state CHECK IN ('pending','approved','rejected'), created_at)`; `media_variant(media_asset_id, variant, object_key UNIQUE, format CHECK IN ('avif','webp','jpeg'), width, height, bytes, UNIQUE (media_asset_id, variant, format))` filled once by `media.derive_variants`; `product_media(product_id, media_asset_id, sort, is_primary bool, UNIQUE (product_id, media_asset_id))` with a partial unique index enforcing one primary per product; `product_media_alt(product_media_id, locale_code, alt text NOT NULL, UNIQUE (product_media_id, locale_code))` — alt text is per locale and stored, never generated at render (`plan/01` §6).

**`partners`** — `fulfillment_partner(id, code UNIQUE, legal_name, city_label, country_id, status CHECK IN ('demo','onboarding','active','paused','offboarded'), public_profile_opt_in bool DEFAULT false, notification_channel CHECK IN ('email','whatsapp'), contact_email, contact_phone, payout_currency_code, capacity_per_day integer, rating_90d_bp integer NULL, acceptance_rate_bp integer NULL, source)`; `partner_translation(partner_id, locale_code, display_name, blurb_md, reviewed bool)`; `partner_member(user_id FK users, partner_id, role CHECK IN ('owner','staff'), invited_at, accepted_at, UNIQUE (user_id, partner_id))` — the org-scoping mechanism of `plan/11` §1, with one person able to belong to several partners (chains); `partner_coverage(partner_id, city_id NULL, postcode_zone_id NULL, capacity_per_day integer, CHECK (city_id IS NOT NULL OR postcode_zone_id IS NOT NULL))`; `partner_blackout(partner_id, date, reason, UNIQUE (partner_id, date))`; `partner_catalog_mapping(partner_id, product_id, tier_key NULL, can_fulfil bool, partner_payout_minor bigint, currency_code, source, UNIQUE (partner_id, product_id, tier_key))`; `partner_application(id, business_name, city, country_id, coverage_note, capacity_per_day, registration_number, instagram_url, website_url, preferred_channel, language_code, contact_email, contact_phone, media_asset_ids uuid[], status CHECK IN ('new','contacted','rejected','converted'), converted_partner_id NULL)` — written by spec 011's form; `payout(id, partner_id, period_start, period_end, status CHECK IN ('draft','statement_sent','paid'), total_minor bigint, currency_code, statement_media_asset_id NULL)`; `payout_line(payout_id, order_id, amount_minor bigint, currency_code, kind CHECK IN ('order','adjustment','goodwill'), note)`.

**`customers`** — `customer(id, user_id NULL FK users, email_normalised text NOT NULL, name, phone_e164, country_iso2, marketing_consent bool DEFAULT false, redacted bool DEFAULT false, redacted_at, UNIQUE INDEX ON (lower(email_normalised)) WHERE redacted = false)`; `address(id, customer_id, kind CHECK IN ('billing'), lines text[], postal_code, city, country_iso2)`; `recipient(id, customer_id, full_name, phone_e164, is_self bool, linked_customer_id NULL, contact_redacted bool DEFAULT false, contact_redacted_at)` — **there is no `email` column on `recipient`, by design and by gate** (`plan/07` §1.3 "no recipient email at all"; `db:check` enforces it, AC-27); `recipient_address(recipient_id, lines text[], postal_code, city, country_id, postcode_zone_id NULL, delivery_note, place_kind CHECK IN ('home','work','hospital','funeral_home','hotel','cemetery','church') NULL)`; `consent_log(id, subject_kind CHECK IN ('visitor','customer','recipient'), subject_ref, anonymous_id NULL, choices jsonb, policy_version, occurred_at, source_ip_truncated NULL)` — truncated, never a full IP (`plan/07` §5).

**`orders`** — `order_status(key PK, is_terminal bool, buyer_projection CHECK IN ('placed','confirmed','with_florist','being_made','on_its_way','delivered','closed','cancelled'))` seeded with every internal state of `plan/11` §2 mapped to the buyer-visible projection that §2's design rules require. `order_transition(from_status FK, event, to_status FK, requires_actor CHECK IN ('buyer','partner','admin','system','any'), PRIMARY KEY (from_status, event))` seeded from the diagram and the event catalogue of `plan/11` §2 (`order.placed` … `country.status_changed`). `"order"(id, public_token UNIQUE, customer_id, recipient_id, destination_country_id, destination_city_id NULL, destination_postcode, delivery_date date, delivery_window, status FK order_status, buyer_currency_code, buyer_total_minor bigint, fx_rate_ppm bigint NULL, base_currency_code, base_total_minor bigint, vat_breakdown jsonb, supply_model, terms_version, recipient_snapshot jsonb NOT NULL, source, created_at, updated_at)` — `recipient_snapshot` is the point of `plan/11` §3 (later address-book edits must not rewrite history) and is the only place recipient contact data is retained past the 90-day sweep in pseudonymised form. `order_line(order_id, kind CHECK IN ('product','addon','surcharge'), product_id NULL, addon_id NULL, tier_key NULL, qty, unit_retail_minor, currency_code, partner_payout_minor NULL, payout_currency_code NULL, vat_rate_bp)`. `order_event(id, order_id, seq integer NOT NULL, type, from_status NULL, to_status NULL, actor text NOT NULL, payload jsonb, payload_version integer NOT NULL, occurred_at timestamptz NOT NULL DEFAULT now(), UNIQUE (order_id, seq))` — append-only by trigger; `actor` follows `plan/11` §1's `system:{job}` convention. `order_assignment(id, order_id, partner_id, offered_at, sla_expires_at, responded_at NULL, response CHECK IN ('accept','decline','timeout') NULL, reason NULL, attempt integer, UNIQUE (order_id, attempt))`. `substitution_note(order_id, proposed_by, proposal_md, media_asset_id NULL, resolution CHECK IN ('accepted','rejected','auto_accepted') NULL, resolved_at)`. `delivery_proof(order_id, media_asset_id, uploaded_by, uploaded_at, approved bool, public_use_consent bool DEFAULT false)` — asset `visibility = 'private'` and `exif_stripped = true` are enforced by a trigger on insert (`plan/01` §6, `plan/07` §1.2). `review(id, order_id UNIQUE NULL, source CHECK IN ('trustpilot','internal'), rating smallint CHECK (rating BETWEEN 1 AND 5), text, verified_order bool, moderation_status CHECK IN ('pending','published','hidden'), locale_code, published_at)` — `verified_order` provenance per Omnibus (`plan/07` §7); seed inserts none.

**`payments`** — `payment(id, order_id, provider CHECK IN ('stripe','mollie'), provider_ref UNIQUE, status, method, amount_minor bigint, currency_code, three_ds_outcome NULL, risk_score NULL, captured_at NULL)`; `refund(id, payment_id, provider_ref UNIQUE, amount_minor bigint, currency_code, reason, status, created_at)`; `webhook_inbox(id, provider, event_id, signature_verified bool, received_at, processed_at NULL, attempts integer, raw jsonb NOT NULL, UNIQUE (provider, event_id))` — the store-before-process rule of `plan/01` §9, ready for 013.

**`notifications`** — `outbox(id, event_id, order_id NULL, targets text[], payload jsonb, payload_version integer, status CHECK IN ('pending','sent','failed','dead'), available_at, attempts integer, last_error NULL)`; `outbox_attempt(outbox_id, attempt, at, status, error NULL, provider_ref NULL)`. Written by 015, consumed by 017.

**platform / ops** — `feature_flag(key PK, description, enabled_default bool)`, `feature_flag_scope(flag_key, scope CHECK IN ('global','country','locale','payment_method','partner'), scope_ref, enabled bool, UNIQUE (flag_key, scope, scope_ref))` — flags in the database with a 60 s cache, never in env (`plan/12` §2 "Flags", `plan/08` §10). `audit_log(id, actor_user_id NULL, actor_label, action, entity_table, entity_id, before jsonb NULL, after jsonb NULL, request_id, at)` — the country/partner status flips of `plan/10` §4 are audit-logged. `retention_policy(key PK, entity_table, basis, retention_days integer NULL, action CHECK IN ('delete','pseudonymise','keep'), legal_note)` seeded from `plan/07` §1.2 (recipient contact 90 days after delivery → pseudonymise; delivery photo 2 years → delete; buyer personal fields 3 years → pseudonymise with invoice archive kept; fraud signals 12 months; consent records 5 years after withdrawal; Q7 confirms). `db_usage_sample(id, captured_at, period_start, compute_hours_used numeric, compute_hours_cap numeric, storage_bytes bigint, source)` — one row per day, written by the probe.

**`auth`** — `users(id, email UNIQUE, email_verified, name NULL, image NULL)`, `accounts(...)`, `sessions(session_token UNIQUE, user_id, expires)`, `verification_tokens(identifier, token, expires, PRIMARY KEY (identifier, token))` in the shape the chosen library's Drizzle adapter expects (Q1 pins names; Auth.js v5 is the recommendation because ADR-0015 names it first and its adapter schema is documented and stable). Plus `user_role(user_id, role CHECK IN ('admin','ops','finance','content'), UNIQUE (user_id, role))` for the admin sub-roles of `plan/11` §1; the `vendor` role is `partner_member`, and `customer` is `customer.user_id`. Guest buyers have a `customer` row and no `users` row, which `plan/11` §1 requires. Recipients never authenticate (`/r/{code}` signed tokens, spec 029).

**Migrations and rollback.** Numbered SQL generated by Drizzle Kit, one concern per file, each with a hand-written `NNNN_name.down.sql` that `db:check` requires and the round-trip test executes: `0001_roles_and_helpers` (roles `app_owner`/`app_web`, grants, `set_updated_at()`), `0002_i18n_geo`, `0003_catalog_media`, `0004_partners`, `0005_customers`, `0006_orders_payments` (+ triggers), `0007_notifications_platform_ops`, `0008_auth`, `0009_rls_policies`, `0010_pgboss` (pg-boss's own `CREATE SCHEMA` run through the migration runner so the schema is versioned like everything else). Rollbacks drop in reverse dependency order; `0001.down.sql` reassigns ownership before dropping roles. Forward-only exceptions are not permitted in Phase 0 (no production data exists), which is exactly why the round-trip test is cheap to keep green from now on.

### 5.2 API / server actions / jobs / events (zod schemas named)

- `src/lib/db.ts` — `DbContext = z.object({ role: z.enum(["public","customer","partner","admin","system"]), userId: z.string().uuid().optional(), partnerIds: z.array(z.string().uuid()).default([]), requestId: z.string() })`; `withDbContext(ctx, fn)`, `withAdminContext(fn)`, `withSystemContext(job, fn)`. No exported raw client.
- `src/lib/storage.ts` — `ObjectKind = z.enum(["product","delivery_proof","brand","partner","backup"])`; `StorageObject = z.object({ bucket, objectKey, mime, bytes, checksumSha256 })`; interface `Storage { put, head, getSignedUrl, delete, objectKey }`; `InMemoryStorage` fake for tests. Implementation: spec 006.
- `src/jobs/index.ts` — `JobName` union and a `JobRegistry` mapping each name to a versioned payload schema. Implemented here: `MediaDeriveVariantsPayload` (declared, worker in 006), `RetentionSweepPayload = z.object({ dryRun: z.boolean().default(false) })`, `NeonUsageProbePayload = z.object({})`, `BackupVerifyPayload = z.object({ objectKey: z.string() })`. Declared only: `order.route`, `assignment.timeout`, `notification.deliver`, `fx.refresh`, `sitemap.regenerate`, `occasion.reminders`, `review.request`, `payout.accrue` (`plan/01` §8).
- `GET /api/internal/db-health` — `DbHealthResponse = z.object({ ok: z.boolean(), latency_ms: z.number(), latest_migration: z.string(), pending_jobs: z.number() })`; 401 without the `INTERNAL_CRON_SECRET` header; `no-store`; `X-Robots-Tag: noindex`. `POST /api/internal/jobs/tick` — the minute-cron pg-boss tick of `plan/08` §5, same secret, returns counts only.
- Seed entry points — `seed/index.ts` with `--only=<group>` and `--dry-run`; every upsert takes a zod-parsed row so a malformed seed file fails before touching the database.
- No server actions, no public API, no order events emitted (015).

### 5.3 UI (pages, components, states)

None. This spec adds no rendered surface: the two internal routes above return JSON and are `noindex` + secret-gated, and no component, message key or style is touched. States that would normally be enumerated here (loading / empty / error / demo / disabled) are the concern of the specs that render this data — notably the **demo** and **disabled** states, which this spec makes expressible as `country.status` and `fulfillment_partner.status` values so that 007/008 can render them without a code branch per country (`plan/10` §4).

### 5.4 Rendering & caching (per plan/01 §3)

Nothing is rendered, but two constraints on later rendering are set here and are cost as well as SEO constraints (ADR-0015: Neon Free caps monthly compute hours).

- **The database is never on the critical path of a cached page.** Indexable pages stay ISR per `plan/01` §3 with the tags already named there (`home:{locale}`, `country:{iso}`, `catalog:{country}`, `product:{id}`); a cache hit performs zero queries, so crawl volume does not consume compute hours. `generateStaticParams` prebuilding "live countries + top 50 products" (`plan/01` §3) is unchanged and remains the mechanism that keeps first-hit renders bounded.
- **`/api/health` stays database-free** so uptime monitors and Vercel checks never wake the compute; the database probe is the separate secret-gated route in §5.2, called by cron rather than by monitoring.
- `src/lib/cache.ts` (the noop seam from spec 001) is untouched; the first `invalidate()` caller is the admin status flip in spec 012 and the sitemap job in 007. This spec only records which mutations must eventually invalidate which tag, in `docs/runbooks/database.md`, so 007/012 inherit a list instead of guessing.
- Connection posture: web requests use the pooled URL and one short transaction per request (`withDbContext`); migrations, `db:check`, pg-boss and backups use the direct URL (Q6).

## 6. SEO considerations (mandatory)

This spec renders nothing, so its SEO surface is entirely *the data that later pages need in order to be indexable at all*. Every rule in `plan/02` that a page must obey has a column here, and where the rule cannot be enforced in the database it is at least made queryable so `seo-auditor` can check it.

- **Indexability is derived from data, never from code** (ADR-0007, `plan/10` §4). The inputs are `country.status`, `country.guide_published`, `country_locale_content.state`/`reviewed`, `occasion_country.observed`/`indexable_override`, `city.is_indexable`, `product.status`, `product_translation.reviewed`, and a count of active `fulfillment_partner` rows covering a city. A country going live is one `UPDATE` plus `invalidate(['country:PL'])` — no template edit, per `CLAUDE.md`. The six-product rule for a country category and the "≥6 products tagged for the occasion" rule for a country occasion page (`plan/02` §6) are countable from `product_category`/`product_occasion` joined to `country_price`; spec 008 enforces them in its loader, and the seed provides enough products per (country, category) for at least the PL set to pass so 008 can test both sides of the threshold.
- **URL pattern and localised slugs** (`plan/02` §4): slugs live in `*_translation.slug`, ASCII, lowercase, hyphenated, and are unique per locale — `UNIQUE (locale_code, slug)` on `product_translation`, `category_translation`, `occasion_translation`, `country_translation`, and `UNIQUE (locale_code, country_id, slug)` on `city_translation` (city names repeat across countries). Localised *path segments* (`send-flowers-to` / `blumen-verschicken` / `wyslij-kwiaty`) stay in `config/locales.ts` (spec 003), not in the database, because they are code-level routing. Seed slugs are ASCII-folded at seed time (`Kraków Spring` → `krakow-spring`) and the fixture set includes a Polish-diacritic case so 003's transliteration has something to prove.
- **Canonical**: one canonical URL per (locale, country, entity) is a consequence of the uniqueness constraints above plus the country in the shop path (`plan/02` §4.2). Nothing in the schema permits two active slugs for one entity in one locale, so a duplicate-canonical bug cannot originate in the data.
- **hreflang set** (`plan/02` §8): alternates are exactly the locales for which a translation row exists **and** is `reviewed` **and** whose country is indexable — a single query, which is why `plan/02` §8's "generated from the same data as the sitemap" is achievable. `reviewed = false` therefore removes a locale from the alternate set rather than emitting a link to a noindexed page. Spec 007 builds the generator; this spec guarantees the columns exist and are `NOT NULL`-constrained where the query depends on them.
- **Schema.org data** (`plan/02` §9): `Product` + exactly one `Offer` per country-scoped PDP is a database fact — the partial unique index on `country_price` guarantees exactly one active retail row per (product, country, tier, surcharge), and `country.vat_rate_bp` plus VAT-inclusive `retail_minor` guarantee "schema price equals the visible default price to the cent, VAT included". `priceValidUntil` reads `active_to`; `eligibleRegion` reads `country.iso2`; `deliveryTime`/`cutoffTime` read `country.same_day_cutoff_local` + `iana_zone`; `hasMerchantReturnPolicy` is static. `aggregateRating` is emitted only from `review` rows with `verified_order = true` and `moderation_status = 'published'`, of which the seed contains **zero** (`plan/07` §7, `plan/10` §2.1), so a fabricated-rating manual action is impossible in Phase 0 by absence of data. No `LocalBusiness`/`FloristShop` entity exists in the schema at all, which is the structural reason `plan/02` §9's prohibition cannot be violated later by accident.
- **Sitemap membership and `<lastmod>`** (`plan/02` §10): every entity, translation, price and content row carries `updated_at`, so `<lastmod>` can be the real maximum across the four rather than "now". Sitemap children map one-to-one onto queries over the indexability inputs above. `noindex` rows can never appear in a sitemap because both derive from the same predicate.
- **Retired products** (`plan/02` §7): `product.status = 'retired'` + `retired_at` supports the "200 with alternatives for 90 days, then 410" rule without a separate table or a code branch.
- **Thin-content risk**: the seed is where thin content would enter. Mitigations built into this spec: `de`/`pl` product translations are **absent**, not machine-filled, so no unreviewed page can be indexable; `country_locale_content` rows are seeded only for countries whose guide exists, keeping `guide_published = false` elsewhere (`plan/02` §5.1: no corridor page because a country exists in a list); zero reviews, zero delivery photos, zero partner profiles on indexable surfaces (`plan/10` §5). The 84-product skeleton carries names and slugs but **no descriptions** — a product with a null `description_md` must be non-indexable, and spec 005/008 enforce that; this spec makes the null visible rather than papering over it with generated filler.
- **CWV budget impact**: zero client JavaScript is added and no page changes, so spec 001's baseline stands. The indirect risk is TTFB on ISR misses if a render fans out into many queries or a cold Neon compute; §5.4 pins the mitigations (ISR, prebuilt live corridors, DB-free `/api/health`, one transaction per request) and `docs/runbooks/database.md` records the cold-start expectation so 007's Lighthouse runs are read correctly.
- **Crawl efficiency**: unchanged; nothing new is crawlable. The two internal routes are `noindex` + secret-gated and match spec 001's `robots.txt` disallow of `/api/`.

## 7. i18n considerations (mandatory)

No user-facing string ships, but this spec fixes the shape of every localised entity, so getting it wrong here is expensive later.

- **New message keys / namespaces**: none in `messages/*.json` (spec 003 owns the file). Two key *references* are introduced as data: `product_tier.label_key` and `addon.key`, which 005 resolves through the catalogue — data holds a key, never a translated literal, so a new tier never requires a migration to be renamed in three languages.
- **Locale-aware content tables and the reviewed flag**: every localisable entity has a sibling `*_translation` table keyed `(entity_id, locale_code)` with `slug`, the localised fields, and the review triple `translation_status` / `reviewed` / `reviewed_by` / `reviewed_at` / `source_hash` that `plan/03` §6 defines. `source_hash` is what makes drift detectable: changing the English source bumps the hash and flips dependants to stale. The `reviewed` flag is the `noindex` gate of §6 — this is the one mechanism that lets a solo founder run four locales honestly, and it is a column rather than a convention so `seo-auditor` and `/admin/translations` (012) read the same fact.
- **Locale set**: `locale` seeded with `en`, `en-gb`, `de`, `pl` (ADR-0003, `plan/02` §3, pending A1), each with `bcp47`, `rtl = false`, and a `fallback_code` (`en-gb` → `en`). `rtl` and `locale.fallback_code` exist now so the RTL-readiness promise of `plan/03` §4 costs no migration later.
- **Formatting via `Intl`**: nothing formats here, and the schema deliberately stores only formattable primitives — `*_minor` + `currency.minor_unit_exponent` (so `Intl.NumberFormat` can be correct for zero- and three-decimal currencies), `vat_rate_bp` as an integer, `fx_rate_ppm` as an integer, `date` for calendar dates, `timestamptz` for instants, `time` + `iana_zone` for cutoffs (`plan/03` §10: cutoffs are stored as local time plus zone and evaluated in the destination zone). No pre-formatted, locale-baked string is ever stored.
- **Address and phone formats**: `recipient_address.lines text[]` + `postal_code` + `city` + optional `place_kind` accommodates every country order in `plan/03` §8 without a per-country column set; the field order, labels and postcode regex stay in `config/address-formats.ts` (spec 003). Recipient identity is a single `full_name` per `plan/03` §7. Phones are E.164 (`phone_e164`), validated against the **destination** country by 003/005; the seed fixtures include valid and invalid PL/DE/UK postcodes and phones so that validation has a shared corpus.
- **Collation and diacritics**: text is UTF-8 NFC; no `citext` and no custom collation is created, so the dump restores on any Postgres (ADR-0015). Case-insensitive email uniqueness is a `lower(email)` index. Polish collation for city lists is `Intl.Collator` in the application, and `unaccent`-based search arrives with 005 — recorded here so nobody adds an extension in a hurry.
- **Translation review plan and `noindex` gating**: the seed ships `en` only. `de`/`pl` drafts arrive through spec 003's `i18n:draft` script and spec 012's review queue; until a row is `reviewed`, the page is not indexable and carries no hreflang alternate (§6). Corridor guides and legal text never ship machine-only, even noindexed (`plan/03` §6 gate 4) — enforceable here because `country_locale_content.reviewed` is `NOT NULL DEFAULT false` and the seed sets it `false` for every guide it does not have text for.

## 8. Compliance considerations (mandatory)

This is the spec where personal data becomes *storable*, so the controls have to exist before the first row does. No personal data is processed by this spec itself: it creates tables, seeds non-personal demo data, and — apart from the founder's own admin account, if one is created — stores nobody's information until spec 010/013/019 write to it.

- **Data flows added (→ RoPA)**. Two new processors and the flow rows of `plan/07` §1.2 are recorded in `docs/compliance/ropa.md` by the reviewer at PASS (AC-33):
  1. **Neon Inc. — database hosting (Frankfurt, eu-central-1)**. Subjects: buyers, recipients, florist partners, admin users. Categories: everything in §5.1 once written (contact details, delivery addresses, card messages, order and payment metadata, auth sessions). Purpose: operating the service. Basis: Art. 6(1)(b) for buyers, Art. 6(1)(f) for recipients (`plan/07` §1.2), Art. 6(1)(b) for partners. Retention: per `retention_policy` rows + nightly dumps to R2 kept 30 days. Transfers: data at rest in the EU; Neon is a US company, so onward access is covered by its DPA (SCCs/DPF) — DPA to be accepted and filed in `docs/compliance/` (founder action, §12). Sub-processor: AWS eu-central-1.
  2. **Cloudflare — object storage (R2) for product imagery, delivery photos, payout statements, database dumps**. Subjects: recipients (delivery photos may show doorsteps and people), partners, buyers (statements). Categories: image binaries with EXIF stripped before upload, object keys, and encrypted database dumps. Purpose: media delivery and backup. Basis: Art. 6(1)(b)/(f). Retention: delivery photos 2 years (`plan/07` §1.2), dumps 30 days by bucket lifecycle. Transfers: R2 buckets created with **EU jurisdiction restriction**; Cloudflare DPA accepted at account creation.
  The Supabase row that ADR-0008 would have produced is never written, and `plan/07` §1.4's "Supabase project in EU (Frankfurt)" is read as "Postgres per ADR-0015" without editing the plan.
- **Lawful basis and consent**: `retention_policy` is seeded with the basis and period for each flow of `plan/07` §1.2, which makes the privacy policy's tables (§1.6) generated from data rather than transcribed. `consent_log` records choices plus `policy_version` (Art. 7(1) proof, 5-year retention) and stores a truncated IP at most. Nothing in this spec sets a cookie or loads a script, so no consent is required to run it.
- **Data minimisation (`plan/07` §3, §1.3)**, enforced structurally rather than by review:
  - `recipient` has **no email column**; `db:check` fails if one appears (AC-27). Recipient contact is name, address, phone and the card message — nothing else.
  - Delivery-photo assets must be `visibility = 'private'` and `exif_stripped = true`; a trigger rejects a `delivery_proof` row referencing an asset that is not (AC-19). Public gallery use requires `public_use_consent = true`, default false.
  - `consent_log.source_ip_truncated` and `payment.risk_score` are the only fraud-adjacent fields; no full IP or device fingerprint column exists (12-month fraud retention applies to `webhook_inbox.raw` and is in the sweep).
  - `order.recipient_snapshot` is the deliberate exception to deletion: it is pseudonymised in place by the sweep (name initialised, phone dropped, street dropped, city kept for analytics per `plan/07` §1.2) so the order and its invoice survive the 10-year accounting obligation without the recipient's contact details.
- **Retention (`plan/07` §10)**: implemented, not promised. `retention.sweep` runs daily and applies each `retention_policy` row: recipient contact pseudonymised 90 days after delivery unless a dispute is open or the recipient became a customer; delivery photos deleted at 2 years (R2 object and row); buyer personal fields pseudonymised at 3 years with the invoice archive retained and `customer.redacted = true`; fraud signals at 12 months; consent records 5 years after withdrawal. Integration tests drive the clock (AC-20). Periods need founder confirmation (Q7); the seeded values are the plan's.
- **DSAR (`plan/07` §1.5)**: the schema supports export and erasure without special cases — `customer.redacted` / `recipient.contact_redacted` are the "pseudonymise, keep the invoice, flag it" mechanism §1.5 specifies. The self-service routes are spec 019; the runbook (`docs/runbooks/dsar.md`) gains the SQL-level description of what erasure means, since it can now be stated exactly.
- **RLS as the tenant-isolation control**: the partner-scoped policies of §5.5 are the technical measure behind `plan/07` §1.1's "florist partner is a processor for recipient delivery data acting on our instructions" — a partner can read the recipient data of orders offered to them and nothing else, and the boundary is enforced by Postgres rather than by every future query. `app_web` has no `BYPASSRLS`, so a forgotten `WHERE partner_id =` clause leaks nothing. Both a positive and a negative case per partner-scoped table are required tests (AC-16, AC-17), which is `plan/12` §4's integration expectation ("RLS: vendor cannot read another partner's orders") made concrete.
- **Price display (`plan/07` §4, `CLAUDE.md`)**: `country_price.retail_minor` is VAT-and-delivery inclusive by definition, exactly one row is active per (product, country, tier, surcharge), and surcharges are rows (`surcharge_kind`) rather than runtime arithmetic — so "price shown = price charged = schema price" is a data invariant, and the 30-day-lowest-price history the Omnibus Directive requires is the natural consequence of superseding rather than updating rows. `order.vat_breakdown` stores the computed breakdown per rate for invoicing (`plan/06` §4), and `order.supply_model` lets the principal/agent answer (B1) change invoices without a migration.
- **Geo-blocking (EU 2018/302, `plan/07` §3)**: prices are keyed on **destination** country only. There is no buyer-country dimension on `country_price` and no column that could gate access by buyer nationality or location, so the discriminatory-pricing pattern is not expressible. `customer.country_iso2` exists for invoicing and payment-method availability, never for access control (ADR-0006).
- **Accessibility**: no rendered surface, so WCAG conformance is not assessable here — the one accessibility-relevant obligation this spec carries is alt text, and it is discharged structurally: `product_media_alt.alt` is `NOT NULL` per locale and a primary product image without an alt row in a locale makes that locale non-indexable (`plan/01` §6: never auto-generated at render). That is the substantive justification for not having more to say.
- **Logs and PII (`CLAUDE.md`)**: no query text, parameter, row or object key is logged. `withDbContext` logs `request_id`, `app.role` and duration only. The logger redaction list is widened from `name` to `*name` (so `full_name`, `legal_name`, `recipient_name` are caught) and gains `card_message`, `phone_e164`, `postal_code`, `session_token`, `public_token` and `object_key` — the carry-forward recorded on `/review 5` ("spec 002/005 should widen `name` redaction to `*name`"), now due. `order.public_token` and `session_token` never appear in a log, a URL emitted by this spec, or an error message.
- **Security (`plan/01` §9)**: `app_web` is a least-privilege role (DML only, no DDL, no `BYPASSRLS`, no superuser); `app_owner` credentials exist only in the direct URL used by migrations and CI. `webhook_inbox` implements store-before-process ahead of 013. Backups are uploaded to a private R2 bucket with a lifecycle rule; the dump contains personal data once real orders exist, so the runbook requires the bucket to be non-public and the restore drill to use a scratch database (AC-30).

## 9. Acceptance criteria

**Provisioning, env, connection**
- **AC-1** A Neon project exists in Frankfurt; `DATABASE_URL` (pooled) and `DATABASE_URL_UNPOOLED` (direct) are set in `.env.local`, Vercel preview and Vercel production; `pnpm env:check` exits 0 and both `.env.example` and the zod schema list exactly the same keys.
- **AC-2** `ALLOW_PLACEHOLDER_ENV` appears nowhere in the repository (`grep -r` returns 0 hits outside `specs/`, `docs/adr/`, `TASKS.md` and this spec's own history), is absent from both Vercel environments, and the `NEXT_PUBLIC_SUPABASE_URL` / `NEXT_PUBLIC_SUPABASE_ANON_KEY` / `SUPABASE_SERVICE_ROLE_KEY` keys are deleted from the schema, `.env.example`, `PLACEHOLDER_VALUES` and `REAL_VALUE_REQUIRED`. A build with a `.env.local` carrying a placeholder `DATABASE_URL` fails in `preview`/`production` and names only that key.
- **AC-3** `src/lib/db.ts` exports no raw client: a lint/unit assertion proves that the only exported query surface is `withDbContext`, `withAdminContext` and `withSystemContext`, and that every one of them issues `SET LOCAL app.role`, `app.user_id`, `app.partner_ids`, `app.request_id` before the first statement of the transaction.
- **AC-4** `pnpm db:migrate` against an empty Postgres 16 database exits 0 and creates every table of §5.1; `pnpm db:rollback --to 0000` returns the database to zero tables (no leftover type, function, trigger, role or schema), and the pair is re-runnable.

**Schema invariants**
- **AC-5** Every monetary column is named `*_minor`, is `bigint`, and has a currency column in the same table with an FK to `currency`; `pnpm db:check` fails when a `numeric`/`double precision` column matching `/(price|amount|total|payout|fee)/i` is introduced, proven with a temporary fixture migration.
- **AC-6** `fo/no-float-money` is enabled in `eslint.config.mjs` for `src/`, `seed/` and `scripts/`; `pnpm lint` fails on a fixture assigning `const total = 45.9` and passes for `const total_minor = 4590`.
- **AC-7** No Postgres extension is created by any migration (`SELECT extname FROM pg_extension` returns only `plpgsql` after a full migrate), no `enum` type is created, and every status column has a `CHECK` value list.
- **AC-8** `UNIQUE (locale_code, slug)` exists on `product_translation`, `category_translation`, `occasion_translation` and `country_translation`, and `UNIQUE (locale_code, country_id, slug)` on `city_translation`; inserting a duplicate slug in the same locale fails.
- **AC-9** At most one active `country_price` row exists per (product, country, tier, surcharge): inserting a second row with `active_to IS NULL` fails, and superseding by setting `active_to` then inserting succeeds and preserves the previous row.
- **AC-10** Every table has `updated_at` maintained by the shared trigger where it exists: an `UPDATE` that changes no other column still advances `updated_at`, so sitemap `<lastmod>` cannot go stale.
- **AC-11** `product_media` permits exactly one `is_primary = true` row per product, and `product_media_alt.alt` is `NOT NULL`.

**Order integrity**
- **AC-12** `order_event` is append-only: `UPDATE` and `DELETE` on any row raise an exception naming the table; `INSERT` with a duplicate `(order_id, seq)` fails.
- **AC-13** `UPDATE "order" SET status = …` without `app.allow_status_write = 'on'` raises `ORDER_STATUS_DIRECT_WRITE`; with the flag set but a `(from, event, to)` triple absent from `order_transition` it raises `ORDER_TRANSITION_ILLEGAL`; with the flag set and a legal triple it succeeds.
- **AC-14** `order_transition` contains a row for every arrow in `plan/11` §2 and `order_status` a row for every state, each mapped to a buyer-visible projection value; a test enumerates the diagram's transitions from a fixture list and asserts set equality in both directions (no missing arrow, no invented one).
- **AC-15** `fo/no-direct-order-status-write` still fires (spec 001 AC-7 regression) and the new trigger fires independently: a raw SQL update executed from a test — i.e. bypassing the lint rule entirely — is rejected by the database.

**RLS**
- **AC-16** With `app.role = 'partner'` and `app.partner_ids = '{A}'`, a query over `partner_catalog_mapping`, `partner_coverage`, `partner_blackout`, `payout`, `payout_line`, `order_assignment` and `delivery_proof` returns only partner A's rows; the same query with `app.partner_ids = '{B}'` returns zero of A's rows, and an `UPDATE`/`DELETE` against A's rows as B affects 0 rows.
- **AC-17** With `app.role = 'public'`, reads succeed on the public reference set (`country`, `country_translation`, `country_locale_content`, `city`, `product`, `product_translation`, `country_price`, `category*`, `occasion*`, `media_asset` where `visibility = 'public'`, `review` where `moderation_status = 'published'`) and return zero rows on `customer`, `recipient`, `order`, `payment`, `outbox`, `audit_log`, `users`, `sessions` and every partner-scoped table.
- **AC-18** The runtime role has no escape hatch: `SELECT rolbypassrls, rolsuper FROM pg_roles WHERE rolname = 'app_web'` returns `false, false`; `app_web` cannot `CREATE TABLE`, cannot `ALTER` an existing table, and cannot read `pgboss` internals it was not granted.
- **AC-19** A `delivery_proof` insert referencing a `media_asset` with `visibility = 'public'` or `exif_stripped = false` is rejected; the same insert with a private, EXIF-stripped asset succeeds.
- **AC-20** `retention.sweep` with a clock fixture 91 days past delivery pseudonymises the recipient contact fields and `order.recipient_snapshot` (city retained, phone and street removed) while leaving the order, its lines and its payment intact; a dispute-open order is skipped; `--dry-run` writes nothing and reports the same counts.

**Jobs, storage, probes, backup**
- **AC-21** pg-boss is bootstrapped by migration `0010` into the `pgboss` schema; `pnpm db:check` reports it present; a job enqueued and consumed in an integration test completes exactly once, and its payload is rejected when it fails the registered zod schema.
- **AC-22** `src/lib/storage.ts` exports the interface of §5.2 with an `InMemoryStorage` fake; `objectKey()` is deterministic and collision-free for (kind, id, variant) and is unit-tested; no `media_asset` or `media_variant` column stores binary data (`db:check` asserts no `bytea` column exists anywhere).
- **AC-23** `GET /api/internal/db-health` returns 401 without the secret header and, with it, 200 + `no-store` + `X-Robots-Tag: noindex` and a body validating against `DbHealthResponse` whose `latest_migration` equals the newest applied migration name. `GET /api/health` performs **zero** database queries (asserted by a query counter, not by inspection).
- **AC-24** `neon.usage_probe` writes exactly one `db_usage_sample` row per run, is idempotent for a given day, logs `warn` when usage ≥70% and ≥90% of the cap (driven by a mocked API response), and degrades to a single `info` line without failing the job when `NEON_API_KEY` is unset.
- **AC-25** The nightly backup workflow produces a `pg_dump -Fc` artefact in R2 under `backups/YYYY/MM/DD/`, and `db.backup_verify` restores the newest dump into a scratch database and asserts row counts on `country`, `product`, `country_price`, `fulfillment_partner` and `order_transition`. The R2 bucket is not public and carries a 30-day lifecycle rule.

**Gates and CI**
- **AC-26** `pnpm db:check` exits non-zero, one line per problem, for each of: a migration without its `.down.sql`; a duplicate version prefix; **schema drift** (a column added in TS but not in a migration, and the reverse); a table with RLS disabled or with no policy and not on the exemption list; a `*_minor` column with no currency column; a `bytea` column. It exits 0 on the merged tree and prints the applied-migration count and RLS coverage.
- **AC-27** `db:check` fails if a column named `email` (or matching `/e[-_]?mail/i`) is added to `recipient` or `recipient_address`, with a message citing `plan/07` §1.3.
- **AC-28** CI: `test-integration` runs the real suite against the Postgres 16 service container (no `describe.skip` remains anywhere in `tests/integration/`), `db-check` runs against a migrated database, and `db:seed` runs twice in the same job with the second run reporting zero inserts and zero updates.
- **AC-29** Seed idempotency and safety: `pnpm db:seed` run three times produces identical row counts and identical `updated_at` values after the first run; a row manually flipped to `source = 'real'` is left untouched by a subsequent seed (asserted per table that carries `source`).
- **AC-30** `docs/runbooks/database.md` exists, is listed in the runbook index, and its restore drill has been executed once and recorded (timestamped note with the dump key and the row counts observed).

**Portability, data, docs**
- **AC-31** Portability proof: a `pg_dump` of the seeded database restores into a stock `postgres:16` container with zero errors and zero warnings about missing extensions, roles or functions, and `db:check` passes against the restored copy. This is ADR-0015's dump-and-restore promise, tested rather than asserted.
- **AC-32** Seed content matches the plan: 4 locales; 8 countries all `status = 'demo'` with currency, `vat_rate_bp`, `iana_zone` and cutoff per `plan/10` §2.5; 84 products in the 40/14/8/10/12 split of `plan/10` §2.1, each with an `en` translation carrying name and slug and **no** `de`/`pl` row; 6 add-ons with country prices; `country_price` rows within the A4 bands for every (product, country) pair seeded; ≥3 demo partners per country with coverage and payout mappings for all 84 products; `occasion_country` rules yielding the five dates of `plan/09` line 3; `country_holiday` rows for PL/DE/UK 2026–2027; **zero** rows in `review`, `delivery_proof` and `media_asset`.
- **AC-33** Documentation and records: `docs/architecture.md` §1 diagram shows Neon + R2 and no Supabase node, §2 shows the Q2 migrations path, §3 credits spec 002 for the data layer and lists `src/lib/db.ts` and `src/lib/storage.ts`, and §4's `ALLOW_PLACEHOLDER_ENV` row is deleted; `scripts/check-layout.ts` and the doc agree (the existing `tests/unit/architecture-doc.test.ts` is green); `docs/compliance/ropa.md` carries the Neon and Cloudflare rows plus the `plan/07` §1.2 flow rows, and the placeholder row 3 is gone; `README.md` documents every new script.
- **AC-34** `tests/fixtures/index.ts` reserved exports are non-empty and typed: `occasionDates` covers 2026–2027 for PL/DE/UK including the five `plan/09` dates, `currencies` covers the ten seeded currencies with minor-unit exponents and expected `Intl` output per launch locale, `addresses` and `phones` cover valid and invalid PL/DE/UK cases per `plan/03` §8.

## 10. Test cases

| ID | Layer | Given / When / Then | Covers AC |
|---|---|---|---|
| T-01 | integration (CI) | Given an empty PG16 service container / When `pnpm db:migrate` / Then exit 0 and every §5.1 table, trigger, function and role exists (introspection snapshot committed) | AC-4 |
| T-02 | integration | Given a fully migrated database / When `db:rollback --to 0000` / Then `pg_class`, `pg_type`, `pg_proc`, `pg_roles` and `pg_namespace` show no leftover object; repeat migrate/rollback twice | AC-4 |
| T-03 | unit | Given the env schema / When `.env.example` is diffed against `ENV_KEYS` / Then equal in both directions and neither contains `ALLOW_PLACEHOLDER_ENV` or a Supabase key | AC-1, AC-2 |
| T-04 | integration (CI job) | Given `.env.local` with the placeholder `DATABASE_URL` and `VERCEL_ENV=production` / When `pnpm build` / Then non-zero, naming only `DATABASE_URL` | AC-2 |
| T-05 | unit | Given `withDbContext` with a stubbed driver / When a query runs / Then the first four statements are the `SET LOCAL` set in order; the module exports no client | AC-3 |
| T-06 | integration | Given introspection of the migrated schema / When the money-pair, `bytea`, extension, enum and CHECK-list rules are applied / Then no violation | AC-5, AC-7, AC-22 |
| T-07 | unit (ESLint RuleTester) | `const total = 45.9`, `parseFloat(price)`, `amount.toFixed(2)` invalid; `total_minor`, `retailMinor` valid; rule enabled for `src/`, `seed/`, `scripts/` | AC-6 |
| T-08 | integration | Duplicate `(locale, slug)` per translation table rejected; `city_translation` duplicate allowed across countries, rejected within one | AC-8 |
| T-09 | integration | Second open-ended `country_price` for the same (product, country, tier, surcharge) rejected; supersede-then-insert succeeds; the superseded row is still readable with its `active_to` | AC-9 |
| T-10 | integration | `UPDATE country SET status = status` advances `updated_at`; the same for `product`, `product_translation`, `country_price`, `country_locale_content` | AC-10 |
| T-11 | integration | Second `is_primary` product image rejected; `product_media_alt` insert without `alt` rejected | AC-11 |
| T-12 | integration | `UPDATE`/`DELETE` on `order_event` raise; duplicate `(order_id, seq)` rejected; append with next `seq` succeeds | AC-12 |
| T-13 | integration | Status update without the session flag raises `ORDER_STATUS_DIRECT_WRITE`; with flag + illegal triple raises `ORDER_TRANSITION_ILLEGAL`; with flag + legal triple succeeds and the trigger writes nothing else | AC-13, AC-15 |
| T-14 | unit + integration | Given a fixture list transcribed from `plan/11` §2 / When compared with `order_transition` and `order_status` rows / Then set equality both ways; every state has a buyer projection | AC-14 |
| T-15 | unit (RuleTester regression) | spec 001's order-status fixtures still fail lint after the rule config is touched | AC-15 |
| T-16 | integration | For each partner-scoped table: as partner A read/update/delete A's rows (allowed) and B's rows (0 rows); as B the mirror image | AC-16 |
| T-17 | integration | As `app.role = 'public'`: the public reference set returns rows; the private set returns 0 rows; `media_asset` returns only `visibility = 'public'`; `review` only `published` | AC-17 |
| T-18 | integration | `pg_roles` flags for `app_web`; `CREATE TABLE`, `ALTER TABLE`, `SET ROLE app_owner` and a read of an ungranted `pgboss` table all rejected | AC-18 |
| T-19 | integration | `delivery_proof` insert with a public or non-stripped asset rejected; with a private stripped asset accepted; `public_use_consent` defaults false | AC-19 |
| T-20 | integration | Seeded order 91 days past delivery / When `retention.sweep` runs with an injected clock / Then recipient contact and snapshot pseudonymised, city kept, order/lines/payment intact; dispute-open order skipped; `--dry-run` mutates nothing | AC-20 |
| T-21 | integration | pg-boss present in the `pgboss` schema; enqueue → consume once; a payload failing its zod schema is rejected before the handler runs | AC-21 |
| T-22 | unit | `objectKey()` determinism and collision-freedom over a table of (kind, id, variant); `InMemoryStorage` satisfies the interface contract (put/head/get/delete round-trip) | AC-22 |
| T-23 | e2e (Playwright, preview) + unit | `/api/internal/db-health` 401 without the header, 200 with it, headers and body shape correct, `latest_migration` matches; `/api/health` executes 0 queries (counter assertion in a unit test) | AC-23 |
| T-24 | unit + integration | Mocked Neon API at 50/70/90% / When the probe runs / Then one row per day, idempotent, `warn` at the two thresholds; with `NEON_API_KEY` unset the job logs `info` and succeeds | AC-24 |
| T-25 | integration (CI, scheduled workflow dry run) | `pg_dump` artefact uploaded to a test prefix; `db.backup_verify` restores it into a scratch database and matches row counts on five tables; bucket policy asserted non-public | AC-25 |
| T-26 | unit + integration | For each `db:check` failure mode, a temporary fixture (migration file, TS schema edit, RLS-disabled table, money column without currency, `bytea` column) makes it exit non-zero with the offender named; clean tree exits 0 | AC-26 |
| T-27 | unit | A fixture migration adding `recipient.email` makes `db:check` fail with the `plan/07` §1.3 citation; `recipient_contact_note` passes | AC-27 |
| T-28 | integration (CI) | `tests/integration/` contains no `describe.skip`; the job runs the suite against the service container; `db:seed` runs twice with 0 changes on the second | AC-28 |
| T-29 | integration | Seed three times → identical counts and `updated_at`; a row flipped to `source = 'real'` survives a subsequent seed unmodified, per `source`-carrying table | AC-29 |
| T-30 | manual (reviewer, recorded in PR) | Follow `docs/runbooks/database.md` restore drill end to end; record dump key, duration and row counts in the PR and the runbook | AC-30 |
| T-31 | integration | `pg_dump` of the seeded database restored into a stock `postgres:16` container: zero errors, zero extension/role/function warnings, `db:check` passes on the copy | AC-31 |
| T-32 | integration | Seed content assertions: locale, country, product-split, add-on, price-band, partner, occasion-date, holiday counts and the three zero-row tables | AC-32 |
| T-33 | unit | `tests/unit/architecture-doc.test.ts` green after the doc/manifest edits; a script asserts the RoPA has ≥4 rows with no placeholder row and that every new script appears in `README.md` | AC-33 |
| T-34 | unit | Fixture barrel assertions: `occasionDates` contains the five `plan/09` dates; `currencies` exponents match `Intl.NumberFormat(...).resolvedOptions().maximumFractionDigits`; address/phone fixtures include the invalid cases | AC-34 |
| T-35 | contract | Auth.js (or Better Auth, per Q1) adapter contract: the library's own adapter test helper — or, failing that, a scripted create-user → create-session → get-session-and-user → delete-session round trip — passes against our tables unmodified | AC-4 (schema shape), Q1 |

## 11. Observability

- **Neon compute and storage usage** — `db_usage_sample` (one row/day) + `info` line per run + `warn` and a Sentry message at 70% and 90% of the free-tier cap. This is the ADR-0015 guardrail made operational, and the first metric with a business consequence, so it is also the first alert: at 90% the founder decides between throttling preview databases, an earlier Hetzner move, or paying (ADR-0015's recorded alternative).
- **Migration audit** — the applied-migration list (name, hash, applied-at) is printed in the `db-check` CI step summary and exposed as `latest_migration` on `/api/internal/db-health`, so a deployment can be checked against the database it is talking to without opening a console. Every migration run in CI logs its version range; `audit_log` records admin-initiated data changes (country/partner status flips) with `request_id`, actor and before/after.
- **Query-level logging** — `withDbContext` emits one `debug` line per transaction (`request_id`, `app.role`, statement count, duration) and one `warn` above a 200 ms threshold. Never the SQL text, parameters or rows (§8).
- **Job observability** — pg-boss's own tables are the job log; each handler logs start/finish with `request_id` propagated from the enqueuing request (`plan/12` §6) and `actor = system:{job}`. `pending_jobs` on the health route is the cheap signal that the minute tick has stopped.
- **Backups** — the workflow writes the object key and byte size to its step summary; `db.backup_verify` failure is a `warn` + Sentry message, because a silent backup that cannot be restored is the failure mode that matters.
- **What is deliberately not instrumented yet**: order-lifecycle alerts (`paid` without `routed` > 5 min, SLA cascade exhaustion, photo missing > 2 h) belong to spec 025 with the flows that produce them; no dashboard exists in Phase 0 (`plan/08` §7).

## 12. Rollout

- **Feature flags**: none gate this spec (it is schema). It *delivers* the `feature_flag` mechanism every later rollout uses, seeded with the keys Phase 0/1 will flip: `checkout.demo_guard`, `payments.stripe`, `payments.mollie`, `corridor.{iso}`, `locale.{code}`. All default off except `checkout.demo_guard` (on).
- **Environments and data**: local (own Neon branch or a local `postgres:16` container) → PR preview (Q5: per-PR Neon branch, or the shared preview database of `plan/08` §10) → production (the same Neon database in Phase 0; there are no real orders until Phase 1). Previews never point at production data (`plan/01` §10).
- **Migration order**: `0001` … `0010` as listed in §5.1, applied by `pnpm db:migrate` from CI on merge to `main` before the Vercel deployment is promoted; each task's PR adds only its own migration files, so the ordering is the task ordering.
- **Rollback plan**: every migration has an executed `.down.sql` (AC-4, T-02) and every task is one squash commit, so a bad merge is `git revert` + `pnpm db:rollback --to <previous>`. Phase 0 has no production data, so the true escape hatch is `db:rollback --to 0000 && db:migrate && db:seed`, which the round-trip test exercises on every PR. Once real orders exist (Phase 1) the nightly dump plus a rollback file is the recovery path, and `docs/runbooks/database.md` states the order: stop the worker, roll back, redeploy, verify `latest_migration`.
- **Suggested task split for `/plan-tasks`** (each ≤1 day, one PR; dependencies as noted):
  1. **Neon, env, client, config** — Neon project wiring; delete `ALLOW_PLACEHOLDER_ENV` and the Supabase keys; add the new env keys; `src/lib/db.ts` with `withDbContext`; `drizzle.config.ts` and the `db:*` scripts; migrations-directory decision (Q2) applied to `drizzle.config.ts`, `scripts/db-check.ts`, `scripts/check-layout.ts` and `docs/architecture.md`; migration `0001` (roles, grants, `set_updated_at`). Covers AC-1–4 (partial), AC-33 (partial).
  2. **`i18n` + `geo` schema** — migration `0002` and Drizzle schema; `locale`, `message_catalog`, `currency`, `country`, translations, `country_locale_content`, `region`, `city`, `postcode_zone`, `country_holiday`, `occasion*`, `fx_rate`. Covers AC-7, AC-8 (partial), AC-10 (partial).
  3. **`catalog` + media schema** — migration `0003`; products, translations, tiers, categories, occasions, add-ons, `country_price` with its partial unique index, `media_asset`/`media_variant`/`product_media`/`product_media_alt`. Covers AC-5, AC-8, AC-9, AC-11.
  4. **`partners` + `customers` schema** — migration `0004`, `0005`; partner tables, applications, payouts; customers, addresses, recipients, `consent_log`; the recipient-minimisation shape. Covers AC-27 (schema half).
  5. **`orders` + `payments` schema and DB guards** — migration `0006`; order tables, `order_status`/`order_transition` reference data seeded from `plan/11` §2, append-only trigger, direct-write and legal-transition triggers, `delivery_proof` asset trigger, `webhook_inbox`. Covers AC-12–15, AC-19.
  6. **Platform, ops and auth schema** — migrations `0007`, `0008`; `outbox`/`outbox_attempt`, `feature_flag*`, `audit_log`, `retention_policy`, `db_usage_sample`, the Auth.js table set, `user_role`. Covers AC-4 (schema shape), T-35.
  7. **RLS** — migration `0009`; enable RLS everywhere, deny-by-default, the public reference set, partner and customer policies, grants; positive/negative integration tests per table. Covers AC-16–18. Depends on 2–6.
  8. **Jobs, storage seam, probes, backup** — migration `0010` (pg-boss); `src/jobs/index.ts` registry; `retention.sweep`, `neon.usage_probe`, `db.backup_verify`; `src/lib/storage.ts` + fake; `/api/internal/db-health` and the job tick route; `.github/workflows/db-backup.yml`. Covers AC-20–25.
  9. **`db:check` real + CI + lint** — drift, RLS coverage, money-pair, `bytea`, PII-column and rollback gates; enable `fo/no-float-money`; wire `test-integration` and `db-check` service containers and step summaries. Covers AC-6, AC-26–28.
  10. **Seed, fixtures, docs, RoPA** — `seed/` scripts and data, `tests/fixtures/index.ts`, the portability dump/restore test, `docs/runbooks/database.md` + executed restore drill, architecture doc and RoPA rows, README. Covers AC-29–34. Depends on 1–9.
- **Founder actions (blocking, in this order)**: (1) create the Neon project in **Frankfurt** and copy both the pooled and direct connection strings; (2) create a Cloudflare account, an **EU-jurisdiction** R2 bucket for media and a private one for backups, plus a scoped API token; (3) put the real values in Vercel preview and production and in `.env.local`, then **remove `ALLOW_PLACEHOLDER_ENV` from both Vercel environments** once task 1 merges; (4) create a Neon API key and note the project id for the usage probe (optional but recommended); (5) accept and file the Neon and Cloudflare DPAs in `docs/compliance/`; (6) answer §13; (7) if Q2 is answered A, add the new migrations root to the guarded-path list in `.claude/hooks/task-guard.sh` and the matching `CLAUDE.md` bullet (a one-line founder edit, as with spec 001 §13 Q8 — this spec may not touch either file).
- **Exit signal**: `/status` shows `002 implemented`, Phase 0 progress `2/12` specs, all tasks `done` with recorded `/review` PASS, CI green including a real `test-integration` and `db-check`, and a seeded preview database from which spec 003 can start.

## 13. Open questions

**Resolved 2026-09-08 (founder delegated the decision to the orchestrator; each recommendation below is accepted as the decision):** Q1 Auth.js v5 + Drizzle adapter. Q2 Option A: `db/migrations/`; the guard (`.claude/hooks/task-guard.sh`) and `CLAUDE.md` gain `db/` as a protected root the same day. Q3 `orderService.transition` runtime ships in spec 015; 002 ships tables, guard rows and triggers. Q4 RLS design confirmed: `app_owner` (migrations, direct URL) + `app_web` (runtime, no BYPASSRLS), per-request `SET LOCAL app.*` inside one transaction; admin is a session variable, no second connection string. Q5 Shared seeded preview database in Phase 0; per-PR Neon branches revisited at the stated triggers. Q6 pg-boss, migrations, `db:check`, backups on `DATABASE_URL_UNPOOLED`; web on pooled `DATABASE_URL`. Q7 Retention seeded per plan/07 §1.2 verbatim; the 10-year invoice and 2-year photo figures stay flagged for accountant (B1) and legal review (B4) — numbers, not mechanism, may move. Q8 A3 and A4 defaults accepted as the seed basis; no product-name vetoes. Original questions kept below for the record.

- **Q1 — Auth.js or Better Auth, and therefore the exact auth table names?** ADR-0015 decided "Auth.js (or Better Auth) with sessions and accounts in our own Postgres tables" and deliberately left the choice open. It matters now because the adapter dictates table and column names (`users`/`accounts`/`sessions`/`verification_tokens` for the Auth.js Drizzle adapter; `user`/`session`/`account`/`verification` for Better Auth), and switching later is a rename migration plus a re-test of every session. **Recommendation: Auth.js v5 with the Drizzle adapter**, because ADR-0015 names it first, the adapter's schema is documented and stable, and magic-link email is the launch flow (`plan/11` §1) which both libraries support. Better Auth is the better fit only if we want passkeys and organisation support built in sooner than Phase 2. Whichever is chosen, T-35 runs the library's own adapter contract against our tables.
- **Q2 — Migrations directory now that Supabase is gone: `db/migrations/` or stay inside a guarded root?** `supabase/migrations/` is now actively misleading (ADR-0015), and `scripts/db-check.ts`, `scripts/check-layout.ts`, `docs/architecture.md` and `plan/01` §5 all name it. The catch is that `.claude/hooks/task-guard.sh` guards `src/ app/ supabase/ emails/ seed/ tests/`: renaming to `db/` removes migrations from the guard, so an agent could write SQL with no active task. **Option A (recommended): `db/migrations/`** — honest, host-neutral, matches Drizzle conventions; requires the one-line founder edit to the hook and `CLAUDE.md` in §12 item 7. **Option B (zero-touch): `src/db/migrations/`** — inherits the guard and the ESLint boundary rules automatically, at the cost of `.sql` files under `src/`. **Option C: keep `supabase/migrations/`** — no code churn, permanently wrong name; not recommended. Answer determines task 1.
- **Q3 — Does `orderService.transition` ship in this spec or in the orders spec?** This spec proposes **spec 015**, on three citations: `plan/11` §2 ("this machine, implemented in `015`"), `plan/09` Phase 1 (`015` order state machine + events + outbox), and `docs/architecture.md` §3 (`orders` owned by 015). 002 ships the tables, the `order_transition` guard rows and the database triggers, so 015's service is a thin, well-tested wrapper rather than the whole guarantee. Confirm, or ask for a minimal `transition()` here — which would pull the event zod schemas, the outbox write and the buyer projection forward into Phase 0 for no Phase 0 benefit (nothing places orders until spec 010, and that is behind a demo guard).
- **Q4 — RLS role and session-variable mechanism: confirm the design.** Proposed: two roles (`app_owner` owns everything and is used only by migrations over the direct URL; `app_web` is the runtime role with DML only, no `BYPASSRLS`), with per-request `SET LOCAL app.role`, `app.user_id`, `app.partner_ids`, `app.request_id` inside one transaction, read by policies via `current_setting(..., true)`. Alternatives: (a) `SET LOCAL ROLE app_partner`/`app_admin` per request and policies keyed on `current_user` — purer, but needs one login role per role and complicates pooling; (b) application-level scoping only, no RLS — cheaper, and rejected because `plan/01` §9 and `plan/07` §1.1 make partner isolation a compliance control, not a convenience. Confirm the proposal, in particular that **admin access is a session variable rather than a second connection string**, so there is no service-role key to leak (ADR-0015 removed the one we had).
- **Q5 — Neon branching for preview deploys: a database branch per PR, or one shared preview database?** Per-PR branches (Neon's `branch` API + the GitHub integration) give true isolation and let a migration be tested before merge, but each branch consumes compute hours against the free cap and needs a teardown hook. A shared, seeded preview database is what `plan/08` §10 assumed and costs one compute allowance, but two PRs with conflicting migrations will collide. **Recommendation: shared preview database in Phase 0** (few PRs, no real data, cap is the binding constraint), with per-PR branches revisited when two migrations land in the same week or when Phase 1 data makes a shared preview database a compliance question. Confirm — it changes CI wiring in tasks 1 and 9.
- **Q6 — Pooled or direct connection for pg-boss (and for the migration runner)?** Neon's pooled endpoint is PgBouncer in transaction mode: `SET LOCAL` inside an explicit transaction is safe (which is why web requests can use it), but session-scoped state, `LISTEN/NOTIFY` and advisory session locks are not. pg-boss uses maintenance locks and long-lived connections. **Recommendation: pg-boss, migrations, `db:check` and the backup workflow on `DATABASE_URL_UNPOOLED`; web requests on `DATABASE_URL`.** Confirm, and confirm that two connection strings in the env contract is acceptable (it is one extra Vercel variable per environment and is also how the Hetzner move will look).
- **Q7 — Retention periods to confirm before they are seeded as `retention_policy` rows.** The seed proposes `plan/07` §1.2 verbatim: recipient contact **90 days** after delivery → pseudonymise; delivery photos **2 years** → delete; buyer personal fields **3 years** → pseudonymise with the invoice archive retained **10 years** (EE/PL accounting, 🧾); fraud signals **12 months**; consent records **5 years** after withdrawal; webhook payloads **12 months**. Two need a decision rather than a nod: the **10-year invoice retention** should be confirmed by the accountant with B1 (`plan/06` §4), and the **2-year photo retention** is chargeback-window reasoning that a lawyer may want to shorten. The sweep is implemented and tested either way; only the numbers move.
- **Q8 — A3 and A4 defaults: accepted as the seed's basis?** A3 (taxonomy and the 84-product seed shape, `plan/10` §1–§2) and A4 (price bands and payout shares, `plan/10` §2.3) are both open with the default "as written", and both are due 21 Sep — inside this spec's week. This spec seeds skeletons and prices from those defaults, so if they change afterwards the correction is a seed re-run (cheap, idempotent) plus new `country_price` rows (also cheap, because prices are superseded rather than updated). Confirm the defaults so the seed is not written twice, and veto any product name now rather than after spec 006 generates imagery against it.

## 14. Amendments (post-approval corrections)

**A1 — Additive schema requests from specs 005 and 006, folded into the unimplemented migration `0003` (§5.1, §2 seed).**
Original: §5.1 as approved; §2 promises seed literals for the 84-product skeleton, countries, prices and partners.
Corrected (founder accepted both specs' defaults, 2026-09-09; spec 002 is parked and `0003` unwritten, so these are free now and a second migration later): (a) `addon_country_price.vat_rate_bp` and a partial unique index on `addon_country_price (addon_id, country_id) WHERE active_to IS NULL` (spec 005 Q3/Q8); (b) `product_tier.is_default` with a one-per-product partial unique index (spec 005 Q6/Q8); (c) `media_asset.generator_model`, `.credit`, `.licence`, `.depicts`, `.reviewed_by`, `.reviewed_at` and `media_variant.checksum_sha256` (spec 006 Q11); (d) §2's seed task 10 reads `seed/data/**` through spec 006's schemas and spec 005's `src/config/catalogue/*.data.ts` projections instead of restating `plan/10` (spec 005 Q9, spec 006 Q10) — spec 002 keeps the tables, the upsert plumbing, partners and geo rows. Each column has a one-line rollback in the migration's down file.
Raised by: `/spec 005` §13 Q8 and `/spec 006` §13 Q10–Q11, approved 2026-09-09.

**A2 — Two more additive corrections from spec 005's projections (§5.1; TASK-061, 2026-09-09).**
Original: §5.1 names `category` without a column list; `product_tier.stems` is a non-null integer.
Corrected (orchestrator under the founder's delegation): (a) `category(key, kind)` with `kind ∈ product-type | occasion | flower-type`, the shape `toCategoryRow()` projects by analogy with `occasion(id, key, kind)`; (b) `product_tier.stems` is **nullable** — spec 005 §13 Q4's S/M/L tiers for arrangements and `single` for plants have no stem count, and inventing one for a wreath would be a false product claim. Both fold into the unimplemented migration `0003` with one-line rollbacks.
Raised by: TASK-061 (PR #34), 2026-09-09.
