# CI (spec 001 §2 "CI"; TASK-002 + TASK-008 + TASK-009 + TASK-010 + TASK-011). AC-21: this file
# is the source of truth for the required-status-check set — `scripts/branch-protection.ts` reads
# the job names straight out of it, so a job added here is part of the branch-protection contract
# without anybody editing a list (`pnpm branch-protection --verify`).
#
# Jobs (22): lint · typecheck · test-unit · test-integration · test-contract · build · container ·
# db-check · env-build-failure · commitlint · seo-validate · i18n-check · catalogue-check ·
# corridor-check · seed-check · dev-os-check · audit · preview · e2e · visual · a11y · lighthouse.
# The lint job also runs the three orientation gates of §14 A15: `codebase:map --check`,
# `specs:index --check`, `tasks:check`.
#
# Order, and where it departs from spec 001 §2 / plan/12 §5 (TASK-011 reconciliation; the
# corrections are recorded on TASK-012 for the spec text):
#
#  - `lint` → `typecheck` is the spine. Everything that needs neither a browser nor a preview
#    deployment hangs off `typecheck` and runs in parallel: `test-unit`, `test-integration`,
#    `test-contract`, `env-build-failure`, `db-check`, `seo-validate`, `dev-os-check`, `audit`.
#    The spec lists them as a chain; running them as one would triple the wall-clock time of every
#    PR without catching anything a parallel fan-out misses, since none of them consumes another's
#    output. `build` keeps `needs: test-unit` (do not spend build minutes on a red unit suite).
#  - `i18n-check` joins that fan-out on `needs: typecheck` (spec 003 §2 "Lint, checks, CI", spec 001
#    §14 A9's placement rule): it reads committed JSON and greps `src/`, touches no preview
#    deployment, and its step summary is the translation-debt number the founder acts on (spec 003
#    §11), so it must be visible on every PR rather than behind the Playwright chain.
#  - `catalogue-check` joins it for the same reasons (spec 005 §2 "Docs, fixtures, gates", AC-5;
#    TASK-062): it reads committed dataset files and `messages/en.json`, and its step summary is
#    the per-destination price-coverage table.
#  - `seed-check` joins it for the same reasons again (spec 006 §2.3, AC-10, AC-30; TASK-075): it
#    reads `seed/data/**` and nothing else, and its step summary is the standing catalogue-health
#    report of spec 006 §11 — the coverage table `plan/02` §6's six-product rule is judged on.
#  - **`env:check` has no job of its own.** It is a step of `lint` (added by TASK-005): it is a
#    key-set diff between `.env.example` and the zod schema that takes milliseconds and needs the
#    same checkout and install as the other lint steps. A separate job would add a runner, an
#    install and a required check for one `node` invocation. `pnpm env:check` therefore appears in
#    the `lint` job's step summary, and AC-11 is enforced on every PR.
#  - `db:check` runs as its own `db-check` job rather than a step of `build` (spec §2 puts it
#    after `build`): from spec 002 it grows a Postgres connection and drift detection, and a
#    separate job is where a service container can be attached without touching `build`.
#  - `corridor-check` (spec 007 AC-2, TASK-087) joins the same `needs: typecheck` fan-out as
#    `i18n-check`, `catalogue-check` and `seed-check`: it reads committed markdown only.
#  - `seo-validate` and `dev-os-check` sit early, on `needs: typecheck`, not after `lighthouse`
#    where §2 lists them. Neither one touches a preview deployment (the SEO validators read
#    committed fixtures; the dev-OS checks run the hooks against temp projects), so making them
#    wait 15 minutes for a Vercel deployment would delay two of the fastest gates in the file for
#    no reason. Spec correction recorded on TASK-012.
#  - `audit` likewise sits early: `pnpm audit` and the secret scan are the cheapest way to find
#    out that a PR should not be looked at further.
#  - `container` (spec 040 AC-8 / T-08; TASK-135, from TASK-099's carry-forward) hangs off
#    `test-unit` beside `build`: it is the only gate that exercises the artefact Railway actually
#    runs — `docker build` with **no credential in the environment**, then the running image's
#    `/api/health`, its user, and the two fail-closed cases the build stopped owning when spec 001
#    §14 A17 split the env gate. It is on the spine rather than behind `ci:full` because a broken
#    image is invisible until a deploy fails; the expensive steps are skipped when no file of the
#    container contract changed, so the budget of §14 A14 is spent only on the pull requests that
#    can break it.
#  - `preview` → `e2e`/`visual`/`a11y` is the one real chain. Since **TASK-137** `preview` no
#    longer waits on a third party for it: it builds the app on the runner from the committed
#    `.env.example` placeholders, publishes that build, serves it and gates on `/api/health` 200,
#    and the three Playwright suites re-serve the same build through
#    `.github/actions/preview-origin`. Before that they were `needs: preview` on a Vercel preview
#    whose `/api/health` answers 500 (ADR-0018's cold fallback, empty environment store), so no
#    Playwright suite had ever run in CI — skipped identically on PRs 84, 85 and 87. `lighthouse`
#    hangs off `build`, not off this chain: since TASK-056 it builds and serves what it measures
#    behind `scripts/seo/brotli-origin.ts`.
#
# spec 001 §11: **every job writes a GitHub step summary.** A green tick says a command exited 0;
# the summary says what it measured, and it is what the reviewer quotes in the PR.
name: ci

# Actions-minutes budget (spec 001 §14 A14, 2026-09-09). GitHub Free gives this private repo 2,000
# minutes a month and a full run costs ~30, so the workflow runs only when it is asked to:
#  - a PR marked ready for review (`ready_for_review`) runs the spine — `lint` → `typecheck` →
#    `test-unit` → `build` — once. Drafts and every later push cost nothing; implementers run the
#    whole gate set locally and push once when it is green.
#  - the fan-out jobs and the preview → Playwright → Lighthouse chain run only with the `ci:full`
#    label (`labeled` is in the trigger types) or a manual `workflow_dispatch`; the reviewer runs the
#    same suites locally against `pnpm start` and records the result in `TASKS.md`.
#  - nothing runs on push to `main`: the squash-merged tree is the PR head that already passed.
on:
  pull_request:
    types: [ready_for_review, labeled]
  workflow_dispatch:

concurrency:
  group: ci-${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

defaults:
  run:
    # `shell: bash` is `bash --noprofile --norc -eo pipefail`, where the bare default is only
    # `bash -e`. Several steps below pipe a command into `tee` so its output can be summarised;
    # without `pipefail` those pipelines would report `tee`'s exit status and a failing gate would
    # show green.
    shell: bash

jobs:
  lint:
    name: lint
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/setup
      # ESLint (incl. the five `fo/*` rules and the module boundaries) + Stylelint. `--max-warnings
      # 0` is not passed because every rule in `eslint.config.mjs` is configured as an error; the
      # count below is therefore the error count.
      - name: ESLint and Stylelint
        id: eslint
        run: pnpm lint 2>&1 | tee lint.log
      - name: No `eslint-disable` for fo/no-literal-strings under src/ (AC-6)
        run: pnpm check:no-literal-disable | tee no-literal-disable.log
      # The no-database seam of spec 003 (AC-2): the locale/currency/address config and the i18n
      # module must stay free of a database client, an ORM, a Postgres driver and `DATABASE_URL`,
      # which is what makes that spec implementable while spec 002's provisioning is parked.
      - name: No database import in the spec 003 file set (spec 003 AC-2)
        run: pnpm check:no-db | tee no-db.log
      # Spec 040 AC-2 (TASK-097): `VERCEL_ENV`, `NEXT_PUBLIC_VERCEL_ENV` and `VERCEL_GIT_COMMIT_SHA`
      # may be read in `src/lib/env.schema.ts` and `src/lib/sentry.ts` and nowhere else. It sits
      # beside `check:no-db` because it is the same kind of gate: the assertion behind a claim the
      # architecture makes ("the app is host-agnostic"), and the thing that makes the eventual
      # Vercel unlink (spec 040 §13 Q5) a two-file diff.
      - name: No platform env read outside the abstraction (spec 040 AC-2)
        run: pnpm check:no-vercel-env | tee no-vercel-env.log
      - name: Prettier
        run: pnpm format:check | tee format.log
      # `.env.example` and the zod schema must agree in both directions (AC-11, TASK-005). It runs
      # here rather than in a job of its own: see the reconciliation note at the top of this file.
      - name: env:check — `.env.example` against the zod schema (AC-11)
        run: pnpm env:check | tee env-check.log
      # The three orientation artefacts of spec 001 §14 A15 (AC-33, AC-34, AC-35). They are
      # documentation gates, so they sit beside Prettier rather than in a job of their own: a stale
      # `docs/codebase-map.md` or a spec index with wrong line numbers sends the next agent to the
      # wrong file, and an un-migrated notes cell puts 11 KB of prose back into `TASKS.md`.
      - name: codebase:map --check — docs/codebase-map.md is current (AC-33)
        run: pnpm codebase:map --check | tee codebase-map.log
      - name: specs:index --check — every spec's `## 0. Index` is current (AC-35)
        run: pnpm specs:index --check | tee specs-index.log
      - name: tasks:check — the TASKS.md ledger, notes cap and briefs (AC-31, AC-34)
        run: pnpm tasks:check | tee tasks-check.log
      # The visual gate's own orientation check (AC-17, TASK-139). `linux` is the authoritative
      # baseline set — it is the only platform the `visual` job compares against — and `darwin` is
      # a local convenience. A baseline that exists only for `darwin` is a `visual` failure waiting
      # to happen on the next pull request that carries the `ci:full` label, so it is caught here,
      # in the cheapest job, by reading committed files. `docs/runbooks/visual-baselines.md` says
      # how to refresh the Linux set from a Mac.
      - name: visual:baselines --check — every baseline exists for Linux (AC-17)
        run: pnpm visual:baselines --check | tee visual-baselines.log
      - name: Summarise the lint gate
        if: always()
        run: |
          set -uo pipefail
          # `problems` counts ESLint/Stylelint problem lines; zero on a green run, which is the
          # only run that reaches this step with the earlier ones green.
          problems=0
          if [ -f lint.log ]; then
            problems=$(grep -cE '^\s+[0-9]+:[0-9]+\s+(error|warning)' lint.log || true)
          fi
          {
            echo "### lint"
            echo ""
            echo "| check | command | result |"
            echo "|---|---|---|"
            echo "| ESLint + Stylelint | \`pnpm lint\` | ${{ steps.eslint.outcome }}, $problems problem(s) |"
            echo "| eslint-disable audit (AC-6) | \`pnpm check:no-literal-disable\` | $( [ -f no-literal-disable.log ] && tr '\n' ' ' < no-literal-disable.log || echo 'did not run' ) |"
            echo "| no-db seam (spec 003 AC-2) | \`pnpm check:no-db\` | $( [ -f no-db.log ] && tr '\n' ' ' < no-db.log || echo 'did not run' ) |"
            echo "| host-agnostic env (spec 040 AC-2) | \`pnpm check:no-vercel-env\` | $( [ -f no-vercel-env.log ] && tr '\n' ' ' < no-vercel-env.log || echo 'did not run' ) |"
            echo "| Prettier | \`pnpm format:check\` | $( [ -f format.log ] && echo 'all matched' || echo 'did not run' ) |"
            echo "| env:check (AC-11) | \`pnpm env:check\` | $( [ -f env-check.log ] && tr '\n' ' ' < env-check.log || echo 'did not run' ) |"
            echo "| codebase map (AC-33) | \`pnpm codebase:map --check\` | $( [ -f codebase-map.log ] && tr '\n' ' ' < codebase-map.log || echo 'did not run' ) |"
            echo "| spec indexes (AC-35) | \`pnpm specs:index --check\` | $( [ -f specs-index.log ] && tr '\n' ' ' < specs-index.log || echo 'did not run' ) |"
            echo "| ledger + briefs (AC-31, AC-34) | \`pnpm tasks:check\` | $( [ -f tasks-check.log ] && tr '\n' ' ' < tasks-check.log || echo 'did not run' ) |"
            echo "| visual baselines (AC-17) | \`pnpm visual:baselines --check\` | $( [ -f visual-baselines.log ] && head -1 visual-baselines.log || echo 'did not run' ) |"
          } >> "$GITHUB_STEP_SUMMARY"

  typecheck:
    name: typecheck
    runs-on: ubuntu-latest
    timeout-minutes: 10
    needs: lint
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/setup
      - name: tsc --noEmit
        id: tsc
        run: pnpm typecheck 2>&1 | tee tsc.log
      - name: Summarise the typecheck gate
        if: always()
        run: |
          set -uo pipefail
          errors=0
          if [ -f tsc.log ]; then
            errors=$(grep -cE 'error TS[0-9]+' tsc.log || true)
          fi
          # The strict flags themselves (AC-2) are asserted by `pnpm typecheck:fixtures` in the
          # unit suite (`tests/unit/*`, TASK-001), not here; this job's job is the project tree.
          {
            echo "### typecheck"
            echo ""
            echo "- \`pnpm typecheck\` (tsc --noEmit, strict): ${{ steps.tsc.outcome }}, $errors error(s)"
          } >> "$GITHUB_STEP_SUMMARY"

  # AC-16 / T-17 (TASK-008): the unit suite is the gate the other jobs stand on, so it runs before
  # `build`. Coverage is collected on every run (thresholds come from `vitest.coverage.json`,
  # empty in 001 and raised per module by specs 005/007 — plan/12 §4).
  test-unit:
    name: test-unit
    runs-on: ubuntu-latest
    timeout-minutes: 15
    needs: typecheck
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/setup
      # `tests/unit/audit-secrets.test.ts` (T-28) skips its scans when gitleaks is absent, and the
      # summary step below fails the job on *any* skipped unit test. Installing the binary here is
      # what makes "skipped locally, enforced in CI" true rather than a comment.
      - uses: ./.github/actions/gitleaks
      - name: Run the unit suite with coverage
        run: >
          pnpm test:coverage
          --reporter=default
          --reporter=json
          --outputFile=vitest-unit.json
      - name: Summarise the run (T-17)
        if: always()
        run: |
          set -euo pipefail
          if [ ! -f vitest-unit.json ]; then
            echo "::error::vitest produced no JSON report; cannot summarise the unit run"
            exit 1
          fi
          files=$(jq '.numTotalTestSuites' vitest-unit.json)
          total=$(jq '.numTotalTests' vitest-unit.json)
          passed=$(jq '.numPassedTests' vitest-unit.json)
          failed=$(jq '.numFailedTests' vitest-unit.json)
          skipped=$(jq '.numPendingTests + .numTodoTests' vitest-unit.json)
          {
            echo "### test-unit (AC-16 / T-17)"
            echo ""
            echo "- test files: $files"
            echo "- tests: $total (passed $passed, failed $failed, skipped $skipped)"
          } >> "$GITHUB_STEP_SUMMARY"
          # A skipped unit test is a test nobody runs: the unit layer has no allowance for one
          # (the *integration* suite is the only skipped suite in 001).
          if [ "$skipped" != "0" ]; then
            echo "::error::$skipped unit test(s) are skipped; the unit suite must have none (definition of done: no skipped tests)"
            exit 1
          fi
      - name: Upload coverage
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: coverage
          path: |
            coverage/
            vitest-unit.json
          retention-days: 7
          if-no-files-found: error

  # AC-16 / T-17 (TASK-008): the integration job — and its Postgres service container — exist from
  # 001 so spec 002 only has to delete a `.skip`. The suite is reported as skipped; the job still
  # proves the database the suite will use is up.
  test-integration:
    name: test-integration
    runs-on: ubuntu-latest
    timeout-minutes: 15
    needs: typecheck
    if: ${{ github.event_name == 'workflow_dispatch' || contains(github.event.pull_request.labels.*.name, 'ci:full') }}
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: flowers_overseas_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
    env:
      # Consumed by `tests/integration/setup.ts`; it falls back to the `.env.example` placeholder
      # locally, so a clean clone needs no database.
      DATABASE_URL: postgres://postgres:postgres@localhost:5432/flowers_overseas_test
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/setup
      - name: Assert the Postgres service is healthy (T-17)
        run: |
          set -euo pipefail
          pg_isready -h localhost -p 5432 -U postgres
          server_version=$(PGPASSWORD=postgres psql -h localhost -U postgres -d flowers_overseas_test -tAc 'show server_version')
          {
            echo "### test-integration (AC-16 / T-17)"
            echo ""
            echo "- postgres service: healthy, server_version $server_version"
          } >> "$GITHUB_STEP_SUMMARY"
      - name: Run the integration suite (skipped until spec 002)
        run: >
          pnpm test:integration
          --reporter=default
          --reporter=json
          --outputFile=vitest-integration.json
      - name: Assert the suite is reported as skipped, not absent
        run: |
          set -euo pipefail
          files=$(jq '.numTotalTestSuites' vitest-integration.json)
          skipped=$(jq '.numPendingTests + .numTodoTests' vitest-integration.json)
          failed=$(jq '.numFailedTests' vitest-integration.json)
          {
            echo "- integration suite: $files file(s), $skipped skipped, $failed failed (spec 002 TODO)"
          } >> "$GITHUB_STEP_SUMMARY"
          if [ "$failed" != "0" ]; then
            echo "::error::the integration suite reported failures"
            exit 1
          fi
          if [ "$skipped" = "0" ]; then
            echo "::error::the integration suite reported no skipped test; AC-16 requires the spec-002 placeholder to be reported as skipped"
            exit 1
          fi

  # spec 001 §2 "CI" lists `test:contract` between `test-integration` and `build`. The layer is
  # empty in 001 — there is no adapter to pin and a fabricated fixture would pin a shape nobody
  # observed (`tests/contract/README.md` names the boundaries and their owning specs) — so the
  # project runs with `--passWithNoTests`. The job exists from the first PR for the same reason
  # `test-integration` does: spec 013/014/017 add a file, not a gate.
  test-contract:
    name: test-contract
    runs-on: ubuntu-latest
    timeout-minutes: 10
    needs: typecheck
    if: ${{ github.event_name == 'workflow_dispatch' || contains(github.event.pull_request.labels.*.name, 'ci:full') }}
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/setup
      - name: Run the contract suite
        run: >
          pnpm test:contract
          --reporter=default
          --reporter=json
          --outputFile=vitest-contract.json
      - name: Summarise the run
        if: always()
        run: |
          set -euo pipefail
          files=0
          total=0
          failed=0
          if [ -f vitest-contract.json ]; then
            files=$(jq '.numTotalTestSuites' vitest-contract.json)
            total=$(jq '.numTotalTests' vitest-contract.json)
            failed=$(jq '.numFailedTests' vitest-contract.json)
          fi
          {
            echo "### test-contract"
            echo ""
            echo "- test files: $files, tests: $total, failed: $failed"
            if [ "$total" = "0" ]; then
              echo "- the contract layer is empty in spec 001 by design; the first fixtures arrive"
              echo "  with specs 013 (Stripe), 014 (Mollie) and 017 (Resend) —"
              echo "  see \`tests/contract/README.md\`."
            fi
          } >> "$GITHUB_STEP_SUMMARY"
          if [ "$failed" != "0" ]; then
            echo "::error::the contract suite reported failures"
            exit 1
          fi

  # The offline migration gate (spec 001 §5.1, extended by spec 002 §2 / TASK-014): rollback
  # pairing, version sequence, the `SET LOCAL ROLE app_owner` preamble and table-level drift
  # between `db/schema/` and `db/migrations/`. No database and no credentials, by design — the
  # connected half of AC-26 arrives with TASK-027.
  db-check:
    name: db-check
    runs-on: ubuntu-latest
    timeout-minutes: 10
    needs: typecheck
    if: ${{ github.event_name == 'workflow_dispatch' || contains(github.event.pull_request.labels.*.name, 'ci:full') }}
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/setup
      - name: Check the migration set
        id: db
        run: pnpm db:check 2>&1 | tee db-check.log
      - name: Summarise the migration set
        if: always()
        run: |
          set -uo pipefail
          migrations=$(find db/migrations -maxdepth 1 -type f -name '*.sql' ! -name '*.down.sql' | wc -l | tr -d ' ')
          rollbacks=$(find db/migrations -maxdepth 1 -type f -name '*.down.sql' | wc -l | tr -d ' ')
          {
            echo "### db-check (spec 001 §5.1)"
            echo ""
            echo "- \`pnpm db:check\`: ${{ steps.db.outcome }} — $( [ -f db-check.log ] && tr '\n' ' ' < db-check.log || echo 'did not run' )"
            echo "- forward migrations: $migrations; rollback files: $rollbacks"
            if [ "$migrations" = "0" ]; then
              echo "- no migrations committed yet."
            fi
          } >> "$GITHUB_STEP_SUMMARY"

  # AC-27 / T-28 (TASK-011): the two halves of "is anything in here dangerous" — a vulnerable
  # production dependency and a committed credential. `pnpm run audit` runs both, so one command
  # reproduces this job locally. (`pnpm run`, not a bare `pnpm audit`: pnpm has a built-in `audit`
  # command that shadows the script of the same name. The two halves are separate steps here so
  # each gets its own status in the log and the step summary.)
  #
  # `--prod`: a devDependency advisory cannot reach a user, and failing every PR on one would
  # teach everybody to ignore this job. `--audit-level=high` is the gate spec 001 §8 asks for;
  # Renovate's `vulnerabilityAlerts` (see `renovate.json`) opens the fixing PR out of band.
  audit:
    name: audit
    runs-on: ubuntu-latest
    timeout-minutes: 10
    needs: typecheck
    if: ${{ github.event_name == 'workflow_dispatch' || contains(github.event.pull_request.labels.*.name, 'ci:full') }}
    steps:
      - uses: actions/checkout@v4
        with:
          # The secret scan reads the working tree, so a shallow checkout is enough; `fetch-depth`
          # is left at its default deliberately rather than pulling the whole history for a scan
          # that would not act on it.
          fetch-depth: 1
      - uses: ./.github/actions/setup
      - uses: ./.github/actions/gitleaks
      - name: pnpm audit (production dependencies, high and above)
        id: deps
        run: |
          set -uo pipefail
          pnpm audit --prod --audit-level=high --json > audit.json 2>audit.err || true
          pnpm audit --prod --audit-level=high 2>&1 | tee audit.log
          exit "${PIPESTATUS[0]}"
      - name: gitleaks (writes its own summary section)
        id: secrets
        run: pnpm audit:secrets
      - name: Summarise the dependency audit
        if: always()
        run: |
          set -uo pipefail
          # `pnpm audit --json` reports `{"advisories":{…},"metadata":{"vulnerabilities":{…}}}`.
          # `// empty` keeps the summary honest when the file is missing rather than printing 0.
          counts="unavailable"
          if [ -s audit.json ]; then
            counts=$(jq -r '[.metadata.vulnerabilities | to_entries[] | "\(.key): \(.value)"] | join(", ")' audit.json 2>/dev/null || echo "unavailable")
          fi
          {
            echo "### audit — dependencies (AC-27 / T-28)"
            echo ""
            echo "- \`pnpm audit --prod --audit-level=high\`: ${{ steps.deps.outcome }}"
            echo "- advisories by severity: $counts"
            echo "- gitleaks step: ${{ steps.secrets.outcome }} (its finding count is in the section below)"
          } >> "$GITHUB_STEP_SUMMARY"

  build:
    name: build
    runs-on: ubuntu-latest
    timeout-minutes: 15
    needs: test-unit
    env:
      NEXT_TELEMETRY_DISABLED: "1"
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/setup
      # A clean clone builds from the committed placeholders (spec 001 §13 Q10, AC-1).
      - run: cp .env.example .env.local
      - name: next build
        id: next
        run: pnpm build 2>&1 | tee build.log
      # spec 003 AC-27 / T-27, restated by spec 004 §13 Q13 (TASK-046): the client-JS and
      # client-message-payload budgets, measured from the prerendered documents this build just
      # wrote. Three things the step now reports, in `budget.log`:
      #
      #  - per-URL script transfer as **Brotli**, against 122 880 bytes. That is the founder's
      #    2026-09-08 restatement of `plan/01` §7 (option (a)): the same number against the
      #    encoding Vercel serves and Lighthouse measures, instead of gzip, which made the same
      #    build "over" or "under" depending on the reader.
      #  - the `localeDocument` namespace subset per locale, against 4 KB gzipped (unchanged) —
      #    an upper bound since TASK-085, which removed the client provider: no payload is sent.
      #  - **no zod and no browser Sentry SDK in any measured route's client bundle** (spec 004
      #    AC-25), asserted from the chunks the documents reference *and* from the route's
      #    `next/dynamic` chunks — the manifest `/review 26` found the script ignoring, which is
      #    where zod actually was.
      #  - **no `home.*`/`finder.*`/`catalog.*`/`media.*` catalogue value in any fetched chunk**
      #    (spec 004 §14 A1 addendum, TASK-085): a static `messages/*.json` import ships the whole
      #    catalogue once it crosses Turbopack's tree-shaking threshold, which is too small a byte
      #    total to notice and was 4 751 B Brotli on every document.
      #
      # **Blocking since TASK-056** (spec 004 AC-25): the `continue-on-error: true` that used to sit
      # on this step is gone. Every URL of the set is inside the budget — `/` 116 778 B Brotli and
      # each locale document 122 360 B against 131 072 B — what is left is the framework floor with
      # no application byte to remove, and spec 004 §13 Q13 option (b) was taken rather than raising
      # the number. The step also fails on a >5 KB regression against
      # `tests/fixtures/seo/bundle-baseline.json`, which is the table spec 004 §11 asks to be
      # reviewable in a diff.
      - name: Measure the client-JS budgets (AC-27 / T-27, AC-25)
        id: budget
        run: pnpm budget:client-js 2>&1 | tee budget.log
      - name: Summarise the client-JS budgets (AC-27 / T-27, AC-25)
        if: always()
        env:
          BUDGET_OUTCOME: ${{ steps.budget.outcome }}
        run: |
          set -uo pipefail
          {
            echo "### client-JS budgets (spec 003 AC-27 / T-27, spec 004 AC-25)"
            echo ""
            echo "\`pnpm budget:client-js\`: $BUDGET_OUTCOME (required since TASK-056)."
            echo ""
            echo "Script transfer is measured as **Brotli** against 131 072 B — \`plan/01\` §7 as"
            echo "spec 004 §13 Q13 restated it and §14 A1 corrected it. The gzip number is"
            echo "printed beside it because every older note in this repository quotes that one."
            echo "The run also asserts that no measured route ships \`zod\` or \`@sentry/\`."
            echo ""
            if [ -f budget.log ]; then
              sed -n '/^| URL /,/^$/p;/^| locale /,/^$/p' budget.log
              echo ""
              echo "<details><summary>chunks per URL</summary>"
              echo ""
              echo '```'
              sed -n '/^\//,$p' budget.log
              echo '```'
              echo ""
              echo "</details>"
            else
              echo "The step produced no output; read the step log."
            fi
          } >> "$GITHUB_STEP_SUMMARY"
          rm -f budget.log

      # AC-32 / T-33: `install && build` must leave the tree clean. The check belongs to whichever
      # job actually builds, and this is it.
      - name: Assert the working tree is clean after the build (AC-32 / T-33)
        run: |
          set -uo pipefail
          rm -f .env.local build.log
          porcelain=$(git status --porcelain)
          {
            echo "### build (AC-1, AC-32 / T-01, T-33)"
            echo ""
            echo "- \`pnpm build\` from \`.env.example\` placeholders: ${{ steps.next.outcome }}"
            if [ -z "$porcelain" ]; then
              echo "- \`git status --porcelain\` after install + build: empty"
            else
              echo "- \`git status --porcelain\` after install + build: **not empty**"
              echo ""
              echo '```'
              echo "$porcelain"
              echo '```'
            fi
          } >> "$GITHUB_STEP_SUMMARY"
          if [ -n "$porcelain" ]; then
            echo "::error::the working tree is dirty after install + build (AC-32); see the step summary"
            exit 1
          fi

  # AC-8 / T-08 (spec 040 §5.3): the daemon half of the container's acceptance criterion — the
  # image **builds with no credential in the environment**, runs as a non-root user, carries no
  # `.env*` file and serves `/api/health` from the standalone output. `tests/unit/container.test.ts`
  # asserts the file; this job asserts the artefact.
  #
  # It is TASK-099's carry-forward, taken by **TASK-135**: no Docker daemon was available on the
  # machine the build-time env split was written on (spec 001 §14 A17, spec 040 §14 A1), so CI is
  # where "a container build needs no credential" is demonstrated rather than argued. The job also
  # owns the two negative cases that used to be `pnpm build` failures and are now server-start
  # failures: a missing server key and a `.env.example` placeholder in a deployed environment both
  # keep `/api/health` from answering 200, which is what fails the Railway healthcheck.
  #
  # Budget (spec 001 §14 A14). The job sits on the spine so that a change to the container contract
  # cannot merge unproven, but its expensive steps run **only** when the pull request touches that
  # contract — the `Dockerfile`, the env modules, the health route, `next.config.ts`,
  # `instrumentation.ts`, the manifest or this workflow. Otherwise it costs one runner start
  # (~15 s) and writes a summary saying which files it looked at.
  container:
    name: container
    runs-on: ubuntu-latest
    timeout-minutes: 25
    needs: test-unit
    env:
      IMAGE: fo-web:ci
      # The runtime variable set, in the shape Railway supplies it: `.env.example` placeholders,
      # which are valid in `development` (spec 001 §13 Q10). No real credential is used, and none
      # is passed to `docker build` — that is the whole point of the job.
      RUNTIME_ENV: >-
        -e DATABASE_URL=postgres://user:pass@localhost:5432/fo
        -e DATABASE_URL_UNPOOLED=postgres://user:pass@localhost:5432/fo_direct
        -e INTERNAL_CRON_SECRET=placeholder-cron-secret
        -e R2_ACCOUNT_ID=00000000000000000000000000000000
        -e R2_BUCKET=placeholder-bucket
        -e R2_BACKUPS_BUCKET=placeholder-backups-bucket
        -e R2_S3_ENDPOINT=https://00000000000000000000000000000000.eu.r2.cloudflarestorage.com
        -e R2_ACCESS_KEY_ID=placeholder-r2-access-key-id
        -e R2_SECRET_ACCESS_KEY=placeholder-r2-secret-access-key
        -e R2_PUBLIC_BASE_URL=https://placeholder.r2.dev
    steps:
      - uses: actions/checkout@v4
      - name: Decide whether the container contract changed
        id: scope
        env:
          BASE_REF: ${{ github.base_ref }}
        run: |
          set -uo pipefail
          paths="Dockerfile .dockerignore next.config.ts instrumentation.ts package.json pnpm-lock.yaml src/lib src/app/api/health config/railway.json .github/workflows/ci.yml"
          if [ -z "$BASE_REF" ]; then
            echo "changed=true" >> "$GITHUB_OUTPUT"
            echo "A manual run builds the image unconditionally." > scope.txt
            exit 0
          fi
          git fetch --no-tags --depth=200 origin "$BASE_REF" || true
          base=$(git merge-base FETCH_HEAD HEAD 2>/dev/null || true)
          if [ -z "$base" ]; then
            echo "changed=true" >> "$GITHUB_OUTPUT"
            echo "No merge base with \`$BASE_REF\` was found, so the image is built anyway." > scope.txt
            exit 0
          fi
          # shellcheck disable=SC2086
          files=$(git diff --name-only "$base" HEAD -- $paths)
          if [ -n "$files" ]; then
            echo "changed=true" >> "$GITHUB_OUTPUT"
            printf 'The container contract changed:\n\n```\n%s\n```\n' "$files" > scope.txt
          else
            echo "changed=false" >> "$GITHUB_OUTPUT"
            echo "No file of the container contract changed; the image was not rebuilt." > scope.txt
          fi

      # No `--build-arg` carrying a secret, and no secret in the environment either: the step
      # refuses to run if one is present, so "the build needed no credential" is a fact the log
      # can be read for (spec 001 §14 A17 — a build argument survives in layer history).
      - name: Build the image with no credential in the environment (AC-8)
        id: image
        if: steps.scope.outputs.changed == 'true'
        run: |
          set -uo pipefail
          if env | grep -qE '^(DATABASE_URL|DATABASE_URL_UNPOOLED|INTERNAL_CRON_SECRET|R2_)'; then
            echo "::error::a server credential is present in the build environment (spec 001 §14 A17)"
            exit 1
          fi
          set -o pipefail
          docker build --progress=plain --tag "$IMAGE" . 2>&1 | tail -40

      - name: Run it and probe /api/health (T-08)
        id: probe
        if: steps.scope.outputs.changed == 'true'
        run: |
          set -uo pipefail
          # shellcheck disable=SC2086
          docker run -d --name fo-web -p 3000:3000 $RUNTIME_ENV "$IMAGE"
          code=000
          for _ in $(seq 1 45); do
            code=$(curl -s -o health.json -w '%{http_code}' http://127.0.0.1:3000/api/health || echo 000)
            [ "$code" = "200" ] && break
            sleep 1
          done
          echo "health_code=$code" >> "$GITHUB_OUTPUT"
          if [ "$code" != "200" ]; then
            docker logs fo-web | tail -40
            echo "::error::/api/health answered $code from the image (AC-8)"
            exit 1
          fi
          cat health.json
          grep -q '"status":"ok"' health.json || { echo "::error::health body is not ok"; exit 1; }
          user=$(docker exec fo-web id -un)
          echo "user=$user" >> "$GITHUB_OUTPUT"
          [ "$user" = "node" ] || { echo "::error::the image runs as $user, not node (AC-8)"; exit 1; }
          envfiles=$(docker exec fo-web sh -c 'ls -a /app | grep -c "^\.env" || true')
          echo "envfiles=$envfiles" >> "$GITHUB_OUTPUT"
          [ "$envfiles" = "0" ] || { echo "::error::the image contains $envfiles .env file(s) (AC-8)"; exit 1; }

      # The other half of the split (spec 001 §14 A17): the build stopped demanding the server keys,
      # so the *server* must. Same image, no server variables — `/api/health` must not answer 200,
      # because that is the Railway healthcheck and a deployment that cannot read its own database
      # must never turn `● Active`.
      - name: Assert the runtime gate is fail-closed (spec 001 §14 A17, AC-10's intent)
        id: bare
        if: steps.scope.outputs.changed == 'true'
        run: |
          set -uo pipefail
          docker run -d --name fo-web-bare -p 3001:3000 "$IMAGE"
          sleep 8
          code=$(curl -s -o bare.json -w '%{http_code}' http://127.0.0.1:3001/api/health || echo 000)
          echo "health_code=$code" >> "$GITHUB_OUTPUT"
          if [ "$code" = "200" ]; then
            echo "::error::/api/health answered 200 with no server variable set (spec 001 §14 A17)"
            exit 1
          fi
          logs=$(docker logs fo-web-bare 2>&1 | tail -40)
          echo "$logs"
          echo "$logs" | grep -q 'DATABASE_URL' || {
            echo "::error::the failure does not name DATABASE_URL"; exit 1; }

      # spec 002 T-04 / AC-2 and spec 040 T-29 / AC-28, at their new gate: a deployed environment
      # carrying a `.env.example` placeholder is refused. Until TASK-135 this was a `pnpm build`
      # failure; the build no longer sees the key, so the same refusal is asserted here, where the
      # value actually is. `APP_ENV=production`, every other key real-looking, `DATABASE_URL` the
      # placeholder: the report must name that key and print no value.
      - name: Assert a placeholder in production is refused at server start (spec 002 AC-2)
        id: placeholder
        if: steps.scope.outputs.changed == 'true'
        run: |
          set -uo pipefail
          docker run -d --name fo-web-prod -p 3002:3000 \
            -e APP_ENV=production \
            -e DATABASE_URL=postgres://user:pass@localhost:5432/fo \
            -e DATABASE_URL_UNPOOLED=postgresql://u:p@ep-x.eu-central-1.aws.neon.tech/fo \
            -e INTERNAL_CRON_SECRET=ci-cron-secret \
            -e R2_ACCOUNT_ID=0123456789abcdef0123456789abcdef \
            -e R2_BUCKET=ci-media \
            -e R2_BACKUPS_BUCKET=ci-backups \
            -e R2_S3_ENDPOINT=https://0123456789abcdef0123456789abcdef.eu.r2.cloudflarestorage.com \
            -e R2_ACCESS_KEY_ID=ci-access-key-id \
            -e R2_SECRET_ACCESS_KEY=ci-secret-access-key \
            -e R2_PUBLIC_BASE_URL=https://media.example.com \
            "$IMAGE"
          sleep 8
          code=$(curl -s -o prod.json -w '%{http_code}' http://127.0.0.1:3002/api/health || echo 000)
          echo "health_code=$code" >> "$GITHUB_OUTPUT"
          if [ "$code" = "200" ]; then
            echo "::error::/api/health exited 0 with a placeholder DATABASE_URL in production (spec 002 AC-2)"
            exit 1
          fi
          logs=$(docker logs fo-web-prod 2>&1 | tail -40)
          echo "$logs"
          echo "$logs" | grep -q 'DATABASE_URL: must be a real value in production' || {
            echo "::error::the report does not name DATABASE_URL as the placeholder offender (spec 002 T-04)"; exit 1; }
          if echo "$logs" | grep -q 'ci-secret-access-key'; then
            echo "::error::the report echoed another variable's value (AC-10)"
            exit 1
          fi

      - name: Summarise the container run (AC-8 / T-08)
        if: always()
        env:
          CHANGED: ${{ steps.scope.outputs.changed }}
          IMAGE_OUTCOME: ${{ steps.image.outcome }}
          HEALTH_CODE: ${{ steps.probe.outputs.health_code }}
          RUN_USER: ${{ steps.probe.outputs.user }}
          ENV_FILES: ${{ steps.probe.outputs.envfiles }}
          BARE_CODE: ${{ steps.bare.outputs.health_code }}
          PROD_CODE: ${{ steps.placeholder.outputs.health_code }}
        run: |
          set -uo pipefail
          {
            echo "### container (spec 040 AC-8 / T-08; spec 001 §14 A17)"
            echo ""
            if [ -f scope.txt ]; then cat scope.txt; fi
            echo ""
            if [ "${CHANGED:-false}" = "true" ]; then
              echo "| fact | value |"
              echo "|---|---|"
              echo "| \`docker build\` with no credential in the environment | ${IMAGE_OUTCOME:-skipped} |"
              echo "| \`GET /api/health\` from the running image | ${HEALTH_CODE:-n/a} |"
              echo "| runtime user | ${RUN_USER:-n/a} |"
              echo "| \`.env*\` files inside the image | ${ENV_FILES:-n/a} |"
              echo "| same image with no server variable → \`/api/health\` | ${BARE_CODE:-n/a} (must not be 200) |"
              echo "| placeholder \`DATABASE_URL\` in production → \`/api/health\` | ${PROD_CODE:-n/a} (must not be 200) |"
            fi
          } >> "$GITHUB_STEP_SUMMARY"
          docker rm -f fo-web fo-web-bare fo-web-prod >/dev/null 2>&1 || true

  # T-11 / AC-10 (TASK-005) and spec 002 T-04 / AC-2 (TASK-013), as spec 001 §14 A17 and spec 040
  # §14 A1 split them (TASK-135). The job asserts what a **build** owns, and asserts that it no
  # longer owns the rest:
  #   1. a missing variable **the build consumes** (`NEXT_PUBLIC_SITE_URL`) fails the build, names
  #      the offending key and echoes no other variable's value (a sentinel injected into a second
  #      key must not appear in the output);
  #   2. a missing **server** variable (`R2_BUCKET`) leaves the build green — that is the split,
  #      asserted positively, and it is what lets the container build with no credential. The key
  #      is still refused, at server start, by the `container` job's fail-closed run above;
  #   3. a deployed environment (`APP_ENV=production`) whose `NEXT_PUBLIC_SITE_URL` is still the
  #      `.env.example` placeholder fails the build naming **only** that key — spec 002 deleted the
  #      escape hatch spec 001 carried, so there is no way to opt out. The same refusal for
  #      `DATABASE_URL` is asserted by the `container` job, where the value now lives.
  env-build-failure:
    name: env-build-failure
    runs-on: ubuntu-latest
    timeout-minutes: 15
    needs: typecheck
    if: ${{ github.event_name == 'workflow_dispatch' || contains(github.event.pull_request.labels.*.name, 'ci:full') }}
    env:
      NEXT_TELEMETRY_DISABLED: "1"
      SENTINEL: SENTINEL_DO_NOT_PRINT_9f3a
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/setup
      - name: Build with NEXT_PUBLIC_SITE_URL removed and a sentinel in another key
        run: |
          set -o pipefail
          grep -v '^NEXT_PUBLIC_SITE_URL=' .env.example > .env.local
          sed -i "s|^R2_SECRET_ACCESS_KEY=.*|R2_SECRET_ACCESS_KEY=$SENTINEL|" .env.local
          set +e
          pnpm build > build.log 2>&1
          status=$?
          set -e
          cat build.log
          if [ "$status" -eq 0 ]; then
            echo "::error::pnpm build exited 0 with NEXT_PUBLIC_SITE_URL missing (AC-10)"
            exit 1
          fi
          if ! grep -q 'NEXT_PUBLIC_SITE_URL' build.log; then
            echo "::error::build output does not name NEXT_PUBLIC_SITE_URL (AC-10)"
            exit 1
          fi
          if grep -q "$SENTINEL" build.log; then
            echo "::error::build output echoed another variable's value (AC-10)"
            exit 1
          fi
          {
            echo "### env-build-failure (T-11 / AC-10, build half)"
            echo ""
            echo "- \`pnpm build\` exit status: $status (non-zero, as required)"
            echo "- output names \`NEXT_PUBLIC_SITE_URL\`: yes"
            echo "- output contains the sentinel value of another key: no"
          } >> "$GITHUB_STEP_SUMMARY"
      - name: Remove the tampered env file
        if: always()
        run: rm -f .env.local build.log
      # The split itself (spec 001 §14 A17): a **server** key is not a build input any more, so its
      # absence must leave the build green. Without this assertion the amendment would be a comment
      # rather than a gate, and the next person to "fix" the config by asserting everything again
      # would break the container build exactly as the 2026-09-18 Railway build was broken.
      - name: Build with a server key removed — it must now succeed (spec 001 §14 A17)
        run: |
          set -o pipefail
          grep -v '^R2_BUCKET=' .env.example > .env.local
          set +e
          pnpm build > build.log 2>&1
          status=$?
          set -e
          tail -20 build.log
          if [ "$status" -ne 0 ]; then
            echo "::error::pnpm build failed with R2_BUCKET missing; the build gate still demands a server key (spec 001 §14 A17)"
            exit 1
          fi
          {
            echo "### the build gate demands no server key (spec 001 §14 A17)"
            echo ""
            echo "- \`pnpm build\` with \`R2_BUCKET\` absent: exit $status (zero, as required)"
            echo "- the key is refused at server start instead — see the \`container\` job"
          } >> "$GITHUB_STEP_SUMMARY"
      - name: Remove the tampered env file (split)
        if: always()
        run: rm -f .env.local build.log
      # spec 040 T-29 / AC-28: real values everywhere except `NEXT_PUBLIC_SITE_URL`, which keeps the
      # `.env.example` placeholder, and `APP_ENV=production` — `APP_ENV`, not `VERCEL_ENV`, since
      # spec 040 §5.2 made it the one environment signal, which is what makes this job reproduce a
      # **Railway** production build as well as a Vercel one. The origin is inlined into every
      # canonical the build writes, so it is a build key and this stays a build failure; the report
      # must name that key and no other. The `DATABASE_URL` half of spec 002 T-04 moved to the
      # `container` job with the gate (spec 001 §14 A17).
      - name: Build in production with a placeholder NEXT_PUBLIC_SITE_URL (spec 040 T-29)
        env:
          APP_ENV: production
        run: |
          set -o pipefail
          sed \
            -e 's|^DATABASE_URL=.*|DATABASE_URL=postgresql://u:p@ep-ci-pooler.eu-central-1.aws.neon.tech/fo|' \
            -e 's|^DATABASE_URL_UNPOOLED=.*|DATABASE_URL_UNPOOLED=postgresql://u:p@ep-ci.eu-central-1.aws.neon.tech/fo|' \
            -e 's|^INTERNAL_CRON_SECRET=.*|INTERNAL_CRON_SECRET=ci-cron-secret|' \
            -e 's|^R2_ACCOUNT_ID=.*|R2_ACCOUNT_ID=0123456789abcdef0123456789abcdef|' \
            -e 's|^R2_BUCKET=.*|R2_BUCKET=ci-media|' \
            -e 's|^R2_BACKUPS_BUCKET=.*|R2_BACKUPS_BUCKET=ci-backups|' \
            -e 's|^R2_S3_ENDPOINT=.*|R2_S3_ENDPOINT=https://0123456789abcdef0123456789abcdef.eu.r2.cloudflarestorage.com|' \
            -e 's|^R2_ACCESS_KEY_ID=.*|R2_ACCESS_KEY_ID=ci-access-key-id|' \
            -e 's|^R2_SECRET_ACCESS_KEY=.*|R2_SECRET_ACCESS_KEY=ci-secret-access-key|' \
            -e 's|^R2_PUBLIC_BASE_URL=.*|R2_PUBLIC_BASE_URL=https://media.example.com|' \
            -e 's|^ENABLE_PSEUDO_LOCALES=.*|ENABLE_PSEUDO_LOCALES=false|' \
            -e 's|^ENABLE_DEV_UI=.*|ENABLE_DEV_UI=false|' \
            .env.example > .env.local
          set +e
          pnpm build > build.log 2>&1
          status=$?
          set -e
          cat build.log
          if [ "$status" -eq 0 ]; then
            echo "::error::pnpm build exited 0 with a placeholder NEXT_PUBLIC_SITE_URL in production (spec 040 AC-28)"
            exit 1
          fi
          if ! grep -q 'NEXT_PUBLIC_SITE_URL: must be a real value in production' build.log; then
            echo "::error::build output does not name NEXT_PUBLIC_SITE_URL as the placeholder offender (spec 040 T-29)"
            exit 1
          fi
          if grep -qE '^\s+- (R2_|INTERNAL_CRON_SECRET|DATABASE_URL)' build.log; then
            echo "::error::build output named a server key the build no longer grades (spec 001 §14 A17)"
            exit 1
          fi
          {
            echo "### placeholder NEXT_PUBLIC_SITE_URL in production (spec 040 T-29 / AC-28)"
            echo ""
            echo "- \`pnpm build\` exit status: $status (non-zero, as required)"
            echo "- output names \`NEXT_PUBLIC_SITE_URL\` and no server key: yes"
          } >> "$GITHUB_STEP_SUMMARY"
      - name: Remove the tampered env file (T-04)
        if: always()
        run: rm -f .env.local build.log

  # AC-20 / T-21 (TASK-002): every commit of the branch under `@commitlint/config-conventional`.
  #
  # The range is resolved in its own step instead of being interpolated from the event, because
  # this job also runs on `workflow_dispatch` — which is how CI is re-fired on a pull request that
  # is already ready and already labelled (b8dc8d1) — and on that event
  # `github.event.pull_request.base.sha` and `.head.sha` are both empty. Every dispatch run
  # therefore carried a guaranteed red job: commitlint exited 9 with "--from and --to point to the
  # same commit", and the summary step went on to run `git rev-list --count ..`. Found by
  # `/review 84` round 2, fixed by TASK-137. On a dispatch the range is the merge base with the
  # default branch, which is the same set of commits the pull-request event would have given; when
  # that leaves nothing to lint (a dispatch on the default branch itself) the job says so and
  # passes, because an empty range is not a convention violation.
  #
  # Every step below keeps its script free of `${{ }}` interpolation and takes the range through
  # `env:` from `steps.range.outputs`, so no command can be handed a value the event did not have.
  commitlint:
    name: commitlint
    if: ${{ github.event_name == 'workflow_dispatch' || contains(github.event.pull_request.labels.*.name, 'ci:full') }}
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: ./.github/actions/setup
      - name: Resolve the commit range
        id: range
        env:
          EVENT_BASE_SHA: ${{ github.event.pull_request.base.sha }}
          EVENT_HEAD_SHA: ${{ github.event.pull_request.head.sha }}
          DEFAULT_BRANCH: ${{ github.event.repository.default_branch }}
        run: |
          set -uo pipefail
          head="${EVENT_HEAD_SHA:-}"
          if [ -z "$head" ]; then
            head=$(git rev-parse HEAD)
          fi
          base="${EVENT_BASE_SHA:-}"
          source="the pull_request event"
          if [ -z "$base" ]; then
            branch="${DEFAULT_BRANCH:-main}"
            source="the merge base with $branch"
            git fetch --no-tags --quiet origin "$branch"
            if ! base=$(git merge-base FETCH_HEAD "$head"); then
              echo "::error::no merge base between $head and origin/$branch: the commit range cannot be resolved"
              exit 1
            fi
          fi
          {
            echo "base=$base"
            echo "head=$head"
            echo "source=$source"
          } >> "$GITHUB_OUTPUT"
          echo "range: $base..$head ($source)"
      - name: Lint every commit in the range
        id: commitlint
        env:
          BASE_SHA: ${{ steps.range.outputs.base }}
          HEAD_SHA: ${{ steps.range.outputs.head }}
        run: |
          set -uo pipefail
          if [ -z "$BASE_SHA" ] || [ "$BASE_SHA" = "$HEAD_SHA" ]; then
            echo "no commits in range; nothing to lint"
            exit 0
          fi
          pnpm exec commitlint \
            --from "$BASE_SHA" \
            --to "$HEAD_SHA" \
            --verbose 2>&1 | tee commitlint.log
          exit "${PIPESTATUS[0]}"
      - name: Summarise the commit range (AC-20)
        id: summary
        if: always()
        env:
          BASE_SHA: ${{ steps.range.outputs.base }}
          HEAD_SHA: ${{ steps.range.outputs.head }}
          RANGE_SOURCE: ${{ steps.range.outputs.source }}
          RESULT: ${{ steps.commitlint.outcome }}
        run: |
          set -uo pipefail
          commits=0
          if [ -n "$BASE_SHA" ] && [ -n "$HEAD_SHA" ] && [ "$BASE_SHA" != "$HEAD_SHA" ]; then
            commits=$(git rev-list --count "$BASE_SHA..$HEAD_SHA")
          fi
          problems=0
          if [ -f commitlint.log ]; then
            problems=$(grep -cE '^\s*✖' commitlint.log || true)
          fi
          {
            echo "### commitlint (AC-20 / T-21)"
            echo ""
            echo "- commits linted: $commits (\`$BASE_SHA..$HEAD_SHA\`)"
            echo "- range from: ${RANGE_SOURCE:-unresolved}"
            echo "- config: \`@commitlint/config-conventional\`; result: ${RESULT:-unknown}, $problems problem(s)"
          } >> "$GITHUB_STEP_SUMMARY"

  # AC-22 / T-23 (TASK-009): the three SEO validator CLIs over their committed fixture
  # directories. In spec 001 those directories hold only a `.gitkeep`, so the job prints
  # `no fixtures` three times and passes; spec 007 drops the real sitemap, hreflang and JSON-LD
  # fixtures in and this job turns red on a malformed one without a workflow change.
  #
  # `needs: typecheck` rather than `needs: lint`: the validators are TypeScript run under Node
  # type stripping, so a type error in them is a broken gate, and there is no reason to wait for
  # the unit suite.
  seo-validate:
    name: seo-validate
    runs-on: ubuntu-latest
    timeout-minutes: 10
    needs: typecheck
    if: ${{ github.event_name == 'workflow_dispatch' || contains(github.event.pull_request.labels.*.name, 'ci:full') }}
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/setup
      - name: Validate the SEO fixtures
        run: pnpm seo:validate | tee seo-validate.log
      - name: Summarise the fixture counts (AC-22)
        if: always()
        run: |
          set -euo pipefail
          {
            echo "### seo-validate (AC-22 / T-23)"
            echo ""
            echo "| validator | fixture dir | fixtures |"
            echo "|---|---|---|"
            for kind in sitemap hreflang schema; do
              case "$kind" in
                sitemap) ext="xml" ;;
                *) ext="json" ;;
              esac
              count=$(find "tests/fixtures/seo/$kind" -maxdepth 1 -type f -name "*.$ext" | wc -l | tr -d ' ')
              echo "| validate-$kind | \`tests/fixtures/seo/$kind\` | $count |"
            done
            echo ""
            echo "Bad-fixture cases live in \`tests/fixtures/seo/_cases/\` and are exercised by the unit"
            echo "suite in a temp directory, so this job's directories stay in their committed state."
          } >> "$GITHUB_STEP_SUMMARY"

  # spec 003 §2 "Lint, checks, CI", §11, AC-13, AC-22 / T-13, T-22 (TASK-040): the catalogue gate.
  # `pnpm i18n:check` fails on a key missing after fallback resolution, an unused `en` key without
  # `retained: true`, an ICU syntax error, an argument-set mismatch between a key and a
  # translation, a redundant `en-gb` override, a missing or orphan review record, a stale
  # `sourceHash`, or a `pathSegments` value that is not a unique lowercase-ASCII URL segment.
  #
  # `--summary` is what makes this job the observability surface of spec 003 §11: the script
  # appends the per-locale table (keys, missing after fallback, unreviewed count and share, stale
  # count, indexable) to `$GITHUB_STEP_SUMMARY` itself, computed from `unreviewedShare()` and
  # `isLocaleIndexable()` — the same functions spec 007 gates hreflang and sitemap membership
  # with, so a locale cannot look ready here and be excluded in code. While `de` and `pl` are
  # machine-drafted echoes of English they read 100 % unreviewed and `no`, which is the correct
  # answer, not a failure.
  i18n-check:
    name: i18n-check
    runs-on: ubuntu-latest
    timeout-minutes: 10
    needs: typecheck
    if: ${{ github.event_name == 'workflow_dispatch' || contains(github.event.pull_request.labels.*.name, 'ci:full') }}
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/setup
      - name: i18n:check — catalogues, manifests and pathSegments (AC-13, AC-22)
        id: i18n
        run: pnpm i18n:check --summary 2>&1 | tee i18n-check.log
      - name: Summarise the verdict
        if: always()
        run: |
          set -uo pipefail
          {
            echo ""
            echo "#### verdict"
            echo ""
            echo "| check | command | result |"
            echo "|---|---|---|"
            echo "| catalogue gate (AC-13, AC-22) | \`pnpm i18n:check --summary\` | ${{ steps.i18n.outcome }} |"
            echo ""
            if [ -f i18n-check.log ] && grep -q 'i18n:check failed' i18n-check.log; then
              echo "<details><summary>problems</summary>"
              echo ""
              echo '```'
              grep -A 200 'i18n:check failed' i18n-check.log
              echo '```'
              echo ""
              echo "</details>"
            fi
          } >> "$GITHUB_STEP_SUMMARY"

  # spec 005 §2 "Docs, fixtures, gates", AC-5 (TASK-062): the catalogue and price gate. It reads
  # committed TypeScript data and `messages/en.json` — no browser, no database, no preview
  # deployment — so it joins the `needs: typecheck` fan-out beside `i18n-check` and `seo-validate`
  # (spec 001 §14 A9's placement rule) rather than queueing behind the Playwright chain.
  #
  # What turning it red means: a product with no price in a destination, two open-ended rows for
  # one (product, country, tier, surcharge), a price outside its `plan/10` §2.3 band or off the
  # currency's psychological ending, a label key `messages/en.json` cannot resolve, or a projection
  # key set that no longer matches spec 002 §5.1 — each of which is a wrong price or a raw key on a
  # page, and each of which is cheapest to see here. Its step summary is the per-destination price
  # coverage table (spec 005 §11), which is the early warning that a country flip would produce
  # pages with no amount.
  catalogue-check:
    name: catalogue-check
    runs-on: ubuntu-latest
    timeout-minutes: 10
    needs: typecheck
    if: ${{ github.event_name == 'workflow_dispatch' || contains(github.event.pull_request.labels.*.name, 'ci:full') }}
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/setup
      - name: catalogue:check — dataset, prices, FX and projections (AC-5)
        id: catalogue
        run: pnpm catalogue:check --summary 2>&1 | tee catalogue-check.log
      - name: Summarise the verdict
        if: always()
        run: |
          set -uo pipefail
          {
            echo ""
            echo "#### verdict"
            echo ""
            echo "| check | command | result |"
            echo "|---|---|---|"
            echo "| catalogue + price gate (AC-5) | \`pnpm catalogue:check --summary\` | ${{ steps.catalogue.outcome }} |"
          } >> "$GITHUB_STEP_SUMMARY"

  # spec 007 §2, §11, AC-2 / T-03 (TASK-087): the corridor content gate. Eighteen rules over
  # `content/corridors/**` — does it parse, is it human-written, is the body long enough, is the
  # intro inside its band, is it distinct from its siblings, is the FAQ the right size and actually
  # about the country, do the SEO fields fit, does a slug or another country leak into the prose,
  # does it use one of the nine banned words, does a guide-state page claim a delivery time, a
  # cutoff, a florist count, a rating, a review or a delivery photo, does it invent a number or a
  # price, may a live file exist at all, does an `en-gb` override override, and are the related
  # destinations reciprocal — each reported as one line naming the file, the field and the rule.
  #
  # `needs: typecheck` beside `i18n-check`, `catalogue-check` and `seed-check` (spec 001 §14 A9's
  # placement rule): it reads committed markdown, touches no browser, no database and no preview
  # deployment. `--summary` writes the published-set summary of spec 007 §11 — per locale, the
  # number of corridor files, how many are reviewed and which countries they cover — which is the
  # number the `seo-auditor` compares against the existence rule and the number the founder
  # watches as native reviews land.
  corridor-check:
    name: corridor-check
    runs-on: ubuntu-latest
    timeout-minutes: 10
    needs: typecheck
    if: ${{ github.event_name == 'workflow_dispatch' || contains(github.event.pull_request.labels.*.name, 'ci:full') }}
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/setup
      - name: corridor:check — the eighteen content rules and the published-set summary (AC-2)
        id: corridor
        run: pnpm corridor:check --summary 2>&1 | tee corridor-check.log
      - name: Summarise the verdict
        if: always()
        run: |
          set -uo pipefail
          {
            echo ""
            echo "#### verdict"
            echo ""
            echo "| check | command | result |"
            echo "|---|---|---|"
            echo "| corridor content gate (AC-2) | \`pnpm corridor:check --summary\` | ${{ steps.corridor.outcome }} |"
            echo ""
            if [ -f corridor-check.log ] && grep -q 'corridor:check failed' corridor-check.log; then
              echo "<details><summary>problems</summary>"
              echo ""
              echo '```'
              grep -A 200 'corridor:check failed' corridor-check.log
              echo '```'
              echo ""
              echo "</details>"
            fi
          } >> "$GITHUB_STEP_SUMMARY"

  # spec 006 §2.3, §11, AC-10, AC-30 / T-10, T-30 (TASK-075): the seed-dataset gate. Nine rule
  # families over `seed/data/**` — schema and `version` headers, counts and the 40/14/8/10/12
  # split, referential integrity, slugs, prices, copy, media, "no PII and no third-party marks",
  # and the committed-imagery byte budgets — each reported as one line naming the file, the entity
  # key and the rule. Spec 009 AC-2 (TASK-124) added a tenth, `calendar`: a destination whose
  # picker renders dates carries holiday rows for every year of its 366-day horizon **from today**
  # (so this job can turn red on the day the holiday data runs out, which is the point), every
  # holiday has a `nameKey`, every dated occasion rule is datable; plus `slugs/product-slug-required`.
  # `--report` also prints spec 009 §11's picker state per destination as its first table.
  #
  # `needs: typecheck` beside `i18n-check` and `catalogue-check` (spec 001 §14 A9's placement
  # rule): it reads committed JSON, touches no browser, no database and no preview deployment, and
  # its step summary is the thing the founder reads. `--report` is what writes that summary — the
  # standing catalogue-health report of spec 006 §11: product counts by type, the per-(country,
  # category) and per-(country, occasion) coverage table against `plan/02` §6's six-product rule
  # with both sides of the threshold visible, price-band extremes, the description word-count
  # distribution, copy review shares per locale, committed image bytes, per-slot maxima, the count
  # of products still on a placeholder and the `seoTitle` near-duplicate pairs spec 008 must
  # nominate a primary for. Every readiness column in it is computed from the predicate the
  # application gates on, so a locale or a country cannot look ready here and be gated in code.
  seed-check:
    name: seed-check
    runs-on: ubuntu-latest
    timeout-minutes: 10
    needs: typecheck
    if: ${{ github.event_name == 'workflow_dispatch' || contains(github.event.pull_request.labels.*.name, 'ci:full') }}
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/setup
      - name: seed:check — the ten rule families, the health report and the picker-state summary (AC-10, AC-30; spec 009 AC-2)
        id: seed
        run: pnpm seed:check --report 2>&1 | tee seed-check.log
      - name: Summarise the verdict
        if: always()
        run: |
          set -uo pipefail
          {
            echo ""
            echo "#### verdict"
            echo ""
            echo "| check | command | result |"
            echo "|---|---|---|"
            echo "| seed dataset gate (AC-10, AC-30) | \`pnpm seed:check --report\` | ${{ steps.seed.outcome }} |"
            echo ""
            if [ -f seed-check.log ] && grep -q 'seed:check failed' seed-check.log; then
              echo "<details><summary>problems</summary>"
              echo ""
              echo '```'
              grep -A 200 'seed:check failed' seed-check.log
              echo '```'
              echo ""
              echo "</details>"
            fi
          } >> "$GITHUB_STEP_SUMMARY"

  # AC-24, AC-25, AC-26 / T-25, T-26, T-27 (TASK-010): the dev-OS gate. Runs the shell checks in
  # `tests/dev-os/` against the real `.claude/hooks/task-guard.sh`, `.claude/hooks/tasks-reminder.sh`
  # and `.claude/bin/task.sh`, so a change to a hook that breaks the "no code without a task"
  # rule turns a PR red instead of being noticed the next time an agent writes to `src/`.
  # spec 001 §11: this job's output is the audit trail that the guard and `task.sh` behave as
  # documented, so `scripts/dev-os-check.ts` writes the per-check assertion counts to the step
  # summary itself.
  #
  # Runner requirements: `bash`, `git` and `python3` (the guard parses its PreToolUse payload with
  # it) — all three are preinstalled on `ubuntu-latest`. The checks never touch the checkout's
  # `.claude/state/` or `TASKS.md`: each one points `CLAUDE_PROJECT_DIR` at a temp project.
  #
  # `needs: typecheck` rather than `needs: test-unit`: `pnpm test` already runs the same checks
  # through `tests/unit/dev-os.test.ts`, and this job exists to report them separately and
  # readably, so it should not queue behind the whole unit suite.
  dev-os-check:
    name: dev-os-check
    runs-on: ubuntu-latest
    timeout-minutes: 10
    needs: typecheck
    if: ${{ github.event_name == 'workflow_dispatch' || contains(github.event.pull_request.labels.*.name, 'ci:full') }}
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/setup
      - name: Assert the runner has what the hooks need
        run: |
          set -euo pipefail
          bash --version | head -n1
          git --version
          python3 --version
      - name: Run the dev-OS checks
        run: pnpm dev-os:check

  # **The origin the browser suites run against is CI's own (TASK-137).**
  #
  # What this job used to be, and why it changed. It polled the GitHub Deployments API for up to
  # fifteen minutes for a non-production deployment on the PR head SHA and handed that
  # `environment_url` to `e2e`, `visual` and `a11y` (each `needs: preview`). On this repository
  # the deployment either never appeared or answered `/api/health` with **500**, because the
  # Vercel project is the cold fallback of ADR-0018 and carries an empty environment store —
  # observed identically on PR 84, PR 85 and PR 87. The consequence, stated so nobody
  # re-discovers it: **no Playwright suite had ever run in CI on this project.** Every "CI green"
  # before 2026-09-21 meant lint, types, units and Lighthouse.
  #
  # So the job now produces the origin itself: `next build` from the committed `.env.example`
  # placeholders, `next start` on the runner, and a `/api/health` 200 gate before it declares
  # success. The build is uploaded once and re-served by the three browser jobs through
  # `.github/actions/preview-origin`, so all three measure the same bytes and the Actions-minutes
  # budget (spec 001 §14 A14) pays for one build, not four. `preview_url` still names the origin,
  # so no downstream job changed how it consumes it.
  #
  # **No secret is a build argument or a run-time value here** — the ruling of spec 001 §14 A17 /
  # spec 040 §14 A1 (TASK-135) stands, and the first step asserts it. `hostPlatform()` is
  # untouched: on a runner it resolves `local`, exactly as it does for the reviewer's `pnpm start`.
  #
  # Vercel's own check keeps its place on the pull request and keeps meaning what it says. The
  # last step still probes whatever Vercel deployed for Deployment Protection, the `fra1` function
  # region and `X-Robots-Tag: noindex` (AC-29 / T-30) and writes the verdict into the step
  # summary — but as **evidence**, `continue-on-error`, because a host we are leaving must never
  # again deny four suites an origin. When spec 040 §5.5 gives PRs a Railway environment (AC-26),
  # that is the deployment this job probes, and this step is where it lands.
  preview:
    name: preview
    # Pull requests only (a manual dispatch has no PR to probe), and only with the `ci:full` label.
    if: github.event_name == 'pull_request' && contains(github.event.pull_request.labels.*.name, 'ci:full')
    runs-on: ubuntu-latest
    timeout-minutes: 25
    permissions:
      contents: read
      # Read-only, and now only so the evidence step can *look at* what Vercel deployed.
      deployments: read
    outputs:
      preview_url: ${{ steps.serve.outputs.preview_url }}
    env:
      NEXT_TELEMETRY_DISABLED: "1"
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/setup

      # The same clean-clone build the `build` job makes (spec 001 §13 Q10, AC-1), and the same
      # assertion the `container` job makes about its environment: a server credential in scope
      # here would end up inlined or in a layer, and neither is allowed (spec 001 §14 A17).
      - name: Build the app from the committed placeholders, with no credential in scope
        id: next
        run: |
          set -euo pipefail
          if env | grep -qE '^(DATABASE_URL|DATABASE_URL_UNPOOLED|INTERNAL_CRON_SECRET|R2_)'; then
            echo "::error::a server credential is present in the build environment (spec 001 §14 A17)"
            exit 1
          fi
          cp .env.example .env.local
          pnpm build

      # `.next/cache` is the incremental cache and `.next/standalone` is a second copy of the
      # server that `next start` never reads: excluding both is what keeps the artifact in the
      # tens of megabytes instead of hundreds.
      - name: Package the build for the browser jobs
        id: package
        run: |
          set -euo pipefail
          tar -czf "$RUNNER_TEMP/preview-build.tgz" \
            --exclude='.next/cache' --exclude='.next/standalone' .next
          size=$(du -h "$RUNNER_TEMP/preview-build.tgz" | cut -f1)
          echo "size=$size" >> "$GITHUB_OUTPUT"
          echo "packaged $size"

      - name: Publish it
        uses: actions/upload-artifact@v4
        with:
          name: preview-build
          path: ${{ runner.temp }}/preview-build.tgz
          retention-days: 1
          if-no-files-found: error

      # The gate: this step cannot succeed unless `/api/health` answers 200 with `"status":"ok"`,
      # so `preview` can never again hand four suites a URL that is not serving the application.
      - name: Serve it and gate on /api/health (TASK-137)
        id: serve
        run: bash ./.github/actions/preview-origin/serve.sh

      # AC-29 / T-30, as evidence rather than as a gate. Three facts about whatever Vercel
      # deployed for this head SHA — Deployment Protection refuses an unauthenticated request,
      # the function region is `fra1`, the response is `noindex` — reported in the step summary
      # and in the job log. Absences (no bypass secret, no deployment) are facts about the
      # platform, not defects in the pull request, and are recorded rather than failed.
      - name: Vercel's own preview, probed as evidence (AC-29 / T-30)
        id: vercel
        continue-on-error: true
        env:
          GH_TOKEN: ${{ github.token }}
          REPO: ${{ github.repository }}
          HEAD_SHA: ${{ github.event.pull_request.head.sha }}
          BYPASS_SECRET: ${{ secrets.VERCEL_AUTOMATION_BYPASS_SECRET }}
          # One short look, not fifteen minutes: the browser suites are waiting on this job.
          ATTEMPTS: "3"
          INTERVAL: "10"
        run: |
          set -uo pipefail
          summary() { printf '%s\n' "$@" | tee -a "$GITHUB_STEP_SUMMARY"; }
          summary "### Vercel preview, as evidence (AC-29 / T-30)" ""
          if [ -z "$BYPASS_SECRET" ]; then
            summary "- no \`VERCEL_AUTOMATION_BYPASS_SECRET\` is set in this repository, so" \
                    "  Deployment Protection cannot be probed. Recorded, not failed: the browser" \
                    "  suites run against CI's own origin (TASK-137), and \`docs/runbooks/vercel-setup.md\`" \
                    "  says how to create the bypass if this evidence is wanted again."
            exit 0
          fi
          set -o pipefail
          preview_url=""
          for attempt in $(seq 1 "$ATTEMPTS"); do
            # `select(... != "production")`: a push to `main` and a PR can share a head SHA, and
            # the Production deployment must never be probed as if it were a preview.
            deployments=$(gh api "repos/$REPO/deployments?sha=$HEAD_SHA&per_page=100" \
              --jq '.[] | select(((.environment // "") | ascii_downcase) != "production") | .id' || true)
            for id in $deployments; do
              url=$(gh api "repos/$REPO/deployments/$id/statuses?per_page=100" \
                --jq 'map(select(.state == "success" and (.environment_url // "") != "" and (((.environment // "") | ascii_downcase) != "production"))) | .[0].environment_url // ""' || true)
              if [ -n "$url" ]; then
                preview_url="$url"
                break
              fi
            done
            if [ -n "$preview_url" ]; then break; fi
            echo "no successful deployment with an environment_url yet for $HEAD_SHA (attempt $attempt/$ATTEMPTS)"
            sleep "$INTERVAL"
          done
          if [ -z "$preview_url" ]; then
            summary "- no non-production Vercel deployment with an \`environment_url\` for" \
                    "  \`$HEAD_SHA\` after $((ATTEMPTS * INTERVAL)) s. ADR-0018 keeps the Vercel project as a" \
                    "  cold fallback with an empty environment store, so this is the expected" \
                    "  state; it is recorded, not failed. The \`Vercel\` check on this pull" \
                    "  request reports what the platform itself did."
            exit 0
          fi
          preview_url="${preview_url%/}"
          summary "- deployment: $preview_url"

          # 1. Without the bypass: Deployment Protection must refuse the request. Vercel
          # Authentication does not answer 401/403 to a plain GET: it answers a 302 to
          # `https://vercel.com/sso-api?url=...` (plus a `_vercel_sso_nonce` cookie). Both shapes
          # count as protected; a 200, or a redirect anywhere else, does not.
          unauth_headers=$(mktemp)
          unprotected_status=$(curl -sSI -o "$unauth_headers" -w '%{http_code}' "$preview_url/api/health")
          unprotected_location=$(grep -i '^location:' "$unauth_headers" | head -n1 | cut -d' ' -f2- | tr -d '\r' || true)
          rm -f "$unauth_headers"
          case "$unprotected_status" in
            401|403)
              protection_evidence="$unprotected_status"
              ;;
            301|302|303|307|308)
              case "$unprotected_location" in
                https://vercel.com/sso-api|https://vercel.com/sso-api[?/]*)
                  protection_evidence="$unprotected_status to vercel.com/sso-api"
                  ;;
                *)
                  echo "::error::GET $preview_url/api/health without the bypass header returned $unprotected_status redirecting to '$unprotected_location'; expected 401, 403, or a redirect to https://vercel.com/sso-api. Deployment Protection is not enabled on previews (AC-29)."
                  summary "- **unauthenticated \`/api/health\`: $unprotected_status to '$unprotected_location' — not protected**"
                  exit 1
                  ;;
              esac
              ;;
            *)
              echo "::error::GET $preview_url/api/health without the bypass header returned $unprotected_status; expected 401, 403, or a 302 to https://vercel.com/sso-api. Deployment Protection is not enabled on previews (AC-29)."
              summary "- **unauthenticated \`/api/health\`: $unprotected_status — not protected**"
              exit 1
              ;;
          esac

          # 2. With the bypass: 200, function region fra1, and noindex.
          headers=$(mktemp)
          bypass_status=$(curl -sSI -o "$headers" -w '%{http_code}' \
            -H "x-vercel-protection-bypass: $BYPASS_SECRET" \
            -H "x-vercel-set-bypass-cookie: false" \
            "$preview_url/api/health")
          vercel_id=$(grep -i '^x-vercel-id:' "$headers" | head -n1 | cut -d' ' -f2- | tr -d '\r')
          robots=$(grep -i '^x-robots-tag:' "$headers" | head -n1 | cut -d' ' -f2- | tr -d '\r')
          rm -f "$headers"
          summary "- unauthenticated \`/api/health\`: $protection_evidence (protected)" \
                  "- with bypass: $bypass_status" \
                  "- \`x-vercel-id\`: ${vercel_id:-absent}" \
                  "- \`X-Robots-Tag\`: ${robots:-absent}"
          if [ "$bypass_status" != "200" ]; then
            echo "::error::GET $preview_url/api/health with the bypass header returned $bypass_status; expected 200 (AC-29). ADR-0018's cold fallback has an empty environment store, which is what makes this 500 — it is why the browser suites no longer depend on it (TASK-137)."
            exit 1
          fi
          if [ -z "$vercel_id" ]; then
            echo "::error::no x-vercel-id response header; cannot verify the function region (AC-29)."
            exit 1
          fi
          # `x-vercel-id` is `<cdn-node>::<function-region>::<id>`: the first segment is the edge
          # node that took the request (wherever the runner is), so `fra1` must be *a* segment,
          # never the prefix.
          case "::$vercel_id::" in
            *"::fra1::"*) ;;
            *)
              echo "::error::x-vercel-id is '$vercel_id'; expected 'fra1' as one of its '::'-separated segments (spec 001 §5 Hosting, §8 EU data residency)."
              exit 1
              ;;
          esac
          case "$robots" in
            *noindex*) ;;
            *)
              echo "::error::X-Robots-Tag is '$robots'; every Phase 0 response must be noindex (spec 001 §5, AC-15)."
              exit 1
              ;;
          esac

  # AC-17 / T-15, T-16, T-18 (TASK-008), on the origin TASK-137 gave them. The three suites ran
  # against the Vercel preview until 2026-09-21 — that is what the AC asked for, and it is why
  # they never ran at all: the deployment answers `/api/health` with 500 (ADR-0018's cold
  # fallback, empty environment store), so `preview` failed and all three were skipped on PRs 84,
  # 85 and 87. Each job now unpacks the build `preview` published and serves it on its own runner
  # through `./.github/actions/preview-origin`, which does not return until `/api/health` answers
  # 200. `PLAYWRIGHT_BASE_URL` is still `${{ needs.preview.outputs.preview_url }}`; it now names
  # that origin (`http://localhost:3000`), which is also the origin `.env.example`'s
  # `NEXT_PUBLIC_SITE_URL` inlines, so every canonical under test matches the host serving it.
  #
  # What this costs: the Vercel-only assertions (`fra1`, Deployment Protection, the `vercel.live`
  # allowance) are not exercised by these suites any more — the specs that make them branch on
  # the hostname already, and `preview`'s evidence step reports them. What it buys: the suites
  # run.
  #
  # `VERCEL_AUTOMATION_BYPASS_SECRET` is gone from all three: a localhost origin has no
  # Deployment Protection to get past, and sending the header would put a secret on the wire for
  # nothing.
  #
  # These jobs are skipped on `push` to `main`, because `preview` is a pull-request job.
  e2e:
    name: e2e
    runs-on: ubuntu-latest
    timeout-minutes: 20
    needs: preview
    env:
      PLAYWRIGHT_BASE_URL: ${{ needs.preview.outputs.preview_url }}
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/setup
      # Unpack the build `preview` published, serve it, and do not proceed until `/api/health`
      # answers 200 (TASK-137).
      - uses: ./.github/actions/preview-origin
      - run: pnpm exec playwright install --with-deps chromium
      - name: pnpm test:e2e
        env:
          PLAYWRIGHT_JSON_OUTPUT_NAME: playwright-e2e.json
        run: pnpm test:e2e --reporter=github,json,list
      - name: Upload the Playwright report
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report-e2e
          path: |
            playwright-report/
            test-results/
          retention-days: 7
          if-no-files-found: warn
      - name: Summarise the e2e run (AC-14, AC-15 / T-15, T-16)
        if: always()
        run: |
          set -uo pipefail
          if [ ! -f playwright-e2e.json ]; then
            echo "::error::Playwright produced no JSON report; cannot summarise the e2e run"
            exit 1
          fi
          # `suites` is a tree; `..|.tests?//empty` flattens it to every test, whatever the nesting.
          expected=$(jq '[.suites | .. | .tests? // empty | .[]] | length' playwright-e2e.json)
          statuses=$(jq -r '[.suites | .. | .specs? // empty | .[] | if .ok then "passed" else "failed" end] | group_by(.) | map("\(.[0]): \(length)") | join(", ")' playwright-e2e.json)
          # The projects that actually ran, not every project in `playwright.config.ts`.
          projects=$(jq -r '[.suites | .. | .specs? // empty | .[] | .tests[].projectName] | unique | join(", ")' playwright-e2e.json)
          {
            echo "### e2e (AC-14, AC-15 / T-15, T-16)"
            echo ""
            echo "- target: ${{ needs.preview.outputs.preview_url }}"
            echo "- projects: $projects"
            echo "- tests: $expected — $statuses"
          } >> "$GITHUB_STEP_SUMMARY"

  visual:
    name: visual
    runs-on: ubuntu-latest
    timeout-minutes: 20
    needs: preview
    env:
      PLAYWRIGHT_BASE_URL: ${{ needs.preview.outputs.preview_url }}
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/setup
      # Unpack the build `preview` published, serve it, and do not proceed until `/api/health`
      # answers 200 (TASK-137).
      - uses: ./.github/actions/preview-origin
      - run: pnpm exec playwright install --with-deps chromium
      # Baselines are committed (AC-17) and, since spec 003 gave `/` real text, per platform:
      # this job compares against `__screenshots__/<project>/linux/`. `--update-snapshots` is
      # never passed here, so a missing or changed baseline fails the job — and the artifact
      # below carries the screenshots Playwright wrote, which is how the `linux/` baseline is
      # produced without a Linux machine (see `playwright.config.ts`).
      - name: pnpm test:visual
        env:
          PLAYWRIGHT_JSON_OUTPUT_NAME: playwright-visual.json
        run: pnpm test:visual --reporter=github,json,list
      - name: Upload the Playwright report (with the diff images)
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report-visual
          path: |
            playwright-report/
            test-results/
            tests/visual/__screenshots__/
          retention-days: 7
          if-no-files-found: warn
      - name: Summarise the visual run (AC-17 / T-18)
        if: always()
        run: |
          set -uo pipefail
          if [ ! -f playwright-visual.json ]; then
            echo "::error::Playwright produced no JSON report; cannot summarise the visual run"
            exit 1
          fi
          # `suites` is a tree; `..|.tests?//empty` flattens it to every test, whatever the nesting.
          expected=$(jq '[.suites | .. | .tests? // empty | .[]] | length' playwright-visual.json)
          statuses=$(jq -r '[.suites | .. | .specs? // empty | .[] | if .ok then "passed" else "failed" end] | group_by(.) | map("\(.[0]): \(length)") | join(", ")' playwright-visual.json)
          # The projects that actually ran, not every project in `playwright.config.ts`.
          projects=$(jq -r '[.suites | .. | .specs? // empty | .[] | .tests[].projectName] | unique | join(", ")' playwright-visual.json)
          {
            echo "### visual (AC-17 / T-18)"
            echo ""
            echo "- target: ${{ needs.preview.outputs.preview_url }}"
            echo "- projects: $projects"
            echo "- tests: $expected — $statuses"
            echo "- screenshots compared against the committed baselines at a 0.1 % threshold;"
            echo "  a missing baseline fails the job (\`--update-snapshots\` is never passed in CI)."
          } >> "$GITHUB_STEP_SUMMARY"

  a11y:
    name: a11y
    runs-on: ubuntu-latest
    timeout-minutes: 20
    needs: preview
    env:
      PLAYWRIGHT_BASE_URL: ${{ needs.preview.outputs.preview_url }}
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/setup
      # Unpack the build `preview` published, serve it, and do not proceed until `/api/health`
      # answers 200 (TASK-137).
      - uses: ./.github/actions/preview-origin
      - run: pnpm exec playwright install --with-deps chromium
      # spec 001 §8: zero serious/critical axe violations on `/`.
      - name: pnpm test:a11y
        env:
          PLAYWRIGHT_JSON_OUTPUT_NAME: playwright-a11y.json
        run: pnpm test:a11y --reporter=github,json,list
      - name: Upload the Playwright report (with the axe attachment)
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report-a11y
          path: |
            playwright-report/
            test-results/
          retention-days: 7
          if-no-files-found: warn
      - name: Summarise the a11y run (AC-17 / T-18)
        if: always()
        run: |
          set -uo pipefail
          if [ ! -f playwright-a11y.json ]; then
            echo "::error::Playwright produced no JSON report; cannot summarise the a11y run"
            exit 1
          fi
          # `suites` is a tree; `..|.tests?//empty` flattens it to every test, whatever the nesting.
          expected=$(jq '[.suites | .. | .tests? // empty | .[]] | length' playwright-a11y.json)
          statuses=$(jq -r '[.suites | .. | .specs? // empty | .[] | if .ok then "passed" else "failed" end] | group_by(.) | map("\(.[0]): \(length)") | join(", ")' playwright-a11y.json)
          # The projects that actually ran, not every project in `playwright.config.ts`.
          projects=$(jq -r '[.suites | .. | .specs? // empty | .[] | .tests[].projectName] | unique | join(", ")' playwright-a11y.json)
          {
            echo "### a11y (AC-17 / T-18)"
            echo ""
            echo "- target: ${{ needs.preview.outputs.preview_url }}"
            echo "- projects: $projects"
            echo "- tests: $expected — $statuses"
            echo "- axe: zero serious/critical violations required (spec 001 §8)."
          } >> "$GITHUB_STEP_SUMMARY"

  # AC-23 / T-24 (TASK-009); spec 003 AC-27 / T-27 (TASK-043); **spec 004 AC-24 (TASK-056), which
  # is where this job stopped being informational**: Lighthouse CI with the `plan/01` §7 budgets as
  # assertions, over the five URLs of `tests/fixtures/seo/lighthouse-urls.json` — `/`, `/en`,
  # `/en-gb`, `/de`, `/pl`.
  #
  # **No `continue-on-error`.** spec 001 §13 Q4 made the job informational "until spec 004"; spec
  # 003 §14 A12 recorded why a date was not enough (the framework's own client runtime was over the
  # script budget on a page with no application JavaScript); spec 004 §13 Q13 and §14 A1 restated
  # the budget as 128 KB **Brotli**, TASK-046 and TASK-085 took zod and the client message provider
  # out of the browser, and TASK-056 measured the finished pages and fixed the last red assertion
  # (see `lighthouserc.json`'s note and `tests/e2e/lcp.spec.ts`). A red check here is now a
  # regression, which is the whole point of a budget.
  #
  # **It measures this repository's bytes, not a preview.** The job builds, starts `next start` and
  # puts `scripts/seo/brotli-origin.ts` in front of it, because `resource-summary:script:size` is
  # transfer size and neither previously available origin sent the encoding the budget is written
  # in: `next start` negotiates gzip, and a protected Vercel preview injects `vercel.live` — tens of
  # KB of platform script that is absent from production and in no build output of ours. That is
  # the decision `lighthouserc.json` left to TASK-056 ("production URLs, or a first-party-only
  # budget"), and it makes this number equal to the one `pnpm budget:client-js` prints in `build`.
  # The consequence to keep in mind: this job no longer proves anything about the *deployment*
  # (TTFB, edge cache, real headers) — the `e2e` suite against the preview does that.
  #
  # `NO_FCP` — Lighthouse aborting because the page painted nothing — has not been the expected
  # outcome since TASK-035 gave `/` real text. The branch below stays because that outcome must
  # never be read as a verdict in either direction, and the step summary is written *after* the run
  # so it can say which of the three happened instead of guessing in advance. Spec 001 AC-23's
  # queued correction to that wording is discharged here (spec 004 §14 A17).
  #
  # `@lhci/cli` is fetched pinned by `npx` instead of being a devDependency: it pulls in Lighthouse
  # and Chrome plumbing that every other job would then install on every run, and it is needed in
  # exactly this one place.
  lighthouse:
    name: lighthouse
    runs-on: ubuntu-latest
    # Five URLs x three runs on a shared runner, plus a build.
    timeout-minutes: 30
    needs: build
    env:
      NEXT_TELEMETRY_DISABLED: "1"
      LHCI_BASE_URL: http://127.0.0.1:3001
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/setup
      # The same clean-clone build the `build` job makes, from the committed placeholders; the
      # measured document must be one this repository produced, not one a platform rewrote.
      - run: cp .env.example .env.local
      - name: Build and serve the production output behind the Brotli origin
        run: |
          set -euo pipefail
          pnpm build
          pnpm start &
          npx --yes wait-on http://127.0.0.1:3000/en --timeout 120000
          pnpm lighthouse:origin &
          npx --yes wait-on http://127.0.0.1:3001/en --timeout 60000
          # Warm every measured URL once before the run. Measured locally: the first request to a
          # cold `next start` reported LCP 2 679 ms on `/` against 1 459-1 492 ms on the two runs
          # after it — a server start-up cost, not a page cost, and one that would decide a
          # required check by whichever run happened to be first.
          for path in $(node -e 'process.stdout.write(require("./tests/fixtures/seo/lighthouse-urls.json").join(" "))'); do
            curl -sS -o /dev/null "http://127.0.0.1:3001$path"
          done
      - name: Run Lighthouse CI against the Brotli origin
        id: lhci
        run: |
          set -euo pipefail
          # Assigned first so `set -e` aborts when the URL list is unreadable, instead of running
          # Lighthouse against nothing. Left unquoted below on purpose: one word per URL.
          urls=$(node scripts/seo/lighthouse-urls.ts)
          # shellcheck disable=SC2086
          npx --yes @lhci/cli@0.15.1 autorun \
            --config=lighthouserc.json \
            $urls 2>&1 | tee lhci-output.log

      # Written after the run, with `if: always()`, because "did the budgets hold?" is not
      # answerable before it. The per-URL table is spec 004 §11's requirement: a regression must
      # name the locale it happened in.
      - name: Summarise what Lighthouse actually measured
        if: always()
        env:
          LHCI_OUTCOME: ${{ steps.lhci.outcome }}
        run: |
          set -uo pipefail
          log=lhci-output.log
          # `upload.target: filesystem` writes the manifest **inside `upload.outputDir`**, not at
          # the root of `.lighthouseci/` — that is where the raw `lhr-*.json` of the collect step
          # land. Guarding on the wrong one is silent: the `if` is simply false and the per-URL
          # table §11 asks for never prints, on a green run as on a red one (`/review 61`). The
          # path is pinned against `lighthouserc.json` by `tests/unit/ci-workflow.test.ts`, so
          # moving `outputDir` fails a unit test instead of emptying this summary.
          manifest=.lighthouseci/reports/manifest.json
          {
            echo "### lighthouse (spec 004 AC-24 — required)"
            echo ""
            echo "URL set \`tests/fixtures/seo/lighthouse-urls.json\`, measured against"
            echo "\`next start\` behind \`scripts/seo/brotli-origin.ts\` (Brotli, quality 11) —"
            echo "this repository's own bytes, with no platform script in the total."
            echo "Budgets: performance / accessibility / best-practices >= 0.95, LCP < 2000 ms,"
            echo "CLS < 0.05, script transfer <= 131 072 B, image transfer <= 204 800 B."
            echo ""
            if [ -f "$manifest" ]; then
              echo "| URL | perf | a11y | best-practices | LCP (ms) | CLS | script (B, br) |"
              echo "|---|---|---|---|---|---|---|"
              jq -r '.[] | select(.isRepresentativeRun) | "| \(.url) | \(.summary.performance) | \(.summary.accessibility) | \(.summary["best-practices"]) | \(.jsonPath) |"' \
                "$manifest" > /tmp/lh-rows.txt || true
              # The manifest carries the category scores; the metric values come from each run's
              # own report, so the table names the number that failed rather than a score.
              while IFS= read -r row; do
                json=$(echo "$row" | awk -F'|' '{print $6}' | xargs)
                metrics=$(jq -r '"\(.audits["largest-contentful-paint"].numericValue | floor) | \(.audits["cumulative-layout-shift"].numericValue) | \([.audits["resource-summary"].details.items[] | select(.resourceType == "script")][0].transferSize)"' "$json")
                echo "$row" | awk -F'|' -v m="$metrics" '{printf "|%s|%s|%s|%s| %s |\n", $2, $3, $4, $5, m}'
              done < /tmp/lh-rows.txt
              echo ""
            else
              # Said out loud rather than skipped, so the table going missing is a visible defect
              # in this summary and not an absence nobody notices.
              echo "_No per-URL table: \`$manifest\` was not written. Lighthouse did not reach the"
              echo "upload step, or \`upload.outputDir\` in \`lighthouserc.json\` moved._"
              echo ""
            fi
            if [ "$LHCI_OUTCOME" = "success" ]; then
              echo "**Budgets measured and met.** Every assertion in \`lighthouserc.json\` is an"
              echo "\`error\` and none fired."
            elif [ -f "$log" ] && grep -qE 'NO_FCP|did not paint any content' "$log"; then
              echo "**No budget was measured: this is neither a budget breach nor a pass.**"
              echo ""
              echo "Lighthouse aborted with \`NO_FCP\` (\"the page did not paint any content\")"
              echo "before a single metric existed, so the assertions were never evaluated and no"
              echo "conclusion about the budgets can be drawn from this run in either direction."
              echo "Every measured URL renders text since TASK-035, so a \`NO_FCP\` here means the"
              echo "origin did not serve the document — read the step log."
            else
              echo "**The run ended with at least one assertion over budget, or failed before a"
              echo "verdict.** Only a failure that lists a metric against its budget is a budget"
              echo "breach; anything else measured nothing. The failing lines of the run:"
              echo ""
              echo '```'
              grep -E 'expected|failure|Assertion' "$log" | head -40 || true
              echo '```'
            fi
            # `tee`, not `>>`: the same verdict belongs in the step log, so it can be read from
            # `gh run view --log` without opening the run summary page.
          } | tee -a "$GITHUB_STEP_SUMMARY"

      - name: Upload the Lighthouse reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: lighthouseci
          path: .lighthouseci/
          retention-days: 7
          if-no-files-found: ignore
