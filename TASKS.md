# TASKS.md — single source of truth

Maintained by the orchestrator (`/plan-tasks`, `/status`) and updated by implementers (`/implement`). Task IDs are sequential and never reused. Statuses: `todo` · `in_progress` · `in_review` · `blocked` · `done` · `dropped`.

## Phase progress
| Phase | Specs approved | Tasks done / total | Gate status |
|---|---|---|---|
| 0 Demo | 6 / 12 | 37 / 85 | specs 001 and 003 complete (2026-09-08); spec 002 approved and planned, provisioning parked (TASK-013 `blocked`); spec 004 approved and planned 2026-09-08 against the founder-approved design in `docs/design/homepage-v1/` (TASK-045…TASK-056); specs 005 and 006 approved 2026-09-09 with every §13 recommendation binding and planned the same day (TASK-060…TASK-071 and TASK-072…TASK-083; TASK-070/071/082/083 `blocked` on TASK-013 provisioning); next: `/implement TASK-051` (spec 004's unblocked frontend chain) and `/implement TASK-060` (spec 005's module skeleton and the `fo/no-float-money` flip) in parallel |
| 1 MVP | 0 / 13 | 0 / 0 | — |
| 2 Portal | 0 / 6 | 0 / 0 | — |
| 3 CRM | 0 / 8 | 0 / 0 | — |
| 4 Scale | — | — | — |

## Tasks
Every task: one PR, branch `task/TASK-NNN-<slug>`, PR title `<type>(<scope>): … (TASK-NNN)`, definition of done = `CLAUDE.md` "Definition of done" (7 items). "Spec / AC owned" lists the acceptance criteria the reviewer verifies in that PR; each AC of a spec is owned by exactly one task. Test IDs refer to the spec's §10 table.

| ID | Title | Spec / AC owned | Phase | Status | Owner agent | PR | Depends on | Blockers / notes |
|---|---|---|---|---|---|---|---|---|
| TASK-001 | Scaffold: Next.js App Router in TS strict, `plan/01` §5 tree, Tailwind logical vocabulary, Prettier + base ESLint, Vitest baseline, `.gitignore` | `specs/001-repo-dev-os-bootstrap.md` · AC-1, AC-2, AC-3, AC-32 | 0 | done | backend-implementer | [#1](https://github.com/itsahmeds/flowers-overseas/pull/1) | — | Branch `task/TASK-001-scaffold`. pnpm via `packageManager` + Corepack, Node 24 major (§13 Q2/Q3). Includes `scripts/check-layout.ts` (T-03), `tsconfig.fixtures.json` + `tests/fixtures/ts/unchecked-index.ts` (T-02), Vitest node env + `pnpm test` with one smoke test (enables RuleTester tests in 003/004; AC-16 itself owned by TASK-008), empty module barrels with owning-spec comment, `supabase/migrations/`, `seed/`, `messages/` with `.gitkeep`. Tests: T-01, T-02, T-03, T-33 (CI job for T-01/T-33 is added by TASK-002/011; local run here). PR must already be titled `… (TASK-001)` (§12 note c). |
| TASK-002 | Git hooks and PR policy: husky + lint-staged + commitlint, minimal `ci.yml` (lint → typecheck → build), `pr-policy.yml` | `specs/001` · AC-19, AC-20 | 0 | done | backend-implementer | [#2](https://github.com/itsahmeds/flowers-overseas/pull/2) | TASK-001 | Branch `task/TASK-002-hooks-pr-policy`. Title regex, branch regex, bot + `no-task` exemption per §13 Q5; commitlint on PR commits. Later tasks append their own `ci.yml` jobs; final job set/order verified in TASK-011. Tests: T-20 (throwaway PR or `act`), T-21. |
| TASK-003 | Lint rules `fo/no-physical-css` (+ Stylelint disallow list) and `fo/no-literal-strings`; local `fo/` plugin skeleton; `tests/fixtures/lint/` excluded from main lint | `specs/001` · AC-4, AC-5, AC-6 | 0 | done | backend-implementer | [#3](https://github.com/itsahmeds/flowers-overseas/pull/3) | TASK-001 | Branch `task/TASK-003-lint-css-i18n-rules`. Mechanises §7 (logical CSS, no literal strings). AC-6 check ships as a script/unit test (T-07). Tests: T-04, T-05, T-06, T-07. Deviation: spec §5 names Stylelint `declaration-property-disallowed-list`, which does not exist in Stylelint 17; implemented as `property-disallowed-list` + `declaration-property-value-disallowed-list`. Fixtures stay excluded from `pnpm lint`; `pnpm lint:fixtures` is the executable form of AC-4/AC-5. |
| TASK-004 | Lint rules `fo/no-direct-order-status-write`, `fo/no-geo-redirect`, `fo/no-float-money`; `import/no-restricted-paths` module boundaries | `specs/001` · AC-7, AC-8, AC-9 · plus §2/§7/§8 `fo/no-float-money` fixtures (no AC id; explicit here) | 0 | done | backend-implementer | [#4](https://github.com/itsahmeds/flowers-overseas/pull/4) | TASK-003 | Branch `task/TASK-004-lint-domain-rules`. ADR-0006, ADR-0009. `fo/no-float-money` fixture-only in 001 (enforced from spec 005). Tests: T-08, T-09, T-10 + RuleTester for no-float-money (decimal literal / `parseFloat` / `toFixed` invalid; `*_minor` valid). |
| TASK-005 | Env schema (`lib/env.ts`, `.env.example`, `env:check`), JSON logger with PII redaction, `x-request-id` middleware, `no-console`, Sentry no-op wiring with `beforeSend` scrub | `specs/001` · AC-10, AC-11, AC-12, AC-13 | 0 | done | backend-implementer | [#5](https://github.com/itsahmeds/flowers-overseas/pull/5) | TASK-004 | Branch `task/TASK-005-env-logger-sentry`. Placeholder policy §13 Q10; Sentry EU region §13 Q6; release = SHA, environment = `VERCEL_ENV`; request start/end `info` lines only (§11). Adds `tests/fixtures/env/{valid,missing-key,extra-key}`, `tests/fixtures/lint/console.ts`, and the AC-10 build-failure CI job. Tests: T-11 (CI), T-12, T-13, T-14. Compliance: reviewer records Sentry processor row in `docs/compliance/ropa.md` at PASS (§8 item 1). |
| TASK-006 | Health route, placeholder shell (`layout`, `page`, `not-found`, `error`), `robots.ts` disallow-all, non-production `X-Robots-Tag: noindex` header, `lib/cache.ts` noop seam | `specs/001` · AC-14, AC-15 · plus §5.4 cache seam and §5.3 404/500 shells (no AC id; explicit here) | 0 | done | frontend-implementer | [#6](https://github.com/itsahmeds/flowers-overseas/pull/6) | TASK-005 | Branch `task/TASK-006-health-shell-noindex`. §6 indexability belt-and-braces (meta + header + robots), zero `Set-Cookie`, no third-party scripts, `<html lang="en" dir="ltr">` (lang literal documented as 003 removal in TASK-012). Reviewer verifies AC-14/15 locally via `pnpm build && pnpm start` + `curl -I`; preview re-check happens in TASK-008's e2e (T-15, T-16). |
| TASK-007 | Vercel project: `vercel link`, Git integration, function region `fra1`, Deployment Protection on preview, preview/production env vars from `.env.example` keys, wait-for-preview step in `ci.yml` | `specs/001` · AC-29 | 0 | done | backend-implementer | [#7](https://github.com/itsahmeds/flowers-overseas/pull/7) | TASK-006 | Branch `task/TASK-007-vercel-project`. Founder setup done (project linked, Git integration, Vercel Authentication Standard Protection, bypass secret, preview+production env vars incl. `ALLOW_PLACEHOLDER_ENV=true` on both). Tests: T-30 recorded automatically by the `preview` CI job and quoted in PR #7 (302 to vercel.com/sso-api unauthenticated, 200 with bypass, `x-vercel-id: iad1::fra1::…`, `X-Robots-Tag: noindex`); gate green on run 34143223514. Hobby plan carries Vercel Authentication (not Password Protection), so §13 Q9's preview default holds without Pro. Compliance: reviewer records Vercel processor row in RoPA at PASS (§8 item 2). Cloudflare/domain out of scope (§2 Hosting). Follow-ups from review of PR #5: add `NEXT_PUBLIC_VERCEL_GIT_COMMIT_SHA` to env schema + `.env.example` so client Sentry `release` is set; harden `beforeSend` in `src/lib/sentry.ts` to deep-scrub (`redact()`) incl. breadcrumbs/contexts/exception values before the first DSN is set; add the Vercel processor row to `docs/compliance/ropa.md` at PASS (§8 item 2). From review of PR #6: `deploymentEnvironment()` treats unset `VERCEL_ENV` as development, so the global `X-Robots-Tag: noindex` would fire on the ADR-0012 Railway fallback in production; key it on a host-independent signal or document. |
| TASK-008 | Test harness: `vitest.coverage.json` thresholds, integration suite (`describe.skip` + 002 TODO) with Postgres service job, Playwright `e2e`/`visual`/`a11y` projects + `pseudo-rtl` device stub, MSW skeleton (`onUnhandledRequest: "error"`), `tests/fixtures` barrel + README, smoke tests, preview-run CI jobs | `specs/001` · AC-16, AC-17, AC-18 · plus §7 pseudo-RTL device stub (no AC id; explicit here) | 0 | done | frontend-implementer | [#8](https://github.com/itsahmeds/flowers-overseas/pull/8) | TASK-007 | Branch `task/TASK-008-test-harness`. `PLAYWRIGHT_BASE_URL` = preview in CI, `localhost:3000` locally; visual baseline for `/` committed; axe zero serious/critical (§8 accessibility). Tests: T-15, T-16, T-17, T-18, T-19. From review of PR #7: add the `test:unit` CI job here (252 unit tests currently ungated in CI); wait-for-preview step should filter out deployments labelled `Production`; T-15 asserts `application/json` prefix. PR #8 green (13/13 checks). Two decisions for the reviewer, both in the PR body: (a) axe reports `document-title` (serious) on the copy-less shell, so the a11y test asserts the serious/critical set *equals* `["document-title"]` (self-clearing when spec 003 adds the localised title) rather than zero; (b) Vercel injects `vercel.live/_next-live/feedback/feedback.js` into protected previews, so T-16 allows that one origin on non-local targets (removable by turning Comments off in the Vercel project settings). Visual baselines are platform-agnostic while the shell has no text/fonts — spec 004 must switch to per-platform baselines. |
| TASK-009 | SEO validator CLIs (`validate-sitemap`, `validate-hreflang`, `validate-schema`) with failure-path unit tests, `tests/fixtures/seo/` (+ `lighthouse-urls.json`), `lighthouserc.json` budgets, `seo:validate` + `lighthouse` CI jobs | `specs/001` · AC-22, AC-23 | 0 | done | backend-implementer | [#9](https://github.com/itsahmeds/flowers-overseas/pull/9) | TASK-008 | Branch `task/TASK-009-seo-validators-lighthouse`. §6 harness contract with spec 007 (`@type` allow-list from `plan/02` §9, `Offer.price == visiblePrice`). Lighthouse informational until spec 004 (§13 Q4) but assertions must already pass on `/`. Tests: T-23, T-24. AC-23 pass clause deferred to spec 004 (ruled 2026-09-07; correction queued on TASK-012). |
| TASK-010 | Dev-OS checks: `tests/dev-os/` shell + Vitest tests for PreToolUse guard, `task.sh`, Stop hook; `dev-os:check` script + CI job | `specs/001` · AC-24, AC-25, AC-26 | 0 | done | backend-implementer | [#10](https://github.com/itsahmeds/flowers-overseas/pull/10) | TASK-008 | Branch `task/TASK-010-dev-os-check`. Must not edit `.claude/hooks/*` or `CLAUDE.md` (§3) — respected; the checks run the real scripts against throwaway project roots via `CLAUDE_PROJECT_DIR` and never read or write the repo's `.claude/state/active-task` or `TASKS.md` (`assert_not_repo_root` aborts with exit 99 if one ever resolves to the repo root). Tests: T-25 (19), T-26 (26), T-27 (15) = 60 shell assertions + 17 unit tests (`tests/unit/dev-os.test.ts`, incl. five negative cases proving the harness reports failure). T-27's "temp clone" implemented as a minimal temp project (`mktemp -d` + `.claude/state/` + fixture `TASKS.md`, `git init` for the Stop hook) rather than a clone of this repo — rationale in the PR body. CI 16/17 (`lighthouse` informational-red by design). `.claude/settings.json` allow-list entry `Bash(pnpm dev-os:check*)` (spec line 72) left to TASK-012. |
| TASK-011 | CI completion and repo policy: `test:contract`, `db:check` stub, `env:check`, `audit` (pnpm audit + gitleaks), step summaries, job order per `plan/12` §5; PR template, CODEOWNERS, `renovate.json`; branch-protection verification | `specs/001` · AC-21, AC-27, AC-28 | 0 | done | backend-implementer | [#11](https://github.com/itsahmeds/flowers-overseas/pull/11) | TASK-009, TASK-010 | Branch `task/TASK-011-ci-completion-repo-policy`. Founder configures branch protection after the first green run and before merging PR 2 (§12 note a); implementer verifies via `gh api` (T-22). Renovate per §13 Q7; owner per Q1. Tests: T-22, T-28, T-29. Consider parsing ci.yml as YAML in `tests/unit/vercel-config.test.ts` rather than raw-text asserts. From review of PR #8: branch protection must require the PR-side `e2e`/`visual`/`a11y` checks (they do not run on push to main); T-16 localhost detection should match hostname (`localhost`/`127.0.0.1`/`::1`) not a string prefix. From review of PR #9: `lighthouse` is a red check on every PR until spec 004 (`continue-on-error: true`), so branch protection must exclude `lighthouse` from the required-check set while that is in force. From review of PR #10: fix `tests/dev-os/lib.sh` leaking `errexit` after `run_*` helpers (a red check aborts before its summary line, footer says 0 failed next to a FAIL row) and add a negative test that calls `run_guard` before a failing assertion; reconcile `dev-os-check`/`seo-validate` job positions with spec §2 order or correct the spec. **Branch-protection probe (2026-09-07):** `gh api repos/itsahmeds/flowers-overseas/branches/main/protection` → HTTP **403** `{"message":"Upgrade to GitHub Pro or make this repository public to enable this feature."}` — not 404 `Branch not protected`. GitHub Free offers neither branch protection nor rulesets on a private repository, so AC-21's account half is **not applicable** on the current plan; the code half (job set + `scripts/branch-protection.ts --verify/--print-commands`, deriving the required set from `ci.yml` + `pr-policy.yml`) is done and `pnpm branch-protection` exits 1 with that message. **Founder action:** (1) upgrade `itsahmeds` to GitHub Pro, then run `pnpm branch-protection --print-commands` and apply both commands after PR #11's `ci` run is green (§12 note (a)); or record option 3 (unenforced protection, `/review` verdict as the gate) as the accepted deviation. (2) Independent of the plan and needed now: `gh repo edit itsahmeds/flowers-overseas --enable-squash-merge --enable-merge-commit=false --enable-rebase-merge=false --squash-merge-commit-title=PR_TITLE --squash-merge-commit-message=COMMIT_MESSAGES --delete-branch-on-merge` — the repo currently reports `allow_merge_commit: true`, `allow_rebase_merge: true`, `squash_merge_commit_title: COMMIT_OR_PR_TITLE`. Runbook: `docs/runbooks/branch-protection.md`. **Deviations recorded for TASK-012 to correct in spec 001:** (a) §2 "one approving review" → `required_approving_review_count: 0` + `require_code_owner_reviews: false`, because GitHub forbids self-approval and a solo founder with `CODEOWNERS = * @itsahmeds` would be unable to merge anything; the recorded `/review` verdict is the gate. (b) §2 job order: `seo-validate` and `dev-os-check` stay on `needs: typecheck` rather than after `lighthouse` — neither touches a preview deployment, so the spec's order would make the two fastest gates wait ~15 min for Vercel; and `env:check` is a step of `lint`, not a job, being a millisecond key-set diff. (c) `lighthouse` is excluded from the required-check set while `continue-on-error: true` (§13 Q4); the verifier derives this from the workflow, so spec 004 removing the flag makes it required with no code change. (d) `pnpm run audit` (not `pnpm audit`) is the composite gate: pnpm's built-in `audit` shadows the script name §2 gives it. Dependency added: `yaml` (devDependency, workflow parsing). Carry-forwards from PRs #7/#8/#10 all done: `ci.yml` YAML-parsed in `tests/unit/vercel-config.test.ts`, hostname-based local detection in `tests/e2e/shell.spec.ts`, `errexit` leak fixed in `tests/dev-os/lib.sh` with three negative tests proven red first. |
| TASK-012 | Docs and ledger: README rewrite (≤8 commands, <15 min), `docs/runbooks/local-setup.md` + runbook index, `docs/architecture.md` (Mermaid + module table with owning spec), `.claude/settings.json` allow-list, TASKS.md open-decisions parser test, branch-protection runbook section | `specs/001` · AC-30, AC-31 | 0 | done | backend-implementer | [#12](https://github.com/itsahmeds/flowers-overseas/pull/12) | TASK-011 | Branch `task/TASK-012-docs-ledger`. Architecture doc records CSP deferral to 004 (§8) and the `<html lang="en">` 003 removal item (§7). Tests: T-31 (manual, timed by reviewer), T-32. Spec exit signal: this PR merged with recorded `/review` PASS (§12). Spec 001 text corrections queued: Stylelint rule name (TASK-003 review) and §5.2 `middleware.ts` → note Next 16 `proxy.ts` deprecation; rename itself is a spec 003 task that must also widen `fo/no-geo-redirect`'s filename matcher and AC-8 fixtures; AC-23's clause "the run passes on the empty shell" is unsatisfiable while `/` has no painted content (Lighthouse aborts with `NO_FCP` before any metric exists) — reword to "assertions configured as errors; no budget regression once measurable (spec 004)". Also queue: AC-26/T-27 "temp clone" → "isolated temp project with `CLAUDE_PROJECT_DIR` redirection" to match the implementation. From review of PR #11: spec corrections for `required_approving_review_count: 0` (solo founder), `lighthouse` exclusion while informational, `strict: false` contexts, `pnpm run audit` naming, job-order (`seo-validate`/`dev-os-check` on typecheck; `env:check` as lint step); README troubleshooting must state that without gitleaks installed `pnpm test` reports 5 skipped; verifier should also assert `dismiss_stale_reviews`, `required_conversation_resolution`, `delete_branch_on_merge`. Open founder decision: branch protection needs GitHub Pro (private repo on Free returns 403) — option 1 upgrade (recommended) or option 3 record unenforced protection as a deviation. **Done 2026-09-08 (PR #12):** README rewritten (7 numbered setup commands, every `package.json` script documented once, troubleshooting incl. the 5 gitleaks-absent skips, the `proxy.ts` deprecation and Lighthouse `NO_FCP`); `docs/runbooks/local-setup.md` + runbook index table; `docs/architecture.md` (Mermaid, 11-module table checked against the `check-layout` manifest and each barrel's owning-spec comment, five deferred decisions); `.claude/settings.json` allow-list extended with build/format/env:check/dev-os:check; T-32 ledger parser (`scripts/tasks-open-decisions.ts`) — which found and fixed an unescaped `\| TASK-NNN \|` in the log's `/review 10` row; verifier now asserts `dismiss_stale_reviews`, `required_conversation_resolution`, `delete_branch_on_merge`; spec 001 §14 records all ten queued corrections (§1–§13 untouched). **T-31: 8 s** clone → healthy `/api/health` (27 s including `lint`+`typecheck`+`test`), warm pnpm store and local remote — budget 15 min. Tests: 540 unit (44 new/extended), 0 skipped with gitleaks. CI 18/19 (`lighthouse` informational-red, `NO_FCP`). **Escalations:** (a) founder decision still open on branch protection (GitHub Pro vs recorded deviation), (b) this PR widens the session permission allow-list per spec §2 and asks for explicit human confirmation. |
| TASK-013 | Provisioning and env contract: Neon (Frankfurt) + R2 buckets wired into `lib/env.ts` and `.env.example`; delete `ALLOW_PLACEHOLDER_ENV` and the three Supabase keys; widen logger redaction to `*name` + the §8 field list | `specs/002-schema-v1.md` · AC-1, AC-2 | 0 | blocked | backend-implementer | — | — | Branch `task/TASK-013-provisioning-env-contract`. **Founder actions (do first):** (1) Neon project in Frankfurt eu-central-1, copy pooled + direct URLs; (2) Cloudflare account, EU-jurisdiction R2 media bucket + private backups bucket, scoped API token; (3) put the real values in `.env.local` and both Vercel environments and **delete `ALLOW_PLACEHOLDER_ENV` from Vercel preview + production** once this merges; (4) Neon API key + project id (optional, for TASK-029); (5) accept and file the Neon and Cloudflare DPAs in `docs/compliance/`. §12 item 7 is already satisfied — `db/` is a protected root in `.claude/hooks/task-guard.sh` and `CLAUDE.md` today, so no hook edit is needed for Q2 option A. New keys: `DATABASE_URL_UNPOOLED`, `R2_ACCOUNT_ID`, `R2_BUCKET`, `R2_ACCESS_KEY_ID`, `R2_SECRET_ACCESS_KEY`, `R2_PUBLIC_BASE_URL`, `NEON_API_KEY?`, `NEON_PROJECT_ID?`, `INTERNAL_CRON_SECRET`. Also in this PR (first task of spec 002, so it carries the small ledger/doc debts): (the `2 / 12` pin in `scripts/tasks-open-decisions.ts` and its test was already flipped by the orchestrator when the plan landed) fix the README/local-setup Next 16 deprecation symptom cell together with its pin in `tests/unit/docs.test.ts` (log 2026-09-08). Tests: T-03, T-04. §8: logger list gains `*name`, `card_message`, `phone_e164`, `postal_code`, `session_token`, `public_token`, `object_key` (`/review 5` carry-forward). **PARKED 2026-09-08 by founder:** provisioning (Neon, Cloudflare R2) waits until the domain and business mailbox exist; work on the plan/09 §0 non-code critical path first. |
| TASK-014 | Data layer foundation: `db/migrations/` root with manifest/architecture/`db-check` path updates, `drizzle.config.ts` + `db:generate`/`db:migrate`/`db:rollback`/`db:studio`, `src/lib/db.ts` (`withDbContext`/`withAdminContext`/`withSystemContext`), migration `0001` roles + grants + `set_updated_at()` | `specs/002` · AC-3 · plus §2 migrations-directory decision (Q2 option A) and the `db:check` rollback-pairing/version/drift skeleton (no AC id; AC-26 itself owned by TASK-027) | 0 | todo | backend-implementer | — | TASK-013 | Branch `task/TASK-014-db-foundation-runner`. `MIGRATIONS_DIR` in `scripts/db-check.ts`, `DIRECTORIES` in `scripts/check-layout.ts`, `docs/architecture.md` §2/§5 all move to `db/migrations/` in this one PR; `plan/01` §5's `supabase/migrations/` line is recorded as superseded by ADR-0015 in the architecture doc (the plan is not edited) and `supabase/` is removed from the layout manifest. Q6: pooled `DATABASE_URL` for web, `DATABASE_URL_UNPOOLED` for migrations, `db:check`, pg-boss and backups. `app_owner` owns every object, `app_web` is `NOSUPERUSER` with no `BYPASSRLS` and DML grants only. Every migration ships a hand-written `NNNN_name.down.sql` from here on. Tests: T-05; the migrate/rollback round trip (T-01/T-02) is run per PR from here and owned as AC-4 by TASK-022. |
| TASK-015 | Schema `i18n` + `geo` (migration `0002`): `locale`, `message_catalog`, `currency`, `country`, `country_translation`, `country_locale_content`, `region`, `city`, `city_translation`, `postcode_zone`, `country_holiday`, `occasion`, `occasion_translation`, `occasion_country`, `fx_rate` | `specs/002` · AC-7 | 0 | todo | backend-implementer | — | TASK-014 | Branch `task/TASK-015-schema-i18n-geo`. `locale` and `currency` are created before their dependants inside `0002`. No extension, no `enum`, every status column a `CHECK` value list (AC-7). Integer-only money adjacency: `vat_rate_bp`, `fx_rate.rate_ppm`, `currency.minor_unit_exponent`. §7: `rtl` and `fallback_code` exist now so RTL costs no later migration; `occasion_country.rule_type` verbatim from `plan/03` §9 (evaluator is spec 009). §6: `UNIQUE (locale_code, slug)` on `country_translation`/`occasion_translation` and `UNIQUE (locale_code, country_id, slug)` on `city_translation` land here and are verified as AC-8 by TASK-016. Tests: T-06 (extension/enum/CHECK half), T-08 (geo half). |
| TASK-016 | Schema `catalog` + pricing (migration `0003`): `product`, `product_translation`, `product_tier`, `category*`, `product_category`, `product_occasion`, `addon*`, `addon_country_price`, `country_price` with the one-active-row partial unique index, shared `updated_at` trigger coverage | `specs/002` · AC-5, AC-8, AC-9, AC-10 | 0 | todo | backend-implementer | — | TASK-015 | Branch `task/TASK-016-schema-catalog-pricing`. AC-9 is the SEO/compliance keystone: exactly one active `country_price` per (product, country, tier, surcharge) via `UNIQUE … WHERE active_to IS NULL` + `CHECK (active_to IS NULL OR active_to > active_from)`, rows superseded and never updated in place, which is what gives §6 "schema price = visible price" and the Omnibus 30-day price history (§8). Money convention proven here first: `*_minor bigint` + `*_currency` FK to `currency` (AC-5). §7: `product_tier.label_key` and `addon.key` hold message keys, never literals; `description_md` stays null in the seed so a thin PDP is non-indexable by data. Tests: T-06 (money-pair half), T-08, T-09, T-10. |
| TASK-017 | Schema media (migration `0004`) + R2 storage seam: `media_asset`, `media_variant`, `product_media` with one-primary partial index, `product_media_alt`; `src/lib/storage.ts` interface + `objectKey()` + `InMemoryStorage` fake | `specs/002` · AC-11, AC-22 | 0 | todo | backend-implementer | — | TASK-016 | Branch `task/TASK-017-schema-media-storage-seam`. Keys, never bytes (ADR-0015): no `bytea` column anywhere, asserted by `db:check` in TASK-027. S3-API client, upload, EXIF stripping and variant generation are spec 006; this task ships only the interface, the key convention and the tables. §8 accessibility: `product_media_alt.alt` is `NOT NULL` per locale, never generated at render. Tests: T-11, T-22. |
| TASK-018 | Schema `partners` + `customers` (migrations `0005`, `0006`): `fulfillment_partner`, `partner_translation`, `partner_member`, `partner_coverage`, `partner_blackout`, `partner_catalog_mapping`, `partner_application`, `payout`, `payout_line`; `customer`, `address`, `recipient`, `recipient_address`, `consent_log`; the recipient-email `db:check` gate | `specs/002` · AC-27 | 0 | todo | backend-implementer | — | TASK-017 | Branch `task/TASK-018-schema-partners-customers`. §8 minimisation is structural: `recipient` has no email column and `db:check` fails on any `/e[-_]?mail/i` column added to `recipient`/`recipient_address`, citing `plan/07` §1.3 (AC-27); `consent_log.source_ip_truncated` only, never a full IP; case-insensitive buyer email is a `lower(email_normalised)` unique index, no `citext`. `partner_member` is the org-scoping mechanism the AC-16 policies key on. `partner_application.media_asset_ids` references TASK-017's assets. Tests: T-27. |
| TASK-019 | Schema `orders` + `payments` (migration `0007`) and the state-machine reference data: `order_status`, `order_transition` seeded from `plan/11` §2, `"order"`, `order_line`, `order_event`, `order_assignment`, `substitution_note`, `delivery_proof`, `review`, `payment`, `refund`, `webhook_inbox` | `specs/002` · AC-14 | 0 | todo | backend-implementer | — | TASK-018 | Branch `task/TASK-019-schema-orders-payments`. Reference rows are data, not code, so spec 015 reads guards instead of hard-coding them; AC-14 asserts set equality both ways against a fixture list transcribed from `plan/11` §2, and every state carries a buyer-visible projection. `order.recipient_snapshot` is the history-preserving snapshot of `plan/11` §3 and the one place recipient contact survives the retention sweep, pseudonymised (§8). `webhook_inbox` implements store-before-process ahead of 013. Seed inserts zero `review` rows (§6: a fabricated `aggregateRating` is impossible by absence of data). Triggers are TASK-020. Tests: T-14. |
| TASK-020 | Order integrity triggers (migration `0008`): `order_event` append-only, `order.status` writable only under `app.allow_status_write`, illegal `(from, event, to)` rejected against `order_transition`, `delivery_proof` asset guard | `specs/002` · AC-12, AC-13, AC-15, AC-19 | 0 | todo | backend-implementer | — | TASK-019 | Branch `task/TASK-020-order-integrity-triggers`. The database half of ADR-0009 whose lint half has been live since spec 001: `ORDER_STATUS_DIRECT_WRITE` and `ORDER_TRANSITION_ILLEGAL` raised from Postgres, proven by raw SQL that bypasses `fo/no-direct-order-status-write` entirely (AC-15). `orderService.transition` runtime stays in spec 015 (§13 Q3) — this task must not add it. §8: `delivery_proof` rejects any asset that is not `visibility = 'private'` and `exif_stripped = true`; `public_use_consent` defaults false. Tests: T-12, T-13, T-15, T-19. |
| TASK-021 | Schema `notifications` + platform/ops (migration `0009`): `outbox`, `outbox_attempt`, `feature_flag`, `feature_flag_scope`, `audit_log`, `retention_policy` seeded per `plan/07` §1.2, `db_usage_sample` | `specs/002` · no AC of its own; explicit scope — §5.1 notifications/platform/ops tables, §12 feature-flag keys, §8 `retention_policy` seed rows (their sweep is AC-20 on TASK-025, the probe AC-24 on TASK-029) | 0 | todo | backend-implementer | — | TASK-020 | Branch `task/TASK-021-schema-notifications-ops`. Flags live in the database with a 60 s cache, never in env: seed `checkout.demo_guard` (on), `payments.stripe`, `payments.mollie`, `corridor.{iso}`, `locale.{code}` (off) per §12. `retention_policy` rows carry basis + period + legal note verbatim from `plan/07` §1.2 so the privacy policy tables are generated from data; §13 Q7 keeps the 10-year invoice and 2-year photo figures flagged for B1 (accountant) and B4 (legal) — numbers may move, mechanism does not. `audit_log` is the record for the country/partner status flips of `plan/10` §4. Writers are 015/012/017. |
| TASK-022 | Schema `auth` (migration `0010`): Auth.js v5 Drizzle-adapter tables `users`, `accounts`, `sessions`, `verification_tokens` plus `user_role`; full migrate → rollback → migrate round trip on an empty PG16 | `specs/002` · AC-4 | 0 | todo | backend-implementer | — | TASK-021 | Branch `task/TASK-022-schema-auth-roundtrip`. §13 Q1 resolved: Auth.js v5 + Drizzle adapter, table and column names exactly as the adapter expects, verified by T-35 (the library's own adapter contract, or a scripted create-user → create-session → get-session-and-user → delete-session round trip) against our tables unmodified. Guest buyers keep a `customer` row and no `users` row (`plan/11` §1); recipients never authenticate. AC-4 is owned here because this is the last table-creating migration: `db:rollback --to 0000` must leave no table, type, function, trigger, role or schema behind, twice in a row. Tests: T-01, T-02, T-35. |
| TASK-023 | RLS and roles (migration `0011`): enable RLS on every table, deny-by-default, the public reference set, partner-scoped and customer-scoped policies keyed on `current_setting('app.*', true)`, `app_web` grants | `specs/002` · AC-16, AC-17, AC-18 | 0 | todo | backend-implementer | — | TASK-022 | Branch `task/TASK-023-rls-policies`. §13 Q4 resolved: session-variable RLS, admin is `app.role = 'admin'` and not a second connection string, so there is no service-role key to leak. Positive *and* negative case per partner-scoped table is mandatory (`plan/12` §4): as partner B, reads of A's rows return 0 and `UPDATE`/`DELETE` affect 0 rows. §8: this is the technical measure behind `plan/07` §1.1's processor boundary — `app_web` has no `BYPASSRLS`, so a forgotten `WHERE partner_id =` leaks nothing. RLS *coverage* gating in `db:check` is AC-26 on TASK-027; the exemption list for pure reference data is written here. Tests: T-16, T-17, T-18. |
| TASK-024 | Jobs and internal probes: pg-boss bootstrapped by migration `0012` into the `pgboss` schema, `src/jobs/index.ts` registry with versioned zod payloads, `GET /api/internal/db-health`, `POST /api/internal/jobs/tick` | `specs/002` · AC-21, AC-23 | 0 | todo | backend-implementer | — | TASK-023 | Branch `task/TASK-024-jobs-internal-probes`. pg-boss runs on `DATABASE_URL_UNPOOLED` (§13 Q6 — PgBouncer transaction mode has no session locks or `LISTEN/NOTIFY`); its schema is created through the migration runner so it is versioned like everything else, owned by `app_owner` with usage granted to `app_web`. Registered handlers: `media.derive_variants` (declared, worker in 006), `retention.sweep`, `neon.usage_probe`, `db.backup_verify`; declared only: `order.route`, `assignment.timeout`, `notification.deliver`, `fx.refresh`, `sitemap.regenerate`, `occasion.reminders`, `review.request`, `payout.accrue`. §6/§5.4: `/api/health` stays database-free (asserted by a query counter, not by inspection) so uptime pings never wake Neon; both internal routes are secret-gated, `no-store`, `X-Robots-Tag: noindex` and inside spec 001's `/api/` robots disallow. Tests: T-21, T-23. |
| TASK-025 | `retention.sweep` job: apply every `retention_policy` row on an injected clock — recipient contact and `order.recipient_snapshot` pseudonymised at 90 days, photos deleted at 2 years, buyer fields at 3 years, fraud signals at 12 months, consent at 5 years; `--dry-run` | `specs/002` · AC-20 | 0 | todo | backend-implementer | — | TASK-024 | Branch `task/TASK-025-retention-sweep`. §8 retention is implemented, not promised. Pseudonymisation keeps the city for analytics and drops phone and street; the order, its lines and its payment stay intact for the accounting obligation; a dispute-open order is skipped; a recipient who became a customer is skipped. `--dry-run` writes nothing and reports the same counts. R2 object deletion goes through TASK-017's seam (fake in tests). Numbers are §13 Q7-flagged (B1/B4); the sweep is period-driven from `retention_policy` so a changed number is a data edit. Tests: T-20. |
| TASK-026 | Seed and shared fixtures: `seed/index.ts` with `--only`/`--dry-run`, idempotent upserts by natural key that never touch `source = 'real'`; 4 locales, 10 currencies, 8 demo countries, occasion rules + 2026–27 holidays, the 84-product skeleton, 6 add-ons, `country_price` bands, 3 demo partners per country; `tests/fixtures/index.ts` reserved exports | `specs/002` · AC-29, AC-32, AC-34 | 0 | todo | backend-implementer | — | TASK-025 | Branch `task/TASK-026-seed-and-fixtures`. §13 Q8 resolved: A3 and A4 defaults are the seed basis, no product-name vetoes. §6 thin-content mitigations are load-bearing and must be verified as written: `de`/`pl` product translations **absent** (not machine-filled), `description_md` null, `country_locale_content` seeded only where a guide exists (`guide_published = false` elsewhere), and **zero** rows in `review`, `delivery_proof` and `media_asset`. §6 also requires enough products per (country, category) for the PL set to pass spec 008's six-product threshold and enough to test the failing side. Slugs ASCII-folded at seed time with a Polish-diacritic case in the fixtures for 003's transliteration. Fixtures: `occasionDates` (incl. the five `plan/09` dates), `currencies` (exponents vs `Intl`), `addresses`/`phones` valid and invalid PL/DE/UK. Tests: T-29, T-32, T-34. |
| TASK-027 | Gates and CI: `pnpm db:check` real (drift, RLS coverage, money-pair, `bytea`, rollback pairing, duplicate version), enable `fo/no-float-money` for `src/`, `seed/`, `scripts/`, real `test-integration` against the PG16 service container, `db-check` service container, `db:seed` twice per run, step summaries | `specs/002` · AC-6, AC-26, AC-28 | 0 | todo | backend-implementer | — | TASK-026 | Branch `task/TASK-027-db-check-ci-gates`. Every `db:check` failure mode gets a temporary fixture that proves it exits non-zero with the offender named, and one line per problem (AC-26); the clean tree exits 0 and prints applied-migration count and RLS coverage. No `describe.skip` may remain anywhere in `tests/integration/` after this task (AC-28) — this is the spec 001 TODO being paid off. §13 Q5: shared seeded preview database in Phase 0, so CI wiring assumes one database and revisits per-PR Neon branches at the recorded triggers. `fo/no-float-money` moves from fixture-only to enforced (spec 001 §2 said 005; 002 brings it forward as the first code with money columns). Tests: T-07, T-26, T-28. |
| TASK-028 | Nightly backups to R2: `.github/workflows/db-backup.yml` (`pg_dump -Fc` over the direct URL → `backups/YYYY/MM/DD/`), 30-day lifecycle, `db.backup_verify` restoring the newest dump into a scratch database and asserting row counts on five tables | `specs/002` · AC-25 | 0 | todo | backend-implementer | — | TASK-027 | Branch `task/TASK-028-nightly-backups-r2`. **Founder prerequisite** (from TASK-013): the private backups bucket, its scoped API token as a GitHub secret, and the 30-day lifecycle rule; the workflow and the verify job can be written and dry-run before that, but AC-25 cannot be signed off until the bucket exists. ADR-0015 guardrail: automated backups to R2 from the first migration. §8: the bucket must be non-public and the restore drill must use a scratch database, because the dump carries personal data once real orders exist; a `db.backup_verify` failure is `warn` + Sentry, since a silent unrestorable backup is the failure mode that matters. Tests: T-25. |
| TASK-029 | `neon.usage_probe` (daily): read the Neon API for compute-hours and storage this billing period, write one `db_usage_sample` row, `warn` + Sentry message at 70% and 90% of the free-tier cap, degrade to one `info` line when `NEON_API_KEY` is unset | `specs/002` · AC-24 | 0 | todo | backend-implementer | — | TASK-028 | Branch `task/TASK-029-neon-usage-probe`. **Founder prerequisite** (from TASK-013 item 4): `NEON_API_KEY` + `NEON_PROJECT_ID`; the job must degrade cleanly without them, so the task is not blocked. ADR-0015 makes the compute cap a monitored guardrail rather than a note, and §11 makes this the first alert with a business consequence: at 90% the founder chooses between throttling previews, an earlier Hetzner move, or paying. Idempotent per day. Tests: T-24. |
| TASK-030 | Portability proof and the database runbook: `pg_dump` of the seeded database restored into stock `postgres:16` with zero errors and zero extension/role/function warnings + `db:check` green on the copy; `docs/runbooks/database.md` and one executed, recorded restore drill | `specs/002` · AC-30, AC-31 | 0 | todo | backend-implementer | — | TASK-029 | Branch `task/TASK-030-portability-and-runbook`. AC-31 is ADR-0015's dump-and-restore promise tested rather than asserted, and is the reason no extension, `enum`, `citext` or custom collation exists anywhere in the schema. Runbook covers connect, generate, migrate, roll back one migration, restore a dump, create/drop a Neon branch, what to do near the compute cap, the cold-start expectation (so 007's Lighthouse runs are read correctly), the rollback order (stop the worker, roll back, redeploy, verify `latest_migration`), and the mutation → cache-tag invalidation list that 007/012 inherit (§5.4). T-30 is a manual drill the reviewer records in the PR and the runbook with dump key, duration and row counts. Tests: T-30, T-31. |
| TASK-031 | Docs, records and spec close: `docs/architecture.md` §1–§4 (Neon + R2, no Supabase node, `db/migrations/` path, data layer credited to 002, `ALLOW_PLACEHOLDER_ENV` row deleted, Neon-compute-cap row added), `docs/compliance/ropa.md` Neon + Cloudflare + `plan/07` §1.2 flow rows, `README.md` script table, spec 002 §14 corrections | `specs/002` · AC-33 | 0 | todo | backend-implementer | — | TASK-030 | Branch `task/TASK-031-docs-ropa-close`. RoPA rows 3–4 replace the placeholder row: Neon Inc. (Frankfurt eu-central-1, sub-processor AWS, DPA filed) and Cloudflare R2 (EU jurisdiction restriction, DPA at account creation), each with subjects, categories, basis, retention and transfer notes per §8; the Supabase row ADR-0008 would have produced is never written. `tests/unit/architecture-doc.test.ts` must stay green against `scripts/check-layout.ts`. Spec 002 §14 records the deviations accumulated by this spec's tasks — notably that migration numbering runs `0001`…`0012` because §5.1's ten-file grouping was split for one-day tasks (concerns and order preserved), plus anything the reviewers rule on. Spec exit signal: this PR merged with a recorded `/review` PASS, Phase 0 specs `2 / 12`, CI green including real `test-integration` and `db-check`, and a seeded preview database spec 003 can start from. Tests: T-33. |
| TASK-032 | `proxy.ts` rename and the ADR-0006 lint gate widened: `src/middleware.ts` → `src/proxy.ts`, `fo/no-geo-redirect` filename matcher covering both names, a new violation for importing `next-intl/middleware` or calling `createMiddleware(`, the `geo-redirect-proxy.ts` fixture | `specs/003-i18n-foundation.md` · AC-10, AC-11 · plus the `src/lib/request-id.ts` extraction (no AC id; explicit here — `/review 6` carry-forward) | 0 | done | backend-implementer | [#13](https://github.com/itsahmeds/flowers-overseas/pull/13) | — | Branch `task/TASK-032-proxy-rename-lint-gate`. First task of spec 003 and the one task unblocked today; §12 order 1, because it closes the gate before any routing code exists that could slip through it. Spec 001 §14 A2 is the authority: matcher widening, the `proxy.ts` fixture and `plan/12` §6's naming land in the **same** PR as the rename, never apart. Discharges three carry-forwards: the `docs/architecture.md` §4 row "middleware.ts → proxy.ts" is removed; the README and `docs/runbooks/local-setup.md` troubleshooting cell for the Next 16 deprecation warning is deleted **together with** its pin in `tests/unit/docs.test.ts` (log 2026-09-08 — that pin is why the earlier attempt was reverted); `src/lib/health.ts` imports `REQUEST_ID_HEADER`/`resolveRequestId` from `@/middleware`, so the rename moves that contract to `src/lib/request-id.ts` and re-points both importers (`/review 6` note addressed to spec 003 — not an AC of 003, recorded here so it is not lost). §6 crawl efficiency and §8 geo-blocking: banning `next-intl/middleware` closes the most likely accidental route to a locale redirect. `x-request-id` behaviour unchanged, so spec 001 AC-14 still passes. Tests: T-10, T-11. |
| TASK-033 | Locale, currency and address config with zod: `src/config/locales.ts` (four launch locales, `pathSegments` per `plan/02` §4.1), `src/config/currencies.ts` (ten currencies), `src/config/address-formats.ts` (PL, DE, AT, GB and generic), `LocaleRegistrySchema` / `CurrencyConfigSchema` / `AddressFormatSchema`, `toLocaleRow` / `toCurrencyRow`, the no-database static check | `specs/003` · AC-1, AC-2, AC-4 (partial: `toLocaleRow`/`toCurrencyRow` clauses; the `MessageMetaSchema` clause is verified by TASK-038) | 0 | done | backend-implementer | [#14](https://github.com/itsahmeds/flowers-overseas/pull/14) | TASK-032 | Branch `task/TASK-033-locale-currency-address-config`. The no-database seam is the point (§1, §5.1): AC-2's static check asserts that no file added by spec 003 imports `src/lib/db*`, `drizzle*`, `pg` or `postgres`, and that `pnpm build` plus `pnpm test` pass with `DATABASE_URL` unset — which is why this spec is implementable while TASK-013 is parked. AC-4 pins `toLocaleRow` to spec 002 §5.1's `locale` column set and `toCurrencyRow` to `currency`'s, so TASK-015 and TASK-026 seed from these projections instead of restating the locale set and the two cannot drift. §6: the localised URL segments are authored here, human-written from `plan/02` §4.1, never machine-drafted; their shape and per-locale uniqueness are gated as AC-13 by TASK-040. §8 consumer information: `/de/rechtliches/agb`, `/de/impressum` and `/pl/regulamin` are fixed now so spec 004/007's legal pages land on the URLs the German and Polish regimes expect. `scripts/check-layout.ts`'s manifest and `docs/architecture.md` §2 gain `src/config/`. Resolved inputs: §13 Q1 (four prefixes, A1) and Q8 (`isLaunch` as the Phase 0 go-live switch — an accepted, bounded deviation from the CLAUDE.md data-flip rule, read only through `LocaleRegistryProvider`). Tests: T-01, T-02, T-04. |
| TASK-034 | i18n module seam and `[locale]` routing: `routing.ts`, `registry.ts`, `messages.ts`, `request.ts`, a barrel that exports no provider, the `[locale]` segment with the empty `plan/01` §5 route groups, `<html lang dir>` from `bcp47`/`dir`, minimal `messages/en.json` shell namespaces, localised 404/500, unknown-locale 404, header-invariant responses | `specs/003` · AC-3, AC-5, AC-6, AC-8, AC-9 · plus `src/proxy.ts` attaching `locale` from the path prefix and the non-PII Sentry `locale` tag (§11; no AC id, explicit here) | 0 | done | frontend-implementer | [#15](https://github.com/itsahmeds/flowers-overseas/pull/15) | TASK-033 | Branch `task/TASK-034-locale-routing-documents`. §5.3 gives latitude on the document shape (two root layouts, or one `src/app/layout.tsx` with a shared `resolveDocumentLocale()`); what is not negotiable is AC-6, AC-7, AC-8 and AC-11, and that whichever shape is chosen is recorded in `docs/architecture.md` §2. Deletes the `lang="en"` literal and its `docs/architecture.md` §4 row (the last locale literal in the repo, spec 001 §7). AC-9 is ADR-0006 in its strongest form: the same URL with and without `Accept-Language: de-DE`, and under a Googlebot UA, byte-identical by hash, 200, no `Location`, no `Vary`. No next-intl middleware, no `localeDetection`, no `NEXT_LOCALE` cookie — the gate for that shipped in TASK-032. AC-5 is the seam test: a fake five-locale `LocaleRegistryProvider` injected inside the module changes rendered output with zero changes outside `src/modules/i18n/`, which is how spec 002/012 hydrate from Postgres without touching callers. `loadMessages(locale, namespaces)` hands the client a per-route subset only, never the whole catalogue (§6 CWV; the byte budget is AC-27 on TASK-043). §8 accessibility: WCAG 3.1.1 Language of Page closes here per locale; 2.4.2 follows with AC-25 on TASK-035. §6 indexability is unchanged — `robots.ts` keeps `Disallow: /` and localised pages stay `noindex,nofollow` until spec 007. Tests: T-03, T-05, T-06, T-08, T-09. **Implemented on `task/TASK-034-locale-routing-documents`, PR #15, `in_review` 2026-09-08.** §5.3's recommended two-root-layout shape was implemented first and measured to break AC-8 on Next 16.3.4 — with two root layouts `src/app/not-found.tsx` *is* reached, but the framework wraps it in a bare `<html>` of its own, so the effective document has nested `html`/`body` and no `lang` attribute at all (AC-8 and WCAG 3.1.1 broken); the shipped shape is §5.3's accepted alternative in its cheapest form — a pass-through `src/app/layout.tsx` plus one document per leaf (`(chooser)/` for `/`, `[locale]/` per locale, `not-found.tsx` for the 404 in x-default), no `headers()` read, so `/{locale}` stays ISR — recorded in `docs/architecture.md` §2. Three deviations for the reviewer, all in the PR body: no `[locale]/not-found.tsx` (a nested 404 boundary *is* rendered in 16.3.4 when a matching route calls `notFound()`, but inside the framework's `<html id="__next_error__">` with no `lang`, so a per-locale 404 document is not expressible and `/de/nope` answers in x-default); `dynamicParams = false` rather than `notFound()` is the unknown-locale gate, which closes the locale set at build time; and `/EN` is skipped locally on macOS APFS (case-insensitive prerender lookup) and asserted on Linux, where CI proved it (e2e 46 passed, 0 skipped, against the preview). `next-intl@4.14.2` is the only new runtime dependency. CI green except the pre-existing informational `lighthouse` (`NO_FCP` on the still-copy-free `/`; the URL set gains `/en` at TASK-043). |
| TASK-035 | `/` locale chooser, localised titles and the axe exception removal: server-rendered zero-JS chooser whose links carry `nativeName` plus `lang`/`hreflang`, `noindex,follow`, `generateMetadata` titles and descriptions from `meta`, the no-JS locale switcher links, and the spec 001 shell e2e / a11y / visual-baseline updates | `specs/003` · AC-7, AC-25, AC-26 | 0 | done | frontend-implementer | [#16](https://github.com/itsahmeds/flowers-overseas/pull/16) | TASK-034 | Branch `task/TASK-035-locale-chooser-titles`. Closes the TASK-008 axe decision recorded at `/review 8`: `EXPECTED_PHASE_0_VIOLATIONS = ["document-title"]` in `tests/a11y/shell.spec.ts` is an *equality* assertion, so it must be deleted in the same PR that gives every document a localised `<title>` — the self-clearing exception, self-clearing on schedule. `tests/e2e/shell.spec.ts` today asserts `lang="en"` and `noindex,nofollow` on `/`; it becomes the chooser contract (200, `noindex,follow`, a non-empty `<title>`, one crawlable link per launch locale, still zero `Set-Cookie`, no `Location`, no `Vary`), and the `/` visual baseline is re-committed. §6: `/` is the only `follow` document in Phase 0 and gives crawl depth 1 to every locale root (`plan/02` §7, §11). §8 accessibility: WCAG 2.4.2 Page Titled and 3.1.2 Language of Parts — the locale links declare their target language so a screen reader pronounces "Deutsch" in German; language names, never country flags (`plan/03` §2). AC-25's fourth URL, `/ar-XB`, is re-verified at TASK-042 where that route first exists. Visual design belongs to spec 004; markup here is semantic, accessible and unstyled (§13 Q3). Tests: T-07, T-25, T-26. Carry-forward from `/review 15` (TASK-034): every page under `[locale]` must export `dynamicParams = false`, or the gate moves to one central place — the `[locale]` layout resolves an unknown segment to x-default silently, so a real page at `/fr/about` without the export would be a fabricated duplicate of the English URL, forbidden by spec 003 §6 (recorded in `docs/architecture.md` §2). §5.3 names `src/app/global-error.tsx` rendering an x-default document; not shipped in 034 — add it or record why. **Implemented on `task/TASK-035-locale-chooser-titles`, PR #16, `in_review` 2026-09-08.** Four decisions for the reviewer, all argued in the PR body: (1) the `/review 15` carry-forward is discharged by moving `dynamicParams = false` from the page to the `[locale]` **layout** — a segment config option on a layout governs the whole subtree, so the gate is central and no future page can forget it; the `notFound()`-in-the-layout alternative was rejected because TASK-034 measured that a `notFound()` from a matching route renders inside the framework's `<html id="__next_error__">` with no `lang`, breaking AC-8. (2) `src/app/global-error.tsx` ships as §5.3 asks, x-default with `lang`/`dir` from the registry and its four strings read from the catalogue **as data** rather than through a client `next-intl` provider, because a failure document must not depend on the machinery that just failed; `metadata` cannot be exported from a Client Component so the `<title>` is a rendered element. (3) `src/app/(chooser)/error.tsx` is deleted — it was the last Client Component in the `/` route tree and `global-error.tsx` covers it, which is what makes "served without JavaScript" exact. (4) visual baselines become per-platform (`{platform}` back in `snapshotPathTemplate`, `darwin/` local and `linux/` from the `visual` job's failure artifact, both committed) because `/` now has text and a macOS baseline cannot verify a Linux run at 0.1 %. One deliberate deviation: §7's third `chooser` key, the link `aria-label`, is not shipped — an English accessible name would override the `lang`-declared native link text and defeat the WCAG 3.1.2 pronunciation AC-7 asks for, and an unused key fails `i18n:check` at TASK-040; the `<nav>` is named from `a11y` instead. CI green on all required checks (e2e 50/50 on Linux including the `/EN` case macOS skips, a11y 10/10 with no exception list, visual 2/2, unit 660); `lighthouse` red and informational, and measuring for the first time now that `/` paints: `resource-summary.script.size` 304,272 B against a 122,880 B budget and `categories.performance` flapping 0.91–0.93 against 0.95. Measured before/after: `global-error.tsx` adds one 8.5 KB chunk and the pre-existing browser Sentry SDK from `instrumentation-client.ts` accounts for nearly all of the rest, so AC-27 and the budgets are TASK-043's with real numbers to work from. |
| TASK-036 | Formatters: `src/modules/i18n/format.ts` (`formatMoney` over the integer-to-decimal-string path, `formatNumber`, `formatPercentFromBasisPoints`, `formatDate`, `formatTimeInZone`, `formatRelativeTime`, `formatList`, `formatRange`), `collate.ts`, `MoneySchema`, the `currencies` fixture | `specs/003` · AC-15, AC-16, AC-17, AC-18 | 0 | done | backend-implementer | [#17](https://github.com/itsahmeds/flowers-overseas/pull/17) | TASK-033 | Branch `task/TASK-036-formatters`. §8 price display becomes mechanically supportable rather than promised: integer minor units plus the currency's `minorUnitExponent` are turned into a decimal **string** and handed to `Intl.NumberFormat` (ES2023 string input), so no float ever holds money — proven by formatting 9007199254740993 minor units with no precision loss, and by `MoneySchema` rejecting a non-integer at the boundary. `formatTimeInZone` takes a required IANA zone and renders the zone label; there is deliberately no "local time" overload, because "local" is ambiguous in a relay (§5.2, `plan/03` §10). §7's formatting table gets at least one test per launch locale: separators (`1,234.50` / `1.234,50` / `1 234,50`), symbol position (`£45.00` / `45,00 €` / `45,00 zł`), percent spacing from basis points (`20%` / `19 %` / `23%`), short and delivery dates, list joiners and Polish `ł` collation. Fills `currencies` in `tests/fixtures/index.ts`; spec 002's AC-34 must extend rather than redefine it (§12). No date is *computed* here — occasion arithmetic, cutoffs and DST are spec 009. Tests: T-15, T-16, T-17, T-18. |
| TASK-037 | `fo/no-adhoc-intl` and address formatting: the new lint rule banning `Intl.*` construction, `toLocaleString`/`toLocaleDateString`/`toLocaleTimeString`, `toFixed` and currency/percent template concatenation everywhere in `src/` except `format.ts` and `collate.ts`, with fixture pairs and RuleTester tests; `src/modules/i18n/address.ts` (`formatAddressBlock`, `postcodeRegex`, `normalisePostcode`) and the `addresses` / `phones` fixtures | `specs/003` · AC-19, AC-21 | 0 | done | backend-implementer | [#18](https://github.com/itsahmeds/flowers-overseas/pull/18) | TASK-036 | Branch `task/TASK-037-adhoc-intl-ban-address`. The rule is the mechanism behind "exactly one way to render a price, a date, a list or an address block" (§4, §7) and must land immediately after the formatters exist, before specs 004–011 can invent a second way; it also removes the `toFixed` and string-concatenation paths through which a rounding error would otherwise enter a displayed price (§8 price display). The fixture pair proves the identical code is valid inside a path simulating `src/modules/i18n/format.ts` and invalid outside it. Address data is `plan/03` §8 verbatim (PL `ul. … 10/5` with postcode `00-001`, DE house number after the street, GB postcode after the town) with label **keys**, so a new country is data plus four message keys; `formatAddressBlock` is pure and fixture-tested. E.164 phone validation, postcode lookups and the address *forms* stay in spec 010; recipient phone being required in every format is recorded in the data now. Fills `addresses` and `phones` in `tests/fixtures/index.ts`. Tests: T-19, T-21. |
| TASK-038 | Catalogues and `i18n:draft`: `messages/en.json` completed for the shell namespaces (`meta`, `chooser`, `banner`, `errors`, `a11y`), the `en-gb` thin override, `de` and `pl` machine drafts, the `messages/*.meta.json` manifests, `MessagesSchema` / `MessageMetaSchema`, next-intl typed-key augmentation generated from `en.json`, and `DraftProvider` + `echoDraftProvider` behind `pnpm i18n:draft` | `specs/003` · AC-20, AC-23, AC-4 (third clause: `MessageMetaSchema` keys ↔ `message_catalog` review columns) | 0 | done | backend-implementer | [#20](https://github.com/itsahmeds/flowers-overseas/pull/20) | TASK-034 | Branch `task/TASK-038-catalogues-i18n-draft`. Resolved inputs: §13 Q5 (`en-gb` is a thin override; a redundant override fails `i18n:check`), Q7 (deterministic echo stub — no LLM, no API key, no network, proven by MSW `onUnhandledRequest: "error"`) and Q10 (honest unreviewed echoes are acceptable on the protected preview). §7 plurals: Polish is the acid test and `1 kwiaciarnia / 2 kwiaciarnie / 5 kwiaciarni` at counts 1, 2, 5, 22, 25 and 1.5 must resolve through the real catalogue, not a fixture (AC-20). Two consecutive `i18n:draft --locale pl` runs are byte-identical, every written key carries `source: "machine"`, `reviewed: false` and a `sourceHash`, and a key whose meta says `reviewed: true` is never overwritten. `sourceHash` is the drift record that flips dependants stale when an English string changes. `MessageMetaSchema`'s field names equal spec 002's `message_catalog` review columns (pinned by AC-4 on TASK-033) so spec 012 mirrors them rather than translating them. Substantive `de`/`pl` copy is founder plus native-reviewer work (`plan/13` B12) and explicitly not a task here (§3). Tests: T-20, T-23. Implemented: `MESSAGE_META_COLUMNS` pins the five `message_catalog` review columns and names `retained` repo-only — spec 002 §5.1 declares no `retained` column, verified in the PR body rather than invented; `i18n:draft` also refuses to overwrite `source: "human"` (reported stale instead) so an English edit cannot replace the authored `de`/`pl` plural forms with an echo. CI 19/20 with informational `lighthouse`. |
| TASK-039 | Review gate and the hreflang generator: `src/modules/i18n/review.ts` (`unreviewedShare`, `localeBetaTag`, `isLocaleIndexable`), `alternates.ts` (`alternatesFor`), and the fifth-locale "a new locale is data" proof | `specs/003` · AC-14, AC-24, AC-31 | 0 | done | backend-implementer | [#21](https://github.com/itsahmeds/flowers-overseas/pull/21) | TASK-038, TASK-044 | Branch `task/TASK-039-review-gate-alternates`. §6's answer to the one real risk in this spec: four locale prefixes whose `de` and `pl` catalogues are echoed English is textbook duplicate content, and `isLocaleIndexable()` (`isLaunch` and unreviewed share ≤ 5%) is the hard gate spec 007 consumes for robots meta, hreflang membership and sitemap membership (`plan/02` §10, §12, `plan/03` §6.4). With echoed drafts it returns `false` for `de` and `pl`, which is the correct answer; marking every `de` key reviewed in a fixture manifest flips it with no code change. It is pure and synchronous so `generateMetadata` and the sitemap builder get the same answer. `alternatesFor()` is the single generator `plan/02` §8 mandates — spec 007's `seo` module wraps it and must not reimplement it: `x-default → /en`, the regional aliases that share a URL (`en-IE`, `en-NL`, `en-150`, `de-DE`, `de-AT`, `pl-PL`), reciprocal and complete by construction, no entry for a non-indexable locale and structurally none for a pseudo-locale, in the shape `tests/fixtures/seo/hreflang/*.json` already consumes so spec 001's `validate-hreflang` CLI inherits real fixtures. A cross-locale canonical is unexpressible by construction. AC-31 tests `plan/09`'s promise: a fifth locale with no catalogue renders, is non-indexable and beta-tagged, with no change under `src/app/` or `src/modules/`. Tests: T-14, T-24, T-31. From `/review 17`: Google honours only ISO 3166-1 alpha-2 region subtags in `hreflang` and ignores UN M.49 (`en-150`) — verify against plan/02 §3 before emitting it in `alternatesFor()`; and `formattingTag` (TASK-044) must never leak into hreflang output.  **Implemented on `task/TASK-039-review-gate-alternates`, PR #21, `in_review` 2026-09-08.** Two spec-silent points decided and argued in the PR body rather than improvised. (1) **`en-150` is configured data but is not emitted in hreflang**: Google documents ISO 639-1 language plus an optional ISO 3166-1 alpha-2 region (and `x-default`), UN M.49 `150` is not alpha-2, and one unsupported value can void the whole annotation — `emitInHreflang()` filters non-alpha-2 regions for every locale rather than special-casing one value, so a Phase 4 `es-419` inherits the protection; `plan/02` §3's alias table needs that correction recorded → **carry-forward for TASK-043's doc pass**, entry added to `docs/decisions-log.md`. (2) **`alternatesFor()` returns `[]` when the x-default locale is not indexable** rather than promoting another locale or emitting a cluster without `x-default` (unreachable while `en` is the authored source; pinned by a test). **Unreviewed-share definition** (module header + tests): unreviewed keys ÷ keys in the *resolved* catalogue, where a key inherited from a fallback locale counts as reviewed only when that locale shares the primary language subtag — `en-gb` ← `en` is reviewed English (share 0, indexable), `de`/`fr` ← `en` is untranslated (share 1), so the thin override, the echoed drafts and the catalogue-less fifth locale all get their correct answer from one rule with no special case. `alternatesFor(target, { baseUrl })` takes the origin as an argument so the module stays pure (§5.3) and the `NEXT_PUBLIC_SITE_URL` read stays at spec 007's call site. `tests/fixtures/seo/hreflang/` now holds real fixtures generated by `scripts/seo/generate-hreflang-fixtures.ts`, so `pnpm seo:validate` gates live data (2 fixtures ok) and a test asserts regeneration is byte-identical; `tests/unit/seo-validate-hreflang.test.ts`'s "no fixtures" assertion updated accordingly. `LocaleSwitcher` renders the `plan/03` §6 beta marker with `betaLocales` defaulted to `localeBetaTag()`, so no page computes a share and AC-31 stays true; one new key `common.beta` (`en` human/reviewed, `de`/`pl` re-drafted, `i18n:draft` still byte-identical on a re-run). `isLocaleIndexable` is exported only — robots/metadata consumption is spec 007. Unit 853 green (+37, 0 skipped); both new modules mutation-checked. CI 19/20 green (e2e, a11y, visual, seo-validate included); `lighthouse` red and informational — the pre-existing `resource-summary.script.size` budget carried by TASK-043. |
| TASK-040 | `pnpm i18n:check` and the `i18n-check` CI job: missing keys after fallback resolution, unused keys with `meta.retained` as the only escape, ICU parse errors, argument-set mismatches between a key and its translations, redundant `en-gb` overrides, meta-manifest completeness, stale `sourceHash`, and `pathSegments` shape plus per-locale uniqueness; GitHub step summary with the per-locale reviewed share | `specs/003` · AC-13, AC-22 | 0 | done | backend-implementer | [#22](https://github.com/itsahmeds/flowers-overseas/pull/22) | TASK-039 | Branch `task/TASK-040-i18n-check-ci`. §11 makes CI the observability surface for translation debt: the step summary's per-locale table (total keys, missing after fallback, unreviewed count and share, stale count, indexable yes or no) is computed from the same functions `isLocaleIndexable()` uses, so a locale cannot look ready in CI and be gated in code. Job placement follows spec 001 §14 A9's rule — `needs: typecheck`, not behind `lighthouse` — and it is a required check in the derived set (`scripts/branch-protection.ts` reads `ci.yml`, so no separate config edit). Each of AC-22's seven seeded faults gets a fixture proving a non-zero exit that names the file and the key, in the spec 001 validator style; the clean tree exits 0. AC-13 also pins that `localePath()` is the only URL builder in the app (data authored in TASK-033, builder in TASK-034) and that uppercase, non-ASCII, trailing-slash or duplicated `pathSegments` fail the check (§6 URL pattern). The pseudo-locale regeneration-determinism clause of §2 is appended to this script by TASK-042, once the generator exists. Tests: T-13, T-22. From `/review 16`: `meta.error.description` in `messages/en.json` is currently unused — consume it (layout default description) or the unused-key rule will fail; every other key is used. From `/review 20`: `banner.*`, `common.floristCount`, `common.homeLink`, `a11y.localeChooser` have no consumer until TASK-041 — scope the unused-key rule to shipped routes or mark them `retained` in the interim, never delete; have `i18n:draft` validate the manifest it writes before writing. **Implemented on `task/TASK-040-i18n-check-ci`, PR #22, `in_review` 2026-09-08.** Eight checks, each naming the file and the key; the catalogue half runs inside `withLocaleRegistry` + `withMessageSource` with a disk-backed `MessageSource`, so `resolveCatalogue()`, `fallbackChain()`, `unreviewedShare()` and `isLocaleIndexable()` themselves answer about the tree under test and §11's promise ("the same number `isLocaleIndexable()` uses") holds by construction rather than by convention. Five spec-silent points decided and argued in the PR body: (1) **stale is an error in both directions** — a `reviewed: true` record whose `en` source moved shows the reader copy that no longer exists *and* makes `unreviewedShare()` count it as reviewed, so the gate would be lied to; an unreviewed stale record means `i18n:draft` was not re-run; only the remedy in the message differs. (2) **A redundant override is only redundant in the same language** (`en-gb` ← `en`), the same primary-subtag rule `review.ts` uses, or every echoed `de`/`pl` key would be reported as redundant instead of as unreviewed debt. (3) **Plural/select categories are not part of a message's argument signature** (`name:kind` only) because CLDR gives `pl` four categories where `en` has two; `requiresOtherClause: true` catches a missing `other` as a syntax error instead. (4) The missing-key pass also reports a **translation of a key `en` does not have** (the reverse direction of the same property; `i18n:draft` removes it). (5) The **usage scan is a documented, deliberately inclusive text heuristic** over `--src` (namespace binding + key literal, or the fully-qualified dotted path, which is what covers `global-error.tsx` reading the catalogue as data): it can under-report a dead key, never accuse a live one, and `tests/` is not scanned so a key cannot keep itself alive through its own test. New devDependency `@formatjs/icu-messageformat-parser@3.5.17`, dev-only and pinned to the version already in the tree through `next-intl` → `use-intl` (`downloaded 0`, three lockfile lines): validating against the parser next-intl actually formats with is the point. Carry-forwards discharged: `meta.error.description` is consumed as the `[locale]` metadata default's `description`; `banner.headline`/`switch`/`stay`/`dismiss` and `common.floristCount` are marked `retained: true` in `messages/en.meta.json` rather than deleted or exempted by a route allowlist — **TASK-041 removes the four `banner.*` flags** and the first spec 004 page rendering a florist count removes `common.floristCount`'s (`MessageMetaSchema` has no `retainedReason` field, so the reason lives in the PR body and here); `draftLocale()` parses both files with `MessagesSchema`/`MessageMetaManifestSchema` before writing, and `i18n:draft --locale de\|pl` still produces byte-identical output. Fixtures: `tests/fixtures/i18n/_cases/` holds one directory per AC-22 fault plus `clean` (miniature catalogues with their own `src/usage.ts`) and five registries for AC-13; Prettier-ignored, `--messages-dir`/`--src`/`--registry` keep the committed catalogues untouched. Unit 890 green (+37, 0 skipped); `pnpm i18n:check --summary` exits 0 with `en`/`en-gb` at 0.0 % indexable and `de`/`pl` at 100.0 % non-indexable. `pnpm branch-protection --print-commands` picks up `i18n-check` from `ci.yml` with no config edit; CI 21/22 green including the new job, `lighthouse` red and informational (the pre-existing 304 KB script budget carried by TASK-043). |
| TASK-041 | Suggestion banner, switcher beta tags and the `fo_locale` cookie: `hints.ts` (`parseAcceptLanguage`, `preferredLocale` — language preferences only), the lazily imported client island, `LocaleCookieSchema`, the switcher's beta marker above the 5% threshold, and the e2e banner matrix | `specs/003` · AC-12, AC-28 | 0 | done | frontend-implementer | [#23](https://github.com/itsahmeds/flowers-overseas/pull/23) | TASK-035, TASK-040 | Branch `task/TASK-041-suggestion-banner-cookie`. ADR-0006 in its positive form: the banner decides entirely from `navigator.languages` and `document.cookie` after hydration, so no response varies by request header — no `Vary: Accept-Language`, no bot-UA branch, one cache entry per path, and no `Set-Cookie` on any cached page (§5.4, AC-12; spec 001 AC-15 preserved). §7 records the deliberate narrowing of `plan/03` §2 (§13 Q9): the IP-country hint is dropped entirely, so `fo/no-geo-redirect`'s `hints.ts` allowance stays unused rather than exercised, and `hints.ts` reads no IP or geo header at all. §8 compliance: `fo_locale` is strictly necessary and functional per `plan/07` §6 — written only on an explicit user action, holding one of four locale codes and no identifier, `Path=/`, `Max-Age` 31536000, `SameSite=Lax`, `Secure` outside development, not `HttpOnly`, no consent gate (§13 Q4); a forged value such as `fo_locale=zz` is ignored and rewritten. No new RoPA processing row is required (no new processor, category or transfer); the reviewer confirms the `fo_locale` row in the cookie register at PASS. Accessibility: non-modal, dismissible, `Esc`-closable, `aria-live="polite"`, steals no focus, reserves no layout space (CLS delta 0 — WCAG 2.2.2 and 1.4.13). The switcher's links work with JavaScript disabled. Tests: T-12, T-28. **Implemented on `task/TASK-041-suggestion-banner-cookie`, PR #23, `in_review` 2026-09-08.** Four files under `src/modules/i18n`: `hints.ts` (pure — `parseAcceptLanguage`, `languagePreferences`, `preferredLocale`, `readLocaleCookie`, `serialiseLocaleCookie`, `decideSuggestion`, importing nothing but `./schemas.ts` and touching no browser global), `ui/LocaleSuggestionBanner.tsx` (Server Component projecting the registry into four fields plus a `localePath()`-built href), `ui/LocaleSuggestionBannerLoader.tsx` (`next/dynamic`, `ssr: false` — the boundary lives in the module because `ssr: false` is invalid in a Server Component and `plan/01` §5 keeps `app/` thin) and `ui/LocaleSuggestionBannerIsland.tsx`. `LocaleCookieSchema` + `AcceptLanguageSchema` in `schemas.ts`; barrel gains `parseAcceptLanguage`, `preferredLocale`, `LocaleSuggestionBanner`, `LocaleCookieSchema` (pin updated, the island's own seams go to `FORBIDDEN_EXPORTS`); `banner` joins `namespacesFor("localeDocument")`; the four `banner.*` `retained` flags are gone from `messages/en.meta.json` (TASK-040 carry-forward discharged). Five spec-silent points decided and argued in the PR body: (1) **Dismiss and `Esc` write no cookie** — §2 attaches "sets `fo_locale`" to Switch and Stay only, a dismissal is a "not now" rather than a choice, so it is remembered per tab in `sessionStorage` (a boolean, no identifier, same strictly-necessary basis) and "never reappears" stays true exactly for the two actions §2 names; (2) the `next/dynamic` boundary is a module-internal client loader, not the layout; (3) `preferredLocale(preferences, registry)`'s second argument is a **structural list** (`LocaleConfig[]` satisfies it), which is what lets the server and the island share one pure matcher and keeps the registry out of the browser; (4) `decideSuggestion` takes the raw `document.cookie` and validates `fo_locale` itself, so "a forged value is ignored" is a property of the decision; (5) `timeZone="UTC"` added to the layout's `NextIntlClientProvider`, mirroring `request.ts`, because the banner is the first component to read copy in the browser. Measured, not asserted: the island is **1,096 B gzipped** in one chunk that is absent from the prerendered HTML and is the only late request on `/en`; initial `/{locale}` JS moves +4,104 B gz, and only because `src/app/global-error.tsx` is `"use client"` and imports the module **barrel** — flagged for TASK-043/AC-27 as the cheap lever it owns, alongside the pre-existing 304 KB Sentry+zod weight. Unit 999 green (+109, 0 skipped), e2e 19 new cases × 2 projects, a11y in a **new** `tests/a11y/banner.spec.ts` so TASK-042's edits to `shell.spec.ts` do not collide. CI 22/23 green; `lighthouse` red and informational (`resource-summary.script.size` on `/`, which ships no island). **Carry-forward for the reviewer and for TASK-043:** the `fo_locale` cookie-register row §8 asks the reviewer to confirm has no file to live in — `docs/compliance/` holds `ropa.md` only (no RoPA row is needed, per §8). `plan/07` §6 already names the cookie essential, but the register itself needs an owner: TASK-043's docs pass or spec 004's CMP. Also recorded: `pnpm test:visual` fails on macOS on `origin/main` too — the committed `darwin` baselines are an unstyled render (no Tailwind preflight) while the `linux` baselines CI gates on are correct; proven by stashing this branch and re-running, and by the byte-identical `/` HTML with and without this PR. No baseline touched (TASK-042 owns `tests/visual/**`). |
| TASK-042 | Pseudo-locales and the real visual RTL gate: `pseudo.ts` plus `pnpm i18n:pseudo` generating `en-XA` and `ar-XB` deterministically from `en.json` into git-ignored files, `ENABLE_PSEUDO_LOCALES` in `src/lib/env.schema.ts` and `.env.example` with the production refusal, the Playwright `pseudo-rtl` project made real with `forcePseudoRtl` deleted, and committed baselines | `specs/003` · AC-29, AC-30 | 0 | done | frontend-implementer | [#24](https://github.com/itsahmeds/flowers-overseas/pull/24) | TASK-040 | Branch `task/TASK-042-pseudo-locales-visual`. Pays off two spec 001 debts at once: `tests/visual/pseudo-rtl.ts`'s `forcePseudoRtl` init-script stub from TASK-008 is deleted because `/ar-XB` is now a real `dir="rtl"` document, and the `/review 8` note that "the two visual baselines are byte-identical blanks today, so pseudo-RTL is a gate only once content lands" is discharged. §8 security: the zod env schema **fails the build** when `ENABLE_PSEUDO_LOCALES=true` while `VERCEL_ENV=production`, so a pseudo-locale route cannot reach buyers through a configuration mistake; §13 Q6 accepts protected-preview exposure because that is where the visual and a11y suites run. Pseudo-locales are absent from `launchLocales`, `generateStaticParams`, the switcher and `alternatesFor()` by construction and are always `noindex`, so no phantom URL space is discoverable (§6 crawl efficiency). Adds the pseudo-regeneration determinism check to `pnpm i18n:check` (§2) and extends the axe URL set with `/ar-XB` — AC-25's fourth URL, owned by TASK-035 and re-verified here. `pnpm env:check` stays green. Tests: T-29, T-30. **Implemented on `task/TASK-042-pseudo-locales-visual`, PR #24, `in_review` 2026-09-08.** Seven decisions argued in the PR body; the three load-bearing ones: (1) **registry gating** — `PSEUDO_LOCALES` lives in `src/config/locales.ts` and is appended to the registry only when `ENABLE_PSEUDO_LOCALES=true`, so the flag is read in one place and the switcher, `alternatesFor()`, sitemap membership and `isLocaleIndexable()` exclude the pseudo-locales through the `isLaunch` gate they already had; two new `LocaleConfigSchema` refinements make it structural rather than conventional (a pseudo-locale may not be `isLaunch`, and it must declare **no** `hreflangAliases` value, so `hreflangAliases` lost its blanket `min(1)`), and the registry-level \"exactly one `x-default`\", alias-ownership and fallback-terminus refinements are asserted to still hold with six locales. `isPseudo` is a new `LocaleConfig` field defaulting to `false` (three existing fixtures gained one line). (2) **No `prebuild` and no runtime file read**: `messages.ts` derives both catalogues in memory from `generatePseudoCatalogues(en)`, gated by the registry, because the files are git-ignored (a static import breaks `tsc`/`next build` on a clean clone, a computed `readFileSync` is invisible to Next's build tracing) and a `prebuild` step would add a build-order dependency to every clone, CI job and deployment for data that is a pure function of a committed file; `pnpm i18n:check`'s new **check 9** pins the generated files to the same function whenever they exist, so a stale file fails CI instead of diverging silently, and the pseudo-locales are excluded from checks 1–7 (no authored catalogue, no review manifest). `pnpm i18n:draft` refuses them by name. (3) **`routableLocale`/`routableLocaleCodes`** is the new \"locales that have URLs\" concept (launch **or** pseudo) that `generateStaticParams` and the `[locale]` layout/page/`request.ts` read; `launchLocales()` stays the buyer- and crawler-facing set, and with the flag off — which the env schema guarantees in production — the two are equal, which is §5.4's \"never a pseudo-locale in `generateStaticParams`\". Also: `ar-XB` mirrors with bidi controls (`U+202E`…`U+202C`, `U+200F` marker) rather than Arabic homoglyphs, because §7 loads no Arabic font and tofu boxes would stop the screenshot measuring layout; one Playwright project per spec file (`visual` → `/`, `/en`, `/de`; `pseudo-rtl` → `/ar-XB`), `forcePseudoRtl` and `tests/visual/pseudo-rtl.ts` deleted. Baselines: `darwin/` regenerated locally for all four and verified by a second non-updating run — `darwin/visual/home.png` **changed on purpose**, the committed one had been captured without the stylesheet applied while the committed `linux/home.png` and the served HTML both have Tailwind's preflight. Recorded local caveat: on a case-insensitive macOS volume a request for `/ar-xb` resolves to the `ar-XB` prerender and replaces that cache entry with a 404 document (the APFS artifact TASK-034 documented for `/EN`); no test requests a mis-cased pseudo path and `routableLocale(\"ar-xb\")` is asserted `undefined` at the unit layer. Local gates all green: unit 917 (+27, 0 skipped), e2e 56 (2 pre-existing `/EN` skips), a11y 14 with no exception list, visual 4, `i18n:pseudo` twice with no diff, `i18n:check`/`env:check`/`check:no-db`/`check-layout`/`format:check` clean, and `VERCEL_ENV=production ENABLE_PSEUDO_LOCALES=true pnpm build` exits 1 naming the key. **No founder action needed after all: the Vercel preview environment already carries `ENABLE_PSEUDO_LOCALES=true`** — measured, not assumed (`e2e` and `a11y` green including `/ar-XB` and `/en-XA`, and the `visual` job got past its `expect(status).toBe(200)` guard), so the `linux/` baselines for `/en`, `/de` and `/ar-XB` were taken from that job's artifact and committed; `linux/home.png` came back byte-identical, confirming the darwin one was the anomaly. CI on the head commit: 17 of 18 jobs green (`visual` 4/4, `a11y` 14/14, `e2e` 56 passed / 2 pre-existing `/EN` skips, `i18n-check`, `build`, `env-build-failure`); `lighthouse` red and informational for the pre-existing `categories.performance` and `resource-summary.script.size` breaches TASK-043's AC-27 owns — this PR adds no client JavaScript. |
| TASK-043 | Docs, budgets and spec close: README (`i18n:check`, `i18n:draft`, `i18n:pseudo`, the changed `test:visual` target and the locale-URL rule), `docs/runbooks/i18n-translations.md` plus its index row, `content/i18n/glossary.en.md` and a stub per launch locale, `docs/architecture.md` §2/§3/§4 final state, and the Lighthouse plus client-JS budget assertions | `specs/003` · AC-27, AC-32 | 0 | done | backend-implementer | [#25](https://github.com/itsahmeds/flowers-overseas/pull/25) | TASK-041, TASK-042 | Branch `task/TASK-043-docs-budgets-close`. AC-27 turns the §6 CWV contract into a test: `lighthouserc.json` budgets still pass on `/` and `/en`, total client JS on `/{locale}` stays ≤ 120 KB gzipped, the serialised messages payload handed to the client is ≤ 4 KB gzipped (asserted from the build output), and `/` ships zero application JavaScript. AC-32 leaves `docs/architecture.md` §4 with both spec-003 rows gone — the `lang="en"` literal (removed by TASK-034) and `middleware.ts → proxy.ts` (removed by TASK-032) — verified absent here, with the `i18n` module status updated and the new config files listed; `tests/unit/architecture-doc.test.ts` and `tests/unit/docs.test.ts` must stay green. The glossary carries the brand terms that are never translated, tone, taboo words and §13 Q2's resolved Polish register (informal `Ty` in the UI, formal in legal copy) so the native reviewer inherits the decision instead of re-deciding it per string (§13 Q11); the runbook covers adding a key, running `i18n:draft`, reading the reviewed share, handing a locale to a native reviewer, what makes a locale indexable, and the `<bdi>` rule that spec 004 inherits. Spec 003 §14 records this spec's deviations, including which §5.3 root-layout shape was actually chosen and the §12 locale-code-flip deviation. Spec exit signal: this PR merged with a recorded `/review` PASS, `i18n-check` green with `de` and `pl` at 0% reviewed and non-indexable, four locale URLs live on the preview with correct `lang`/`dir`/`<title>`, and the axe exception list gone. Tests: T-27, T-32. From `/review 16` (AC-27 evidence): Lighthouse on preview `/` measures script 304 KB vs 120 KB budget and performance 0.88 vs 0.95; ~76% is the browser Sentry SDK (~146 KB gz) + zod (~84 KB gz) pulled in via `instrumentation-client.ts` → `sentry.client.config.ts` → `@/lib/sentry` → `./logger` → zod (TASK-005 chain), not i18n. Levers: break the zod import out of the client Sentry path (plain PII key array), lazy-load/trim or drop the browser Sentry SDK on `/`, disable Vercel preview Comments so the measured number is production-like. Also: correct the false "`/` ships zero application JS" comments in `src/app/(chooser)/{page,layout}.tsx` and `src/modules/i18n/messages.ts`; record in architecture §2 that the `[locale]` layout metadata default is the error title and that Next `dynamicParams` is unavailable when Cache Components is enabled (gate tripwire for spec 004); fix the unescaped backticks in the `visual` job summary echo. From `/review 17`, §14 amendments to record: `MoneySchema.amountMinor` is `number().int() \| bigint` (2^53+1 unrepresentable as number); `formatDate` requires an IANA zone; `formatRange` is dates only; `sortBy` added to §5.2; zone label is ICU `longGeneric` not "Warsaw time"; plan/03 §7 weekday example corrected (14 Feb 2027 is a Sunday) — applied by the orchestrator. Runbook: ICU zone/locale names are exact-string assertions and may move on a Node bump (expected maintenance point); space-folding rationale. Also to record: spec 002 §5.1 `locale` gains `formatting_tag` (TASK-044). From `/review 20`: glossary must state the `en` house style (`-ize`; `en-gb` `-ise`); note for B12 reviewers that `common.floristCount` renders people in en/de and shops (`kwiaciarnia`) in pl. README `i18n:draft` row already shipped by TASK-038. From `/review 21`: record the plan/02 §3 correction that `en-150` (UN M.49) is never emitted in hreflang (alpha-2 regions only, `emitInHreflang()`). From `/review 23`: create `docs/compliance/cookie-register.md` with rows for `fo_locale` (first-party, functional, 365 d, `Path=/`, `SameSite=Lax`, closed 4-value domain, set only on explicit action) and `fo_locale_suggestion_dismissed` (sessionStorage, per-tab); split `src/app/global-error.tsx`'s barrel import so i18n exports stop landing in every page's initial client bundle; record in architecture/runbook that no global time zone default is correct in a relay — every rendered time passes its own IANA zone; for spec 004: the banner overlay needs a background token, and a permanently mounted `role="status"` region is the reliable live-announcement pattern; founder decision: banner copy is in the current page's language per §7 — bilingual would be a spec change. **Implemented on `task/TASK-043-docs-budgets-close`, PR #25, `in_review` 2026-09-08.** Docs half complete: README (locale-URL rule section, `budget:client-js`, the informational-gate paragraph with the measured reason, three troubleshooting rows), `docs/runbooks/i18n-translations.md` + index row (add a key, `i18n:draft`, reading the reviewed share, handing a locale to a reviewer, the five indexability conditions, the `<bdi>` rule, ICU-strings-move-on-a-Node-bump and space-folding as named maintenance points, no-global-time-zone), `content/i18n/glossary.{en,en-gb,de,pl}.md` (brand terms, relay vocabulary, tone, taboo list, `-ize`/`-ise` house style, Polish `Ty`/`Panstwo`, the `common.floristCount` people-vs-shops note), `docs/compliance/cookie-register.md` + a pointer from `ropa.md` (whose row 1 now says server/edge only), `docs/architecture.md` §2 (metadata default = error title, the Cache-Components `dynamicParams` tripwire, the measured one-file barrel exception, `content/i18n/`), §3 (i18n complete; the two Sentry entrypoints), §4 (a client-Sentry row deferred to spec 013; both spec-003 rows confirmed gone), §5 pointers; spec 003 §14 A2-A13 appended. Budget half: `scripts/client-js-budget.ts` + `tests/unit/client-js-budget.test.ts` (15 tests) measure the fetched script set per URL from the prerendered documents and the serialised message payload per locale; the `build` job prints the table. **AC-27 is two-thirds met and one-third escalated.** Measured: browser Sentry deleted (`instrumentation-client.ts`, `sentry.client.config.ts`; server/edge and `beforeSend` untouched, per spec 004 §13 Q8's accepted default) and `global-error.tsx`'s barrel import narrowed -> `/` 297.0 -> **221.3 KB gz / 186.2 br**, `/en` 309.1 -> **233.4 / 197.0** (-75.7 KB each: 72.2 Sentry, 3.5 the barrel); client message payload **0.6 KB gz** per locale against 4 KB; `(chooser)` has no Client Component of its own. Local Lighthouse on the shipped tree: performance 0.96-0.99, a11y 1.00, CLS 0, SEO 0.63 (deliberate `noindex`). **Blocker for the founder, not loosened:** Next 16.3.4's own client runtime is **130.1 KB gz / 111.2 KB br** on a document with zero application JS, so `plan/01` §7's 120 KB gz budget is below the framework floor and `resource-summary:script:size` (230,667 B on `/`, 246,239 on `/en`) plus LCP (2.0-2.5 s) stay red. The remaining 91.1 KB gz above the floor is zod plus the locale config/catalogues, reaching the browser through the one chain a Client Component cannot avoid: Next's root error boundary `global-error.tsx` -> `messages.ts`/`registry.ts` -> `schemas.ts` + `src/config/locales.ts`, both of which `.parse()` at module load; stubbing it measures `/` at 132.1 gz / 113.0 br and `/en` at 144.2 / 123.8. Breaking it needs either a zod-free locale data module (against AC-1's "parses at module load") or a registry-independent constant for the 500 document (against AC-5) - a structural change spec 003 did not list, so it was **not improvised**. Therefore `lighthouse` keeps `continue-on-error: true` (it is consequently still outside the derived required-check set - verified with `pnpm branch-protection --print-commands`) and the new budget step is informational: both measure and publish on every PR, neither blocks. Three options for the founder, in spec 003 §14 A12: raise the budget, measure Brotli transfer size (`/` would pass at 113.0 KB br, `/en` 3% over), or reduce the framework. Spec 004 task 2 already owns the flip (spec 004 §2, §12). `tests/fixtures/seo/lighthouse-urls.json` is `["/", "/en", "/de"]`, budgets in `lighthouserc.json` unchanged, `visual` job backticks escaped, lighthouse timeout 20 -> 30 min for 3 URLs x 3 runs. Verified: lint, typecheck, `pnpm test` 1052/1052, `format:check`, `i18n:check` (4 locales ok, `de`/`pl` 0% reviewed and non-indexable), `env:check` (16 keys), `check:no-db`, `check-layout`, `seo:validate`, `pnpm build` from `.env.example` placeholders, porcelain clean. |
| TASK-044 | `formattingTag` split for `en`: add `formattingTag` (default = `bcp47`, refined to be `Intl.Locale`-parseable and to share `bcp47`'s primary language subtag) to `LocaleConfigSchema`, set `en` → `en-150`; `tagFor()` in `format.ts` and `collator()` read it; `toLocaleRow` gains `formatting_tag` (AC-4 pin and spec 002 §5.1 `locale` table amended); `en` rows added to the date/time/number/week-start tables in `tests/unit/i18n-format.test.ts` | `specs/003-i18n-foundation.md` · no AC of its own; discharges `/review 17` observation (b) so AC-16 conventions hold for the x-default locale; `<html lang>` stays `en` (AC-6 untouched) | 0 | done | backend-implementer | [#19](https://github.com/itsahmeds/flowers-overseas/pull/19) | TASK-036 | Branch `task/TASK-044-en-formatting-tag`. Decision (orchestrator under founder delegation, 2026-09-08): two-field approach — document language tag `bcp47` unchanged, new `formattingTag` for `Intl`; `en-150` chosen over `en-IE` (pan-European intent, already in `hreflangAliases`). Must land before TASK-039 bakes tags into `alternatesFor()` and before spec 004's date picker reads week start (`Intl.Locale.getWeekInfo`). Also pin the previously unasserted `en` outputs so the change is visible as a test diff. Spec 002 §5.1 amendment: `locale.formatting_tag` column (recorded in spec 003 §14 by TASK-043). **Implemented on `task/TASK-044-en-formatting-tag`, PR #19, `in_review` 2026-09-08.** `formattingTag` is optional when authored and defaulted to `bcp47` in a transform (a per-field `z.default()` cannot see another field), then refined to be a canonical tag `Intl.Locale` accepts and to share `bcp47`'s primary language subtag — which also rejects Unicode extensions such as `en-u-kf-upper`, whose `baseName` does not round-trip; a later spec needing a collation keyword in the formatting tag must relax that. `en` renders European conventions now (`14/02/2027`, `14:00 Central European Time`, `a, b and c`, `45.00 £`, `getWeekInfo().firstDay === 1`) with the plain-`en` answers (`02/14/2027`, `02:00 PM`, `a, b, and c`, `£45.00`, `firstDay === 7`) pinned as negative assertions so a revert to `bcp47` fails a test. Three deviations in the PR body: ICU gives `en-150` the delivery date `Sat, 13 Feb` (comma), not the `en-GB` `Sat 13 Feb` the row predicted, and both are pinned side by side; the three `en` rows of `tests/fixtures/index.ts` are relabelled `en-150` with the two foreign-currency values updated (`45.00 £`, `45.00 PLN`) because the fixture is by contract the module's own output, shape untouched for spec 002 AC-34; and neither spec file was edited — spec 003 AC-4's key list and spec 002 §5.1's column list are TASK-043's §14 amendments (noted on that row). No barrel export added (AC-3 list untouched), no new dependency; `LocaleConfigInput` (`z.input`) is exported from `src/config/locales.ts` only. Unit 769 green (+10); e2e 50 passed re-run against the production build with `/en` → `lang="en"` (AC-6 unmoved); CI 19/20 green, `lighthouse` red and informational (the pre-existing 304 KB script budget carried by TASK-043). |
| TASK-045 | Tokens, fonts and the `ui` module foundation: `@theme` token set lifted from the approved `tokens.css`, contrast manifest + test, `fo/no-raw-color` + Stylelint additions, self-hosted Newsreader + IBM Plex Sans (Latin + Latin-Ext), `@layer base` reset, focus ring, reduced-motion, `@utility mirror-in-rtl`, the icon set and `Mark` wordmark, primitives (`Container`/`Stack`/`Row`/`Cluster`/`Grid`/`VisuallyHidden`/`SkipLink`), `/dev/components` gallery + `ENABLE_DEV_UI` | `specs/004-design-system-layout.md` · AC-1, AC-3, AC-4, AC-5, AC-6, AC-28 | 0 | done | frontend-implementer | [#27](https://github.com/itsahmeds/flowers-overseas/pull/27) | — | Branch `task/TASK-045-tokens-fonts-primitives`. First of the two unblocked tasks (spec §12 task 1); starts immediately. Spec §13's 2026-09-08 resolution note is binding and overrides §5 defaults where they differ. Design source of truth (match pixel-for-pixel): `docs/design/homepage-v1/README.md`, `docs/design/homepage-v1/tokens.css`, `docs/design/homepage-v1/homepage-desktop.dc.html`, `docs/design/homepage-v1/homepage-mobile.dc.html`, `docs/design/homepage-v1/identity.dc.html`, `content/brand/mark.svg`. **Two** families, not one: §13 Q2 resolves to Newsreader (display, weight 500, roman only — no italic axis) + IBM Plex Sans (body), both self-hosted through `next/font/local` with Latin + Latin-Ext subsets, `font-display: swap`, `preload` and fallback metrics; §2's "one variable font" and "no second display face" sentences are superseded by the resolution note and the deviation is recorded in spec §14 by TASK-056. AC-4's ≤45 KB per-page font transfer therefore covers **both** faces together — subset the weight axes to what the canvas actually uses (display 500; body 400/600) and assert from the build output. Tokens are the canvas values verbatim in OKLCH (`--color-paper`/`-2`/`-3`, `--color-ink`/`-2`/`-3`, `--color-rule`, `--color-accent` oklch(42% 0.10 155), `--color-accent-ink`, `--color-photo` gradient) plus the semantic aliases §2 names (`--color-surface*`, `--color-ink*`, `--color-border*`, `--color-focus`, `--color-success\|warning\|danger` with their `--color-on-*` partners) mapped onto them, the canvas type steps (`--text-display` 68 / `-display-s` 42 / `2xl` 34 / `xl` 24 / `lg` 18 / `md` 15 / `sm` 13 / `xs` 11, `clamp()`-fluid between the mobile and desktop artboards), the canvas space scale (4/8/16/24/40/72/128), `--radius-sm` 2 / `--radius-md` 4, `--rule`, the `--layer-header\|banner\|overlay` z-scale and `--measure`. Also lift the canvas's three utility classes as `@utility`: `.display` (Newsreader 500, `-0.015em`), `.label` (11 px, 600, `0.14em`, uppercase, `--color-ink-3`) and `.photo` (gradient + caption) — they are used by every later 004 task, so they must not be re-invented per component. Contrast manifest must include the low-chroma pairs the canvas relies on (`--color-ink-3` on `--color-paper`, `.photo` caption ink on the gradient's lightest stop, accent-ink on accent) — if a canvas pair fails AC-3, darken the token and record the delta in the PR body rather than shipping an exception. `docs/architecture.md` §3 `ui` module row and the `MODULES` manifest of `scripts/check-layout.ts` land in this PR (§2). Gallery renders every token ramp and every component state added by this and every later 004 task (each task extends it — enforced by the reviewer, not by a test). Photography is a placeholder until the founder supplies imagery: every `.photo` box in the canvas renders the `--color-photo` token gradient with its uppercase caption and **no `<img>`** (`plan/10` §3 honesty rule); the consolidated "photo slots" list is part of TASK-053's PR body. Tests: T-01, T-02, T-04, T-05, T-06, T-07 (lint half), T-08, T-30. **Implemented on `task/TASK-045-tokens-fonts-primitives`, PR #27, `in_review` 2026-09-09.** AC-1/AC-3/AC-4/AC-5/AC-6/AC-28 all proven by tests quoted in the PR body. Measurements: **font transfer 40 844 B (39.9 KiB) of the 45 KB budget** for three faces (Newsreader 500 `opsz` 32 = 17 468 B, IBM Plex Sans 400 = 11 272 B, 600 = 12 104 B), and **32 contrast pairs, 30 with a threshold, all passing with no token darkened** (the tightest are `--color-ink-3` on `--color-paper-3` at 4.69:1 and the photo caption on the gradient's darkest stop at 4.94:1, both over 4.5:1). Four load-bearing decisions, argued in the PR: (1) **two Plex weights, not three** — a third face breaks AC-4, so 400/600 ship and `--font-weight-medium: 600` maps the canvas's 500 onto a real face instead of letting CSS font-matching render it as 400; buttons and nav are therefore one step heavier than the artboard, reversible in one token. (2) **Subsetting is a committed artefact**: `pnpm fonts:build` (new devDependency `subset-font@2.7.0`) pins the axes, drops TT hinting and every OpenType feature except `kern`, and writes the WOFF2 files + `subset.json` + both OFL licences; measured alternatives were 98 KB + 66 KB from Google's CSS endpoint, 65.7 KB with hinting and the full feature set, and 45.4 KB for Newsreader alone with its `opsz` axis alive. The build never fetches a font (AC-2). (3) **`@theme static`**, because Tailwind v4 tree-shakes theme variables and AC-1 would be false for the semantic aliases no utility uses yet; ~1 KB of CSS. (4) **Stylelint's colour ban needs no `@theme` exception** — `declaration-property-value-disallowed-list` keys on the property name and a token's property is `--color-*`, so the block is exempt by construction while `color: #26282f` in that same file still fails. Also: `motion-fast\|base\|slow` rather than `duration-*` (Tailwind's own functional `duration-*` swallows a named value and emits nothing — caught here, now pinned by an assertion); `next/font/local` stubbed in the unit suite via a `vitest.config.ts` alias, with `swap`/preload/subsetting asserted in `tests/unit/fonts.test.ts` and `tests/e2e/fonts.spec.ts` instead; the gallery's own strings kept in a data module as developer-facing identifiers, so no component gains a literal and `i18n:check` gains no unused key. **Primitive set is the union** of this row's list and the brief's (`Button` 8 states, `Field`, `Chip`, `Photo`, `Placeholder`, `Price`, `Display`, `Text`, `Label`), because TASK-048/TASK-049 depend on exactly those. **Carry-forward for TASK-046/TASK-053:** `src/app/global-error.tsx` was left untouched (TASK-046's client-data chain), so the 500 document renders in the fallback font stack — one line adds `fontVariables` to its `<html>`. `docs/runbooks/design-system.md` stays TASK-056's; the gallery baseline stays out of the visual suite until AC-27's nine-baseline list lands with TASK-056. All four visual baselines regenerated on both platforms (`darwin/` locally, `linux/` from the `visual` job artifact) because the `@layer base` reset moved every pixel of text. CI on head `7026ca7`: 20 of 21 jobs green (`e2e` 2m55s, `visual` both platforms, `a11y` 18/18 with no exception list, including `/dev/components` — **the preview environment already carries `ENABLE_DEV_UI=true`, measured not assumed**); `lighthouse` red and informational for the pre-existing script-size and performance breaches TASK-046 restates and TASK-056 flips to required. **`/review 27` round 1 = FAIL, fixed 2026-09-09:** (1) Tailwind's automatic source detection compiled `tests/fixtures/lint/**` (and prose in specs, docs and the ESLint rules) into the production stylesheet — `@import "tailwindcss" source(none)` plus one explicit `@source "../../src/**/*.{ts,tsx}"`, and `tests/unit/tokens.test.ts` now asserts on the **scanned build** (Tailwind's own `@tailwindcss/oxide` scanner, offline) that no colour literal and no physical utility ships, with an unscoped scan as the control; built CSS 30 878 B → 24 200 B and zero hits for `#ff0000`/`hsl(`/`.ml-4`/`.text-left`/`.left-0`. (2) `Field` and `Price` crossed spec §3/§8 and are **deleted** with their gallery sections, samples and tests (orchestrator ruling); the icon set is now the canvas's eleven plus the two `MIRRORED_IN_RTL` ones, each cited to its canvas element and geometry-checked against `docs/design/homepage-v1/*.dc.html`. Nits 4–9 also done, including a token-value drift guard against `docs/design/homepage-v1/tokens.css`. **T-06 half-delivered, substitute accepted:** the `pl`-diacritics *visual* baseline was not produced; the `tests/e2e/fonts.spec.ts` measurement (the diacritics are painted by the webfont, not a fallback) and the `tests/unit/fonts.test.ts` repertoire assertion are accepted in its place as the stronger evidence — a screenshot cannot tell a webfont from a lookalike fallback. Genuinely rebased on `origin/main` (`cb4a20a`, TASK-046 merged) on 2026-09-09; conflicts were `.env.example`/`env.schema.ts`/the env fixtures (both keys kept) and the `no-adhoc-intl` timeout comment (merged). **CI:** no workflow run covers this head — Actions minutes are exhausted (TASK-057), so every gate was run locally: lint, typecheck, 1 448 unit, format, env, no-db, layout, i18n, build, 162 e2e, 18 a11y, 4 visual (baselines unchanged); `pnpm lighthouse` stays informational for the pre-existing script-size (uncompressed local server) and `/de` LCP breaches TASK-046 restated and TASK-056 flips to required. |
| TASK-046 | Client-JS budget restatement, zod-free client locale data, CSP and security headers: `plan/01` §7's budget restated as ≤120 KB **Brotli** transfer (spec §13 Q13 option (a)), zod removed from the client bundle via a zod-free locale-data module for the `global-error` document, per-environment `Content-Security-Policy-Report-Only` from `next.config.ts` with a build-time hash for the consent bootstrap, HSTS/`X-Content-Type-Options`/`Referrer-Policy`/`frame-ancestors`/`Permissions-Policy`/COOP, `POST /api/csp-report`, `CSP_REPORT_ONLY` env, ADR-0016 | `specs/004` · AC-23 · plus §2/§13 Q13 budget restatement and the zod-free client locale data that are AC-25's precondition (no AC id; AC-25 itself is owned by TASK-056) | 0 | done | backend-implementer | [#26](https://github.com/itsahmeds/flowers-overseas/pull/26) | — | Branch `task/TASK-046-budget-csp-security-headers`. Second of the two unblocked tasks (spec §12 task 2); independent of TASK-045 — it touches `next.config.ts`, `src/lib`, `src/config` and CI, not CSS or components. Spec §13's 2026-09-08 resolution note is binding and overrides §5 defaults where they differ. **Write ADR-0016** in this PR (`/adr CSP shape on cached HTML`, §13 Q5 accepted): allowlist + build-time `'sha256-'` hash for the single ≤1 KB inline Consent Mode block on cached routes, per-request nonce + `'strict-dynamic'` on `no-store` routes deferred to the spec that ships the first one; the `plan/01` §9 "nonces" sentence is superseded, not edited. **`vercel.live` decision (spec 001's second deferred row, `/review 8`):** the origin appears in `script-src`, `connect-src` and `frame-src` of the **preview** policy only and is absent from the production policy, both header strings asserted (AC-23) — `frame-src` because the feedback widget renders in an iframe, disclosed in ADR-0016 and pinned by the whole-string tests — this is what lets `tests/e2e/shell.spec.ts` drop its non-local allowance later. Budget: spec 003 TASK-043 already removed browser Sentry (spec §13 Q8 needs no work), so the remaining client-bundle offender is zod, reached from the `global-error` document's locale lookup — ship a zod-free `src/config/locale-data.ts`-shaped module (plain typed constants, still the single source with the zod-parsed registry validating it in a unit test) so no public route's client bundle contains zod, and restate the budget in `lighthouserc.json`, `plan/01`-derived docs and `scripts/` comments as ≤120 KB Brotli / 122 880 B script transfer. `lighthouse` stays informational until TASK-056 flips it. Escalate rather than guess if the Brotli number still exceeds the budget after zod leaves: the fallback is spec §13 Q13 option (b) and it is a founder decision. Tests: T-25, plus a unit assertion that the built client chunks for `/`, `/en` and `/de` contain neither `@sentry/` nor `zod`.  **Delivered (PR #26, `/review` pending):** ADR-0016 accepted (`docs/adr/ADR-0016-csp-allowlist-hash-on-cached-html.md`) with the decisions-log line and the `plan/00` §6 row; `src/lib/csp.ts` + `next.config.ts` `headers()` sending `Content-Security-Policy-Report-Only` per environment (`vercel.live` in preview's `script-src` / `connect-src` / `frame-src`, absent from production, both whole strings asserted) plus `X-Content-Type-Options`, `Referrer-Policy`, `X-Frame-Options: DENY`, a 16-entry `Permissions-Policy`, COOP and production-only HSTS; `POST /api/csp-report` (both shapes, 415 on a wrong content type, 204 on everything else, never 5xx, only `directive` + `blocked_origin` logged); `CSP_REPORT_ONLY` (absent ⇒ report-only, only `false` enforces); `src/config/locales.data.ts` + `src/modules/i18n/error-document.ts` taking zod off every public route's client bundle; `scripts/client-js-budget.ts` now measuring Brotli against 122 880 B and asserting no route ships `zod` or `@sentry/`. `docs/architecture.md` §4's CSP row and its `vercel.live` note removed; §2 records the two-step narrowing. **Bundle, before → after** (browser-measured against `pnpm build && pnpm start`, Brotli q11 over every `.js` the page fetches, lazy chunks included): `/` 226 575 B gz / 190 706 B br → 136 067 / **116 393 (113.7 KB) — within, 6 487 B spare**; `/en` and `/de` 240 633 / 203 197 → 150 992 / **129 638 (126.6 KB) — 6 758 B, 5.5% over**. **ESCALATED, founder decision, blocks TASK-056 (AC-24, AC-25): spec 004 §13 Q13 option (b).** `/` is within the restated budget and the locale documents are 6 758 B over, and the remainder is not application code: `/`'s whole 116 393 B is the framework floor (react-dom 62 564 + App Router runtime 39 763 + ~13.5 KB bootstrap and route shells), leaving 6 487 B, while `NextIntlClientProvider` + `@formatjs` costs 10 705 B and the suggestion-banner island's own chunk 2 173 B. Options: (a) raise the budget (128 KB br = 131 072 B covers today's locale documents with 1.4 KB spare; the floor grows with Next); (b) drop the client message provider from `src/app/[locale]/layout.tsx` — its only client consumers are the suggestion-banner island, which already receives everything as props, and `[locale]/error.tsx`, both of which could read the zod-free path this task added, but that is a spec 003 surface and changes AC-28's island contract; (c) exempt locale documents in `lighthouserc.json`, i.e. loosening, listed only to be rejected. Not loosened here. Nothing blocks merge: both gates keep `continue-on-error: true` and TASK-056 owns the flip. **Two out-of-scope repairs, disclosed in the PR:** (1) `origin/main`'s `PHASE_0_SPECS_APPROVED` `3 / 12` → `4 / 12` flip, which this branch carried until `main` shipped it as `97ac494`; the duplicate was dropped in the rebase and the fix is `main`'s. (2) `tests/unit/no-adhoc-intl.test.ts`'s whole-`src/` ESLint test given an explicit 60 s timeout after five added files pushed it past Vitest's 5 s default on the runner. **Measured on the preview, informational:** `lighthouse` red on `resource-summary:script:size` — `/` 141 769 B, `/en` 249 766 B, `/de` 249 762 B against 122 880 — and on `/de` performance 0.90 / LCP 3 088 ms. Reconciled in `lighthouserc.json`'s note (`/review 26` item 5): Lighthouse's transfer size **is** Brotli and it **does** include the platform's `vercel.live` feedback script, which a protected preview injects, production does not serve and our build output does not contain — `/`'s 141 769 B is our 116 393 B plus 25 376 B of `vercel.live`, and `/en`'s 249 766 B was our then-198.4 KB plus ~48 KB of it. With this round's build a locale document should read ≈178 KB there and still be red. The attributable measurement is `pnpm budget:client-js`; deciding where the blocking measurement is taken (production URLs, or a first-party-only budget) is TASK-056's. **`/review 26` round 1 FAIL, addressed:** (1) zod was still fetched on every locale page — `LocaleSuggestionBannerIsland.tsx` → `hints.ts` → `schemas.ts` → zod, on a chunk `[locale]/layout.tsx` loads unconditionally through `next/dynamic({ ssr: false })`, 70.8 KB br. `hints.ts` now imports no validator: `LANGUAGE_RANGE_PATTERN` and `isLaunchLocaleCode` (from the zod-free `locales.data.ts`) carry the two rules, `schemas.ts` is built from the same constants and stays the server-side boundary parser, and `tests/unit/i18n-hints-zod-free.test.ts` walks the import graph from the island, its loader and `global-error.tsx` and proves the predicates and the schemas accept and reject the same values. (2) `scripts/client-js-budget.ts` was blind to `next/dynamic`: it now follows `.next/server/app/<entry>/react-loadable-manifest.json`, counts those chunks and scans them, prints the document / lazy split, and `tests/unit/client-js-budget.test.ts` has a fixture whose document is clean and whose lazy chunk carries zod. Its static total is an upper bound — `/` is charged 12.6 KB for a chunk group `(chooser)` never renders (129 271 B against the browser's 116 393 B), locale documents match the browser exactly — and which number the blocking gate reads is TASK-056's. (3)–(5) numbers re-measured with a browser and rewritten in the PR body, this row, `README.md`, `docs/architecture.md` §2, `lighthouserc.json`, the CI comment and `src/app/(chooser)/page.tsx`. Non-blocking: `CSP_REPORT_MAX_BYTES` (16 KB) declared-`Content-Length` guard on `/api/csp-report`; the per-instance limiter's consequence for the enforce flip recorded in ADR-0016; the `vercel.live` `frame-src` wording aligned above; README's e2e section now says the server is yours to start. **Left to TASK-050 by design:** the consent bootstrap's `'sha256-'` hash and the GA4 origin land in the same PR as the script they authorise (`cspHeader`'s `inlineHashes` slot is the documented seam, empty today). |
| TASK-047 | Phase-0 data registries as config with zod and projections: `src/config/countries.ts` (7 destinations, per-locale slug, name key, `corridorPagePublished`, `guidePublished`, `status`), `src/config/site-links.ts` (every header/footer/category target with owning spec and `published`), `src/config/categories.ts` (the canvas category row), `src/config/company.ts` (`registered: false` colophon identity), `CountryConfigSchema` / `SiteLinkSchema` / `CategoryConfigSchema` / `CompanySchema`, `toCountryRow()`, `isPublished()`, and the `nav` / `footer` / `company` / `destinations` message namespaces | `specs/004` · no AC of its own; explicit scope — §2 "Everything data-gated is config", §5.1's four-row projection table, §7's `nav`/`footer`/`company`/`destinations` namespaces, §12's two config-file flags (AC-9's `CompanySchema` refinement is verified by TASK-049, AC-11's data-flip proof by TASK-052, AC-14's published-only rule by TASK-054) | 0 | done | backend-implementer | [#29](https://github.com/itsahmeds/flowers-overseas/pull/29) | TASK-045 | Branch `task/TASK-047-config-registries`. Spec §12 task 3 split: the data layer lands before the two chrome tasks so header and footer are pure consumers and can be implemented in parallel. Spec §13's 2026-09-08 resolution note is binding and overrides §5 defaults where they differ. Follows spec 003's precedent exactly (`src/config/locales.ts`): zod-parsed at module load, `pnpm check:no-db` clean, one predicate per flag, `toCountryRow()` pinned by a unit test to spec 002 §5.1's `country` columns that exist in Phase 0 (`iso2`, `status`, `guide_published`) so TASK-015/TASK-026 seed from the projection and the two cannot drift. Destinations per §13 Q12: PL (`status: 'live'`, delivering now) plus DE, FR, ES, IT, RO, NL (`guide · waiting list`) — the canvas's destinations grid renders exactly these seven with those two states. `categories.ts` carries the canvas category row verbatim (Best sellers · Birthday · Sympathy · Occasions · Bouquets · Roses · Plants · Add-ons · Same-day delivery · Destinations) **each with `published: false` and the owning spec (005/008/009/011)**, so in Phase 0 the row renders as non-link text per the design's own rule and becomes navigation when 008 flips the flags — no template edit (`plan/09`'s "a new country is data" applied to the nav). `company.ts` holds the colophon copy the canvas shows as `[COMPANY LEGAL NAME], [REGISTERED ADDRESS], [REGISTRATION NO]`: `registered: false`, trading name + contact channel only, registry/VAT/address optional and refined so `registered: true` requires all three (the OÜ does not exist until `plan/09` 1 Nov; inventing a number is a `plan/07` §6 offence). `docs/architecture.md` §2 gains the four config files in this PR. Design source of truth (match pixel-for-pixel): `docs/design/homepage-v1/README.md`, `docs/design/homepage-v1/tokens.css`, `docs/design/homepage-v1/homepage-desktop.dc.html`, `docs/design/homepage-v1/homepage-mobile.dc.html`, `docs/design/homepage-v1/identity.dc.html`, `content/brand/mark.svg`. Tests: T-03, plus the `toCountryRow` column pin and a `CompanySchema` refinement fixture; T-31 keys land here and are gated by TASK-056. |
| TASK-048 | `SiteHeader` — the approved commerce anatomy: utility strip (cutoff, price-includes, guarantee), masthead with the `mark.svg` logo lockup at the inline start, wide search field with a dark Search button, labelled `Sign in` / `My orders` / `Basket (0)`, `EN · EUR` locale-switcher + currency chip, and the full category row from `categories.ts`; sticky, reserved-height, zero-CLS | `specs/004` · AC-7, AC-8 | 0 | done | frontend-implementer | [#31](https://github.com/itsahmeds/flowers-overseas/pull/31) | TASK-045, TASK-047 | Branch `task/TASK-048-site-header-commerce`. Spec §13's 2026-09-08 resolution note is binding and overrides §5 defaults where they differ. The §5.3 "56 px mobile, wordmark + switcher + currency chip" header is **superseded** by the canvas's commerce header; AC-7's testable content becomes: reserved fixed height per breakpoint (mobile masthead + scrollable category row, desktop utility strip + masthead + category row), header box byte-identical pre/post hydration, CLS ≤0.05, logo → locale home, `LocaleSwitcher` markup and the `src/modules/i18n` barrel unchanged (spec 003 AC-3 still green). Record the height numbers taken from the two artboards in the PR body — they are what the visual baselines pin. Design source of truth (match pixel-for-pixel): `docs/design/homepage-v1/README.md`, `docs/design/homepage-v1/tokens.css`, `docs/design/homepage-v1/homepage-desktop.dc.html`, `docs/design/homepage-v1/homepage-mobile.dc.html`, `docs/design/homepage-v1/identity.dc.html`, `content/brand/mark.svg`. **The search field is UI only in 004 — there is no search backend; spec 008 wires it.** Ship it as a real `<form>` whose labelled input and Search button are present, accessible and keyboard-operable, `action` pointing at an unpublished `site-links.ts` target and therefore rendered **disabled-free but inert** (no `action` to a non-200 URL — AC-14 forbids it): concretely, the form has no `action`/`method` and no client handler in Phase 0, with `aria-describedby` copy from `nav.search.help` saying search opens with the shop; the reviewer checks that submitting it neither navigates nor 404s. `Sign in` / `My orders` / `Basket (0)` are likewise labelled text-with-icon items whose targets are unpublished (010/013/014 own them) and so render as text, not links. Currency chip stays server-rendered text (`en`→EUR, `en-gb`→GBP, `de`→EUR, `pl`→PLN) with a localised `aria-label`, byte-identical with and without an `fo_currency` cookie, zero `Set-Cookie` (AC-8); the currency **menu** is spec 008's. Category row wraps rather than truncates (German compounds, `en-XA`); tap targets ≥44 px; no hover-only affordance. `Mark` component from TASK-045's icon set — never an inline `<svg>` at the call site. Photography is a placeholder until the founder supplies imagery: every `.photo` box in the canvas renders the `--color-photo` token gradient with its uppercase caption and **no `<img>`** (`plan/10` §3 honesty rule); the consolidated "photo slots" list is part of TASK-053's PR body. Tests: T-09, T-10, and the header states added to `/dev/components`. Search placeholder copy (founder): "Search flowers, occasions, a city or a country" — the search key in `nav.*` must say so in all locales; spec 008 wires the backend to match products, occasions, cities and countries. **Amended by `/review 31` (spec §14 A4, 2026-09-09):** the search band renders as the artboards literally draw it — placeholder text plus a `Search` label, no `<input>`/`<button>` — until spec 008 publishes search (the "labelled input, keyboard-operable, disabled-free but inert" clause is withdrawn as unsatisfiable at zero JS); the `LocaleSwitcher` and the currency chip live in the utility strip, not the category row; every rendered link meets 44 px. **Implemented on `task/TASK-048-site-header-commerce`, PR #31, `in_review` 2026-09-09; `/review 31` round 1 FAIL addressed in the same PR (spec §14 A4).** AC-7/AC-8 proven by `tests/e2e/header.spec.ts` (T-09, T-10) and `tests/unit/ui-site-header.test.tsx`; AC-14's header half proven in both directions, the published one through a mocked registry so a `published: true` flip is shown to produce `localePath(...)` with **no template edit**. **Measurements after the fix round:** client JS unchanged at **129 638 B Brotli on `/en` and `/de`** (budget 131 072 B) because the header ships **zero** client JavaScript; **reserved height 245 px mobile / 183 px desktop**, identical at 390 px and 412 px and at 1280 px and 1440 px in `en`, `en-gb`, `de`, `pl` and `ar-XB` (`en-XA` grows the desktop row to 263 px, the specified wrap); the currency chip at 390 px sits at x 327–371, y 75–105, inside the header's visible box; every rendered link measures 44 px tall at both artboard widths and the WhatsApp icon-link 44 × 44. **The three §14 A4 changes:** the search band is the text the artboards draw (no `<form>`, `<input>` or `<button>`, no focus stop, `nav.search.help` as the screen-reader sentence); the `LocaleSwitcher` and the chip moved to the utility strip on every breakpoint, which restored the canvas's single 52 px category row; every link is padded to 44 px from the header's wrapper, so `src/modules/i18n` is still untouched. **Two consequences the orchestrator should note:** the utility strip is 44 px rather than the artboards' 34 (it now carries links) and runs to three lines at 390 px (113 px), because spec 003's switcher is four language names — the canvas's compact `EN · EUR` control, which spec 008 owns with the currency menu, is what brings both numbers back down; and from `md` to `xl` the strip prints the short cutoff line and the help channel only, the two standing claims joining at `2xl`, because four claims plus the two controls need 1 375 px of the 1 328 px the 1440 px artboard has (`flex-wrap` keeps a longer translation wrapping rather than overflowing). Also: `mobileOrder` added to `src/config/categories.ts` so the mobile artboard's own row order is data and one `order-*` utility rather than a second list; Tailwind's arbitrary `min-[…]:` variant compiled the whole stylesheet down to its base layer with no error raised (reproduced twice on a clean `.next`), so named breakpoints only — pinned by an assertion. **Gates, all local (no CI on a draft):** lint, typecheck, **1 827** unit (one pre-existing failure on `origin/main`, see below), format, env, no-db, layout, i18n, build, budget (byte-identical), **268** e2e, **12** visual, **28** a11y — all green against `pnpm start` on :3600. **Carry-forward:** the four header baselines and the three shared ones (`en`, `de`, `ar-XB`) exist for **`darwin/` only**; the `linux/` half must come from the `visual` job's artifact on the next full CI run (the TASK-045 procedure). **Rebased on `origin/main` `b08e0ec`** (PR #30's footer and TASK-059 round 2 merged): conflicts were `layout.tsx` (both mounts kept), `playwright.config.ts` (main's glob form kept), the gallery page and its e2e (both sections kept, `SECTIONS` reordered so the footer follows the header and the contrast manifest is last), `TASKS.md`, and the three shared darwin baselines — `en`, `de`, `ar-XB` regenerated, plus `footer-en-mobile`/`footer-de-mobile`, which now show the sticky header overlaying the top of the colophon in an element screenshot (the footer's own markup is untouched). `tests/e2e/banner.spec.ts`'s five `main nav` selectors were re-scoped to `[data-fo-header-switcher] nav`, because the switcher left `<main>` for the header. **One pre-existing failure on `origin/main`, not this PR's:** `tests/unit/company-config.test.ts` asserts `company.description` against the desktop artboard, and TASK-059 round 2 rewrote the artboard to the first-person voice of §14 A5 while the catalogue still says "International flower relay…" — TASK-084 owns that copy. Reproduced with `origin/main`'s `messages/en.json` in place of this branch's. `pnpm lighthouse` not run: it needs a deployed preview and stays informational until TASK-056. **Round 2 addendum (§14 A4 addendum) — RESOLVED, implemented as mechanism (iii).** The orchestrator's final ruling (spec §14 "A4 addendum", now in the spec at `06828d0`) is (iii): `SiteHeader` returns a fragment — the utility strip first as a plain `<div data-fo-utility>` that scrolls with the page, then `<header role="banner" data-fo-header>` (masthead + category row) with `position: sticky; top: 0`. The strip is **not** a landmark (no `role="region"`, no new label key — "same copy" holds); its `tel:`/`wa.me` links, the `LocaleSwitcher` (still `nav[aria-label="Change language"]`) and the currency chip are unchanged, just outside the banner. Measured on the served build: sticky `[data-fo-header]` **132 px at 390 px / 138 px at 1440 px**, `[data-fo-utility]` 113 / 45, so the chrome AC-7 reserves is unchanged as the sum — **245 / 183** — with CLS still 0 and the JS-off boxes still identical to the hydrated ones. Chip at 390 px: x 327→371, y 75→105, inside `[data-fo-utility]`'s box and the viewport, neither box scrolling horizontally. Re-scoped over both elements: the 44 px-link, `href`-200, no-form-control and chip-visibility e2e assertions, the axe `include()`, the unit band slicing, and the four `header-*` baselines, which became eight (`header-{desktop,de,mobile,rtl}-{utility,banner}.png`). One stated deviation from the ruling's test plan: the scroll assertion runs on `/en` at 390×300 (scroll 600) and 1440×300 (scroll 235, the page's maximum) rather than on `/dev/components` — the gallery renders a **demo copy** of the header inside `<main>` in a bordered wrapper whose containing block is the header's own box, so that instance scrolls with the page by construction (measured: top 7 277 px after a 600 px scroll) and cannot show stickiness. Both widths assert masthead top 0 and the whole strip above the viewport. Zero client JS unchanged and byte-identical: 129 638 B Brotli on `/en` and `/de`. Earlier round-2 escalation, kept for the record: The addendum asks for `position: sticky` on an inner wrapper holding the masthead + category row with the outer `<header role="banner">` (all three bands, `[data-fo-header]`) left static. A sticky box is bounded by its parent's padding box, and the masthead + category row sit at the bottom of the header, so the inner wrapper has zero px of room: measured on the served DOM at 390 px it ends up at top **−25** (masthead off the viewport), not 0. Two structures do produce the addendum's numbers (sticky 132 mobile / 138 desktop, chrome total unchanged at 245 / 183), and each breaks one sentence of it. **(ii)** negative sticky `top` on the outer header — keeps the landmark, the `[data-fo-header]` scope and every AC-7 number, but needs the strip height as a per-breakpoint literal while the strip's height is content-driven (113 px <640 · 93 px 640–767 · 85 px 768–~890 · 65 px ~900–1279 · 45 px 1280–1535 · 65 px 1536–~1900 · 45 px at 1920; two boundaries are not Tailwind breakpoints and arbitrary `min-[…]:` variants are banned here), so any mapping leaves a 20 px strip sliver pinned at 900–1023 px or clips 20 px of the masthead at ≥~1900 px. **(iii)** strip and a sticky `<header>` as body-level siblings (one fragment, same DOM order) — exact at every width and locale, but the `banner` landmark stops containing the strip (needs a ruling: leave it outside a landmark, or add `role="region"` + a new `nav.utility.label` key), `[data-fo-header]` scopes the sticky part only (132 / 138, total still 245 / 183 over two elements), and the 44 px-link, chip-visibility, `href`, axe-scope, four `header-*` baselines and unit band-slicing assertions all re-scope. Also measured: the requested "scroll 600 px" e2e cannot run on `/en` — the Phase-0 homepage scrolls 138 px at 390 × 844 and 0 px at 1440 × 900, so it needs a short viewport or `/dev/components`. Question and evidence in PR #31 (`## Round 2 addendum`). |
| TASK-049 | `SiteFooter` — the dense colophon: four link columns (Sending · Destinations · Occasions · Delivery times · The guarantee \| Company · How it works · For florists · Help and contact · Imprint) from `site-links.ts`, the company identity line from `company.ts`, the payment colophon (market methods + "card payments processed by Stripe"), the legal row (Terms · Privacy · Cookies · Withdrawal and refunds), the language list, `TrustMarks` empty state and the "Cookie settings" control | `specs/004` · AC-9 | 0 | done | frontend-implementer | [#30](https://github.com/itsahmeds/flowers-overseas/pull/30) | TASK-045, TASK-047 | Branch `task/TASK-049-site-footer-colophon`. Spec §13's 2026-09-08 resolution note is binding and overrides §5 defaults where they differ. Design source of truth (match pixel-for-pixel): `docs/design/homepage-v1/README.md`, `docs/design/homepage-v1/tokens.css`, `docs/design/homepage-v1/homepage-desktop.dc.html`, `docs/design/homepage-v1/homepage-mobile.dc.html`, `docs/design/homepage-v1/identity.dc.html`, `content/brand/mark.svg`. Footer anatomy is the canvas's Ft4 dense colophon, not §5.3's minimal list. Every link comes from `site-links.ts` and **unpublished targets are not rendered at all** — never a dead link, never a disabled-looking one (AC-14, verified by TASK-054), so in Phase 0 the columns render their headings and only the entries 004 itself publishes; record in the PR body which of the canvas's link labels are present as text vs absent, because that list is what 007/008/011 flip. Company block renders the trading name and contact channel and **no** registry code, VAT id or address while `company.registered` is false (AC-9) — the canvas's `[COMPANY LEGAL NAME], [REGISTERED ADDRESS], [REGISTRATION NO]` placeholders must not ship as literal brackets; render the sentence without the missing clauses. **Payment colophon is a compliance trap:** the canvas lists BLIK · Klarna · PayPal · Visa · Mastercard · Apple Pay and "Card payments processed by Stripe". No payment method is live and the marks are third-party trademarks (§2, §8), so Phase 0 renders **no logo image**; ship the methods as a `payment-methods` entry in `site-links.ts`-style config with `available: false` and render only the Stripe processing sentence plus the localised "payment methods shown at checkout" line — the founder must not be able to read a live-method claim off the demo. Escalate if the design intent is read as requiring the method names in Phase 0; the honest form is the reviewer's call recorded in the PR. `TrustMarks` empty state renders nothing visible and reserves no box (no hole in the footer); "Cookie settings" ships here as the control and is **wired** to re-open the banner by TASK-051 (AC-9's re-open clause is verified after that task — note it in the PR body). Language list reuses spec 003's `LocaleSwitcher` links; no country flags (`plan/03` §2). Tests: T-11, and the five footer states in `/dev/components`. Design round 6: footer gains an occasion-reminder signup (UI + `POST /api/reminders` stub, double opt-in copy, provider seam, no email sent in 004); utility strip gains the help line — phone and WhatsApp +1 (213) 592-5150, Mon–Sat 8–20 CET — from `company.ts`. **Linux visual baselines — orchestrator ruling (`/review 30`, 2026-09-09):** `linux/` baselines (`en`, `de`, `ar-XB`, `footer-*`) are refreshed once from a single `ci:full` visual-job artifact after TASK-048, TASK-049 and TASK-052 have merged — owned by TASK-053's PR (orchestrator adds the label). No task produces them alone. |
| TASK-050 | Consent plumbing and the GA4 seam: `src/config/cookies.ts` cookie register, `ConsentDecisionSchema`, `ConsentSink` + `logConsentSink`, `POST /api/consent`, the ≤1 KB inline Consent-Mode-v2 default-denied bootstrap with its CSP hash, the env-gated GA4 loader (`NEXT_PUBLIC_GA4_MEASUREMENT_ID`), and the RoPA rows | `specs/004` · AC-18, AC-21, AC-22 | 0 | done | backend-implementer | [#28](https://github.com/itsahmeds/flowers-overseas/pull/28) | TASK-046 | Branch `task/TASK-050-consent-plumbing-ga4-seam`. Spec §12 task 4 split: the server/config/loader half is backend and depends on TASK-046 because the inline bootstrap's `'sha256-'` hash is added to the CSP `script-src` in the **same** PR as the script it authorises (a bootstrap that CSP would report is a false negative in the Report-Only evidence). Spec §13's 2026-09-08 resolution note is binding and overrides §5 defaults where they differ. Register (`essential \| analytics \| marketing`) carries name, category, purpose message key, lifetime, first/third party and owning spec for `fo_locale` (essential, 365 d, spec 003), `fo_consent` (essential, 6 months reject / 12 months accept, this spec) and the declared-but-unset `fo_session`, `fo_csrf`, `fo_currency`, `fo_basket`, GA4 and Stripe rows; it feeds the settings panel, 007's cookie-policy page, the RoPA and AC-22's "no cookie outside the register" test. Reconcile with the existing `docs/compliance/cookie-register.md` (spec 003 TASK-043) so the prose file and the machine register cannot disagree — one of them must import/derive from the other, decided in the PR body. Bootstrap sets `ad_storage`, `ad_user_data`, `ad_personalization`, `analytics_storage`, `functionality_storage`, `personalization_storage` to `denied` (`security_storage` granted, honestly), `url_passthrough: false`, `wait_for_update`; it is the only inline script in the app. GA4 loader: id **unset** (CI, local, preview) → no tag, no `googletagmanager.com` request at all; id set → tag after the default block, cookieless pings under default-denied, `gtag('consent','update')` on accept (wired by TASK-051). `POST /api/consent` is `no-store`, `noindex`, inside the `/api/` robots disallow, and logs `cid` + timestamp + categories + policy version and **no** IP, user agent, `Referer` or page URL (§8). `docs/compliance/ropa.md` gains the consent-record row (basis: legal obligation, GDPR Art. 7(1), 5-year retention) and the Google Analytics processor row marked **inactive-until-configured**, with the runbook note that setting the env var is a RoPA-affecting act. Tests: T-20, T-23, T-24. **Delivered (PR #28, `/review` pending).** `src/config/cookies.ts`: eleven register rows — `fo_locale`, `fo_locale_suggestion_dismissed`, `fo_consent` set; `fo_session`, `fo_csrf`, `fo_currency`, `fo_basket`, `__stripe_mid`, `__stripe_sid`, `_ga`, `_ga_*` declared-but-unset — each with category, consent flag, party, writer, lifetime, `consent.cookies.<id>.purpose` key, attributes and owning spec, zod-parsed at module load and added to `scripts/check-layout.ts`'s `CONFIG_FILES` + `docs/architecture.md` §2. **Register/prose reconciliation, decided:** the machine register is the source of truth and `docs/compliance/cookie-register.md` **derives** its normative table from it inside a generated block (`scripts/cookie-register.ts`, `pnpm cookies:check [--write]`); the narrative stays human and a test fails both on a stale block and on a registered name missing from the prose. `src/lib/consent.ts`: `ConsentDecisionSchema` (strict, uuid `cid`, int `policyVersion`, two booleans, ISO `decidedAt`), `ConsentSink` + `logConsentSink`, and the whole `POST /api/consent` contract — 405 (+`Allow`) / 415 / 413 / 400 / 204 / 500, `no-store`, `noindex`, and **only `content-type` and `content-length` read**, proven by a spy on `Headers.get`, so no IP, user agent, `Referer`, cookie or page URL can reach a log line. `src/lib/consent-bootstrap.ts` + `src/modules/analytics` (`ga4.ts`, `ui/AnalyticsScripts.tsx`, barrel; the module row is now co-owned 004/023): the **527 B** inline Consent Mode v2 default-denied block, hash `sha256-wErp4Fj1fl4URVvrQ7k3Y8t42QJJaK86nCoSBbX5Xjo=` in `script-src` in the same PR, computed in `next.config.ts` from the same constant — the measurement id rides on a `data-fo-ga4` attribute so the hashed bytes never vary — plus the env-gated tag (`defer`, not `async`, because React hoists `async` scripts into `<head>` where they could outrun the default block). Id unset ⇒ no tag element, no `googletagmanager.com` request, no analytics origin in the policy; measured with the id set on a cold build, the tag follows the default block and the policy gains `www.googletagmanager.com` (script/connect) and `*.google-analytics.com` (connect). Mounted in `src/app/[locale]/layout.tsx`'s `<head>`; **`/` deliberately excluded** to keep spec 003 AC-7's zero-script chooser (disclosed as deviation 4). RoPA rows 3 (consent records: legal obligation, Art. 6(1)(c)+7(1), 5 years, Phase 0 log-retention gap stated) and 4 (GA4, inactive-until-configured), plus new `docs/runbooks/analytics-consent.md` recording that setting the env var is a RoPA-affecting act. **Client JS: 0 B added** — `/` 126.2 KB br, `/en` and `/de` 126.6 KB br, byte-identical to TASK-046's numbers, so §14 A1's 1 434 B of headroom is intact for TASK-051. §14 A1's 131 072 B carried into `scripts/client-js-budget.ts` and `lighthouserc.json` (a test pins them to each other); `lighthouse` stays `continue-on-error` and TASK-056 still owns the flip. Tests: 62 new unit + 26 e2e (T-20, T-23, T-24, the served-CSP hash equality and the route contract); `POST /api/consent`'s half of AC-19 is covered here, the cookie lifetimes and `gtag('consent','update')` are TASK-051's, and T-24's post-accept half needs the banner. Deviation: §5.2's `src/modules/ui/consent/` home is TASK-045's to create, so the contract lives in `src/lib` (the `csp-report.ts` precedent) and the scripts in the registered `analytics` module — TASK-051's islands can still land under `src/modules/ui/consent/` and import both with no call-site change. **`/review 28` round 1 FAIL, addressed:** (1) the cookie-register prose guard searched the whole file, generated block included, so it could never fail — it now slices the block out and mutation-checked red on a register row with no narrative; (2) "the only place in `src/` that emits an inline script" read the one file it hoped was the only one — it now walks `src/` and requires exactly one `dangerouslySetInnerHTML`, mutation-checked red on a second one. Nits: the `scripts/client-js-budget.ts` header and `README.md` both say 128 KB / 131 072 B (§14 A1); `POST /api/consent` now reads the body through a bounded stream read (`readBoundedBody`) so a chunked 200 KB body is cut off at the cap instead of being buffered for zod to refuse, with its own test; the `purposeKey`-resolves-in-the-catalogue assertion is carried to TASK-051's row, because the `consent.*` namespace is that task's and `i18n:check` refuses unused keys. **Orchestrator rulings recorded (spec §14 amendments are the orchestrator's):** §14 A2 — CSP stays Report-Only until a task adds nonce propagation or accepts hash-per-response, the enforce flip leaves TASK-056's scope and becomes a new task; this PR only corrects the `src/lib/csp.ts` comment to "exactly one APPLICATION inline script; Next's flight blocks are unauthorised under this policy and are why the header stays Report-Only (spec 004 §14 A2)". The GA4 tag's ~30–35 KB against §14 A1's 1 434 B headroom is TASK-056's, with the consequence now stated in `docs/runbooks/analytics-consent.md`. The Stripe rows' `essential` classification carries a "confirm in spec 013" note in the register row comment. |
| TASK-051 | `ConsentBanner` and `ConsentSettings` islands: bottom sheet on mobile / inline card on desktop, three equal-weight controls, `fo_consent` read/write, `Esc` closes without deciding, per-category settings panel from the register, the footer "Cookie settings" re-open wiring, `consent.*` messages | `specs/004` · AC-17, AC-19, AC-20 · plus the AC-9 re-open clause wired here (owned by TASK-049) | 0 | done | frontend-implementer | [#36](https://github.com/itsahmeds/flowers-overseas/pull/36) | TASK-045, TASK-049, TASK-050 | Branch `task/TASK-051-consent-banner-islands`. Spec §13's 2026-09-08 resolution note is binding and overrides §5 defaults where they differ. Design source of truth (match pixel-for-pixel): `docs/design/homepage-v1/README.md`, `docs/design/homepage-v1/tokens.css`, `docs/design/homepage-v1/homepage-desktop.dc.html`, `docs/design/homepage-v1/homepage-mobile.dc.html`, `docs/design/homepage-v1/identity.dc.html`, `content/brand/mark.svg`. (the canvas has no consent sheet drawn — style it from the tokens as a `--color-paper` sheet with `--rule` top border, `.label` heading and the canvas's neutral button treatment; the accent green is reserved for the finder's `Continue`, so the three consent controls are ink-on-paper with identical weight). Non-modal, focus not stolen, overlays rather than reflows (CLS delta 0), `aria-live="polite"`, `Esc` records nothing and the banner returns next page view (dismissal is not consent). Accept / Reject / Settings must be **equal in rendered width class, font size, weight and contrast** — AC-20 is asserted from computed styles, not from a screenshot, so write the assertion first and let it drive the markup; no pre-ticked non-essential category; the settings panel lists every register entry in the category with purpose and lifetime, essential locked-on with the reason. `fo_consent`: first-party, `Path=/`, `SameSite=Lax`, `Secure` outside dev, not `HttpOnly`, JSON `{v, a, m, ts, cid}`, 6 months reject / 12 months accept, written **only** on an explicit user action and never by the server on a cached response (spec 001 AC-15, spec 003 AC-12 both stay green). A forged or unparsable value is treated as no consent and rewritten — carry spec 003 `/review 23`'s ruling forward: the cookie reader must not throw on malformed percent-escapes and the island fails closed. Settings panel dynamically imported on open so the default path pays for the banner only. `z-index` from `--layer-banner`/`--layer-overlay` so the sheet paints **above** the spec 003 suggestion banner (AC-13, verified by TASK-055). Tests: T-19, T-21, T-22, and the four banner + three settings states in `/dev/components`. **Carried from `/review 28` item 8:** add the assertion that **every `purposeKey` in `src/config/cookies.ts` resolves in the message catalogue** — it cannot be written in TASK-050, because the `consent.*` namespace is this task's to create and `pnpm i18n:check` refuses unused keys. **Delivered (PR #36, `/review` pending).** `src/modules/ui/consent/`: `ConsentBanner` (a synchronous Server Component that resolves every string and projects the cookie register), `ConsentBannerLoader` (`next/dynamic`, `ssr: false`), `ConsentBannerIsland`, `ConsentBannerView` (+ `ConsentSavedView`), `ConsentSettingsPanel` in its own chunk imported on open, `ConsentGallery` for the seven §5.3 states, plus the zod-free `consentCookie.ts` and the types-only `consentTypes.ts`. Mounted once per **localised** document by `src/app/[locale]/layout.tsx`, before the suggestion banner and above it in the z-scale; `/` has no locale layout and therefore no sheet, so spec 003 AC-7's zero-application-JS chooser is intact. **Q13 option (b) applied to these islands:** copy, categories, lifetimes, the policy version, the endpoint and the footer's `CONSENT_REOPEN_ATTRIBUTE` all cross as props, so no message catalogue, no register and no zod reaches the browser; `consent.*` is deliberately **not** in `namespacesFor("localeDocument")`. `fo_consent` written only on a press (`Path=/`, `SameSite=Lax`, `Secure` off `localhost`, not `HttpOnly`, JSON `{v,a,m,ts,cid}`, 365 d for any acceptance / 183 d for a refusal, both read from the register); a forged, truncated, mis-shaped or stale-version value is deleted and the sheet asks again; the record is POSTed with `credentials: "omit"` and `referrerPolicy: "no-referrer"`, so no cookie and no page URL leave with it. `gtag('consent','update')` on every decision, granting exactly what was given. Tests: 67 unit, 84 e2e (42 × desktop and mobile, four locales), 6 a11y, 4 visual with **8 new `darwin/` baselines** (`consent-{shown,settings}-{en,de}-{mobile,desktop}.png`; `linux/` stays TASK-053's one-off refresh per `/review 30`). The `/review 28` item-8 assertion is in `tests/unit/consent-view.test.ts`: every register `purposeKey` resolves in all four locales through a translator whose fallback throws, and no `consent.cookies.*` branch exists without a register row. **ESCALATION — §14 A1's client-JS budget cannot be met by this task** (accepted as informational by the spec §14 A1 addendum; option (b) is TASK-085's). Re-measured on a cold build after the rebase onto main, with the TASK-048 header in the tree: `/` 116 426 B in the browser (14 646 B under; `budget:client-js`'s pessimistic total is 131 667 B because it charges both island chunk groups plus the message chunk to the chooser's entry), `/en` and `/de` **132 044 B** browser and script alike, i.e. **972 B (0.74 %) over 131 072 B**. The sheet costs **2 363 B** Brotli in one lazy chunk (+967 B nested chunk fetched only when the panel is opened, which the script's route-manifest read does not count — disclosed in the PR); 97.3 % of the budget is the framework floor before this task's first line. Nothing was raised: `Button` was not imported into the island (−900 B, replaced by one shared control constant pinned to `Button`'s skin by a unit test), and `ConsentGallery` was kept **off** the `ui` barrel because a `"use client"` module reachable from the barrel lands in every route's document JS (−1 990 B on `/` and on each locale document). §14 A1's named remedy — Q13 option (b) for the *suggestion* banner, dropping `NextIntlClientProvider` and the client message payload from the locale document (−10 705 B) — is **TASK-085's** since the addendum of 2026-09-09, and TASK-053's finder island plus the ~30–35 KB GA4 tag are the other two consumers of it. `budget:client-js` is still `continue-on-error` in CI. Row left `in_review` rather than `blocked` because the task is complete and every other gate is green; the orchestrator may flip it if it wants the budget resolved first. **Rulings recorded in the PR (reviewer to confirm):** (1) the flow artboard's dimmed, focus-trapped sheet is superseded by §5.3/AC-17/§8's non-modal, focus-preserving, blocks-nothing sheet — the artboard's own note says the consent sheet is "not yet specified"; (2) `Esc` steps back one layer (panel → sheet → gone), recording nothing, because closing everything at once left focus on a detached node; (3) the sheet covers the suggestion banner while a decision is pending (the specified z-order and the artboard's sequence), so four TASK-041 e2e tests and the footer/shell/pseudo-rtl visual specs now seed a recorded refusal first — spec 003 AC-28's matrix is otherwise unchanged and green, and offsetting the banner clear of the sheet stays **TASK-055's** (AC-13); (4) `/dev/components` gains the two consent checkboxes, which §3 permits in the same sentence that forbids a form layer, so its "no form control" test now counts controls outside the footer *and* outside the consent panel and asserts the consent controls exactly; (5) the sheet caps itself at `85dvh` and scrolls internally, because eleven open rows are taller than a 390×844 phone and a bottom-anchored `fixed` sheet cannot be scrolled back (WCAG 1.4.10); (6) the `marketing` category is disclosed with an honest "nothing in this group yet" note rather than hidden, so the first marketing cookie is not consented to on a panel that never named it. **Round 2 (`/review 36`):** rebased onto `origin/main` (six conflicts resolved so the locale layout keeps the header, the footer and both banner mounts, `/dev/components` keeps every section, and the message metadata keeps both key sets); each category toggle is now a label-wrapped, full-width row measuring **358 × 44 px at 390 px** (was 13 × 18) with the browser assertion in `tests/e2e/consent-banner.spec.ts` and the markup pinned in `tests/unit/consent-islands.test.tsx`; an empty category with no note renders no element at all; the stale "one `Button` component" paragraph in `ConsentBannerView` corrected; the sheet's byte figure is one number (2 363 B) in the row and the runbook; the four `consent-settings-*` `darwin/` baselines regenerated (the four `consent-shown-*` are element-clipped and unchanged). **Correction (round 2, `/review 36` item 2):** the earlier claim that HSTS blocks a local Lighthouse run was false. `next start` sends **no** `Strict-Transport-Security` header — `sendsHsts` is production-only — and `pnpm lighthouse` completes locally. The real limitation is that `next start` serves chunks with no `Content-Encoding`, so Lighthouse's script-size audit reads identity bytes (169 223 B on `/en`) which cannot be compared to a Brotli budget; `pnpm budget:client-js` is the local equivalent and its numbers are above. That is why the CI job measures a deployed preview. Docs: `docs/runbooks/analytics-consent.md` §4a. No new dependency; RoPA unchanged (the consent-record row is TASK-050's and this task only sends to it). |
| TASK-052 | Locale-home part 1 — full-bleed hero, the finder card and the four-fact proof row: reserved full-bleed photo slot with the token gradient and its caption, `H1` (text LCP) + proposition, the finder card (type-ahead country field with per-country status → town/city/postcode → delivery date → neutral `Continue` + cutoff line), the four-fact proof row, and the `Media` wrapper (`slots.ts`, `placeholderLoader`) | `specs/004` · AC-11 · plus §2's image conventions and the ADR-0015 R2 loader seam (no AC id; explicit here) | 0 | todo | frontend-implementer | — | TASK-045, TASK-048, TASK-049 | Branch `task/TASK-052-home-hero-finder`. Spec §12 task 5 split into four (hero+finder, sections, gated rows, styling pass) because the approved design is several times the §5 "home skeleton" and one PR of it would exceed a day. Spec §13's 2026-09-08 resolution note is binding and overrides §5 defaults where they differ. Design source of truth (match pixel-for-pixel): `docs/design/homepage-v1/README.md`, `docs/design/homepage-v1/tokens.css`, `docs/design/homepage-v1/homepage-desktop.dc.html`, `docs/design/homepage-v1/homepage-mobile.dc.html`, `docs/design/homepage-v1/identity.dc.html`, `content/brand/mark.svg`. — hero is the canvas's 820 px full-bleed photographic band with the finder card overlaid at the inline start on desktop and stacked below the headline on mobile. Photography is a placeholder until the founder supplies imagery: every `.photo` box in the canvas renders the `--color-photo` token gradient with its uppercase caption and **no `<img>`** (`plan/10` §3 honesty rule); the consolidated "photo slots" list is part of TASK-053's PR body. The hero slot renders the gradient + "Photography to supply · a Warsaw florist finishing a hand-tied bouquet, morning light, full bleed" caption and **no `<img>`**, at a fixed aspect ratio, so 006's imagery changes the LCP element from text to image with no layout shift and no template edit. **AC-11 is the finder's country field**: the seven `countries.ts` destinations in `collator(locale)` order (`pl` sorts `ł` correctly), fully usable with JavaScript disabled — a real `<select>`/`<datalist>`-backed labelled control that renders every destination as text with its status line (`Delivering now` \| `Not yet` + the waiting-list sentence) while `corridorPagePublished` is false, and renders a link/option that resolves to `localePath(locale, "corridorCountry", slug)` when a fixture flips one flag — with **no change under `src/app/`**. Type-ahead is the progressive enhancement on top and must never be the only way to choose (the combobox proper is 007/008's). **What `Continue` does in 004, stated so nobody invents a route:** it is a submit control on a server-rendered form that targets the destinations route the founder's design implies — `/{locale}/{send-flowers-to}/{country}` (the localised `corridorCountry` segment from `src/config/locales.ts`) — and because **spec 007/008 own that route and no country is published in Phase 0**, the button in Phase 0 leads to the published guide/waiting-list surface instead: for PL, the locale home's destinations section anchor; for the six guide countries, the same, with the onboarding line. Concretely: no `action` to a non-200 URL, no client navigation to a route that does not exist, and the target resolution lives in one helper (`finderTarget(locale, iso)`) whose Phase-0 branch returns the on-page anchor and whose published branch returns the corridor path — the branch is unit-tested both ways so 007 flips data, not code. Town/postcode is optional (price depends on the country); the date field defaults to the soonest possible date and the cutoff sentence sits under the button, both through spec 003's `format.ts` (`fo/no-adhoc-intl` must stay clean — no `Intl` call here, and no date **arithmetic**: occasion/cutoff computation is spec 009's, so Phase 0 shows the canvas's static "order by 14:00 Warsaw time" copy as a message key, not a computed cutoff). Four-fact proof row is the canvas's four claims verbatim (7-day freshness guarantee / Made by a florist in their town / The price you see is final / Photo on delivery) — the `Photo on delivery` fact is a promise about the service, not a claim that a photo is shown, and it must not render a photo. Tests: T-13, and the finder + hero states in `/dev/components`. Design round 7: the country field is a plain type-ahead — as the user types, the matching country name appears beneath the field; no status column, no footnote, no pills. Status (live vs guide) is communicated after selection, in the button line and on the destinations grid. |
| TASK-053 | Locale-home part 2 — occasion tiles, the relay explainer and the trust strip: "Shop by occasion" tile grid from config, the three-step "How a relay works" section with its guarantee link, `TrustStrip` (three true claims), `home.*` and `trust.*` messages, and the honesty gates | `specs/004` · AC-10, AC-15, AC-16 | 0 | todo | frontend-implementer | — | TASK-052 | Branch `task/TASK-053-home-occasions-relay-trust`. Spec §13's 2026-09-08 resolution note is binding and overrides §5 defaults where they differ. Design source of truth (match pixel-for-pixel): `docs/design/homepage-v1/README.md`, `docs/design/homepage-v1/tokens.css`, `docs/design/homepage-v1/homepage-desktop.dc.html`, `docs/design/homepage-v1/homepage-mobile.dc.html`, `docs/design/homepage-v1/identity.dc.html`, `content/brand/mark.svg`. Occasion tiles are the canvas's six tiles (Birthday · Name day "Imieniny, the Polish tradition" · Anniversary · Sympathy · Just because · New baby), each a photo slot + title + one-line subtitle, driven by a new `src/config/occasions.ts` in the TASK-047 shape (per-locale slug, name/subtitle message keys, `published: false`, owning spec 008) — so the tiles render as text-and-placeholder cards, **not** links, until 008 publishes the occasion pages, and the grid comes from TASK-045's `Grid` primitive (2-up mobile, 3-up desktop per the artboards). Photography is a placeholder until the founder supplies imagery: every `.photo` box in the canvas renders the `--color-photo` token gradient with its uppercase caption and **no `<img>`** (`plan/10` §3 honesty rule); the consolidated "photo slots" list is part of TASK-053's PR body. Relay explainer is the canvas's `01/02/03` section with its full sentences and the "Read the guarantee in full" control (unpublished target → text, not a link). `TrustStrip` carries the §2/A6 three claims with the guarantee name as its own key (`trust.guarantee.name`) so a rename is a catalogue edit. **AC-10 closes here** — with this PR the locale home is `H1` + proposition + finder/picker + how-it-works + trust strip in all four locales, one `<h1>`, named `banner`/`main`/`contentinfo` landmarks, hero slot present with no `<img>`. **AC-15 is the Phase 0 AC 6 gate and this is the task that can break it:** no review, rating, star, testimonial, review count, Trustpilot mark, partner name, partner photo, delivery-photo claim or florist count anywhere rendered, asserted over all four locale homes and the footer plus a repo grep of `messages/en.json` for the forbidden shapes — note for the reviewer that `common.floristCount` keeps its `retained: true` flag precisely because nothing renders it. **AC-16**: no JSON-LD, canonical or hreflang from any 004 page; `alternatesFor()`/`isLocaleIndexable()` answers unchanged except the recomputed unreviewed share, which keeps `de`/`pl` non-indexable — the honest answer while their copy is a machine draft. The consolidated **"photo slots" list** (hero, six occasion tiles, trending row, review section, delivery-photo band, with the caption and aspect ratio each one expects) is part of this PR's body — it is the shopping list the founder shoots against. Tests: T-12, T-17, T-18. Design round 6: also ship the "Coming up in Poland" occasion-date strip (real dates + order-by cutoffs from config) and the five-question FAQ section (`faq.*` messages, `<details>`). **Inherited from `/review 27` (nit 3):** `/de` LCP is 2.5–2.9 s and its LCP element is the spec-003 language-suggestion banner island (~2.4 s render delay), so the LCP fix belongs to whichever of this task's hero/`Media` work or TASK-056's budget flip touches that island — measure `/de` before and after the hero lands. **Linux visual baselines — orchestrator ruling (`/review 30`, 2026-09-09):** `linux/` baselines (`en`, `de`, `ar-XB`, `footer-*`) are refreshed once from a single `ci:full` visual-job artifact after TASK-048, TASK-049 and TASK-052 have merged — owned by TASK-053's PR (orchestrator adds the label). No task produces them alone. **Dependency added 2026-09-09:** runs after TASK-085 (option (b)), so the trending/occasion sections are measured against a provider-free document. |
| TASK-054 | Locale-home part 3 — the data-gated rows and the destinations grid: "Most sent this week" trending row (florists' picks, labelled, gated on real orders), the verified-reviews section (hidden until real reviews exist, Trustpilot slot), the destinations grid with live/guide status, the "Somewhere else?" waiting-list block, and the AC-14 link crawl | `specs/004` · AC-14 | 0 | todo | frontend-implementer | — | TASK-053 | Branch `task/TASK-054-home-gated-rows-destinations`. Spec §13's 2026-09-08 resolution note is binding and overrides §5 defaults where they differ. Design source of truth (match pixel-for-pixel): `docs/design/homepage-v1/README.md`, `docs/design/homepage-v1/tokens.css`, `docs/design/homepage-v1/homepage-desktop.dc.html`, `docs/design/homepage-v1/homepage-mobile.dc.html`, `docs/design/homepage-v1/identity.dc.html`, `content/brand/mark.svg`. This task ships the three sections the founder's design gates on data that does not exist, and the gating **is** the deliverable: each reads a static JSON/config stub behind the same provider seam spec 003 used for the locale registry (`TrendingProvider`, `ReviewsProvider`, `DestinationStatusProvider` in `src/modules/ui/`, each with a `staticProvider` now and a spec-002-backed implementation later), so spec 002/008/016 swap the provider inside the module with **zero call-site change** and a fake provider in a test proves the populated branch renders. Rules per the canvas and Phase 0 AC 6: **Trending** renders the florists' picks with the verbatim label "Ranking is by real orders in the last 7 days and switches on once we have them; until then this row shows the florists' picks and says so" — and because 004 knows nothing about products and shows no price (§3), the cards are photo-slot + name + "starting at" **without a figure**, or the row degrades to named picks only; the reviewer decides which of the two is honest and records it. Prices, `Offer` data and the "Bouquets for Poland" product row of the canvas are **out of scope** (005/008/009) and must not be approximated. **Verified reviews** renders **nothing** in Phase 0 — the component exists, its populated branch is covered by a fake provider and the gallery, the Trustpilot slot is a named empty region, and the placeholder rows drawn on the canvas (`[Buyer first name] · sent to Warszawa · [date]`) must never reach a rendered page (AC-15 would fail, and TASK-053 owns that assertion). **Destinations grid**: PL `Delivering now` with its five city names as text, the six guide countries as `Guide · waiting list`, all from `countries.ts`, none of them a link while unpublished; the "Somewhere else?" block is copy only — no email input, because a waiting-list form is a new personal-data flow and a RoPA row (010/016 own it), and shipping an inert input would be a dark pattern. **AC-14 closes here**: an e2e crawl of every `<a href>` in the header, footer and home of all four locales plus `/` finds zero non-200 targets and zero unpublished `site-links.ts` ids; `robots.txt` still `Disallow: /`; localised documents still `noindex,nofollow` and `/` still `noindex,follow`. Photography is a placeholder until the founder supplies imagery: every `.photo` box in the canvas renders the `--color-photo` token gradient with its uppercase caption and **no `<img>`** (`plan/10` §3 honesty rule); the consolidated "photo slots" list is part of TASK-053's PR body. Tests: T-16, plus fake-provider tests for all three populated branches and their gallery states. Design round 7: "Meet the florists" REMOVED by the founder (we are the florists to the buyer; partners stay behind the scenes) — do not build it. Founder ruling 2026-09-08: reviews are real-only, never seeded — enforced by the AC-15 honesty gate. |
| TASK-055 | Styling pass on the inherited surfaces: the `/` chooser, the localised 404 and the 500 boundary styled with the design system, and the spec 003 suggestion banner restyled under the consent sheet in the z-layer scale | `specs/004` · AC-12, AC-13 | 0 | todo | frontend-implementer | — | TASK-045, TASK-048, TASK-049, TASK-051 | Branch `task/TASK-055-chooser-error-banner-styling`. Spec §13's 2026-09-08 resolution note is binding and overrides §5 defaults where they differ. Design source of truth (match pixel-for-pixel): `docs/design/homepage-v1/README.md`, `docs/design/homepage-v1/tokens.css`, `docs/design/homepage-v1/homepage-desktop.dc.html`, `docs/design/homepage-v1/homepage-mobile.dc.html`, `docs/design/homepage-v1/identity.dc.html`, `content/brand/mark.svg`. (the chooser and the error documents are not on the canvas — build them from the tokens as centred `--measure` columns with the `Mark` lockup, `.display` heading and `.label` metadata, matching the masthead's type treatment so the three documents read as one site). Contracts that must not move: `/` stays 200, `noindex,follow`, **zero application JS** (so no client island, no consent banner, no suggestion banner on the chooser — it has no locale to suggest), zero `Set-Cookie`, no `Vary`; the 404 keeps status 404 and its x-default document; the 500 boundary renders **without** the client provider and without the machinery that just failed — TASK-046's zod-free locale data is what keeps that true, so re-assert it here. Each error document offers the locale home and, once published, the destinations section (unpublished → no link, AC-14). Suggestion-banner restyle is **tokens, spacing and z-layer only**: no behaviour change, spec 003 AC-28's matrix (T-28) re-run green unchanged, the overlay background token that spec 003 left as a deferred item resolved to a real token here, and the sheet ordering proven — consent above, suggestion below. Also close spec 003's deferred `role="status"` live-region pattern note (`docs/architecture.md` §2–§4) by making the banner's region the documented pattern the design system exports. Tests: T-14, T-15. **From `/review 31` round 2 (2026-09-09):** the mobile menu `<button disabled>` in `SiteHeader` is a dead affordance (looks enabled, does nothing) — render the menu glyph as non-interactive text/icon until spec 008 publishes nav targets, or give it a visibly disabled treatment; same rule as §14 A4's search band. |
| TASK-056 | Gates, budgets, docs and spec close: `lighthouse` made blocking on five URLs with the restated Brotli budget, `scripts/check-bundle-budget.ts` + committed baseline + step summary, the axe URL set extended to eight surfaces with no exception list, visual baselines for `/`, `/en`, `/en-gb`, `/de`, `/pl` and `/ar-XB` on both platforms across every new section, `i18n:check` green with the `banner.*` retained flags removed, `check:no-db` over the added file set, `docs/runbooks/design-system.md`, `docs/architecture.md` §2/§3/§4, RoPA, README, `.env.example`, spec §14 | `specs/004` · AC-2, AC-24, AC-25, AC-26, AC-27, AC-29, AC-30 | 0 | todo | backend-implementer | — | TASK-046, TASK-051, TASK-054, TASK-055 | Branch `task/TASK-056-gates-budgets-docs-close`. Last task by design: it measures the finished thing, which is why the enforcement flip comes after every pixel has landed. Spec §13's 2026-09-08 resolution note is binding and overrides §5 defaults where they differ. **AC-24 with the restated budget:** `continue-on-error: true` deleted from the `lighthouse` job (spec 001's second deferred row, `ci.yml`), URL set `["/", "/en", "/en-gb", "/de", "/pl"]`, assertions performance / accessibility / best-practices ≥0.95, LCP <2000 ms, CLS <0.05, **script transfer ≤131 072 B (128 KB) measured as Brotli** (§13 Q13 option (a) as corrected by spec §14 A1 after TASK-046's measurement; option (b) — strings-as-props islands and no client message provider — is the fallback TASK-056 takes if the finished homepage exceeds it), image transfer ≤204 800 B, and `categories:seo` explicitly **not** asserted with the `noindex` reason in `lighthouserc.json` so nobody indexes early to make a number green. Also update spec 001 AC-23's queued correction (the `NO_FCP` wording) as now satisfied. **AC-25**: per-route first-load JS, ≤4 KB gz client message payload, ≤45 KB font transfer for **both** faces, a 5 KB regression guard against a committed baseline, `/` still zero app JS, and neither `@sentry/` nor `zod` in any public route's client bundle — printed as the §11 table to the step summary. **AC-27**: baselines on `darwin/` **and** `linux/` at 0.1% for `/`, each locale home at mobile **and** desktop with every new section in frame (utility strip, masthead, category row, hero + finder, proof row, occasion tiles, relay explainer, trending row, destinations grid, colophon), the gallery, consent banner shown, settings open, suggestion banner shown, and `/ar-XB` — where the `pseudo-rtl` project now screenshots real mirrored layout (arrow and chevron flipped, `mark.svg` and the check not) instead of spec 003's blank page. **AC-26**: axe zero serious/critical on `/`, `/en`, `/en-gb`, `/de`, `/pl`, `/dev/components`, a 404, the 500 boundary and `/ar-XB`, no exception list. **AC-29**: `i18n:check` green, the four `banner.*` `retained: true` flags removed, `common.floristCount` keeps its flag with the reason recorded, `de`/`pl` re-drafted deterministically and still non-indexable. **AC-2**: `pnpm build` + `pnpm test` with `DATABASE_URL` unset and no network; `check:no-db` covers every file this spec added. **AC-30**: `docs/architecture.md` §2 (new config files + `(dev)` route group), §3 (`ui` module row), §4 with **neither** the CSP row nor the `vercel.live` note and the deferred overlay-token and `role="status"` items discharged by TASK-055; `docs/runbooks/design-system.md` + its index row (how to add a token, the contrast manifest, adding a component to the gallery, `<bdi>` and `mirror-in-rtl`, updating a baseline, reading the bundle table, and the design-source files under `docs/design/homepage-v1/`); RoPA rows verified; README and `.env.example` for `ENABLE_DEV_UI`, `NEXT_PUBLIC_GA4_MEASUREMENT_ID`, `CSP_REPORT_ONLY`. **Spec §14 corrections to record:** two font families instead of one (§13 Q2 vs §2), the commerce header superseding §5.3's minimal header, the four-way split of §12's task 5, the canvas sections §2's home skeleton does not mention (occasion tiles, trending, reviews, destinations grid), the payment-colophon honesty ruling from TASK-049, the `Continue` target helper from TASK-052, and the Brotli budget restatement. **Spec exit signal:** this PR merged with a recorded `/review` PASS, Phase 0 specs `4 / 12`, `lighthouse` green and **required** on five URLs, the bundle table under budget on every public route, axe 8/8 with no exception list. Tests: T-26, T-27, T-28, T-29, T-31, T-32, T-03 (final run). **Inherited from `/review 27` (nit 3):** the `largest-contentful-paint` assertion this task flips to required is currently red on `/de` because the spec-003 suggestion banner island is the LCP element (~2.4 s render delay); decide here whether it is fixed or the banner is deferred, since the flip cannot land red. **From `/review 28` (2026-09-09):** the CSP enforce flip is no longer this task's — TASK-058 owns it (spec §14 A2); this task decides where the GA4 tag (~30–35 KB when `NEXT_PUBLIC_GA4_MEASUREMENT_ID` is set) sits relative to `resource-summary:script:size` before the id is ever set, since §14 A1 leaves 1 434 B of headroom on locale documents. |
| TASK-057 | CI on demand for the Actions-minutes budget: `ci.yml` triggers only on `ready_for_review`, `labeled` and `workflow_dispatch`; the spine (`lint`, `typecheck`, `test-unit`, `build`) runs once per PR marked ready; fan-out jobs, `commitlint` and the preview → Playwright → Lighthouse chain behind the `ci:full` label; `pr-policy` on ready/label events; spec 001 §14 A14; implementer and reviewer agents told to draft first, run gates locally, push once, `gh pr ready` | `specs/001` · §2 "CI" via §14 A14 (AC-21 compensating controls) | 0 | done | orchestrator (inline; tooling) | main (direct `ci`/`docs` commits, 2026-09-09) | — | Founder report 2026-09-09: 90% of GitHub Free's 2,000 monthly minutes used with a month left. Full run ≈ 30 min, spine ≈ 7 min. Job set unchanged, so `ci-workflow.test.ts` and `branch-protection.test.ts` still pin 18 jobs. Revisit: public repository or a paid Actions plan → restore the full triggers. |
| TASK-058 | CSP enforcement: make the policy enforceable with Next 16's React flight inline blocks — nonce propagation on `no-store` routes and/or hash-per-response on cached routes, the enforce-flip runbook (`CSP_REPORT_ONLY=false`), Report-Only evidence read with flight-block reports filtered, per-instance rate-limiter caveat | `specs/004` · §14 A2 · AC-23 (enforcement half) | 0 | todo | backend-implementer | — | TASK-056 | Branch `task/TASK-058-csp-enforce`. Created by the orchestrator from `/review 28` nit 1: the served document carries eleven inline scripts (bootstrap + ten `self.__next_f.push` blocks), so flipping enforcement today blocks hydration. Decide nonce vs hash-per-response in the PR body against ADR-0016's cached-HTML constraint; do not edit ADR-0016 (supersede if the shape changes). Also carries `/review 26` nits: `AcceptLanguageSchema` test-only note, logger reporting 200 for 204/4xx responses. |
| TASK-059 | Living design system and prototype in the repository: `docs/design/` becomes the design source of truth every design and implementer agent reads — `README.md` index and rules, `system/` (tokens + every shipped primitive and its states, kept in step with `src/modules/ui`), `flows/` (Phase 0 buyer journey home → destination → shop → PDP → checkout → confirmation → track, and the florist journey for-florists → application → vendor inbox, as annotated flow artboards), `wireframes/` (one artboard per Phase 0 page type in `plan/05-page-inventory.md` §1–§2 at desktop and mobile, in the approved system), `canvas.json` for the whole set, published to the founder's design canvas; a `docs.test.ts` pin that every UI spec names its design files | `specs/004` · §14 A3 (design source of truth) · founder instruction 2026-09-09 | 0 | in_progress | orchestrator (design skill) + general-purpose design agent | [#32](https://github.com/itsahmeds/flowers-overseas/pull/32) | — | Branch `task/TASK-059-living-design-system`. Founder, 2026-09-09: "maintain a full design system/prototype as well in HTML in the code … so design agents always know the design. full user flow journey etc etc wireframe and everything." Extends `docs/design/homepage-v1/` (approved) rather than replacing it; the homepage artboards move under `docs/design/` unchanged in content. Every later UI spec (007–012) adds its wireframes here **before** `/plan-tasks`, and implementers match the artboards pixel-for-pixel — recorded as a CLAUDE.md rule. Wireframes for unbuilt pages are wireframes (structure, copy slots, states), not final art; the founder reviews them on the canvas before the owning spec is approved. Delivered 2026-09-09 on PR #32: `docs/design/README.md` (index, authoring rules, honesty rules, the Phase 0 page-type → wireframe table); `system/` (canonical `tokens.css` + components, typography, colour sheets); `flows/` (buyer, florist, consent-and-locale); `wireframes/` (20 Phase 0 page types × 1440 and 390 = 40 artboards); root, `flows/` and `wireframes/` `canvas.json`; `tests/unit/design-docs.test.ts` as the pin (92 assertions: Phase 0 coverage at both widths, no colour literal or undeclared token in any `.dc.html`, every canvas entry resolves, no overlap). Also fixed `homepage-v1/canvas.json`, which named three files that do not exist. Four documented sheet-vs-code differences, all resolved in favour of the code. Not wireframed and why: `plan/05` rows 5, 11, 12, 15–18, 23, 24 (Phase 1–4 variants of drawn templates); 26, 27, 30, 31, 37–40 (need data we do not have); 55–59 (authenticated / internal, admin awaits spec 012); 60–67 (not pages). |
| TASK-060 | Module skeleton, money types and the `fo/no-float-money` flip: `src/modules/catalog/` barrel with the owning-spec comment, `types.ts` + `schemas.ts` skeleton (`MoneySchema` reused from spec 003, `PricePointSchema` with its net+VAT=gross and `deliveryIncluded: true` refinements), `providers.ts` (`CatalogueProvider` / `PriceProvider` / `FxRateProvider` interfaces + composition root), and `fo/no-float-money` **enabled** for `src/`, `src/config/`, `seed/`, `scripts/` | `specs/005-catalogue-pricing-module.md` · AC-1, AC-2, AC-3, AC-4 | 0 | done | backend-implementer | [#33](https://github.com/itsahmeds/flowers-overseas/pull/33) | TASK-046 | Branch `task/TASK-060-catalog-module-float-money`. Spec 005 §13's 2026-09-09 resolution note is binding: Q1 EUR/GBP `x90`, PLN `x9`, HUF `x90`; Q2 ECB daily rates, 2.5% buffer, 48 h max age then destination currency; Q3 add-ons priced per country with their own VAT rate; Q4 plain stem-count / S-M-L / single tier names; Q5 30-minute signed quote, secret lands in spec 010; Q6 middle tier preselected via `product_tier.is_default`; Q7 surcharges as dated rows; Q8 all three schema amendments folded into spec 002's `0003` (spec 002 §14 A1); Q9 this spec owns the dataset in `src/config/catalogue/*.data.ts`; Q10 no money on destination-less hubs; Q11 EUR/GBP/PLN only behind `currency.{code}` flags. Module directory is `catalog` (US spelling) per §5.2's ruling — `plan/01` §5, `docs/architecture.md` §3, the `MODULES` manifest in `scripts/check-layout.ts` and spec 001 AC-9's lint fixtures all use it; only the *dataset* path is British (`src/config/catalogue/`). This is the task that discharges spec 001 TASK-004's "`fo/no-float-money` fixture-only in 001, enforced from spec 005" promise: enable the rule, keep the existing RuleTester fixtures, and add the four-root wiring assertion — **no `eslint-disable` for it may exist anywhere in the repository** (AC-4). **No client JavaScript**: spec 004 §14 A1's budget is 131 072 B Brotli per document and TASK-046 measured `/en` and `/de` at 129 638 B, so the headroom is 1 434 B and this module must add zero application bytes to any client chunk (AC-3 / AC-22 measure +-0 B, not "small"). Registers the module in `docs/architecture.md` §3 (row moves from "empty barrel" to its Phase 0 state) and extends `pnpm check:no-db` to `src/modules/catalog/**` and `src/config/catalogue/**` in the same PR. No provider object, no dataset path and no `drizzle`/`postgres` import may be reachable from the barrel (AC-2). Depends on TASK-046 only because AC-3's "+-0 B against the committed baseline" needs that task's `budget:client-js` baseline to exist (it is `done`). Gates: `lint`, `typecheck`, `test-unit`, `build`, `check-layout`, `check:no-db`, `architecture-doc.test.ts`. Tests: T-01, T-02. |
| TASK-061 | Catalogue dataset and the spec 002 projections: `src/config/catalogue/{products,tiers,categories,occasions,addons}.data.ts` (84 SKUs in the `plan/10` §2.1 40/14/8/10/12 split, the six facets of §1.1, the tier structure of §2.2, 6 add-ons), `schemas.ts` with closed facet enums, and `projections.ts` — `toProductRow`, `toProductTranslationRow`, `toProductTierRow`, `toCategoryRow`, `toOccasionRow`, `toAddonRow`, `toAddonCountryPriceRow`, `toCountryPriceRow`, `toFxRateRow` | `specs/005` · AC-7 · plus §2's "dataset is authored once, here" contract and §5.1's projection table (AC-6 is verified whole by TASK-062 once the price half lands) | 0 | done | backend-implementer | [#34](https://github.com/itsahmeds/flowers-overseas/pull/34) | TASK-060 | Branch `task/TASK-061-catalogue-dataset-projections`. Spec 005 §13's 2026-09-09 resolution note is binding: Q1 EUR/GBP `x90`, PLN `x9`, HUF `x90`; Q2 ECB daily rates, 2.5% buffer, 48 h max age then destination currency; Q3 add-ons priced per country with their own VAT rate; Q4 plain stem-count / S-M-L / single tier names; Q5 30-minute signed quote, secret lands in spec 010; Q6 middle tier preselected via `product_tier.is_default`; Q7 surcharges as dated rows; Q8 all three schema amendments folded into spec 002's `0003` (spec 002 §14 A1); Q9 this spec owns the dataset in `src/config/catalogue/*.data.ts`; Q10 no money on destination-less hubs; Q11 EUR/GBP/PLN only behind `currency.{code}` flags. Follows spec 003's `toLocaleRow()` / spec 004's `toCountryRow()` precedent exactly: plain constants, zod-parsed at module load, no import beyond the schemas, `check:no-db` clean. Each projection returns **exactly** spec 002 §5.1's column set for its table **including the three spec 002 §14 A1 amendments** (`addon_country_price.vat_rate_bp`, `product_tier.is_default`, the two partial unique indexes' key columns), with one unit test per projection pinning the key list against a transcribed copy of §5.1 so neither side can be edited alone (AC-7). Facet values are **keys, never labels** — every value resolves through `messages/en.json` (§7), which is why `catalogue:check` can assert label coverage in TASK-062. Deliberately absent from the dataset: descriptions, imagery, `de`/`pl` translations, reviews, payouts (§2 — those are spec 006's and the founder's). Product names go into `content/i18n/glossary.en.md`'s "Product and tier names" row (that row is closed by TASK-069's AC-26). Tier steps are **authored rows** at ~+30%/+60% of the smallest, never a percentage applied at render (§5.2). Gates: `lint` (with `fo/no-float-money` now live), `typecheck`, `test-unit`, `check:no-db`. Tests: T-05, and the structural half of T-04. **Implemented 2026-09-09, PR #34 (draft -> ready, gates local).** Dataset: 84 products (40/14/8/10/12), 236 tier rows (one middle default each), 23 categories, 32 occasions, 6 add-ons; zero prices/FX (TASK-062), zero descriptions/media/`de`+`pl`/reviews. Nine projections pinned against a transcribed spec 002 §5.1 in `tests/unit/catalogue-projections.test.ts` (40), dataset in `catalogue-dataset.test.ts` (39), schema failure modes in `catalogue-schemas.test.ts` (31); `budget:client-js` byte-identical to `fe22a24` (`/` 126.2 KB br, `/en`+`/de` 126.6 KB br, same chunk-list hash). **Two spec 002 amendment requests raised in the PR, not applied:** (a) §5.1 names `category` with no column list, so `toCategoryRow()` projects `{key, kind}` by analogy with `occasion(id, key, kind)`; (b) `product_tier.stems` must be nullable, because §13 Q4's S/M/L and `single` tiers have no stem count. Message keys (`catalog.*`) deliberately not added — TASK-067 owns AC-22, so TASK-062's `labelKey`-coverage failure mode needs sequencing against it. Product copy stays spec 006's (TASK-073). |
| TASK-062 | Price dataset, FX snapshot and `pnpm catalogue:check`: `prices.data.ts` for the 8 seeded countries inside the `plan/10` §2.3 bands with `surcharge_kind` rows and `active_from`/`active_to`, `fx.data.ts` (one dated ECB snapshot, `rate_ppm` integers), the `catalogue:check` CLI with all eight failure modes and its per-country coverage step summary, and the `catalogue-check` CI job on `needs: typecheck` | `specs/005` · AC-5, AC-6 | 0 | done | backend-implementer | [#37](https://github.com/itsahmeds/flowers-overseas/pull/37) | TASK-061 | Branch `task/TASK-062-price-dataset-catalogue-check`. Spec 005 §13's 2026-09-09 resolution note is binding: Q1 EUR/GBP `x90`, PLN `x9`, HUF `x90`; Q2 ECB daily rates, 2.5% buffer, 48 h max age then destination currency; Q3 add-ons priced per country with their own VAT rate; Q4 plain stem-count / S-M-L / single tier names; Q5 30-minute signed quote, secret lands in spec 010; Q6 middle tier preselected via `product_tier.is_default`; Q7 surcharges as dated rows; Q8 all three schema amendments folded into spec 002's `0003` (spec 002 §14 A1); Q9 this spec owns the dataset in `src/config/catalogue/*.data.ts`; Q10 no money on destination-less hubs; Q11 EUR/GBP/PLN only behind `currency.{code}` flags. Psychological endings come from §13 Q1's binding answer — EUR/GBP `x90`, PLN `x9`, HUF `x90` — which means **`src/config/currencies.ts` (spec 003) must be corrected in this PR**: PLN moves from `x90` to `x9` and HUF from `x9` to `x90`, three lines plus their test, and `plan/10` §2.3's 149/199/269/359 zł bands become satisfiable. Surcharges are dated rows (+EUR 4 equivalent Sunday, +EUR 6 equivalent Valentine's/Women's Day), never a multiplier. `catalogue:check` exits non-zero with one line per problem for each of: SKU with no tier; SKU with no active price for a seeded country; two active rows for one (product, country, tier, surcharge); zero or two default tiers; a price outside its band; an unknown facet value; a `labelKey` missing from `messages/en.json`; a projection key set disagreeing with spec 002 §5.1 (AC-5) — one temporary-mutation fixture per mode. Job placement follows spec 001 §14 A9 (`needs: typecheck`, as `i18n-check` and `seed-check` do) and must be added to `scripts/branch-protection.ts`'s derived required set. CI minutes: draft first, run gates locally, push once, `gh pr ready` (TASK-057). Gates: `catalogue-check`, `lint`, `typecheck`, `test-unit`, `build`. Tests: T-03, T-04. **From TASK-061 (2026-09-09):** the `catalogue:check` mode "a `labelKey` missing from `messages/en.json`" needs the `catalog` namespace, which is TASK-067's AC-22 — seed the keys this task checks (marked `retained` until 067 uses them) rather than leaving the mode vacuous. Rebase on `origin/main` **before** `gh pr ready`, or the spine never runs. **Implemented 2026-09-09, PR #37 (draft -> ready, gates local).** One authored record per destination in `prices.data.ts` (currency, flower + standard VAT rate, the eight `plan/10` §2.3 bands, an authored tier ladder in each, the two surcharge amounts, the six add-on prices), expanded into **3 416** `country_price` rows (1 652 retail = 236 tiers x 7 destinations, 588 open-ended Sunday rows, 1 176 closed-window peak-day rows for 14 Feb / 8 Mar 2027) and **42** `addon_country_price` rows; `fx.data.ts` commits **9** euro-base ECB rates dated 2026-09-08 with `FX_BUFFER_BP = 250` applied at conversion time and `MAX_FX_AGE_HOURS = 48` reported, not failed. `catalogue:check` implements **13** modes (the eight AC-5 names plus `rounding-ending`, `float-money`, `addon-price`, `fx-snapshot`, `destination-drift`), one fixture each as a one-field mutation of the check's input, and prints the per-destination coverage table; new `catalogue-check` CI job on `needs: typecheck`, derived automatically into `branch-protection`. Tests: 54 new unit (`catalogue-prices` 21, `catalogue-check` 27, `catalogue-fx` 6) plus 7 amended files; suite 1 999 green; `budget:client-js` byte-identical to `ff58840`. **Three corrections called out in the PR, none optional:** (a) `src/config/currencies.ts` PLN `x90`->`x9` and HUF `x9`->`x90` per §13 Q1 (pre-accepted on ADR-0017's decisions-log row); (b) TASK-061's `catalog.facet.occasion.17Mai` -> `.mai17`, because spec 003's message-key alphabet forbids a digit-initial segment in **both** `MessagesSchema` and `MessageMetaManifestSchema`, so the old key could never be authored or reviewed — `messageKeyLeaf` now moves a leading number to the end and `CatalogMessageKeySchema` was tightened to that alphabet; (c) 89 `catalog.*` label keys seeded in `messages/en.json` as `retained` with `de`/`pl` drafts (AC-22 stays TASK-067's; the strings deserve a founder read). **Bands hold exactly; steps are approximate** (spec 005 §14 A1, the orchestrator's ruling on `/review 37`): every tier of every product sits inside its `plan/10` §2.3 band in every currency — no funeral row above 799 zł on PL or EUR 180 elsewhere — and the \"~+30% / +60%\" steps bend to fit (+12%/+23% in the narrowest band, +25%/+47% in the widest). The single funeral band is split into four adjacent, non-overlapping sub-bands spanning its own bounds because funeral pieces carry a `price_tier` facet. Round 1's smallest-tier-only reading is gone: 1 500 of 1 652 retail rows re-laddered, `band` mode now asserts every tier row, and `catalogue-prices.test.ts` re-checks all 1 652 against a transcription of §2.3 that does not read the dataset's own bands. **Seven destinations, not `plan/10` §2.1's eight:** GB is the first buyer market (ADR-0002), not a destination in spec 004's registry, and the `destination-drift` mode fails the moment the two sets disagree. **Founder/accountant action:** VAT rates for the six `demo` destinations are provisional and on `plan/06` §4 item 4's list; only PL's 8% / 23% are plan-documented. For TASK-066/067: `endsInRoundingStyle()` and the two FX constants are authored here and must be re-exported, never restated. **Round 1 fix (2026-09-09):** re-laddered under §14 A1 (1 500 rows moved; widest compression DE/PL/RO `funeral_luxury` at +12%/+23%, 37% of the +60% target); `band` mode asserts every tier and the schema refinement with it; `addon-price` mode asserts `vatRateBp === standardVatRateBp` (PL chocolates at 800 bp fails); new **14th mode `surcharge-amount`** gates each destination's +EUR 4 / +EUR 6 equivalents and every expanded surcharge row against a transcription in the check script (DE Sunday 4 000 fails); `ambiguous-price` also gates overlapping closed windows; nine FX magnitudes pinned; `docs/compliance/vat-rates.md` records the provisional per-destination rates and is the gate on a `demo` -> `live` flip. Five neuter-the-check mutations and three break-the-data mutations verified. Known residual, stated in the PR: `catalogue:check` reads the bands from the same file it checks, so a *widened band* is caught only by `catalogue-prices.test.ts`'s independent transcription, not by the gate. |
| TASK-063 | Taxonomy read API: `read.ts` — `getProduct`, `listProducts`, `getCategory`, `getOccasion`, `resolveFacets(searchParams)` (canonical, order-independent, always `indexable: false`), `isAuthoredFacetPath()` (`false` for everything in Phase 0), `countProductsIn(category, country)` / `countProductsFor(occasion, country)`, `hasIndexableProducts`, and `topProductsForPrebuild(country, locale, n)` | `specs/005` · no AC of its own; explicit scope — §2 "Taxonomy" read API, §6's six-product-rule counters (`plan/02` §6) and facet-`noindex` rule (`plan/02` §7), §5.4's `generateStaticParams` ordering (AC-21 is verified whole by TASK-068, AC-25 by TASK-069) | 0 | todo | backend-implementer | — | TASK-061 | Branch `task/TASK-063-taxonomy-read-api`. Spec 005 §13's 2026-09-09 resolution note is binding: Q1 EUR/GBP `x90`, PLN `x9`, HUF `x90`; Q2 ECB daily rates, 2.5% buffer, 48 h max age then destination currency; Q3 add-ons priced per country with their own VAT rate; Q4 plain stem-count / S-M-L / single tier names; Q5 30-minute signed quote, secret lands in spec 010; Q6 middle tier preselected via `product_tier.is_default`; Q7 surcharges as dated rows; Q8 all three schema amendments folded into spec 002's `0003` (spec 002 §14 A1); Q9 this spec owns the dataset in `src/config/catalogue/*.data.ts`; Q10 no money on destination-less hubs; Q11 EUR/GBP/PLN only behind `currency.{code}` flags. Runs in parallel with TASK-062 — both depend only on TASK-061 and touch disjoint files (`read.ts` vs `prices.data.ts`/`scripts/`), but they must not be dispatched to two implementers at once because both edit `src/modules/catalog/index.ts`; sequence 062 then 063. 005 **supplies the counts, 008 enforces the >=6 threshold** — this task must not encode the threshold. Every facet combination is `indexable: false` and `isAuthoredFacetPath()` returns `false` for all input, so no colour/price facet URL can become indexable by accident (`plan/02` §7); `topProductsForPrebuild` is a deterministic ordering with no relevance model (§3 — search and ranking are 008's). `resolveFacets` is zod-parsed at the boundary (`plan/12` §2) and order-independent so two orderings of the same facets canonicalise identically. Server-side only; **No client JavaScript**: spec 004 §14 A1's budget is 131 072 B Brotli per document and TASK-046 measured `/en` and `/de` at 129 638 B, so the headroom is 1 434 B and this module must add zero application bytes to any client chunk (AC-3 / AC-22 measure +-0 B, not "small"). Gates: `lint`, `typecheck`, `test-unit`, `catalogue-check`, `check:no-db`. Tests: the read-API half of T-04 plus the facet/indexable cases feeding T-19. |
| TASK-064 | Tiers and add-ons read API: the tier read model (`tierKey`, `labelKey`, `stems`, `sort`, `isDefault` with exactly one default per product), the add-on model with **its own `vatRateBp`** per destination country, the `addon.wine.{country}` flag seam with a static Phase-0 implementation, `cake` as `partner_only`, and `card` priced 0 and still rendered as a line | `specs/005` · AC-19 | 0 | todo | backend-implementer | — | TASK-062, TASK-063 | Branch `task/TASK-064-tiers-addons-read-api`. Spec 005 §13's 2026-09-09 resolution note is binding: Q1 EUR/GBP `x90`, PLN `x9`, HUF `x90`; Q2 ECB daily rates, 2.5% buffer, 48 h max age then destination currency; Q3 add-ons priced per country with their own VAT rate; Q4 plain stem-count / S-M-L / single tier names; Q5 30-minute signed quote, secret lands in spec 010; Q6 middle tier preselected via `product_tier.is_default`; Q7 surcharges as dated rows; Q8 all three schema amendments folded into spec 002's `0003` (spec 002 §14 A1); Q9 this spec owns the dataset in `src/config/catalogue/*.data.ts`; Q10 no money on destination-less hubs; Q11 EUR/GBP/PLN only behind `currency.{code}` flags. **CRD Art. 22 is discharged by absence, not by review**: the `Addon` type has no `defaultSelected`/`preselected` field, adding one fails `catalogue:check`, and AC-19 pins both halves plus "every add-on carries `vatRateBp`" (PL chocolates 23% vs flowers 8%, `plan/06` §4 item 4). Middle tier preselected via `isDefault` (§13 Q6) so `plan/04` A/B test #2 ("12 vs 18 stems") is a data change; a tier preselected in code would be untestable. Tier labels are message keys (`catalog.tier.stems` ICU plural, `catalog.tier.size.{s,m,l}`, `catalog.tier.single`); tier *names* do not exist (§7). Flag reads go through a seam so spec 002's `feature_flag` table needs no caller change (§12). Gates: `lint`, `typecheck`, `test-unit`, `catalogue-check`. Tests: T-17. |
| TASK-065 | Pricing core: `pricing/money.ts` (`addMoney`, `sumMoney`, `assertSameCurrency`, integer only), `pricing/resolve.ts` (`resolvePrice`, `dateSurcharges`, `fromPrice`, `tierPrices` reading the one active row and **throwing** on zero or two), and `pricing/vat.ts` (`vatBreakdown`, `netFromGross`) with mixed-rate integer splits | `specs/005` · AC-8, AC-13, AC-16 | 0 | todo | backend-implementer | — | TASK-064 | Branch `task/TASK-065-pricing-core`. Spec 005 §13's 2026-09-09 resolution note is binding: Q1 EUR/GBP `x90`, PLN `x9`, HUF `x90`; Q2 ECB daily rates, 2.5% buffer, 48 h max age then destination currency; Q3 add-ons priced per country with their own VAT rate; Q4 plain stem-count / S-M-L / single tier names; Q5 30-minute signed quote, secret lands in spec 010; Q6 middle tier preselected via `product_tier.is_default`; Q7 surcharges as dated rows; Q8 all three schema amendments folded into spec 002's `0003` (spec 002 §14 A1); Q9 this spec owns the dataset in `src/config/catalogue/*.data.ts`; Q10 no money on destination-less hubs; Q11 EUR/GBP/PLN only behind `currency.{code}` flags. **This is the compliance keystone of spec 005.** `PricePoint` cannot represent a partial price: `deliveryIncluded: true` is a literal type, `netAmountMinor + vatAmountMinor === amountMinor` is a schema refinement, and **no exported function returns a price type lacking those fields** (type-level test, AC-8) — which is how `plan/07` §4's drip-pricing prohibition (CRD Art. 6, Price Indication Directive, UK DMCC) becomes unexpressible rather than reviewed for. `resolvePrice` takes `{ productId, tierKey, countryIso, deliveryDate? }` and **no buyer, IP, header or locale-derived location** (EU 2018/302, ADR-0006 — the enumerating gate is AC-18 on TASK-069, but the signatures are set here). Surcharges are rows returned with their dates and label keys so 009 can put the amount on the date chip **before** selection; a source scan proves no multiplier or percentage in the surcharge path (AC-16). `vatBreakdown` is 1 000 randomised property-test baskets summing to the gross total exactly (AC-13) — the input to `order.vat_breakdown` (spec 015) and the invoice (spec 018); 005 stores nothing. Two active rows is a data error that throws, never a silent pick (§5.2). Gates: `lint` (`fo/no-float-money` and `fo/no-adhoc-intl` both live), `typecheck`, `test-unit`, `catalogue-check`. Tests: T-06, T-11, T-14. |
| TASK-066 | FX and rounding: `pricing/fx.ts` (`fxRateFor` failing **closed** past `MAX_FX_AGE_HOURS = 48`, `convert` pure/total/integer-only, `FX_BUFFER_BP = 250`) and `pricing/round.ts` (`roundToStyle`, upward-only, per the currency's `x99 \| x90 \| x9 \| none`) | `specs/005` · AC-12, AC-15 | 0 | todo | backend-implementer | — | TASK-065 | Branch `task/TASK-066-fx-rounding`. Spec 005 §13's 2026-09-09 resolution note is binding: Q1 EUR/GBP `x90`, PLN `x9`, HUF `x90`; Q2 ECB daily rates, 2.5% buffer, 48 h max age then destination currency; Q3 add-ons priced per country with their own VAT rate; Q4 plain stem-count / S-M-L / single tier names; Q5 30-minute signed quote, secret lands in spec 010; Q6 middle tier preselected via `product_tier.is_default`; Q7 surcharges as dated rows; Q8 all three schema amendments folded into spec 002's `0003` (spec 002 §14 A1); Q9 this spec owns the dataset in `src/config/catalogue/*.data.ts`; Q10 no money on destination-less hubs; Q11 EUR/GBP/PLN only behind `currency.{code}` flags. Conversion is exactly `amountMinor x rate_ppm x (10 000 + FX_BUFFER_BP) / (1 000 000 x 10 000)` in integers, then **psychological rounding upward, never nearest** — rounding down could put the charged amount below the converted cost, and the 250 bp buffer exists to absorb intraday movement, not to be eroded by rounding. The implementation contains no `/` producing a non-integer, no `Math.round` on money and no `Number` division of an amount, asserted by lint **and** a source scan (AC-12); expectations come from the hand-computed PLN→GBP / PLN→EUR / EUR→PLN table in `tests/fixtures/catalogue.ts` (TASK-069 owns the fixture file; transcribe the FX table here and move it there). Fail-closed is the point of AC-15: a rate older than 48 h returns `null`, `priceProjection()` falls back to the destination currency with `reasonKey = "catalog.availability.fxUnavailable"`, and **no converted amount appears anywhere in the output tree** — a stale rate never becomes a displayed price. `roundToStyle` is monotone and never returns below its input, over a generated range per style. ECB publishes on working days only, so a Monday rate is Friday's; that is what the buffer is for (§13 Q2). Gates: `lint`, `typecheck`, `test-unit`, `catalogue-check`. Tests: T-10, T-13. |
| TASK-067 | Projections and the price identity: `pricing/project.ts` — `priceProjection(locale, …)` deriving the display currency from the locale's `currencyDefault`, `priceTable(product, country)` with its <=6-currency / <=512 B refinement, `fromPrice`, `offerProjection()` — plus the `catalog` message namespace in `messages/en.json` and the schema-identity test against spec 001's `validate-schema` CLI | `specs/005` · AC-9, AC-10, AC-11, AC-22, AC-23 | 0 | todo | backend-implementer | — | TASK-066 | Branch `task/TASK-067-price-projections-identity`. Spec 005 §13's 2026-09-09 resolution note is binding: Q1 EUR/GBP `x90`, PLN `x9`, HUF `x90`; Q2 ECB daily rates, 2.5% buffer, 48 h max age then destination currency; Q3 add-ons priced per country with their own VAT rate; Q4 plain stem-count / S-M-L / single tier names; Q5 30-minute signed quote, secret lands in spec 010; Q6 middle tier preselected via `product_tier.is_default`; Q7 surcharges as dated rows; Q8 all three schema amendments folded into spec 002's `0003` (spec 002 §14 A1); Q9 this spec owns the dataset in `src/config/catalogue/*.data.ts`; Q10 no money on destination-less hubs; Q11 EUR/GBP/PLN only behind `currency.{code}` flags. **AC-11 is the manual-action guard of `plan/02` §15**: `offerProjection().price` formatted at the currency's exponent equals what `formatMoney(displayPrice, locale)` renders, `priceCurrency` equals the locale default, `shippingRate` is 0 (delivery is in the price by definition), `hasMerchantReturnPolicy` is `MerchantReturnNotPermitted` (perishables, `plan/07` §2.1), **exactly one `Offer`** per PDP and **no `Offer` at all** for a `demo` country (ADR-0007). Feed the projection to spec 001's `validate-schema` CLI so the `Offer.price == visiblePrice` check that TASK-009 shipped finally has something to validate. **AC-9 is the i18n rule most easily broken**: the display currency comes from the locale, never the `fo_currency` cookie — no exported function accepts a currency override except `priceTable()`, and the same (product, country, locale) must yield a byte-identical projection with any cookie value present in the environment, which is what keeps cached HTML identical for every visitor with no `Vary` (`plan/02` §3, `plan/03` §1). The client repaint island that consumes `priceTable` is **spec 008's, paid from 008's budget** (§3). New `en` keys: `catalog.tier.stems` (ICU plural — Polish few/many/other for 1/2/5/22 is a required test case, AC-23), `catalog.tier.size.{s,m,l}`, `catalog.tier.single`, `catalog.price.inclusive` (a legal formula per locale, not a translation — `plan/07` §4), `catalog.price.from`, `catalog.addon.{key}.{name,description}`, `catalog.addon.allergen`, `catalog.facet.{facet}.{value}`, `catalog.availability.{inStock,outOfStock,countryDemo,noPartner,fxUnavailable}`, `catalog.surcharge.{sunday,peakDay}`. `en-gb` overrides only where British wording differs; `de`/`pl` re-drafted by `pnpm i18n:draft` and therefore unreviewed, which correctly keeps those locales non-indexable. VAT rates render through `formatPercentFromBasisPoints`; no `Intl` object is constructed in this module (`fo/no-adhoc-intl`). **No client JavaScript**: spec 004 §14 A1's budget is 131 072 B Brotli per document and TASK-046 measured `/en` and `/de` at 129 638 B, so the headroom is 1 434 B and this module must add zero application bytes to any client chunk (AC-3 / AC-22 measure +-0 B, not "small"). Gates: `lint`, `typecheck`, `test-unit`, `i18n-check`, `catalogue-check`, `seo:validate`, `build`, `budget:client-js`. Tests: T-07, T-08, T-09, T-20, T-21. |
| TASK-068 | Availability, the date seams, price history and quotes: `availability.ts` (`availability()` data-derived, `CutoffEvaluator` + `CapacityProvider` declarations, `staticCutoffEvaluator`), `pricing/history.ts` (`lowestPriceInLast30Days`), `pricing/quote.ts` (`quote`, `verifyQuote`), and `isProductIndexable()` | `specs/005` · AC-14, AC-17, AC-20, AC-21 | 0 | todo | backend-implementer | — | TASK-067 | Branch `task/TASK-068-availability-history-quote`. Spec 005 §13's 2026-09-09 resolution note is binding: Q1 EUR/GBP `x90`, PLN `x9`, HUF `x90`; Q2 ECB daily rates, 2.5% buffer, 48 h max age then destination currency; Q3 add-ons priced per country with their own VAT rate; Q4 plain stem-count / S-M-L / single tier names; Q5 30-minute signed quote, secret lands in spec 010; Q6 middle tier preselected via `product_tier.is_default`; Q7 surcharges as dated rows; Q8 all three schema amendments folded into spec 002's `0003` (spec 002 §14 A1); Q9 this spec owns the dataset in `src/config/catalogue/*.data.ts`; Q10 no money on destination-less hubs; Q11 EUR/GBP/PLN only behind `currency.{code}` flags. **AC-20 is the data-flip proof**: `country.status = 'demo'` gives `saleable: false` + `reasonKey = "country.demo"` and no `Offer`; flipping the fixture country to `live` with an active price and one covering partner gives `InStock` / `saleable: true` **with no code change** (`CLAUDE.md`: country go-live is a data flip). **AC-21 is the SEO keystone**: `isProductIndexable()` is `true` only when all six hold (live country, active product, active price row, non-null description in that locale, `reviewed` translation, `isLocaleIndexable(locale)`), and the sitemap-membership query and the robots decision **call the same function** — a single-call-site test, which is what makes "a `noindex` product can never appear in a sitemap" structural (`plan/02` §10). **005 owns no calendar**: `CutoffEvaluator` and `CapacityProvider` are declared with the types spec 009 and 024/031 implement, plus a test-grade static evaluator; no date arithmetic, no holiday evaluation and no occasion-date rule may land here (§3, `plan/03` §9/§10). `lowestPriceInLast30Days` is the Omnibus Art. 6a figure over superseded rows, inclusive of the window's first day — no "was/now" UI ships, and none may be added without this number (AC-14). `quote()` is stateless: HMAC over the quote fields, **no PII** (product ids, tier keys, integer amounts, currency, rate, expiry only — key-set equality test), 30-minute expiry per §13 Q5, and `QUOTE_SIGNING_SECRET` **lands in spec 010's env schema, not here** — Phase 0 uses a test secret and adds no `.env.example` key. `verifyQuote` is constant-time on the digest and returns `expired \| tampered \| ok`, never `ok` for an unresolvable `priceVersion`; on `expired` the caller re-derives and shows the new price for explicit re-confirmation — a silently higher charge is not reachable through this API. Gates: `lint`, `typecheck`, `test-unit`, `catalogue-check`, `check:no-db`. Tests: T-12, T-15, T-18, T-19. |
| TASK-069 | Hardening, gates, fixtures and docs (spec 005 Phase-0 close): the geo-blocking signature gate, the log-field test, `cache.ts` (`cacheTagsFor`), `tests/fixtures/catalogue.ts` exported from the fixtures barrel, `docs/runbooks/pricing.md`, the architecture/README/glossary updates, and the provider contract suite run against the `static*` providers | `specs/005` · AC-18, AC-24, AC-25, AC-26 | 0 | todo | backend-implementer | — | TASK-068 | Branch `task/TASK-069-catalog-hardening-docs`. Spec 005 §13's 2026-09-09 resolution note is binding: Q1 EUR/GBP `x90`, PLN `x9`, HUF `x90`; Q2 ECB daily rates, 2.5% buffer, 48 h max age then destination currency; Q3 add-ons priced per country with their own VAT rate; Q4 plain stem-count / S-M-L / single tier names; Q5 30-minute signed quote, secret lands in spec 010; Q6 middle tier preselected via `product_tier.is_default`; Q7 surcharges as dated rows; Q8 all three schema amendments folded into spec 002's `0003` (spec 002 §14 A1); Q9 this spec owns the dataset in `src/config/catalogue/*.data.ts`; Q10 no money on destination-less hubs; Q11 EUR/GBP/PLN only behind `currency.{code}` flags. **AC-18 turns EU 2018/302 into a gate**: a test enumerates every exported function in `src/modules/catalog` via the barrel's type surface and **fails** if any parameter name or type property matches `/buyer(Country\|Location)?\|ipAddress\|geo\|visitor/i`, and asserts the dataset carries no buyer-keyed dimension — so "a French and a German buyer sending to Warsaw see the same price" is a property of the type system, not a promise. **AC-24**: a capturing logger over the read and pricing paths proves the emitted field set is a subset of `{ sku, tier_key, country_iso, currency, fx_as_of, duration_ms, request_id }` — no buyer, no recipient, no address, no basket contents and **no quote digest** (`CLAUDE.md`). **AC-25**: `cacheTagsFor()` is the only tag builder (`catalog:{country}`, `product:{id}`, `country:{iso}` from `plan/01` §3), a source scan finds no hand-built tag string, and `docs/runbooks/pricing.md` carries the mutation → tag map that 007/008/012 inherit rather than guess; 005 calls `invalidate()` nowhere. `docs/runbooks/pricing.md` also documents how a price is set and superseded, how to read a `PricePoint`, what happens when FX is stale, how the 30-day-lowest figure is produced and the checklist for changing a band, and is added to the runbook index. `docs/architecture.md` §2 lists `src/config/catalogue/`; `README.md` documents `catalogue:check`; `content/i18n/glossary.en.md`'s "Product and tier names" row is filled (AC-26). **RoPA is deliberately unchanged** — the catalogue holds no personal data and `quote()` keeps it that way (§8); state that explicitly in the PR body so the reviewer records it rather than looking for an omission. The provider contract suite is written here and run against `staticCatalogueProvider`/`staticPriceProvider`/`staticFxRateProvider`; the `db*` side of it is AC-27 on TASK-070. **T-27 records that spec 005 ships no visual baseline deliberately** — nothing renders; the first price-block baseline is spec 009's. Gates: full local chain (`lint`, `typecheck`, `test-unit`, `build`, `catalogue-check`, `i18n-check`, `check:no-db`, `docs`/`architecture-doc` tests, `seo:validate`, `budget:client-js`) then one push and `gh pr ready`. Tests: T-16, T-22, T-23, T-24, T-25 (static side). **Spec 005 Phase-0 exit signal: this PR merged with a recorded `/review` pass.** **From `/review 37` (2026-09-09):** `country.vat_rate_bp` (deferred by spec 004) projects from `DestinationPricing.flowersVatRateBp`; the six demo destinations' VAT rates stay marked provisional until the accountant confirms (`docs/compliance/vat-rates.md`). **From `/review 37` re-check (2026-09-09):** `catalogue:check` reads the plan/10 §2.3 bands from the same module it checks; the independent transcription in the unit test is the real guard today — move that transcription into the gate here. |
| TASK-070 | Database-backed catalogue providers: `src/modules/catalog/db/{catalogue,price,fx}.ts` with Drizzle queries behind the unchanged interfaces, the provider contract suite run against a migrated and seeded Postgres, the batched-query assertion and the zero-query-on-cache-hit assertion | `specs/005` · AC-27, AC-28 | 0 | blocked | backend-implementer | — | TASK-069, TASK-013 | Branch `task/TASK-070-db-catalogue-providers`. **Blocked on TASK-013** (spec 002 provisioning parked 2026-09-08 by the founder: no Neon project, no Cloudflare R2, waiting on the domain and business mailbox — `plan/09` §0 non-code critical path first). Nothing here is startable before TASK-013, TASK-014…TASK-016 (migrations `0001`–`0003`, which must carry the three spec 002 §14 A1 amendments) and TASK-026 (seed) are `done`. Spec 005 §13's 2026-09-09 resolution note is binding: Q1 EUR/GBP `x90`, PLN `x9`, HUF `x90`; Q2 ECB daily rates, 2.5% buffer, 48 h max age then destination currency; Q3 add-ons priced per country with their own VAT rate; Q4 plain stem-count / S-M-L / single tier names; Q5 30-minute signed quote, secret lands in spec 010; Q6 middle tier preselected via `product_tier.is_default`; Q7 surcharges as dated rows; Q8 all three schema amendments folded into spec 002's `0003` (spec 002 §14 A1); Q9 this spec owns the dataset in `src/config/catalogue/*.data.ts`; Q10 no money on destination-less hubs; Q11 EUR/GBP/PLN only behind `currency.{code}` flags. **AC-27 is the seam's proof**: the `db*` providers pass the *same* contract suite as the `static*` ones against the same seeded dataset, and swapping the composition root changes **no file outside `src/modules/catalog`** — asserted by a diff-scope check in the PR. **AC-28** is a Neon compute-cost constraint as much as a performance one (ADR-0015): a query counter proves `resolvePrice` batches per (product, country) rather than per tier, and a warm ISR page performs **zero** queries (spec 002 §5.4). Gates: `lint`, `typecheck`, `test-unit`, `test-integration` (Postgres service job), `db:check`, `catalogue-check`, `build`. Tests: T-25 (db side), T-26. |
| TASK-071 | `fx.refresh` implementation: the ECB reference-rate fetch behind `FxRateProvider`, `fx_rate` writes idempotent per `as_of`, the daily 06:00 CET schedule, `invalidate(['catalog:{iso}'])` for every live country, and the staleness `warn` + Sentry message | `specs/005` · no AC of its own; explicit scope — §2 "FX and rounding" job half, §5.4's FX/cache-freshness mechanism (c), §11's three business-consequence signals, §12 task 12 | 0 | blocked | backend-implementer | — | TASK-070, TASK-013 | Branch `task/TASK-071-fx-refresh-job`. **Blocked on TASK-013** for the same reason as TASK-070, and additionally on TASK-024 (pg-boss bootstrapped; `fx.refresh` is already a *declared* handler there and this task fills it). Spec 005 §13's 2026-09-09 resolution note is binding: Q1 EUR/GBP `x90`, PLN `x9`, HUF `x90`; Q2 ECB daily rates, 2.5% buffer, 48 h max age then destination currency; Q3 add-ons priced per country with their own VAT rate; Q4 plain stem-count / S-M-L / single tier names; Q5 30-minute signed quote, secret lands in spec 010; Q6 middle tier preselected via `product_tier.is_default`; Q7 surcharges as dated rows; Q8 all three schema amendments folded into spec 002's `0003` (spec 002 §14 A1); Q9 this spec owns the dataset in `src/config/catalogue/*.data.ts`; Q10 no money on destination-less hubs; Q11 EUR/GBP/PLN only behind `currency.{code}` flags. Phase 0 runs entirely on the committed `fx.data.ts` snapshot, so nothing before this task needs a network call or an FX key — `.env.example` and `src/lib/env.schema.ts` gain nothing until here. Idempotency is per `as_of` (re-running the job for a published day inserts nothing); ECB publishes on working days only, so the job must not treat a missing weekend rate as an error. The three `warn` + Sentry signals of §11 land here or are re-asserted here: **FX stale** (newest rate past `MAX_FX_AGE_HOURS` — every non-native display currency has stopped converting), **price missing** (no active row for a (product, country, tier) a page asked for) and **price ambiguous** (more than one active row, which spec 002's partial unique index should make impossible and therefore indicates a migration or seed defect). No PII in any of the three lines. Gates: `lint`, `typecheck`, `test-unit`, `test-integration`, `db:check`, `build`. Tests: the job half of T-13 plus an idempotency-per-`as_of` integration case. |
| TASK-072 | Seed schemas and dataset skeleton: `seed/schema/*.ts` (every `Seed*Schema`, `MediaAssetManifestSchema`, `MediaVariantManifestSchema`, `AltManifestSchema` and every `to*Row()` projection with its pinned column test) and `seed/data/` — `taxonomy.json`, `categories.json` (23), `occasions.json` + `occasion-country.json`, plus the generated-and-committed `products.json` (84), `product-tiers.json` and `addons.json` | `specs/006-seed-catalogue-import-imagery-pipeline.md` · AC-3, AC-4 | 0 | done | backend-implementer | [#38](https://github.com/itsahmeds/flowers-overseas/pull/38) | TASK-061 | Branch `task/TASK-072-seed-schemas-dataset-skeleton`. Spec 006 §13's 2026-09-09 resolution note is binding: Q1 AI-generated under the locked style guide or free-licence stock (CC0 / public-domain / Unsplash-licence class, never paid stock); Q2 names stay English, descriptors localise; Q3 12 products x 2 assets plus the homepage slots; Q4 <=6 MB derived bytes committed until R2; Q5 AVIF+WebP at seven widths plus one 1200 px JPEG per asset for OG/email; Q6 originals in the founder's store plus a git-ignored `.local/imagery/originals/`; Q7 `fo-media` / `fo-media-preview` / `fo-backups`, `media.flowersoverseas.com`; Q8 both lawyer questions on the October legal-drafts list, label ships now; Q9 founder signs off against the §2.4 checklist; Q10 spec 002 task 10 reads `seed/data/`; Q11 the seven media columns fold into `0003` (spec 002 §14 A1); Q12 watermark mechanism kept, no watermarked asset in Phase 0. **Dataset-ownership ruling (orchestrator, 2026-09-09):** spec 005 §13 Q9 makes `src/config/catalogue/*.data.ts` the single authored source of catalogue *rows* (products, tiers, add-ons, prices, FX) while spec 006 §13 Q10 makes `seed/data/` what spec 002's seed reads. Both hold only if 006's `products.json`, `product-tiers.json`, `addons.json`, `prices/{ISO2}.json` and `addon-prices/{ISO2}.json` are **generated projections** of 005's dataset (through 005's `to*Row()` functions), committed, and asserted equal by `seed:check` — never a second hand-authored copy. 006 exclusively owns `taxonomy.json`, `categories.json`, `occasions.json`, `occasion-country.json`, `copy/`, `media.json`, `media-variants.json` and `alt/`. Escalated to the founder: if 006 should instead hand-author its own catalogue rows, spec 006 §2.2 needs an amendment before TASK-072 starts. Every file carries a `version` header and `source: "seed"`. Projections must return exactly spec 002 §5.1's column lists for `product`, `product_translation`, `country_price`, `addon_country_price`, `media_asset`, `media_variant`, `product_media`, `product_media_alt` **including the seven media columns and the two catalogue amendments of spec 002 §14 A1** (`media_asset.generator_model`/`.credit`/`.licence`/`.depicts`/`.reviewed_by`/`.reviewed_at`, `media_variant.checksum_sha256`, `addon_country_price.vat_rate_bp`, `product_tier.is_default`), each pinned by a unit test that fails if either side is edited alone (AC-3). `occasion-country.rule_type`/`rule` shapes are transcribed verbatim from `plan/03` §9 and spec 002 §5.1 — the **evaluator is spec 009's** and must not be written here. No file added by this task or TASK-073…TASK-080 may import `src/lib/db*`, `drizzle*`, `pg` or `postgres`; extend `pnpm check:no-db` to `seed/**` in this PR (the whole-seam assertion is AC-1 on TASK-081). Gates: `lint`, `typecheck`, `test-unit`, `check:no-db`. Tests: T-03, T-04. |
| TASK-073 | Seed copy for `en` and `en-gb`: `copy/en/*.json` + `copy/en-gb/*.json` — 84 product descriptions (60–90 words, contents/size/who-it-suits/substitution, each ending with the local-florist sentence), names and slugs, SEO title/description, category and occasion intros; the `catalogue.*` and `media.*` message keys; and `pnpm i18n:draft` extended to the dataset and run for `de`/`pl` flagged unreviewed | `specs/006` · AC-5 · plus §7's `media.*` / `catalogue.*` namespaces and the machine-draft `noindex` gate | 0 | todo | backend-implementer | — | TASK-072 | Branch `task/TASK-073-seed-copy-en`. Spec 006 §13's 2026-09-09 resolution note is binding: Q1 AI-generated under the locked style guide or free-licence stock (CC0 / public-domain / Unsplash-licence class, never paid stock); Q2 names stay English, descriptors localise; Q3 12 products x 2 assets plus the homepage slots; Q4 <=6 MB derived bytes committed until R2; Q5 AVIF+WebP at seven widths plus one 1200 px JPEG per asset for OG/email; Q6 originals in the founder's store plus a git-ignored `.local/imagery/originals/`; Q7 `fo-media` / `fo-media-preview` / `fo-backups`, `media.flowersoverseas.com`; Q8 both lawyer questions on the October legal-drafts list, label ships now; Q9 founder signs off against the §2.4 checklist; Q10 spec 002 task 10 reads `seed/data/`; Q11 the seven media columns fold into `0003` (spec 002 §14 A1); Q12 watermark mechanism kept, no watermarked asset in Phase 0. **Names stay English, descriptors localise** (§13 Q2, `plan/10` §2.2): `Amber Hour` is `Amber Hour` in all four locales while "hand-tied rose bouquet" becomes "Rosenstrauß, handgebunden" / "bukiet róż wiązany ręcznie" — one brandable entity per product, stable slugs, and a genuine hreflang translation set. Our own text only, no competitor wording; no superlative that cannot be backed; **no two products share a description** (the thin-content guard of §6, enforced by TASK-075). `en` is the source of truth (`plan/03` §6 step 1) and `en-gb` ships **only** as differing overrides. `de`/`pl` copy is either absent or written by the deterministic, network-free `DraftProvider` with `translationStatus: "machine"`, `reviewed: false` and a `sourceHash` — **never present, unreviewed and unflagged** (AC-5). The correct consequence, stated so nobody "fixes" it: German and Polish PDPs are non-indexable until a native reviewer approves them (`plan/03` §6 gate 4, `plan/02` §12, spec 002 §6); substantive `de`/`pl` translation is founder + native-reviewer work (`plan/13` B12), not a task. Corridor guides and legal copy are **never** machine-drafted and are out of scope (§3). New keys: `media.provenance.aiExample`, `media.placeholder.{hero,occasion,product,delivery}` (the "Photography to supply · …" strings spec 004 renders as design copy today), the demo watermark label, `a11y.media.*` (gallery landmark, "image N of M"), plus the localised descriptor patterns, tier labels resolved from `product_tier.label_key`, and the local-florist sentence as **one** key so a rewording is one catalogue edit in four locales rather than 84 description edits. No literal user-facing string in any component (`fo/no-literal-strings`). Gates: `lint`, `typecheck`, `test-unit`, `i18n-check`, `check:no-db`. Tests: T-05. |
| TASK-074 | Seed price files: `prices/{ISO2}.json` + `addon-prices/{ISO2}.json` for the eight seeded countries — projected from spec 005's `prices.data.ts` through its `toCountryPriceRow()` / `toAddonCountryPriceRow()`, with tier steps, `surcharge_kind` rows, `active_from`/`active_to` and integer minor units + currency on every row | `specs/006` · AC-6 | 0 | todo | backend-implementer | — | TASK-072, TASK-062 | Branch `task/TASK-074-seed-price-files`. Spec 006 §13's 2026-09-09 resolution note is binding: Q1 AI-generated under the locked style guide or free-licence stock (CC0 / public-domain / Unsplash-licence class, never paid stock); Q2 names stay English, descriptors localise; Q3 12 products x 2 assets plus the homepage slots; Q4 <=6 MB derived bytes committed until R2; Q5 AVIF+WebP at seven widths plus one 1200 px JPEG per asset for OG/email; Q6 originals in the founder's store plus a git-ignored `.local/imagery/originals/`; Q7 `fo-media` / `fo-media-preview` / `fo-backups`, `media.flowersoverseas.com`; Q8 both lawyer questions on the October legal-drafts list, label ships now; Q9 founder signs off against the §2.4 checklist; Q10 spec 002 task 10 reads `seed/data/`; Q11 the seven media columns fold into `0003` (spec 002 §14 A1); Q12 watermark mechanism kept, no watermarked asset in Phase 0. **Dataset-ownership ruling (orchestrator, 2026-09-09):** spec 005 §13 Q9 makes `src/config/catalogue/*.data.ts` the single authored source of catalogue *rows* (products, tiers, add-ons, prices, FX) while spec 006 §13 Q10 makes `seed/data/` what spec 002's seed reads. Both hold only if 006's `products.json`, `product-tiers.json`, `addons.json`, `prices/{ISO2}.json` and `addon-prices/{ISO2}.json` are **generated projections** of 005's dataset (through 005's `to*Row()` functions), committed, and asserted equal by `seed:check` — never a second hand-authored copy. 006 exclusively owns `taxonomy.json`, `categories.json`, `occasions.json`, `occasion-country.json`, `copy/`, `media.json`, `media-variants.json` and `alt/`. Escalated to the founder: if 006 should instead hand-author its own catalogue rows, spec 006 §2.2 needs an amendment before TASK-072 starts. Depends on TASK-062 because that task authors the price rows and corrects `src/config/currencies.ts` to the binding endings (PLN `x9`, HUF `x90`) — this task must not restate a band or an ending. Exactly **one open-ended row per (product, country, tier, surcharge)**: the file-level mirror of spec 002's partial unique index, which is what preserves the Omnibus Art. 6a 30-day-lowest history by superseding rather than updating (§8, spec 002 AC-9). Every row is VAT-and-delivery-inclusive integer minor units with a currency (`CLAUDE.md`, `plan/07` §4). AC-6 requires a fixture per failure shape — out of band, float amount, wrong psychological ending, a second open-ended row, unknown tier — each failing `pnpm seed:check` with a message naming the file, the SKU and the rule (the check itself lands in TASK-075, so ship the fixtures here and wire them there). No image, badge, strike-through or scarcity claim anywhere in this dataset (`plan/07` §4: no fake urgency). Gates: `lint`, `typecheck`, `test-unit`, `check:no-db`. Tests: T-06. |
| TASK-075 | `pnpm seed:check` and the `seed-check` CI job: all nine rule families of §2.3 with one fixture per family, the per-(country, category) and per-(country, occasion) coverage table, the step summary, and the job on `needs: typecheck` added to the derived required-check set | `specs/006` · AC-7, AC-9, AC-10, AC-30 | 0 | todo | backend-implementer | — | TASK-072, TASK-073, TASK-074 | Branch `task/TASK-075-seed-check-gate`. Spec 006 §13's 2026-09-09 resolution note is binding: Q1 AI-generated under the locked style guide or free-licence stock (CC0 / public-domain / Unsplash-licence class, never paid stock); Q2 names stay English, descriptors localise; Q3 12 products x 2 assets plus the homepage slots; Q4 <=6 MB derived bytes committed until R2; Q5 AVIF+WebP at seven widths plus one 1200 px JPEG per asset for OG/email; Q6 originals in the founder's store plus a git-ignored `.local/imagery/originals/`; Q7 `fo-media` / `fo-media-preview` / `fo-backups`, `media.flowersoverseas.com`; Q8 both lawyer questions on the October legal-drafts list, label ships now; Q9 founder signs off against the §2.4 checklist; Q10 spec 002 task 10 reads `seed/data/`; Q11 the seven media columns fold into `0003` (spec 002 §14 A1); Q12 watermark mechanism kept, no watermarked asset in Phase 0. The nine families: schema/`version` parse; counts and split (84 in 40/14/8/10/12, 23 categories, 6 add-ons); referential integrity (facet, category/occasion edge, price row, media asset); **slugs** (ASCII, lowercase, hyphenated, no trailing slash, unique per locale **across products, categories and occasions**, equal to the ASCII fold of the name — `Kraków Spring` → `krakow-spring`, AC-7); prices (band, integer, currency, ending, tier step tolerance, single open-ended row); copy (60–90 words at the 59/60/90/91 boundaries, closing local-florist sentence, banned superlatives, **duplicate description across two products**, missing `en` name or slug); media (complete provenance, one `is_primary` per product, variant present with matching checksum, alt text per launch locale, no empty alt on a product image); **no PII and no third-party marks** (`@`-shaped, E.164-shaped, postcode-shaped, real-person allowlist, competitor brand names from `docs/research/competitors-*.md`) reported with the file **and the JSON path** (AC-9); and byte budgets. One line per problem, naming file, entity key and rule; exit 0 on the merged tree. The step summary is the standing catalogue-health report of §11 — product counts by type, the six-product-rule coverage table (`plan/02` §6, so 008 can test both sides of the threshold and the PL set is guaranteed to pass), price-band outliers, description word-count distribution, copy review shares per locale, committed image bytes, per-slot maxima and the count of products still on a placeholder. **A locale or country must not be able to look ready in CI while it is gated in code.** Job placement per spec 001 §14 A9 (`needs: typecheck`, as `i18n-check` and `catalogue-check` do) plus `scripts/branch-protection.ts` (AC-30). CI minutes: gates locally, one push, `gh pr ready` (TASK-057). Gates: `seed-check`, `lint`, `typecheck`, `test-unit`, `build`. Tests: T-07, T-09, T-10, T-30. |
| TASK-076 | `pnpm seed:diff`: the `SeedTarget` interface, `snapshotTarget`, the differ, the stable per-table report (inserts / updates / unchanged / conflicts) and the committed JSON snapshot | `specs/006` · AC-11 | 0 | todo | backend-implementer | — | TASK-075 | Branch `task/TASK-076-seed-diff`. Spec 006 §13's 2026-09-09 resolution note is binding: Q1 AI-generated under the locked style guide or free-licence stock (CC0 / public-domain / Unsplash-licence class, never paid stock); Q2 names stay English, descriptors localise; Q3 12 products x 2 assets plus the homepage slots; Q4 <=6 MB derived bytes committed until R2; Q5 AVIF+WebP at seven widths plus one 1200 px JPEG per asset for OG/email; Q6 originals in the founder's store plus a git-ignored `.local/imagery/originals/`; Q7 `fo-media` / `fo-media-preview` / `fo-backups`, `media.flowersoverseas.com`; Q8 both lawyer questions on the October legal-drafts list, label ships now; Q9 founder signs off against the §2.4 checklist; Q10 spec 002 task 10 reads `seed/data/`; Q11 the seven media columns fold into `0003` (spec 002 §14 A1); Q12 watermark mechanism kept, no watermarked asset in Phase 0. **One differ, two targets** — `snapshotTarget` now, `dbTarget` in TASK-083 — so the command the founder learns in Phase 0 is the command that runs against the database later, and `db:seed`'s report shape is this report's shape (spec 002 AC-29 extended to media and copy, AC-26 on TASK-083). AC-11: zero inserts / zero updates / zero conflicts on the merged tree; a snapshot with one changed price reports **exactly one** update naming the SKU, country, tier and **both** amounts; `--dry-run` writes nothing, asserted by a clean `git status` **and** an unchanged snapshot mtime. The report is the audit trail of §11 — post-002 every run also prints the cache tags it dirtied with `request_id` and `actor = system:seed` (`plan/11` §1's actor convention). Runs entirely offline with `DATABASE_URL` unset. Gates: `lint`, `typecheck`, `test-unit`, `seed-check`, `check:no-db`. Tests: T-11. |
| TASK-077 | Imagery intake, style guide and provenance: `content/imagery/style-guide.md` (the locked `plan/10` §3 style guide verbatim, the prompt template and the human review checklist), `content/imagery/prompts/{sku}.json` for the demo set, the git-ignored `.local/imagery/originals/` intake directory, and `media.json` wired to `MediaAssetManifestSchema`'s provenance refinements | `specs/006` · AC-8 | 0 | todo | backend-implementer | — | TASK-072 | Branch `task/TASK-077-imagery-style-guide-provenance`. Spec 006 §13's 2026-09-09 resolution note is binding: Q1 AI-generated under the locked style guide or free-licence stock (CC0 / public-domain / Unsplash-licence class, never paid stock); Q2 names stay English, descriptors localise; Q3 12 products x 2 assets plus the homepage slots; Q4 <=6 MB derived bytes committed until R2; Q5 AVIF+WebP at seven widths plus one 1200 px JPEG per asset for OG/email; Q6 originals in the founder's store plus a git-ignored `.local/imagery/originals/`; Q7 `fo-media` / `fo-media-preview` / `fo-backups`, `media.flowersoverseas.com`; Q8 both lawyer questions on the October legal-drafts list, label ships now; Q9 founder signs off against the §2.4 checklist; Q10 spec 002 task 10 reads `seed/data/`; Q11 the seven media columns fold into `0003` (spec 002 §14 A1); Q12 watermark mechanism kept, no watermarked asset in Phase 0. Style guide verbatim from `plan/10` §3: neutral warm-grey seamless background, soft north light from the left, 4:5 with 8% margin, kraft or white wrap, **no hands, no faces, no text**, one hero + one detail + one in-home context shot per product, colour-accurate to the colour facet, plausible stem count for the tier, funeral pieces on neutral stone, plants in plain terracotta or white. Review checklist: impossible stems, melted petals, wrong flower, implausible count, visible text, any person or premises. **AC-8 makes provenance data, not a comment**: every asset carries `source` (`ai \| photo \| partner`) and `depicts` (`product \| brand \| context \| delivery`); `ai` assets carry generator, model, `prompt_hash` (SHA-256 of the canonicalised prompt record) and seed; `photo` assets carry `credit` and `licence`; `approved` assets carry `reviewed_by` and `reviewed_at`; and a `depicts: "delivery"` asset is **rejected outright in Phase 0** — real delivery photos are specs 018/027 and carry consent (`plan/07` §1.2). **No original is ever committed** (§13 Q6): originals live in the founder's own store of record plus the git-ignored `.local/imagery/originals/`, and move to R2 `originals/{assetId}` in TASK-082. **Founder actions gating this task (row note, not a task):** confirm the generator and **file its commercial-use terms in `docs/compliance/`** — that is ADR-0014's own standing condition — and note that `licence` + `credit` are mandatory for the free-licence stock class (CC0 / public-domain / Unsplash-licence) that §13 Q1 permits for the hero and delivery band. The two lawyer questions of §13 Q8 (label wording under DE/PL transposition, EU AI Act Art. 50 reach) go on the October legal-drafts list and **do not block** — no money changes hands during the demo phase; the reading recorded in §8 is that Art. 50 does not reach a labelled, non-deceptive product illustration, and provenance stays in data so a wrong reading is a configuration change. Gates: `lint`, `typecheck`, `test-unit`, `seed-check`, `check:no-db`. Tests: T-08. |
| TASK-078 | `pnpm media:variants`: the deterministic `sharp` ladder (AVIF q50/effort 4 + WebP q72 at 384/640/828/1080/1200/1600/1920 plus one 1200 px JPEG per asset for OG/email), unconditional EXIF/GPS stripping with the colour profile kept, the declared aspect ratios, manifest writing to `seed/data/media-variants.json`, and the `--check` CI mode | `specs/006` · AC-12, AC-13, AC-14 | 0 | todo | backend-implementer | — | TASK-077 | Branch `task/TASK-078-media-variants-cli`. Spec 006 §13's 2026-09-09 resolution note is binding: Q1 AI-generated under the locked style guide or free-licence stock (CC0 / public-domain / Unsplash-licence class, never paid stock); Q2 names stay English, descriptors localise; Q3 12 products x 2 assets plus the homepage slots; Q4 <=6 MB derived bytes committed until R2; Q5 AVIF+WebP at seven widths plus one 1200 px JPEG per asset for OG/email; Q6 originals in the founder's store plus a git-ignored `.local/imagery/originals/`; Q7 `fo-media` / `fo-media-preview` / `fo-backups`, `media.flowersoverseas.com`; Q8 both lawyer questions on the October legal-drafts list, label ships now; Q9 founder signs off against the §2.4 checklist; Q10 spec 002 task 10 reads `seed/data/`; Q11 the seven media columns fold into `0003` (spec 002 §14 A1); Q12 watermark mechanism kept, no watermarked asset in Phase 0. Aspect ratios per §13 Q5: 4:5 product, 1:1 occasion tile, 16:9 desktop / 4:3 mobile hero band, 3:2 context. **Determinism is the contract** (AC-13): the `sharp` version and every encoder option are **pinned and recorded in the manifest header**, two runs produce byte-identical files and no manifest diff, and changing one option is a manifest-wide diff rather than a silent re-encode — which is also what lets TASK-082's `media.derive_variants` worker prove it produces the same checksums as this CLI (AC-25). **Generation never runs in CI** (no originals there, no network, no generator); CI runs `--check` only, verifying every manifest entry has a file, every file has an entry and every checksum matches, and failing on a deleted file, an added file or a one-byte edit (AC-14). Metadata stripping is unconditional and asserted by reading the output's metadata (`plan/01` §6); any C2PA marking the generator embeds is preserved (§8). `sharp` is a **devDependency used by a local CLI, never at request time**, so its CVE surface is outside the deployed runtime — record the pinned version and keep it inside `pnpm audit`'s scope. Gates: `lint`, `typecheck`, `test-unit`, `audit`, `seed-check`, `check:no-db`. Tests: T-12 (three fixture originals: portrait, square, landscape), T-13, T-14. |
| TASK-079 | Media loader, `Photo`'s asset path and the honesty note: `src/modules/ui/media/` — `manifest.ts`, the `slots.ts` extension, `staticVariantLoader` + `resolveLoader()`, `Photo`'s asset path with the placeholder fallback, `preload.ts` (the single LCP candidate and its matching `<link rel="preload">`), `MediaProvenanceNote`, and the gallery/placeholder/note states in `/dev/components` | `specs/006` · AC-2, AC-17, AC-18, AC-19 | 0 | todo | frontend-implementer | — | TASK-078, TASK-052 | Branch `task/TASK-079-media-loader-photo-provenance`. Spec 006 §13's 2026-09-09 resolution note is binding: Q1 AI-generated under the locked style guide or free-licence stock (CC0 / public-domain / Unsplash-licence class, never paid stock); Q2 names stay English, descriptors localise; Q3 12 products x 2 assets plus the homepage slots; Q4 <=6 MB derived bytes committed until R2; Q5 AVIF+WebP at seven widths plus one 1200 px JPEG per asset for OG/email; Q6 originals in the founder's store plus a git-ignored `.local/imagery/originals/`; Q7 `fo-media` / `fo-media-preview` / `fo-backups`, `media.flowersoverseas.com`; Q8 both lawyer questions on the October legal-drafts list, label ships now; Q9 founder signs off against the §2.4 checklist; Q10 spec 002 task 10 reads `seed/data/`; Q11 the seven media columns fold into `0003` (spec 002 §14 A1); Q12 watermark mechanism kept, no watermarked asset in Phase 0. Depends on TASK-052 for spec 004's `Media` wrapper, `slots.ts` and `placeholderLoader` — **do not dispatch this while TASK-052…TASK-056 are in flight**, it is the same module. Design source of truth (match pixel-for-pixel): `docs/design/homepage-v1/README.md`, `docs/design/homepage-v1/tokens.css`, `docs/design/homepage-v1/homepage-desktop.dc.html`, `docs/design/homepage-v1/homepage-mobile.dc.html`, `docs/design/homepage-v1/identity.dc.html`, `content/brand/mark.svg`. **AC-2 is the R2-flip proof**: swapping `staticVariantLoader` for a fake `VariantLoader` changes every rendered image URL with **zero** changes outside `src/modules/ui/media/`. The manifest is a plain typed build-time import — **no runtime fetch, no database, no `sharp` in the bundle**. **AC-18 is the accessibility and honesty rule in one**: `Photo` renders an `<img>` **only** when the asset is approved, has variants **and** has alt text for the resolved locale; with any of the three missing it renders spec 004's placeholder box with its caption and **no `<img>`** — so a missing Polish alt degrades to a placeholder instead of shipping an English alt on a Polish page (WCAG 1.1.1 + 3.1.2, `plan/07` §8). `alt` is **data, never generated at render**, never empty on a product image and never derived from the product name. **AC-19**: exactly one `priority` candidate per page, its `<link rel="preload" as="image" imagesrcset imagesizes>` emitted from the *same* manifest lookup that produced the `srcset` so the two cannot disagree, and a two-`priority` fixture page **fails** the test; nothing eager above the LCP candidate. **AC-17**: `MediaProvenanceNote` renders "example arrangement · your florist hand-makes each one" as a **message key** whenever any displayed asset has `source: "ai"`, server-rendered and crawlable (so the structured data and the visible page agree, `plan/02` §15), in all four locales pre-hydration, absent on a page with no image, and **not suppressible — the component's prop type admits no such flag** (ADR-0014's condition, UCPD/CRD and UK DMCC misleading-action risk, `plan/07` §2.1/§2.2). Photographs are **never** mirrored: assert no `Photo` output carries `@utility mirror-in-rtl` (§7). `sizes` per `plan/01` §6 (hero 100vw, grid 50vw/33vw/25vw, thumb 96 px); AVIF-first `srcset` with WebP fallback; `loading="lazy"` + `decoding="async"` below the fold; fixed aspect-ratio boxes and logical properties only (`fo/no-physical-css`). `/media/*` served `Cache-Control: public, max-age=31536000, immutable` from `next.config.ts`; a changed image is a **new asset version, never a mutated URL**. **Note for spec 007 (recorded here and in `docs/runbooks/imagery.md`, TASK-081):** when 007 lifts `Disallow: /`, `/media/*` and later the R2 host **must stay crawlable** or Google cannot fetch the images it evaluates for Core Web Vitals — blocking it is the classic own-goal. Gates: `lint`, `typecheck`, `test-unit`, `build`, `budget:client-js`, `test:e2e`, `test:a11y`, `test:visual`. Tests: T-02, T-17, T-18, T-19. |
| TASK-080 | Imagery commit, budgets and the honesty gates: generate/review/commit the spec 004 homepage photo slots plus the 12-product x 2-asset demo set, the per-slot and 6 MB byte caps, page image transfer and Lighthouse (LCP < 2.0 s, CLS < 0.05), the watermark check, the client-JS +-0 B measurement, axe, `ar-XB` visual baselines and the data-flip proof | `specs/006` · AC-15, AC-16, AC-20, AC-21, AC-22, AC-23 | 0 | todo | frontend-implementer | — | TASK-079, TASK-056 | Branch `task/TASK-080-imagery-budgets-honesty-gates`. Spec 006 §13's 2026-09-09 resolution note is binding: Q1 AI-generated under the locked style guide or free-licence stock (CC0 / public-domain / Unsplash-licence class, never paid stock); Q2 names stay English, descriptors localise; Q3 12 products x 2 assets plus the homepage slots; Q4 <=6 MB derived bytes committed until R2; Q5 AVIF+WebP at seven widths plus one 1200 px JPEG per asset for OG/email; Q6 originals in the founder's store plus a git-ignored `.local/imagery/originals/`; Q7 `fo-media` / `fo-media-preview` / `fo-backups`, `media.flowersoverseas.com`; Q8 both lawyer questions on the October legal-drafts list, label ships now; Q9 founder signs off against the §2.4 checklist; Q10 spec 002 task 10 reads `seed/data/`; Q11 the seven media columns fold into `0003` (spec 002 §14 A1); Q12 watermark mechanism kept, no watermarked asset in Phase 0. Depends on TASK-056 as well as TASK-079: TASK-056 makes `lighthouse` blocking on five URLs with the restated 131 072 B Brotli budget, and this task's AC-22 measures **+-0 bytes of application code** for `/`, `/en`, `/de` against it, with neither `sharp` nor the variant CLI in any client chunk. The Phase-0 asset set is **not 252** (§13 Q3): spec 004's homepage photo slots (full-bleed hero, six occasion tiles, four "most sent" cards, the delivery band — the consolidated list is TASK-053's PR body) plus 12 products x 2 assets (hero + detail), one per named archetype of `plan/10` §2.2. **Every other product keeps the captioned `--color-photo` placeholder and no `<img>`** (`plan/10` §3 honesty rule), and **the delivery band stays a placeholder in every case** because we have no delivery photo and may not fake one. **AC-20 is the data-flip proof**: adding one asset row, its variants and its alt text turns a homepage placeholder slot into a rendered image with **no change under `src/app/`**, no component change and a CLS delta of 0 across the swap. **AC-21 keeps `plan/09` Phase 0 AC 6 and spec 004 AC-15 green**: no review, rating, star, testimonial, review count, Trustpilot mark, partner name, partner photo, delivery-photo claim or florist count anywhere rendered, and zero rows in `review`, `delivery_proof` and public partner profiles. **AC-15** budgets: <=204 800 B image transfer per page (hero LCP candidate <=90 000 B at mobile width, each grid tile <=18 000 B), total committed bytes under `public/media/` <=6 MB, per-slot caps enforced by `seed:check` **before** the bytes are committed so a 900 KB hero fails a gate rather than a Lighthouse run. **AC-16**: the watermark mechanism and its pixel check exist and **no watermarked asset ships in Phase 0** (§13 Q12) — assert both. **AC-23**: axe over the gallery, placeholder and provenance-note states in `/dev/components` and a demo PDP fixture, zero serious/critical, **no exception list**. `ar-XB` visual baselines gain the first real photographic layout — the first time the pseudo-RTL gate has images to mirror. **Founder actions gating this task (row notes, not tasks):** (1) generate the demo-set imagery against the locked style guide and **supply real photography or free-licence stock for the homepage photo slots**; (2) **sign off every asset against the §2.4 review checklist** — §13 Q9 makes the founder the signer, any asset failing one checklist item is **regenerated from the same prompt with a new seed, never retouched**, and the accept/reject counts with their reasons are recorded in this PR's body and in `docs/runbooks/imagery.md` (that count is the honest measure of ADR-0014's named risk); (3) originals stay in the founder's own store and the git-ignored local folder — never committed. **Bounded deviation to record in `docs/architecture.md` §4:** derived image bytes are committed to the repository in Phase 0 (a build artefact in version control), accepted at <=6 MB for four weeks, and the row closes with TASK-083's R2 flip. Gates: `lint`, `typecheck`, `test-unit`, `build`, `seed-check`, `budget:client-js`, `lighthouse` (now blocking), `test:e2e`, `test:a11y`, `test:visual`. Tests: T-15, T-16, T-20, T-21, T-22, T-23. |
| TASK-081 | Docs, records and the no-database seam close (spec 006 Phase-0 close): `docs/runbooks/imagery.md` and `docs/runbooks/seed-catalogue.md` with the review-and-reject loop executed once and its counts recorded, the RoPA "no personal data" statement plus the conditional row, the filed generator terms, `docs/architecture.md` §2/§3/§4, `README.md`'s four new scripts, and the whole-seam assertion that `build` / `test` / `seed:check` / `media:variants --check` all pass with `DATABASE_URL` unset and no network | `specs/006` · AC-1, AC-28, AC-29 | 0 | todo | backend-implementer | — | TASK-080 | Branch `task/TASK-081-seed-imagery-docs-records`. Spec 006 §13's 2026-09-09 resolution note is binding: Q1 AI-generated under the locked style guide or free-licence stock (CC0 / public-domain / Unsplash-licence class, never paid stock); Q2 names stay English, descriptors localise; Q3 12 products x 2 assets plus the homepage slots; Q4 <=6 MB derived bytes committed until R2; Q5 AVIF+WebP at seven widths plus one 1200 px JPEG per asset for OG/email; Q6 originals in the founder's store plus a git-ignored `.local/imagery/originals/`; Q7 `fo-media` / `fo-media-preview` / `fo-backups`, `media.flowersoverseas.com`; Q8 both lawyer questions on the October legal-drafts list, label ships now; Q9 founder signs off against the §2.4 checklist; Q10 spec 002 task 10 reads `seed/data/`; Q11 the seven media columns fold into `0003` (spec 002 §14 A1); Q12 watermark mechanism kept, no watermarked asset in Phase 0. Split out of spec 006 §12's task T9 by the orchestrator because T9 as written (generate + review + commit imagery, six gate families, visual baselines, the data-flip proof, two runbooks, RoPA, README and the architecture doc) is several days, not one — the same split precedent as spec 004 §12 task 5 → TASK-052…TASK-055. **AC-1 is the whole no-provisioning seam asserted in one test**: `pnpm build`, `pnpm test`, `pnpm seed:check` and `pnpm media:variants --check` all succeed with `DATABASE_URL` unset and MSW `onUnhandledRequest: "error"`, and `pnpm check:no-db` covers **every** file added by §2.2–§2.5 with none importing `src/lib/db*`, `drizzle*`, `pg` or `postgres`. **AC-28** requires the imagery runbook's review-and-reject loop to have been **executed once** for the demo product set with the outcome recorded (generated / accepted / rejected per batch and the rejection reasons) — T-28 is a manual, reviewer-recorded test; do not mark it done on an unexecuted runbook. **AC-29 compliance records:** `docs/compliance/ropa.md` gains the explicit "spec 006 introduced imagery and catalogue data with **no personal data**" statement **and** the conditional row that must be written the moment the founder supplies photography containing an identifiable person or a named florist's shopfront (that would be personal data needing a lawful basis, a model/property release and a retention decision); the generator's commercial-use terms are filed in `docs/compliance/`; `docs/architecture.md` §2/§3 list the new directories and `ui/media` ownership and §4 carries the committed-bytes deviation row; `README.md` documents `seed:check`, `seed:diff`, `media:variants` (and `media:upload` when TASK-082 lands). Both runbooks are added to the runbook index: `seed-catalogue.md` covers editing a product, adding a country's prices, running the gate, reading the diff, re-seeding safely and what "never touch `source='real'`" means operationally (`plan/10` §4); `imagery.md` covers generate/review/reject/regenerate-from-seed, `media:variants`, the budget check, upload after R2, replacing an AI image with a real photo, the `/media/*` crawlability requirement on spec 007, and the R2 storage/egress probe to add beside spec 002's Neon usage probe (`plan/13` D1). Gates: full local chain then one push and `gh pr ready`. Tests: T-01, T-28 (manual, recorded), T-29. **Spec 006 Phase-0 exit signal: this PR merged with a recorded `/review` pass, with TASK-082 and TASK-083 `blocked` on provisioning rather than dropped.** |
| TASK-082 | R2 storage, upload and the variant worker: `src/lib/storage.ts`'s real S3-API implementation over R2 (`put`, `head`, `getSignedUrl`, `delete`, `objectKey`) passing the same contract suite as `InMemoryStorage`, `pnpm media:upload` (originals to `originals/{assetId}`, idempotent by checksum, enqueuing per asset), and the `media.derive_variants` pg-boss worker | `specs/006` · AC-24, AC-25 | 0 | blocked | backend-implementer | — | TASK-078, TASK-013 | Branch `task/TASK-082-r2-storage-upload-derive-job`. **Blocked on TASK-013** (spec 002 provisioning parked 2026-09-08 by the founder: no Neon project, no Cloudflare account, no R2 bucket — waiting on the domain and business mailbox, `plan/09` §0 non-code critical path first). Also needs TASK-017 (`media_asset`/`media_variant`/`product_media` tables and the `lib/storage.ts` interface + `objectKey()` + `InMemoryStorage` fake) and TASK-024 (pg-boss, where `media.derive_variants` is already a *declared* handler this task fills). Spec 006 §13's 2026-09-09 resolution note is binding: Q1 AI-generated under the locked style guide or free-licence stock (CC0 / public-domain / Unsplash-licence class, never paid stock); Q2 names stay English, descriptors localise; Q3 12 products x 2 assets plus the homepage slots; Q4 <=6 MB derived bytes committed until R2; Q5 AVIF+WebP at seven widths plus one 1200 px JPEG per asset for OG/email; Q6 originals in the founder's store plus a git-ignored `.local/imagery/originals/`; Q7 `fo-media` / `fo-media-preview` / `fo-backups`, `media.flowersoverseas.com`; Q8 both lawyer questions on the October legal-drafts list, label ships now; Q9 founder signs off against the §2.4 checklist; Q10 spec 002 task 10 reads `seed/data/`; Q11 the seven media columns fold into `0003` (spec 002 §14 A1); Q12 watermark mechanism kept, no watermarked asset in Phase 0. **Founder actions gating it:** provision Cloudflare with an **EU-jurisdiction** media bucket and a scoped API token per spec 002 §12 founder action 2, using §13 Q7's binding names — buckets `fo-media` (public, EU jurisdiction), `fo-media-preview`, `fo-backups` (private, spec 002's) and public origin `https://media.flowersoverseas.com` as a Cloudflare custom domain, a first-party origin so `img-src` gains one entry and no third-party host; accept and file the Cloudflare DPA. **AC-24**: the `Storage` contract suite produces identical observable results against `InMemoryStorage` and a real bucket, and `objectKey()` output matches spec 002's fixtures exactly. **AC-25**: the worker produces the **same ladder and the same checksums as TASK-078's CLI** for the same original (which is why that CLI's encoder options are pinned in the manifest header), writes each `media_variant` row **exactly once per (asset, variant, format)**, and a second run inserts nothing and re-encodes nothing (ADR-0015: "size variants generated once by a job"). The fake stays for tests; R2 credentials are env-only and scoped to the media bucket. No object key, prompt, file path or asset id is logged at `info` in a request path. Gates: `lint`, `typecheck`, `test-unit`, `test-integration`, `test:contract`, `db:check`, `audit`, `build`. Tests: T-24, T-25. |
| TASK-083 | Seed importer and the R2 loader flip: `seed/index.ts`'s content half (`--only=catalogue\|copy\|media\|prices\|all`, `--dry-run`, `--report`, idempotent upserts by natural key, never touching `source = 'real'`, the dirtied-tag list) plus `r2VariantLoader` + `resolveLoader()` flip, deletion of `public/media/` and `staticVariantLoader`, the CSP `img-src` change and the RoPA/runbook R2 halves | `specs/006` · AC-26, AC-27 | 0 | blocked | backend-implementer | — | TASK-076, TASK-082, TASK-013 | Branch `task/TASK-083-seed-importer-r2-loader-flip`. **Blocked on TASK-013** for the same reason as TASK-082, and additionally on TASK-026 (spec 002's seed and shared fixtures — spec 002 §14 A1(d) makes its task 10 read `seed/data/**` through this spec's schemas and spec 005's projections, so 002 keeps the tables, the upsert plumbing, partners and geo rows while the catalogue content comes from here). Spec 006 §13's 2026-09-09 resolution note is binding: Q1 AI-generated under the locked style guide or free-licence stock (CC0 / public-domain / Unsplash-licence class, never paid stock); Q2 names stay English, descriptors localise; Q3 12 products x 2 assets plus the homepage slots; Q4 <=6 MB derived bytes committed until R2; Q5 AVIF+WebP at seven widths plus one 1200 px JPEG per asset for OG/email; Q6 originals in the founder's store plus a git-ignored `.local/imagery/originals/`; Q7 `fo-media` / `fo-media-preview` / `fo-backups`, `media.flowersoverseas.com`; Q8 both lawyer questions on the October legal-drafts list, label ships now; Q9 founder signs off against the §2.4 checklist; Q10 spec 002 task 10 reads `seed/data/`; Q11 the seven media columns fold into `0003` (spec 002 §14 A1); Q12 watermark mechanism kept, no watermarked asset in Phase 0. Natural keys: `product.sku`, `category.key`, `occasion.key`, `addon.key`, `media_asset.object_key`, `(product_id, locale_code)` for copy. **AC-26**: three runs produce identical row counts and identical `updated_at` values after the first, a row flipped to `source = 'real'` **survives untouched** (`plan/10` §4), and the report shape equals `seed:diff`'s (spec 002 AC-29 extended to media and copy). **AC-27 and the one real hazard in this spec**: after the flip every image URL is served from `R2_PUBLIC_BASE_URL`, `public/media/` and `staticVariantLoader` are **deleted in the same PR** so there is never more than one image origin, both environments' CSP `img-src` is asserted as a **whole string** (ADR-0016, spec 004 AC-23), and the rendered-markup diff is **origin-only**. **Reverting the loader flip after `public/media/` is deleted leaves no image origin — the flip and the deletion must be one commit and be reverted together** (the same pairing hazard spec 004 §12 recorded for its budget/CSP tasks). Rollback order post-002: revert the flip first (it is the only user-visible step), then delete `source='seed'` media rows, then the additive migration's `.down.sql`. `docs/compliance/ropa.md`'s Cloudflare row (written by spec 002) gains **product imagery** as a data category; `docs/architecture.md` §4's committed-bytes deviation row **closes here**; both runbooks gain their R2 halves. Migration order for the whole post-provisioning sequence, recorded so nobody reorders it: spec 002 `0003` (with the spec 002 §14 A1 amendments) → `db:seed --only=catalogue,copy,prices` → `media:upload` → `media.derive_variants` → loader flip. Gates: `lint`, `typecheck`, `test-unit`, `test-integration`, `test:contract`, `db:check`, `seed-check`, `build`, `test:e2e`, `test:visual`, `lighthouse`. Tests: T-26, T-27. **ADR-0017 clarification (orchestrator, PR 38):** the projected `seed/data/*.json` files carry the authored camelCase records byte-identical to `src/config/catalogue/`; the projection onto spec 002 §5.1's columns happens in the importer through the `to*Row()` functions re-exported from `seed/schema/catalogue.ts` — one import site, never a second row shape. |
| TASK-084 | Brand-voice copy pass on shipped message keys (spec §14 A5): rewrite `nav.*`, `footer.*`, `company.*`, `destinations.*`, `consent.*` and the header/footer trust sentences in the first person ("we", "our florist in …", "our team"), remove "relay"/"corridor"/"partner"/"third party" from customer copy, cap geographic claims at live coverage ("across Europe"), prefer specific over superlative; `de`/`pl` re-drafted deterministically; a `no-literal-strings`-style lint or unit scan that fails on the banned words in `messages/*.json` and `src/` | `specs/004` · §14 A5 · AC-14 unchanged | 0 | todo | frontend-implementer | — | TASK-048, TASK-049 | Branch `task/TASK-084-brand-voice-copy`. Founder, 2026-09-09. Copy only — no component, layout or behaviour change; `en.meta.json` attestations reset to `reviewed: false` for rewritten keys until the founder skims them; the honesty guardrails in A5 are binding (no "anywhere in the world", no "own shops"). Runs after the header and footer merge so it edits keys that exist. **Started 2026-09-09 by the orchestrator on main:** `company.description` rewritten to the round-2 colophon sentence ("We send flowers across Europe. You order from us; our florist in the recipient's town makes the bouquet and hands it over in person.") because TASK-059 round 2 changed the artboard the `company-config` test pins; `de`/`pl` re-drafted; footer test pin updated. The rest of the pass remains. |
| TASK-085 | Client-JS budget option (b): drop `NextIntlClientProvider` and the client message payload from locale documents — the spec 003 suggestion banner island and any other client consumer take translated strings as props from the server; remove the `banner.*`/client message split; keep `useTranslations` server-only; re-measure with `budget:client-js` and a browser | `specs/004` · §14 A1 addendum · AC-25 precondition | 0 | todo | frontend-implementer | — | TASK-051, TASK-052 | Branch `task/TASK-085-strings-as-props-no-client-provider`. Brought forward from TASK-056 after TASK-051 measured 132 047 B br on locale documents (975 B over). Expected saving ~10 705 B br. Spec 003 AC-28's banner matrix (T-28) must re-run green unchanged; the consent and finder islands must already be provider-free (verify, do not rewrite). Runs before TASK-053. **From `/review 36`:** also fix `scripts/client-js-budget.ts`'s attribution of every island chunk group to the `/` entry (131 672 B charged vs 116 429 B fetched) so the chooser is measured as what a browser fetches. |

## Open decisions blocking tasks
Mirrored from `plan/13-open-questions.md` §A. Owner: Ahmed (founder) unless noted. Due: 2026-09-21. Resolved rows stay until the source table is updated.

| # | Question | Blocks | Owner | Due | Default if unanswered | Status |
|---|---|---|---|---|---|---|
| A1 | Confirm the four launch URL prefixes `en`, `en-gb`, `de`, `pl`; any objection to `en-gb` as a separate locale? | spec 003, 007 | Ahmed | 2026-09-21 | Yes, four prefixes | resolved 2026-09-08 (default accepted, spec 003 §13) |
| A2 | Homepage country pick lands on the corridor page vs the country shop root | spec 004, 007 | Ahmed | 2026-09-21 | Corridor; A/B test #1 | resolved 2026-09-08 (spec 004 §13, design canvas) |
| A3 | Approve taxonomy and the 84-product seed list shape; veto any product names | spec 005, 006 | Ahmed | 2026-09-21 | As written | resolved 2026-09-08 (defaults accepted, spec 002 §13 Q8) |
| A4 | Seed price bands and payout shares as starting values | spec 005 | Ahmed | 2026-09-21 | As written; validate with first PL florists | resolved 2026-09-08 (defaults accepted, spec 002 §13 Q8) |
| A5 | Polish UI register: formal (Państwo) vs informal (Ty) for buyer-facing copy | `pl` translations (spec 003 onward) | Ahmed + native reviewer | 2026-09-21 | Informal in UI, formal in legal | resolved 2026-09-08 (default accepted, spec 003 §13) |
| A6 | Name of the guarantee and its exact terms (refund %, redelivery) | spec 004; plan/07 §2 | Ahmed | 2026-09-21 | 7-day freshness, redeliver-or-refund | resolved 2026-09-08 (spec 004 §13, design canvas) |
| A7 | Which six demo destinations besides PL get written guides in Phase 0 | spec 007 content | Ahmed | 2026-09-21 | DE, FR, ES, IT, RO, NL as proposed | resolved 2026-09-08 (spec 004 §13, design canvas) |
| A8 | Authorise Ahrefs (and optionally Firecrawl) connectors for demand re-runs | plan/02 §13, plan/03 §3 orderings | Ahmed | 2026-09-21 | Orderings stay estimate-grade (flagged) | open |
| A9 | Brand assets: logo, palette, type — existing or minimal system in spec 004? | spec 004 | Ahmed | 2026-09-21 | Spec 004 creates a minimal token system | resolved 2026-09-08 (spec 004 §13, design canvas) |
| A10 | GitHub repo name/visibility and owning account | week-1 tasks; spec 001 TASK-007, TASK-011 | Ahmed | 2026-09-21 | Private repo under personal account | resolved 2026-09-07 — spec 001 §13 Q1: private repo `flowers-overseas` under the founder's personal account |

## Log
| Date | Change |
|---|---|
| 2026-09-05 | File created by planning session; no tasks yet |
| 2026-09-07 | `/plan-tasks specs/001-repo-dev-os-bootstrap.md`: TASK-001…TASK-012 created (all 32 ACs owned exactly once); open-decisions table populated with A1–A10 from plan/13 §A, A10 marked resolved; Phase 0 progress set to 1/12 specs, 0/12 tasks |
| 2026-09-07 | TASK-001 implemented on `task/TASK-001-scaffold`; PR #1 opened, status `in_review` (AC-1/2/3/32; T-01/02/03/33 run locally) |
| 2026-09-07 | `/review 1`: VERDICT PASS (inline review; reviewer subagent and `gh pr review` blocked by the session permission classifier, verdict recorded here instead of on the PR). PR #1 squash-merged as ab72c12; TASK-001 `done`; Phase 0 tasks 1/12 |
| 2026-09-07 | `/review 2`: VERDICT PASS (inline; AC-19/AC-20, 45 unit tests, CI lint/typecheck/build/commitlint/pr-policy green). Decision: keep `subject-case` from config-conventional; ledger commit subjects start lowercase. PR #2 squash-merged as ddfad59; TASK-002 `done`; Phase 0 tasks 2/12 |
| 2026-09-07 | TASK-003 implemented on `task/TASK-003-lint-css-i18n-rules`; PR #3 opened, status `in_review` (AC-4/5/6; T-04/05/06/07 + wiring test, 22 new unit tests; CI lint/typecheck/build/commitlint/pr-policy green) |
| 2026-09-07 | `/review 3`: VERDICT PASS (inline; AC-4/5/6, 67 unit tests, CI green, `pnpm lint:fixtures` exits 1 on 13 expected violations). Accepted deviations: Stylelint 17 has no `declaration-property-disallowed-list`, implemented via `property-disallowed-list` + `declaration-property-value-disallowed-list` (spec text to be corrected at TASK-012); executable form of AC-4/5 is `pnpm lint:fixtures`. PR #3 squash-merged; TASK-003 `done`; Phase 0 tasks 3/12 |
| 2026-09-07 | `/review 4`: VERDICT PASS (inline; AC-7/8/9 + float-money fixtures, 150 unit tests, CI green, live probe of `fo/no-direct-order-status-write` in `src/` fires). Notes: `fo/no-float-money` implemented but disabled until spec 005; AC-9 fixtures live in a mirrored `tests/fixtures/lint/src/` tree; boundary plugin comes from `eslint-config-next`. PR #4 squash-merged; TASK-004 `done`; Phase 0 tasks 4/12 |
| 2026-09-07 | `/review 5`: VERDICT PASS (reviewer agent; AC-10…13 reproduced incl. sentinel no-leak and production placeholder rejection; 192 unit tests; CI 6/6 green). RoPA row 1 (Sentry, EU region, inert until DSN set in TASK-007) added to `docs/compliance/ropa.md`. Non-blocking notes carried to TASK-007/TASK-012 rows; spec 002/005 should widen `name` redaction to `*name` (recipient_name). PR #5 squash-merged as 4f96cef; TASK-005 `done`; Phase 0 tasks 5/12 |
| 2026-09-07 | `/review 6`: VERDICT PASS (reviewer agent; AC-14/15 reproduced via local build+start incl. production-mode header check; 220 unit tests; CI 6/6). Deviations accepted: string `metadata.robots`, `robots-headers.ts` extraction, handler in `lib/health.ts`. Notes for spec 003: move request-id contract to `src/lib/request-id.ts` (health imports from `@/middleware`); T-15 should assert `application/json` prefix. PR #6 squash-merged; TASK-006 `done`; Phase 0 tasks 6/12 |
| 2026-09-07 | TASK-007 implemented (repo half) on `task/TASK-007-vercel-project`; draft PR #7 opened, status `blocked` (AC-29 code half: `vercel.json` `fra1`, `preview` CI gate, `ALLOW_PLACEHOLDER_ENV` hatch, PR #5/#6 follow-ups, `docs/runbooks/vercel-setup.md`; 250 unit tests; CI 6/6 green, `preview` red by design until the founder links the Vercel project and sets `VERCEL_AUTOMATION_BYPASS_SECRET`) |
| 2026-09-07 | `/review 7`: VERDICT PASS (reviewer agent; AC-29 verified live: preview 302→vercel.com/sso-api without bypass, 200 with bypass, `x-vercel-id` segment `fra1`, `X-Robots-Tag: noindex`; hatch semantics reproduced; 252 unit tests; CI 9/9). Vercel account facts: Hobby plan, Standard Protection, production branch `main`, `ALLOW_PLACEHOLDER_ENV=true` on preview+production. RoPA row 2 (Vercel) added, row 1 refreshed. Carry-forwards: spec 002 first task must delete `ALLOW_PLACEHOLDER_ENV` everywhere; spec 025 Sentry list gets `logentry` + `stacktrace.frames[].vars`; spec 002/025 DSN task verifies browser event carries a release. PR #7 squash-merged as c9cec53; TASK-007 `done`; Phase 0 tasks 7/12 |
| 2026-09-07 | `/review 8`: VERDICT PASS (reviewer agent; 273 unit / 1 skipped integration / 22 e2e / 2 visual / 1 a11y, all green locally and against the real preview; CI 14/14). Orchestrator decisions: axe `document-title` kept as a pinned equality exception (self-clears when spec 003 adds a title); `https://vercel.live` feedback script allowed on non-local targets only, CSP decision to spec 004. Row 26 table malformation repaired. Spec 004 note: the two visual baselines are byte-identical blanks today, so pseudo-RTL is a gate only once content lands. PR #8 squash-merged; TASK-008 `done`; Phase 0 tasks 8/12 |
| 2026-09-07 | `/review 9`: VERDICT FAIL — required: honest NO_FCP step summary, AC-23 correction queued on TASK-012, lighthouse excluded from required checks noted on TASK-011; fixed on the same PR |
| 2026-09-07 | `/review 9` re-check after fixes: PASS (orchestrator verified the three required changes inline; 342 unit tests; CI 16/17 with `lighthouse` informational-red by design). Spec 007 follow-ups from review: hreflang reciprocity should be (hreflang, href)-pair based and require self-listing (plan/02 §8); enforce exactly one `Offer` per `Product` (plan/02 §9). PR #9 squash-merged; TASK-009 `done`; Phase 0 tasks 9/12 |
| 2026-09-07 | `/review 10`: VERDICT PASS (reviewer agent; 60 shell assertions across guard/task.sh/stop-hook against the real hook scripts in isolated temp projects; active-task pointer and porcelain unchanged before/after; 360 unit tests; CI 16/17 with informational `lighthouse`). Notes carried to TASK-011 (errexit leak in lib.sh, job order) and TASK-012 (T-27 wording). Hardening candidate for a future task allowed to edit `.claude/bin/task.sh`: anchor the `\| TASK-NNN \|` grep to row starts. PR #10 squash-merged; TASK-010 `done`; Phase 0 tasks 10/12 |
| 2026-09-08 | `/review 11`: VERDICT PASS (reviewer agent; 486 unit tests with gitleaks; audit + gitleaks clean incl. planted `sk_live_` negative; branch-protection verifier + print-commands derived from ci.yml, 17 required contexts; CI 18/19 with informational `lighthouse`). Orchestrator applied repo merge settings: squash-only, PR_TITLE/PR_BODY, delete-branch-on-merge (verified via API). Branch protection itself blocked: private repo on GitHub Free (403) — founder decision pending, recorded on TASK-012. PR #11 squash-merged as c665a50; TASK-011 `done`; Phase 0 tasks 11/12 |
| 2026-09-08 | `/review 12`: VERDICT PASS (reviewer agent; 540 unit tests; T-31 clean clone → healthy `/api/health` in 13 s, 45 s incl. lint/typecheck/test; CI 17 jobs + Vercel green, `lighthouse` informational). PR #12 squash-merged; TASK-012 `done`. **Spec 001 exit signal reached: TASK-001…TASK-012 merged with recorded PASS; Phase 0 tasks 12/12.** Orchestrator fixed the paraphrased Next deprecation string in README/local-setup and appended §14 A11 (§12 "6/6" → "12/12"). Open founder items: GitHub Pro for branch protection; spec 002 first task deletes `ALLOW_PLACEHOLDER_ENV`. Next: `/spec 002-schema-v1` |
| 2026-09-08 | Post-close housekeeping: attempted to quote the literal Next 16 deprecation warning in README/local-setup; `tests/unit/docs.test.ts` pins the paraphrased symptom text and `tests/` is task-guarded, so the change was reverted (main green, 540 tests). Follow-up for the first spec 002/003 task that touches README: update the symptom cell and its test together. Production alias `https://flowers-overseas.vercel.app` serves 200 with `X-Robots-Tag: noindex` from `main`. |
| 2026-09-08 | `/plan-tasks specs/002-schema-v1.md`: TASK-013…TASK-031 created (19 tasks, all 34 ACs owned exactly once, §6–§8 mapped to ACs or named explicitly on the row). Order: provisioning/env → `db/` root + runner + `0001` → schema by module (i18n/geo → catalog → media → partners/customers → orders/payments → triggers → notifications/ops → auth) → RLS → jobs/probes → retention sweep → seed/fixtures → `db:check` + CI → backups → usage probe → portability/runbook → docs/RoPA close. §13 all resolved (Auth.js v5, `db/migrations/`, orderService to 015, session-variable RLS, shared preview DB, unpooled URL for pg-boss/migrations/backups, retention per plan/07 pending B1/B4, A3/A4 defaults); A3 and A4 marked resolved here. Phase 0 set to 2/12 specs, 12/31 tasks — which makes `PHASE_0_SPECS_APPROVED` in `scripts/tasks-open-decisions.ts` and its test assertion red until TASK-013 flips both to `2 / 12` (recorded on that row). Founder actions front-loaded on TASK-013 (Neon Frankfurt, Cloudflare + two R2 buckets, Vercel env values, delete `ALLOW_PLACEHOLDER_ENV` from both Vercel environments, Neon API key, Neon + Cloudflare DPAs); §12 item 7 already satisfied (`db/` is guarded today). |
| 2026-09-08 | Founder decision: stay on GitHub Free. Branch protection (spec 001 AC-21 enforcement half) recorded as an accepted deviation with compensating controls; see spec 001 §14 A10 resolution and `docs/runbooks/branch-protection.md`. `pnpm branch-protection` remains as a self-healing check. |
| 2026-09-08 | Founder parked spec 002 provisioning (TASK-013 `blocked`) to work the plan/09 §0 week-1 non-code path first: domain, mailbox, Search Console, GA4, Trustpilot, e-Residency, accountant, florist shortlist. Code work resumes at `/implement TASK-013` once Neon + R2 exist. |
| 2026-09-08 | TASK-034 implemented on `task/TASK-034-locale-routing-documents`; PR #15 opened, status `in_review` (AC-3/5/6/8/9; T-03/05/06/08/09; 643 unit tests, 46 e2e on the preview, a11y and visual unchanged; `next-intl@4.14.2` the only new runtime dependency). Document shape: spec 003 §5.3's accepted alternative — pass-through `src/app/layout.tsx`, one document per leaf — because the recommended two-root-layout arrangement was measured to break AC-8 on Next 16.3.4; recorded in `docs/architecture.md` §2, whose §4 `lang="en"` row is now deleted. |
| 2026-09-08 | `/plan-tasks specs/003-i18n-foundation.md`: TASK-032…TASK-043 created (12 tasks, all 32 ACs owned exactly once, §6–§8 mapped to ACs or named explicitly on the row). Order per spec §12: `proxy.ts` rename plus the widened `fo/no-geo-redirect` matcher first → locale/currency/address config with zod → `[locale]` routing and documents → `/` chooser, titles and the axe exception removal → formatters → `fo/no-adhoc-intl` and address block → catalogues and `i18n:draft` → review gate and `alternatesFor()` → `i18n:check` and its CI job → suggestion banner and `fo_locale` → pseudo-locales and the real `pseudo-rtl` project → docs, budgets and close. Spec §12's nine suggested tasks were split into twelve to keep each under a day (routing split from the chooser, the lint rule split from the formatters, catalogues split from the review gate and from `i18n:check`). AC-25 is owned by TASK-035, where the `document-title` equality exception must die, and re-verified for `/ar-XB` at TASK-042; AC-30's CI half is built at TASK-040 and verified at TASK-042. Zero founder actions and zero database access in any of the twelve tasks (§12), so spec 003 proceeds while TASK-013 stays parked. Carry-forwards discharged: README plus local-setup deprecation cell with its `tests/unit/docs.test.ts` pin and the `src/lib/request-id.ts` move (TASK-032), both `docs/architecture.md` §4 spec-003 rows (TASK-032, TASK-034, verified at TASK-043), the TASK-008 axe exception (TASK-035) and the `forcePseudoRtl` stub (TASK-042). Phase 0 tasks set to 12 / 43; "Specs approved" deliberately left at `2 / 12` — the orchestrator flips it to `3 / 12` together with `PHASE_0_SPECS_APPROVED` in `scripts/tasks-open-decisions.ts` and its test assertion, which must move in one commit. |
| 2026-09-08 | `/review 13`: VERDICT FAIL — required: plan/12 §6 `middleware`→`proxy` naming (spec 001 §14 A2), dead `next/server` Playwright mapping removed; fixed on the same PR |
| 2026-09-08 | `/review 13` re-check after fixes: PASS (orchestrator verified plan/12 §6 naming and the removed dead `next/server` mapping; 550 unit tests; 25 Playwright tests listed; CI 20/21 with informational `lighthouse`). Next 16 `proxy` runs on Node.js — repo has no edge runtime code; noted in logger/sentry comments. PR #13 squash-merged; TASK-032 `done`; Phase 0 tasks 13/43 |
| 2026-09-08 | `/review 14`: VERDICT PASS (reviewer agent; registry refinements, plan/02 §3/§4.1 transcription, ten currency exponents vs `Intl`, plan/03 §8 address formats all verified; 597 unit tests; CI 19/20 with informational `lighthouse`). Bookkeeping fixed: AC-4 split — TASK-033 partial, `MessageMetaSchema` clause added to TASK-038. Carry to spec 005: re-decide HUF `roundingStyle` (`x90` is the Hungarian convention). PR #14 squash-merged; TASK-033 `done`; Phase 0 tasks 14/43 |
| 2026-09-08 | `/review 15`: VERDICT FAIL — required: architecture §2 must record the shipped pass-through-root shape (not the rejected two-root-layout shape), accurate 404 mechanism, `dynamicParams = false` obligation recorded, lang-grep claim corrected; all documentation, fixed on the same PR |
| 2026-09-08 | `/review 15` re-check after fixes: PASS (orchestrator verified architecture §2 records the shipped pass-through-root shape with accurate 404 mechanism, `dynamicParams = false` obligation recorded, lang-grep 0; 647 unit tests; CI 19/20 with informational `lighthouse`). Deviations accepted: pass-through root + three documents; no per-locale 404 document on Next 16.3.4; `dynamicParams = false` per page as the unknown-locale gate. PR #15 squash-merged; TASK-034 `done`; Phase 0 tasks 15/43 |
| 2026-09-08 | `/review 16`: VERDICT PASS (reviewer agent; AC-7/25/26 verified incl. JS-disabled click-through and axe 10/10 with the `document-title` exception gone; central `dynamicParams = false` gate on the `[locale]` layout proven to cover a hypothetical `/fr/about`; 660 unit tests; CI 19/20 with `lighthouse` now measuring: script 304 KB vs 120 KB budget, perf 0.88 — carried to TASK-043). PR #16 squash-merged; TASK-035 `done`; Phase 0 tasks 16/43 |
| 2026-09-08 | `/review 17`: VERDICT PASS (reviewer agent; AC-15…18 reproduced incl. bigint exactness, float/unsafe rejection, ICU zone labels, Polish collation; 699 unit tests; CI 19/20 with informational `lighthouse`). Decision: `en` gets `formattingTag = en-150` while `lang` stays `en` → new TASK-044 inserted before TASK-039. Carry-forwards: bigint transport encoding (decimal string) must be decided before spec 005/010 money schemas; a zone-label-free `formatTime` variant is needed for the "14:00 Warsaw time" copy form (spec 007/009); `plan/03` §7 week-start row needs a mechanism (`Intl.Locale.getWeekInfo`) in the date-picker spec. PR #17 squash-merged; TASK-036 `done`; Phase 0 tasks 17/44 |
| 2026-09-08 | `/review 18`: VERDICT PASS (reviewer agent; 10/10 adversarial lint probes flagged, deny-by-default on `Intl`, address blocks PL/DE/AT/GB verified; 759 unit tests; CI 19/20). Carry to spec 010 (address forms): widen `formatAddressBlock` param to accept `AddressInput` (`string \| undefined` values); validate ISO code before calling the formatter (it throws on malformed input); use Ofcom/regulator drama phone ranges in fixtures. PR #18 squash-merged; TASK-037 `done`; Phase 0 tasks 18/44. Orchestrator switches to parallel execution of independent tasks (038 ∥ 044, later 041 ∥ 042). |
| 2026-09-08 | `/review 19`: VERDICT PASS (reviewer agent; before/after `en` table reproduced, `<html lang>` unchanged, `firstDay === 1` for all four locales; 769 unit tests; CI 19/20). Ruling: `formatMoney` keeps `formattingTag` (money and numbers must share one tag for separator consistency); `45.00 £` on `/en` for GBP is accepted, fix-if-needed lives in per-currency display config at spec 005, never a tag split. Spec 003 §14 amended by the orchestrator (AC-4 key list gains `formatting_tag`; spec 002 §5.1 `locale` column). PR #19 squash-merged; TASK-044 `done`; Phase 0 tasks 19/44 |
| 2026-09-08 | `/review 20`: VERDICT PASS (reviewer agent; plurals verified independently, draft determinism and human/reviewed protection reproduced, meta coverage 25/25 per locale, no network; 806 unit tests; CI 19/20 on the pre-rebase head). PR #20 rebased onto main (816 tests locally) and squash-merged as b726d71 before the rebased head's CI reported — main CI on b726d71 verified by the orchestrator. TASK-038 `done`; Phase 0 tasks 20/44 |
| 2026-09-08 | `/review 21`: VERDICT PASS (reviewer agent; alternates reciprocity/x-default/no-`de`/`pl`/no-`en-150` verified, data-flip to indexable reproduced in a scratch copy, two mutations each broke 5 tests; 853 unit tests; CI 19/20). Carry to spec 007/008: a page existing in `de`+`pl` but not `en` currently gets no hreflang cluster (`[]` rule) — define the rule when translated blog posts arrive. PR #21 squash-merged; TASK-039 `done`; Phase 0 tasks 21/44 |
| 2026-09-08 | Founder authorised light review for internal tasks (docs, tooling, CI, scripts, test harness); full `/review` remains for user-facing, money, compliance and security PRs. Applies from PR #22 onward. |
| 2026-09-08 | `/review 22`: VERDICT PASS (light review by the orchestrator per the 2026-09-08 policy — internal tooling: lint/typecheck/test 890/format green, `i18n:check --summary` exit 0 with the §11 table (en/en-gb indexable, de/pl 100% unreviewed → not indexable), seven fault fixtures + four malformed registries present, `i18n-check` job in the derived required set, CI green except informational `lighthouse`). PR #22 squash-merged; TASK-040 `done`; Phase 0 tasks 22/44 |
| 2026-09-08 | `/review 24`: VERDICT PASS (light review by the orchestrator: gates green, 917 unit tests, `i18n:pseudo` deterministic, flag-off build has no pseudo routes, hreflang fixtures contain none, production refusal fires; CI 17/18 with informational `lighthouse`). Decisions accepted: in-memory pseudo catalogues (no prebuild), `routableLocale` vs `launchLocales`, bidi-control RTL mirror, darwin `visual/home.png` baseline regenerated. PR #24 squash-merged; TASK-042 `done`; Phase 0 tasks 23/44 |
| 2026-09-08 | `/review 23`: VERDICT FAIL — required: `readLocaleCookie` must not throw on malformed percent-escapes (site-wide client DoS via a `%` cookie), island fails closed, tests for both; fixed on the same PR (`2e21823`: `decodeURIComponent` dropped, `useState` initialiser wrapped in try/catch returning `{ show: false, reason: "error" }`, 11 unit + 3 pin + 4 e2e cases added; overlay given explicit `bg-white`/`border-neutral-500`/`text-neutral-900`, a `sameLocale` e2e case, and the corrected Lighthouse note — two assertions fired, `resource-summary.script.size` 313,068 B vs 122,880 B and `categories.performance` 0.94 vs 0.95, both the pre-existing `/review 16` bundle weight TASK-043 owns). Dismiss semantics, `timeZone="UTC"` and the barrel upheld unchanged. Rebased onto main after #24 merged; 1,037 unit / 102 e2e (2 pre-existing macOS skips) / 16 a11y green. PR [#23](https://github.com/itsahmeds/flowers-overseas/pull/23); TASK-041 stays `in_review` for the re-review Post-rebase: TASK-042's `/de` full-page visual baseline picked up the banner on CI's English-configured runner (4,811 px diff), so `tests/visual/shell.spec.ts` now empties `navigator.languages` before each screenshot — the baselines gate the template, not the runner's language; no baseline image changed (`459bbc5`). |
| 2026-09-08 | `/review 23` re-check after fixes: PASS (orchestrator verified `decodeURIComponent` removed from `readLocaleCookie`, island fails closed, 11 new unit + 4 new e2e cases; 1,037 unit tests; CI 20/21 with informational `lighthouse`). Accepted out-of-scope change: `tests/visual/shell.spec.ts` empties `navigator.languages` before screenshots so baselines gate the template, not the runner language. Dismiss writes no cookie (upheld); `timeZone="UTC"` provider default upheld with the rule that every rendered time passes its own zone. PR #23 squash-merged; TASK-041 `done`; Phase 0 tasks 24/44 |
| 2026-09-08 | `/review 25`: VERDICT PASS (light review by the orchestrator: gates green, 1,052 unit tests, runbook/glossary/cookie register/spec §14 A2–A13 present, browser Sentry removed with server+edge kept, `global-error` barrel import narrowed; CI 18/19 with `lighthouse` informational). AC-27 script clause escalated: plan/01 §7's 120 KB gz budget is below Next 16.3.4's runtime floor (~130 KB gz / 111 KB br) — decision moved to spec 004 (raise budget, measure Brotli, or reduce framework); zod reaches the browser only via the localised 500 document (spec 004 task 2). PR #25 squash-merged; TASK-043 `done`. **Spec 003 exit signal reached: TASK-032…TASK-044 all `done` with recorded PASS; `i18n-check` green with `de`/`pl` non-indexable; four locale URLs live on preview with correct `lang`/`dir`/`<title>`; axe exception list gone. Phase 0 tasks 25/44.** |
| 2026-09-08 | `/plan-tasks specs/004-design-system-layout.md`: TASK-045…TASK-056 created (12 tasks, all 30 ACs owned exactly once, §6–§8 mapped to ACs or named explicitly on the row). Spec §13's resolution note is binding: the plan is written against the founder-approved design in `docs/design/homepage-v1/` (commerce header, full-bleed hero + finder card, four-fact proof row, occasion tiles, gated trending row, hidden verified-reviews section, destinations grid, dense colophon) and records where that supersedes §5 — two font families, the commerce header, and the canvas sections §2's home skeleton does not name. Order per spec §12 with two splits for the one-day rule: TASK-045 ∥ TASK-046 (tokens/fonts/primitives/gallery ∥ Brotli budget restatement + zod-free client locale data + CSP/security headers as ADR-0016 with `vercel.live` preview-only) → TASK-047 config registries → TASK-048 header ∥ TASK-049 footer ∥ TASK-050 consent plumbing + GA4 seam → TASK-051 consent islands → TASK-052…TASK-055 the home in three parts plus the chooser/404/500/suggestion-banner styling pass → TASK-056 gates, budgets, docs and close. Every task runs with no database (spec 002 parked): the three data-gated sections read static stubs behind spec 003's provider seam pattern. Phase 0 progress set to `4 / 12` specs, `25 / 56` tasks; `PHASE_0_SPECS_APPROVED` and its three assertions in `tests/unit/tasks-open-decisions.test.ts` flipped `3 / 12` → `4 / 12`. Founder actions, none blocking: hero and section imagery (photo slots listed in TASK-053's PR body), the GA4 measurement id, and the Trustpilot profile claim. |
| 2026-09-09 | `/review 26`: VERDICT FAIL (full reviewer; head `882669e`). CSP/security-headers half (AC-23) passes every check: report-only header on all documents incl. 404 and `/api/*`, `vercel.live` preview-only in `script-src`/`connect-src`/`frame-src` with both strings asserted, HSTS production-only, `/api/csp-report` both shapes, no PII in the log, ADR-0016 accepted. Blocking: zod still reaches the browser on every locale page via `LocaleSuggestionBannerIsland` → `hints.ts` → `schemas.ts`, fetched unconditionally by the `next/dynamic` loader after hydration — real browser totals `/` 113.7 KB br (correct), `/en`/`/de` 196.2 KB br (PR claimed 124.5), net gain 2.2 KB; `scripts/client-js-budget.ts` measures only the document's `<script src>` list and is blind to the loadable manifest, so its "no zod" assertion is a false negative; the Q13 escalation numbers, README and `docs/architecture.md` §2 assert the wrong result; `lighthouserc.json` Brotli note unreconciled with this run's own 249,766 B for `/en`. Fix round dispatched on the same PR. Non-blocking: no body-size guard on `/api/csp-report`, per-instance rate limiter note for the enforce runbook, `frame-src` widening vs the row wording, README should say `test:e2e` needs a hand-started `pnpm start`. PR [#26](https://github.com/itsahmeds/flowers-overseas/pull/26); TASK-046 stays `in_review` |
| 2026-09-09 | TASK-057 (orchestrator, inline): CI made on-demand after the founder reported 90% of the month's Actions minutes gone with a month left — `ci.yml` runs the spine once per PR marked ready, everything else behind `ci:full` / `workflow_dispatch`, nothing on push to `main`; `pr-policy` trimmed to ready/label events; spec 001 §14 A14; implementer/reviewer agent instructions updated (draft PR, local gates, push once, `gh pr ready`). Phase 0 tasks 26/57 |
| 2026-09-09 | `/review 26` round 2: VERDICT PASS (full reviewer, local gates — CI minutes exhausted). All five round-1 blockers discharged: zod absent from every browser-fetched chunk on `/`, `/en`, `/de` (island chunk 72.5 KB → 2 173 B br); `client-js-budget` follows the loadable manifest and matches the browser byte-for-byte on `/en` (129 638 B br); PR body, README, `docs/architecture.md` §2 and `lighthouserc.json` corrected; `Content-Length` guard on `/api/csp-report`. 1 189 unit / 124 e2e / 16 a11y green locally. Nits carried to TASK-056: `/` upper-bound in `budget:client-js`, local `pnpm lighthouse` gzip caveat, `AcceptLanguageSchema` test-only note, logger reports 200 for 204, walker fails on non-TS imports, §13 Q13 wording. Budget decision recorded as spec 004 §14 A1 (128 KB Brotli). PR #26 squash-merged; TASK-046 `done`; Phase 0 tasks 27/57 |
| 2026-09-09 | `/review 27`: VERDICT FAIL (full reviewer, local gates — CI minutes exhausted; head `ebe9bba`). Verified strong: 1 294 unit / 140 e2e / 18 a11y / 4 visual green; every token verbatim against `docs/design/homepage-v1/tokens.css`; all 32 contrast pairs recomputed independently and matching to 0.01; fonts 40 844 B of 46 080 with zero Google Fonts requests and Polish diacritics proven via `document.fonts.check`; gallery gate and production refusal reproduced; `/` still zero JS and zero `Set-Cookie`. Blocking: (1) Tailwind v4 source detection compiles `tests/fixtures/lint/*` into the production stylesheet (~1 114 B of `.bg-[#ff0000]`-style rules, three invalid), and the AC-1 literal assertion reads `globals.css` source instead of the built CSS; (2) `Field` and `Price` cross spec §3's non-goals (form layer written against real checkout fields; no price rendered in 004) — orchestrator ruling: remove both, trim icons to those the canvas uses. Nits: `/de` LCP 2 869 ms is the spec 003 banner island (inherited by TASK-053/056), stale rebase claim, token-value drift guard, `Mark.tsx` test citation, T-06 visual half substituted by the stronger e2e proof. Fix round dispatched on the same PR. PR [#27](https://github.com/itsahmeds/flowers-overseas/pull/27); TASK-045 stays `in_review` |
| 2026-09-09 | `/review 27` round 2: PASS (orchestrator re-check of the two round-1 blockers on head `3e84484`, rebased on main). Root cause was wider than found: Tailwind v4 auto-detection also scanned `.md`/`.html`, so docs leaked too; fixed with `source(none)` + one `src/**` glob, built stylesheet 30 878 → 24 200 B, built CSS grep for `#ff0000`/`hsl(`/`.ml-4`/`.text-left`/`.left-0` = 0 each, `tokens.test.ts` now asserts on the scanned build with an unscoped control. `Field`/`Price` removed; icons trimmed to the 11 the canvas uses plus the mirrored pair with an attribute-level drift guard; token-value drift guard vs `tokens.css` added. 1 448 unit / 162 e2e / 18 a11y / 4 visual green locally (implementer), 1 448 unit + lint + typecheck + build re-run by the orchestrator. Inherited: `/de` LCP is the spec 003 banner island (on TASK-053/056 rows). New devDependency `@tailwindcss/oxide` (test-only). PR #27 squash-merged; TASK-045 `done`; Phase 0 tasks 28/57 |
| 2026-09-09 | `/review 28`: VERDICT FAIL (full reviewer, local gates; head `9023f6e`). Verified: Consent Mode v2 defaults on all locales, bootstrap 527 B with hash recomputed from served bytes and present once in `script-src`; id unset → zero Google requests and no analytics origin in CSP; `/api/consent` 204/405/415/413/400 with no 5xx and 0 PII markers in the log; 11-row register zod-parsed with lifetimes derived; RoPA rows 3–4 and runbook; 0 B client JS added (`/en` 129 638 B br unchanged); 1 247 unit / 154 e2e / 16 a11y / 4 visual. Blocking: two vacuous test assertions — the prose-coverage guard searches the generated block too, and the "only inline script in src" test checks one file. Rulings: `src/lib/consent*.ts` + `src/modules/analytics/` accepted over §5.2's `src/modules/ui/consent/`; Next's ten flight inline blocks make enforcement unsafe → spec §14 A2 + TASK-058 (enforce flip leaves TASK-056); GA4 tag vs budget headroom recorded on TASK-056. Fix round dispatched on the same PR. **No CI run appeared for the `ready_for_review` event — the account's Actions minutes are very likely exhausted; local gates are the compensating control (spec 001 §14 A14).** PR [#28](https://github.com/itsahmeds/flowers-overseas/pull/28); TASK-050 stays `in_review` |
| 2026-09-09 | `/review 28` round 2: PASS (orchestrator re-check on head `8cd9312`). Both guards now fail under mutation (prose guard slices out the generated block; single-inline-script test walks `src/`); chunked bodies bounded by `readBoundedBody` → 413; stale budget numbers corrected to 131 072 B; rulings placed verbatim in `csp.ts`, the analytics-consent runbook and the register. 1 507 unit (one `dev-os` failure reproduced as cross-agent `$TMPDIR` interference, green alone) / 192 e2e / 18 a11y green locally; orchestrator re-ran unit, lint, typecheck, build, `budget:client-js` (0 B added, no zod/sentry). PR #28 squash-merged; TASK-050 `done`; Phase 0 tasks 29/58 |
| 2026-09-09 | `/review 29`: VERDICT PASS (light review by the orchestrator — config, schemas and message keys, nothing rendered): 7 destinations in canvas order with `toCountryRow()` pinned to spec 002 §5.1, 17 site links (1 published) behind `isPublished()`, 10 categories all `published: false` with owning specs, `company.ts` `registered: false` with the both-ways refinement, 47 `en` keys with `de`/`pl` drafts non-indexable; every label checked verbatim against `homepage-desktop.dc.html`; 1 556 unit, lint, typecheck, `i18n:check`, `check-layout` (9 config modules), `check:no-db`, build green after the orchestrator rebased the branch onto TASK-050 (two additive conflicts in `check-layout.ts` and `docs/architecture.md`). CI spine ran on `ready_for_review` (lint, typecheck, pr-policy green; test-unit pending at merge) — so Actions minutes were **not** exhausted; PR 28's missing run is unexplained. Carry-forwards: native review of `pl` slug `holandia` and `de` slug `rumaenien` before spec 007 publishes; `footer.cookieSettings` retained flag for TASK-049/051; `dev-os.test.ts` `$TMPDIR` flake across concurrent worktrees. PR #29 squash-merged; TASK-047 `done`; Phase 0 tasks 30/58 |
| 2026-09-09 | `/plan-tasks specs/005-catalogue-pricing-module.md`: TASK-060…TASK-071 created (12 tasks, all 28 ACs owned exactly once, §6–§8 mapped to ACs or named explicitly on the row). Spec §13's 2026-09-09 resolution note is binding (Q1–Q11 defaults accepted): PLN rounding moves to `x9` and HUF to `x90` in `src/config/currencies.ts` (TASK-062), middle tier preselected via `product_tier.is_default`, add-ons priced per country with their own VAT rate, 30-minute signed quote with `QUOTE_SIGNING_SECRET` deferred to spec 010, and spec 005 owns the dataset in `src/config/catalogue/*.data.ts` (Q9). Order per spec §12: module skeleton + the `fo/no-float-money` flip (spec 001 TASK-004's "enforced from spec 005" promise, TASK-060) → dataset + spec 002 projections → prices + FX snapshot + `catalogue:check` CI job → taxonomy read API → tiers/add-ons → pricing core → FX/rounding → projections and the price identity → availability/history/quote → hardening, gates and docs (TASK-069 is the Phase-0 exit). TASK-070 (db-backed providers) and TASK-071 (`fx.refresh`) are `blocked` on TASK-013 provisioning, not dropped. No database, no migration and no rendered surface in the whole spec; zero client JS against spec 004 §14 A1's 131 072 B Brotli budget with 1 434 B of headroom. Three schema amendments were already folded into spec 002 §14 A1. Phase 0 tasks 30 / 71 at this point. |
| 2026-09-09 | `/plan-tasks specs/006-seed-catalogue-import-imagery-pipeline.md`: TASK-072…TASK-083 created (12 tasks, all 30 ACs owned exactly once, §6–§8 mapped to ACs or named explicitly on the row). Spec §13's 2026-09-09 resolution note is binding (Q1 answered explicitly: AI-generated under the locked style guide **or free-licence stock**, never paid stock). Spec §12's task T9 was **split into two** (TASK-080 imagery/budgets/honesty gates, TASK-081 docs/records/no-db seam close) because as written it is several days — same precedent as spec 004 §12 task 5 → TASK-052…TASK-055; the spec's eleven tasks therefore land as twelve. Order: schemas + dataset skeleton → `en`/`en-gb` copy → price files → `seed:check` + CI job → `seed:diff` → style guide/prompts/provenance → `media:variants` → loader + `Photo` + preload + provenance note → imagery commit and budgets → docs and records (TASK-081 is the Phase-0 exit). TASK-082 (R2 storage, upload, derive worker) and TASK-083 (importer + R2 loader flip) are `blocked` on TASK-013. **Dataset-ownership ruling recorded on TASK-072/074 and escalated to the founder:** spec 005 §13 Q9 (005 owns `src/config/catalogue/*.data.ts`) and spec 006 §13 Q10 (spec 002's seed reads `seed/data/`) hold together only if 006's `products.json`, `product-tiers.json`, `addons.json` and the two price directories are **generated projections** of 005's dataset asserted equal by `seed:check`, never a second hand-authored copy; if the founder wants 006 to author its own catalogue rows, spec 006 §2.2 needs an amendment before TASK-072 starts. Cross-spec dependencies: TASK-072 depends on TASK-061, TASK-074 on TASK-062, TASK-079 on TASK-052 (same `src/modules/ui/media/` module — never concurrent with TASK-052…TASK-056) and TASK-080 on TASK-056 (blocking `lighthouse` and the restated Brotli budget its AC-22 measures against). Founder actions recorded as row notes, not tasks: generator choice and its commercial-use terms filed in `docs/compliance/` (TASK-077), imagery generation and the §2.4 checklist sign-off with accept/reject counts (TASK-080), originals in the founder's own store and never committed (TASK-077/080), Neon + EU-jurisdiction R2 provisioning (TASK-082/083). Phase 0 tasks 30 / 83. |
| 2026-09-09 | `/review 32` round 1: PASS as a docs increment (light review by the orchestrator; `design-docs.test.ts` 92/92, docs/ledger tests, lint, typecheck green; homepage-v1 content untouched except the `canvas.json` filename bug fix). 46 artboards: system sheet (components/typography/colour), three flows, 20 page types × desktop/mobile. Product wireframe checked against the rules: stem-count tiers with `is_default`, surcharge-on-chip before selection, add-ons default-off with own VAT, one all-in price, honesty label, no reviews section. PR #32 squash-merged; **TASK-059 stays `in_progress`**: round 2 reworks every wireframe against the competitor page-type study (founder, 2026-09-09: "look at our competitors and sample sites… how are their product pages"), adds `docs/design/benchmarks/`, then the full canvas is published for founder review before the task closes. Four documented canvas-vs-code gaps (tokens superset, `--space` vs `--spacing`, medium = 600, photo-ink token) are carried in the README |
| 2026-09-09 | `/review 30`: VERDICT FAIL (full reviewer, local gates; head `d5fd253`). Verified: colophon prints nothing false (0 hits for any registration/VAT/address string in five locales — omitting clauses ruled acceptable over bracketed placeholders), all 15 footer links text until a data flip, payment column names no method and ships no logo (`PaymentMethodSchema` strict, available ⇒ processor), `/api/reminders` no-store/noindex/no cookie with 0 PII markers in the log, 0 B client JS, `i18n:check` green, RTL mirrored with the mark unflipped, axe 22, 1 611 unit / 232 e2e / 8 visual. Blocking: `/api/reminders` reads the body with `request.text()` (chunked bodies bypass the size guard) and has no rate limit; `linux/` visual baselines stale/missing (orchestrator ruling: refreshed once from a single `ci:full` artifact after 048/049/052 merge, owned by TASK-053); four undeclared canvas drifts (legal row 13 vs 11 px, footer padding, wordmark size, mobile gaps 40 vs 22/16 px) to fix or declare. Fix round dispatched on the same PR. Founder skim requested for the `footer.reminders.consent` and `footer.payment.processor` sentences. PR [#30](https://github.com/itsahmeds/flowers-overseas/pull/30); TASK-049 stays `in_review` |
| 2026-09-09 | `/review 33`: VERDICT PASS (light review by the orchestrator — module skeleton, schemas, providers and a lint flip; nothing rendered): `fo/no-float-money` enabled on four roots with fixture pair and no-suppression scan; the one violation (`FontManifest.totalBytes`) renamed `subsetBytes`; `PricePointSchema` extends spec 003's `MoneySchema`; static providers throw `CatalogueDatasetPendingError` until TASK-061/062; `check:no-db` covers the module; client JS byte-identical. 1 589 unit, lint, typecheck, build green; CI spine green. Rebased twice by the orchestrator over ledger-row conflicts. PR #33 squash-merged; TASK-060 `done`; Phase 0 tasks 31/83 |
| 2026-09-09 | `/review 31`: VERDICT FAIL (full reviewer, local gates; head `fa6f2b4`). Verified: AC-7 190/171 px reserved, CLS 0, JS-off parity; AC-8 chip localised with body sha identical across `fo_currency` and no cookie; AC-14 all 14 entries `SPAN` with the published direction proven; zero client JS (`/en` 129 638 B br); `/` untouched; i18n, RTL, landmarks, axe 24 green; 1 576 unit / 222 e2e / 8 visual. Blocking: (1) six of seven rendered links miss 44 px while the PR body claims otherwise; (2) the `LocaleSwitcher` and currency chip sit 120–570 px off-screen inside the horizontally scrolling mobile category strip — the AC-8 deliverable is invisible at 390 px; (3) the disabled search submit is pixel-identical to an enabled one with `sr-only`-only explanation. Rulings: switcher + chip move to the utility strip (restores the canvas's one-line category row at 52 px); search band renders as the artboards draw it — text, no input/button — until spec 008 (spec §14 A4; TASK-048 row amended); links padded to 44 px. Merge with PR #30 is not trivial (8 conflicts incl. three shared darwin baselines) — header lands second and regenerates `en`/`de`/`ar-XB`. Fix round dispatched. PR [#31](https://github.com/itsahmeds/flowers-overseas/pull/31); TASK-048 stays `in_review` |
| 2026-09-09 | Founder direction (spec 004 §14 A5): customer copy speaks as Flowers Overseas in the first person — we deliver, our florists, our team — never as a relay handing to a third party; geographic claims capped at live coverage; specific over superlative. TASK-084 created for the shipped keys; TASK-059 round 2 (competitor pass) applies it to every artboard. Four agents (footer fix, header fix, TASK-061, competitor study) were cut off by the founder's usage limit; the study completed (nine files in the scratchpad, moving to `docs/design/benchmarks/` in round 2); the three build agents are re-dispatched |
| 2026-09-09 | `/review 30` round 2: PASS (orchestrator re-check on head `79d8052`, rebased on main). `/api/reminders` reads through `readBoundedBody` (chunked 256 KB body → 413 after ≤5 chunks, mutation-proven) and carries a 60/min per-instance limiter (`429` + `Retry-After`, mutation-proven); canvas metrics restored (legal row 11 px, block padding `40 56 24` / `32 20 20`, wordmark 26/22 px, mobile gaps 24/16) with an e2e that locks them; payment placeholder is the empty-state branch; linux-baseline ruling recorded on the TASK-049 and TASK-053 rows. 1 739 unit / 234 e2e / 8 visual / 22 a11y green locally; orchestrator re-ran unit, lint, typecheck, `i18n:check`, cold build and `budget:client-js` (0 B added). `company.description` copy left for TASK-084 (A5). PR #30 squash-merged; TASK-049 `done`; Phase 0 tasks 32/84 |
| 2026-09-09 | `/review 35` (TASK-059 round 2): PASS as a docs increment (orchestrator; `design-docs.test.ts` 156 incl. the banned-word scan over 49 artboards, prettier/lint/typecheck green). Every wireframe reworked against the competitor study (`docs/design/benchmarks/`, nine files + README): fee on every date chip, destination and date before the basket, recipient-time-zone cutoffs, three-sentence cross-border explanation, priced best-seller rows above every editorial section, first-person voice per §14 A5 throughout, `.btn` box-sizing overflow fix on 390 artboards. PR #35 squash-merged; full 49-artboard canvas published for founder review; 14 founder questions recorded in `docs/design/benchmarks/README.md` (five block copy slots: staffed hours, commission split and payout timing, refund clock, dispute body per market, complaint window). TASK-059 stays `in_progress` until the founder reviews the canvas |
| 2026-09-09 | Main red after PR #35: `company-config.test.ts` pins `company.description` to the homepage artboard, which round 2 rewrote in the first person. Fixed on main under TASK-084 (copy, `en.meta` hash, `de`/`pl` drafts, footer test pin); 1 803 unit green. Lesson recorded: `gh pr ready` fired while a PR is still conflicted with main produces no CI run (`ready_for_review` is consumed, the fix push is a `synchronize`) — this is why PRs #28, #31 and #34 had no spine run; implementers must rebase before marking ready |
| 2026-09-09 | `/review 34`: VERDICT PASS (light review by the orchestrator — authored dataset, schemas, projections, static provider; nothing rendered, no prices): 84 products (40/14/8/10/12 by kind), 236 tier rows with exactly one middle default each, 23 categories, 32 occasions, 6 add-ons, 0 prices/FX/copy/media by design; nine projections pinned against a separately transcribed spec 002 §5.1 incl. §14 A1 columns; deterministic serialisation for ADR-0017. 1 912 unit (one failure = the pre-existing `company-config` pin fixed on main), lint, typecheck, `check:no-db` (module covered), `check-layout` (17 config modules), cold build and `budget:client-js` byte-identical. Spec 002 §14 A2 records the two additive requests (`category(key, kind)`, nullable `product_tier.stems`). PR #34 squash-merged; TASK-061 `done`; Phase 0 tasks 33/84 |
| 2026-09-09 | `/review 31` round 2: PASS (orchestrator re-check on head `4598565`, rebased clean on main). §14 A4 applied: search band is text (0 `<input>`/`<form>` in the header; the one `<button>` is the disabled mobile menu — carried to TASK-055), `LocaleSwitcher` + currency chip in the utility strip and visible at 390 px (chip x 327–371), every header link ≥44 px, category row one line at 52 px; A4 addendum applied as sibling elements — strip scrolls, banner sticky at 132/138 px with the reserved total 245/183 unchanged, CLS 0, zero client JS (129 638 B br). Orchestrator ran unit (1 936; two `dev-os` `$TMPDIR` interference failures green alone), lint, typecheck, `i18n:check`, cold build, `budget:client-js`, and the header e2e (40) + a11y (6) specs against a local build. Baselines: header split into utility/banner pairs, en/de/ar-XB/footer regenerated for the TASK-084 copy. `linux/` baselines still owed to TASK-053's `ci:full` run. PR #31 squash-merged; TASK-048 `done`; Phase 0 tasks 34/84 |
| 2026-09-09 | TASK-051 escalation: consent sheet 2 365 B br puts locale documents at 132 047 B (975 B over §14 A1). Ruling (spec §14 A1 addendum): budget not raised; breach informational while `lighthouse` is `continue-on-error`; option (b) brought forward as TASK-085 (strings-as-props, drop `NextIntlClientProvider`, ~10.7 KB saving) to run after 051/052 and before 053. PR #36 under full review |
| 2026-09-09 | `/review 37`: VERDICT FAIL (full reviewer, money data; head on PR #37). Verified: gross + VAT bp only with 149 pairs recomputed clean; endings on all 1 652 retail rows; one active row per key; FX snapshot correct with the buffer unstored; 13 check modes with fixtures (3 mutation-checked); projections pinned three ways; destination drift fails both directions; zero client JS; 1 991 unit green. Blocking: (1) 58% of retail rows outside plan/10 §2.3's bands (funeral 1 039 zł vs 799 zł on PL) because the implementer let the exact bands yield to the approximate steps — orchestrator ruling recorded as spec 005 §14 A1: bands exact, steps bend; re-ladder; (2) `addon-price` mode does not assert the add-on VAT rate (PL chocolates at 800 bp passes); (3) surcharge amounts ungated (DE Sunday at €40 passes). Nits: contiguity comment, pin all nine FX magnitudes, peak-window overlap check, provisional VAT rates into `docs/compliance/vat-rates.md`, `country.vat_rate_bp` owner on TASK-069. Fix round dispatched. PR [#37](https://github.com/itsahmeds/flowers-overseas/pull/37); TASK-062 stays `in_review` |
| 2026-09-09 | `/review 36`: VERDICT FAIL — mechanical only (full reviewer; "the strongest work reviewed on this repo" for the consent feature itself): every legal check passes with measured evidence (equal-weight controls 358×50 at 18.16:1, 365/183-day lifetimes, no cookie before a decision, Esc/re-open, gtag update, toggles default off, non-modal ruling confirmed against §5.3/AC-17/§8, budget 132 047 B reproduced, z-order 300/50, fail-closed cookie parsing). Blocking: branch 11 commits behind main with six conflicts and baselines captured without the header; a false claim that HSTS blocks local Lighthouse (`next start` sends no HSTS; the real limit is identity-encoded chunks vs a Brotli budget); category checkboxes 13×18 px vs the spec's 44 px. Fix round dispatched. PR [#36](https://github.com/itsahmeds/flowers-overseas/pull/36); TASK-051 stays `in_review` |
| 2026-09-09 | **Session closed at the founder's request (usage limit).** State: main `e52b2b7`, 34/85, 6/12. Four agents were mid-flight and are gone; their worktrees remain: `fo-wt-051` (PR #36 fix round), `fo-wt-062` (PR #37 fix round), `fo-wt-052` (TASK-052, no PR), `fo-wt-072` (TASK-072, no PR). Next session: read `docs/sessions/2026-09-09-orchestrator-handoff.md`, inspect each worktree and PR, re-dispatch, then follow the dispatch order there (052 → 085 → 053 → 054 → 055 → 084 → 056 → 058; 062 → 063…069; 072 → 073/074/075/076, 077 → 078) |
| 2026-09-09 | `/review 37` re-check (orchestrator, fix round head `1e0fe62`): unit 1 999 green (alone; one `dev-os` flake under parallel load), lint, typecheck, `catalogue:check` clean (84 products, 3 416 price rows, 42 add-on rows, 9 FX rates, 0 outside band), cold build, `budget:client-js` within budget. PR [#37](https://github.com/itsahmeds/flowers-overseas/pull/37) squash-merged as `40eba98`; TASK-062 `done`; residual (gate reads bands from the module it checks) recorded on TASK-069. Unblocks TASK-063 and TASK-074 |
| 2026-09-09 | `/review 38` (light orchestrator review, internal schemas/data; CI spine green): PR body's eight decisions read and accepted. Rulings: (1) projected files carry the authored records, `to*Row()` applied at import time — ADR-0017 clarification recorded on TASK-083 and in `docs/decisions-log.md`; (2) `taxonomy.json`/`categories.json`/`occasions.json` are generated too (hand-writing them would be ADR-0017's second copy); (3) `SeedPriceSchema.sku` (not `productSku`) — spec 006 §5.2 to be corrected as a §14 amendment when that spec is next touched; (4) plan/03 §9 gap — Romanian Orthodox Easter and Polish name days carry `rule_type: "none"` with `observed: true`; a seventh rule type (Orthodox Easter offset) is a spec 009 amendment, recorded as `plan/13` B15 (spec 009 amendment, not a Phase 0 blocker). PR [#38](https://github.com/itsahmeds/flowers-overseas/pull/38) squash-merged as `cf6b561`; TASK-072 `done`; Phase 0 tasks 36 / 85. Dispatching TASK-063 and TASK-073; TASK-074 and TASK-077 follow as agent slots free up |
| 2026-09-09 | `/review 36` re-check (orchestrator, fix round head `a9b2e30`): unit 2 005 green (alone; one `dev-os` flake under parallel load), lint, typecheck, cold build, `budget:client-js` `/` 128.6 KB br · `/en`/`/de` 128.9 KB br (≈970 B over — informational per spec 004 §14 A1 addendum until TASK-085), `consent-banner` + `consent` e2e 114/114 and `consent` a11y 6/6 against `pnpm start` on :3601. PR [#36](https://github.com/itsahmeds/flowers-overseas/pull/36) squash-merged as `2760df8`; TASK-051 `done`; Phase 0 tasks 37 / 85. Unblocks TASK-085 (after TASK-052) and TASK-055. In flight: TASK-052 (fo-wt-052, continuing the mid-build worktree), TASK-063 (fo-wt-063), TASK-073 (fo-wt-073) |
